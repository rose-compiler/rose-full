// MACHINE GENERATED SOURCE FILE WITH ROSE (Grammar.h)--- DO NOT MODIFY!

#include "sage3basic.h"

#ifndef ROSE_USE_INTERNAL_FRONTEND_DEVELOPMENT
   #include "AST_FILE_IO.h"
#endif 
// The header file ("rose_config.h") should only be included by source files that require it.
#include "rose_config.h"

#if _MSC_VER
#define USE_CPP_NEW_DELETE_OPERATORS 0
#endif

#define ROSE_ALLOC_TRACE 0
#undef mprintf
#define mprintf Rose::Diagnostics::mfprintf(Rose::ir_node_mlog[Rose::Diagnostics::DEBUG])

#include <Rose/BinaryAnalysis/RegisterDictionary.h>

using namespace std;

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNode::copy(SgCopyHelp& help) const {
    SgNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNode();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSupport::copy(SgCopyHelp& help) const {
    SgSupport* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSupport();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 202 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifier::copy(SgCopyHelp& help) const {
    SgModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 292 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifierNodes::copy(SgCopyHelp& help) const {
    SgModifierNodes* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModifierNodes();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): nodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nodes
  // case: toBeCopied == COPY_DATA for nodes
     SgModifierTypePtrVector nodes_copy = p_nodes; 
     result->p_nodes = nodes_copy; 
  // Copy non-constructor parameter data member (access function): next_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for next
  // case: toBeCopied == COPY_DATA for next
     SgModifierNodes* next_copy = p_next; 
     result->p_next = next_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 393 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConstVolatileModifier::copy(SgCopyHelp& help) const {
    SgConstVolatileModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgConstVolatileModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgConstVolatileModifier::cv_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 489 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStorageModifier::copy(SgCopyHelp& help) const {
    SgStorageModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStorageModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgStorageModifier::storage_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): thread_local_storage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for thread_local_storage
  // case: toBeCopied == COPY_DATA for thread_local_storage
     bool thread_local_storage_copy = p_thread_local_storage; 
     result->p_thread_local_storage = thread_local_storage_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 590 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAccessModifier::copy(SgCopyHelp& help) const {
    SgAccessModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAccessModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgAccessModifier::access_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 686 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionModifier::copy(SgCopyHelp& help) const {
    SgFunctionModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_constructor_destructor_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_constructor_destructor_priority
  // case: toBeCopied == COPY_DATA for gnu_attribute_constructor_destructor_priority
     unsigned long int gnu_attribute_constructor_destructor_priority_copy = p_gnu_attribute_constructor_destructor_priority; 
     result->p_gnu_attribute_constructor_destructor_priority = gnu_attribute_constructor_destructor_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_weak_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_weak_reference
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_weak_reference
     std::string gnu_attribute_named_weak_reference_copy = p_gnu_attribute_named_weak_reference; 
     result->p_gnu_attribute_named_weak_reference = gnu_attribute_named_weak_reference_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_alias
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_alias
     std::string gnu_attribute_named_alias_copy = p_gnu_attribute_named_alias; 
     result->p_gnu_attribute_named_alias = gnu_attribute_named_alias_copy; 
  // Copy non-constructor parameter data member (access function): opencl_vec_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opencl_vec_type
  // case: toBeCopied == COPY_DATA for opencl_vec_type
     SgType * opencl_vec_type_copy = p_opencl_vec_type; 
     result->p_opencl_vec_type = opencl_vec_type_copy; 
  // Copy non-constructor parameter data member (access function): opencl_work_group_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opencl_work_group_size
  // case: toBeCopied == COPY_DATA for opencl_work_group_size
     SgFunctionModifier::opencl_work_group_size_t opencl_work_group_size_copy = p_opencl_work_group_size; 
     result->p_opencl_work_group_size = opencl_work_group_size_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 807 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUPC_AccessModifier::copy(SgCopyHelp& help) const {
    SgUPC_AccessModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUPC_AccessModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgUPC_AccessModifier::upc_access_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): isShared_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isShared
  // case: toBeCopied == COPY_DATA for isShared
     bool isShared_copy = p_isShared; 
     result->p_isShared = isShared_copy; 
  // Copy non-constructor parameter data member (access function): layout_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for layout
  // case: toBeCopied == COPY_DATA for layout
     long layout_copy = p_layout; 
     result->p_layout = layout_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 913 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpecialFunctionModifier::copy(SgCopyHelp& help) const {
    SgSpecialFunctionModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSpecialFunctionModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1009 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElaboratedTypeModifier::copy(SgCopyHelp& help) const {
    SgElaboratedTypeModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElaboratedTypeModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgElaboratedTypeModifier::elaborated_type_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1105 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLinkageModifier::copy(SgCopyHelp& help) const {
    SgLinkageModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLinkageModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgLinkageModifier::linkage_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1201 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBaseClassModifier::copy(SgCopyHelp& help) const {
    SgBaseClassModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBaseClassModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgBaseClassModifier::baseclass_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_accessModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for accessModifier
  // case: toBeCopied == COPY_DATA for accessModifier
     SgAccessModifier accessModifier_copy = p_accessModifier; 
     result->p_accessModifier = accessModifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1302 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStructureModifier::copy(SgCopyHelp& help) const {
    SgStructureModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStructureModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgStructureModifier::jovial_structure_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
  // Copy non-constructor parameter data member (access function): bits_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bits_per_entry
  // case: toBeCopied == COPY_DATA for bits_per_entry
     int bits_per_entry_copy = p_bits_per_entry; 
     result->p_bits_per_entry = bits_per_entry_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1403 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeModifier::copy(SgCopyHelp& help) const {
    SgTypeModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_upcModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upcModifier
  // case: toBeCopied == COPY_DATA for upcModifier
     SgUPC_AccessModifier upcModifier_copy = p_upcModifier; 
     result->p_upcModifier = upcModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_structureModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structureModifier
  // case: toBeCopied == COPY_DATA for structureModifier
     SgStructureModifier structureModifier_copy = p_structureModifier; 
     result->p_structureModifier = structureModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_constVolatileModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constVolatileModifier
  // case: toBeCopied == COPY_DATA for constVolatileModifier
     SgConstVolatileModifier constVolatileModifier_copy = p_constVolatileModifier; 
     result->p_constVolatileModifier = constVolatileModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_elaboratedTypeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for elaboratedTypeModifier
  // case: toBeCopied == COPY_DATA for elaboratedTypeModifier
     SgElaboratedTypeModifier elaboratedTypeModifier_copy = p_elaboratedTypeModifier; 
     result->p_elaboratedTypeModifier = elaboratedTypeModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_extension_machine_mode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_machine_mode
  // case: toBeCopied == COPY_DATA for gnu_extension_machine_mode
     SgTypeModifier::gnu_extension_machine_mode_enum gnu_extension_machine_mode_copy = p_gnu_extension_machine_mode; 
     result->p_gnu_extension_machine_mode = gnu_extension_machine_mode_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_alignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_alignment
  // case: toBeCopied == COPY_DATA for gnu_attribute_alignment
     int gnu_attribute_alignment_copy = p_gnu_attribute_alignment; 
     result->p_gnu_attribute_alignment = gnu_attribute_alignment_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_sentinel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_sentinel
  // case: toBeCopied == COPY_DATA for gnu_attribute_sentinel
     long gnu_attribute_sentinel_copy = p_gnu_attribute_sentinel; 
     result->p_gnu_attribute_sentinel = gnu_attribute_sentinel_copy; 
  // Copy non-constructor parameter data member (access function): address_space_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_space_value
  // case: toBeCopied == COPY_DATA for address_space_value
     unsigned address_space_value_copy = p_address_space_value; 
     result->p_address_space_value = address_space_value_copy; 
  // Copy non-constructor parameter data member (access function): vector_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vector_size
  // case: toBeCopied == COPY_DATA for vector_size
     unsigned vector_size_copy = p_vector_size; 
     result->p_vector_size = vector_size_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationModifier::copy(SgCopyHelp& help) const {
    SgDeclarationModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeclarationModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifierVector
  // case: toBeCopied == COPY_DATA for modifierVector
     SgBitVector modifierVector_copy = p_modifierVector; 
     result->p_modifierVector = modifierVector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_typeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeModifier
  // case: toBeCopied == COPY_DATA for typeModifier
     SgTypeModifier typeModifier_copy = p_typeModifier; 
     result->p_typeModifier = typeModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_accessModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for accessModifier
  // case: toBeCopied == COPY_DATA for accessModifier
     SgAccessModifier accessModifier_copy = p_accessModifier; 
     result->p_accessModifier = accessModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_storageModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageModifier
  // case: toBeCopied == COPY_DATA for storageModifier
     SgStorageModifier storageModifier_copy = p_storageModifier; 
     result->p_storageModifier = storageModifier_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_section_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_section_name
  // case: toBeCopied == COPY_DATA for gnu_attribute_section_name
     std::string gnu_attribute_section_name_copy = p_gnu_attribute_section_name; 
     result->p_gnu_attribute_section_name = gnu_attribute_section_name_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_visability
  // case: toBeCopied == COPY_DATA for gnu_attribute_visability
     SgDeclarationModifier::gnu_declaration_visability_enum gnu_attribute_visability_copy = p_gnu_attribute_visability; 
     result->p_gnu_attribute_visability = gnu_attribute_visability_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_uuid_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_uuid_string
  // case: toBeCopied == COPY_DATA for microsoft_uuid_string
     std::string microsoft_uuid_string_copy = p_microsoft_uuid_string; 
     result->p_microsoft_uuid_string = microsoft_uuid_string_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_property_get_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_property_get_function_name
  // case: toBeCopied == COPY_DATA for microsoft_property_get_function_name
     std::string microsoft_property_get_function_name_copy = p_microsoft_property_get_function_name; 
     result->p_microsoft_property_get_function_name = microsoft_property_get_function_name_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_property_put_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_property_put_function_name
  // case: toBeCopied == COPY_DATA for microsoft_property_put_function_name
     std::string microsoft_property_put_function_name_copy = p_microsoft_property_put_function_name; 
     result->p_microsoft_property_put_function_name = microsoft_property_put_function_name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOpenclAccessModeModifier::copy(SgCopyHelp& help) const {
    SgOpenclAccessModeModifier* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOpenclAccessModeModifier();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): modifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgOpenclAccessModeModifier::access_mode_modifier_enum modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgName::copy(SgCopyHelp& help) const {
    SgName* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgName();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_char
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for char
  // case: toBeCopied == COPY_DATA for char
     std::string char_copy = p_char; 
     result->p_char = char_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 1872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSymbolTable::copy(SgCopyHelp& help) const {
    SgSymbolTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSymbolTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_iterator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iterator
  // case: toBeCopied == COPY_DATA for iterator
     hash_iterator iterator_copy = p_iterator; 
     result->p_iterator = iterator_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): no_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_name
  // case: toBeCopied == COPY_DATA for no_name
     bool no_name_copy = p_no_name; 
     result->p_no_name = no_name_copy; 
  // Copy non-constructor parameter data member (access function): table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for table
  // case: toBeCopied == COPY_DATA for table
      rose_hash_multimap* table_copy = p_table; 
     result->p_table = table_copy; 
  // Copy non-constructor parameter data member (access function): symbolSet_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbolSet
  // case: toBeCopied == COPY_DATA for symbolSet
     SgNodeSet symbolSet_copy = p_symbolSet; 
     result->p_symbolSet = symbolSet_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_case_insensitive
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for case_insensitive
  // case: toBeCopied == COPY_DATA for case_insensitive
     bool case_insensitive_copy = p_case_insensitive; 
     result->p_case_insensitive = case_insensitive_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_aliasSymbolCausalNodeSet
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for aliasSymbolCausalNodeSet
  // case: toBeCopied == COPY_DATA for aliasSymbolCausalNodeSet
     static SgUnorderedNodeSet aliasSymbolCausalNodeSet_copy = p_aliasSymbolCausalNodeSet; 
     result->p_aliasSymbolCausalNodeSet = aliasSymbolCausalNodeSet_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_force_search_of_base_classes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for force_search_of_base_classes
  // case: toBeCopied == COPY_DATA for force_search_of_base_classes
     static bool force_search_of_base_classes_copy = p_force_search_of_base_classes; 
     result->p_force_search_of_base_classes = force_search_of_base_classes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_mode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_mode
  // case: toBeCopied == COPY_DATA for name_qualification_mode
     static bool name_qualification_mode_copy = p_name_qualification_mode; 
     result->p_name_qualification_mode = name_qualification_mode_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAttribute::copy(SgCopyHelp& help) const {
    SgAttribute* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAttribute( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2103 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPragma::copy(SgCopyHelp& help) const {
    SgPragma* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPragma( name_copy, startOfConstruct_copy, endOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_printed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for printed
  // case: toBeCopied == COPY_DATA for printed
     short printed_copy = p_printed; 
     result->p_printed = printed_copy; 
  // Copy non-constructor parameter data member (access function): args_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for args
  // case: toBeCopied == COPY_DATA for args
     SgExprListExp* args_copy = p_args; 
     result->p_args = args_copy; 
  // Copy non-constructor parameter data member (access function): associatedStatement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedStatement
  // case: toBeCopied == COPY_DATA for associatedStatement
     SgStatement* associatedStatement_copy = p_associatedStatement; 
     result->p_associatedStatement = associatedStatement_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2254 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitAttribute::copy(SgCopyHelp& help) const {
    SgBitAttribute* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
    // Copy constructor parameter data member: bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitAttribute( name_copy, bitflag_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2353 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFuncDecl_attr::copy(SgCopyHelp& help) const {
    SgFuncDecl_attr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFuncDecl_attr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
     result->p_bitflag = bitflag_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2454 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDecl_attr::copy(SgCopyHelp& help) const {
    SgClassDecl_attr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassDecl_attr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): bitflag_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitflag
  // case: toBeCopied == COPY_DATA for bitflag
     unsigned long int bitflag_copy = p_bitflag; 
     result->p_bitflag = bitflag_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2555 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* Sg_File_Info::copy(SgCopyHelp& help) const {
    Sg_File_Info* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new Sg_File_Info();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_id
  // case: toBeCopied == COPY_DATA for file_id
     int file_id_copy = p_file_id; 
     result->p_file_id = file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line
  // case: toBeCopied == COPY_DATA for line
     int line_copy = p_line; 
     result->p_line = line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_col
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for col
  // case: toBeCopied == COPY_DATA for col
     int col_copy = p_col; 
     result->p_col = col_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_classificationBitField
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classificationBitField
  // case: toBeCopied == COPY_DATA for classificationBitField
     unsigned int classificationBitField_copy = p_classificationBitField; 
     result->p_classificationBitField = classificationBitField_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physical_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_file_id
  // case: toBeCopied == COPY_DATA for physical_file_id
     int physical_file_id_copy = p_physical_file_id; 
     result->p_physical_file_id = physical_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physical_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_line
  // case: toBeCopied == COPY_DATA for physical_line
     int physical_line_copy = p_physical_line; 
     result->p_physical_line = physical_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_sequence_number
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_number
  // case: toBeCopied == COPY_DATA for source_sequence_number
     unsigned int source_sequence_number_copy = p_source_sequence_number; 
     result->p_source_sequence_number = source_sequence_number_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_fileIDsToUnparse()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileIDsToUnparse
  // case: toBeCopied == COPY_DATA for fileIDsToUnparse
     SgFileIdList fileIDsToUnparse_copy = p_fileIDsToUnparse; 
     result->p_fileIDsToUnparse = fileIDsToUnparse_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_fileLineNumbersToUnparse()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileLineNumbersToUnparse
  // case: toBeCopied == COPY_DATA for fileLineNumbersToUnparse
     SgFileLineNumberList fileLineNumbersToUnparse_copy = p_fileLineNumbersToUnparse; 
     result->p_fileLineNumbersToUnparse = fileLineNumbersToUnparse_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileidtoname_map
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileidtoname_map
  // case: toBeCopied == COPY_DATA for fileidtoname_map
     static std::map<int, std::string> fileidtoname_map_copy = p_fileidtoname_map; 
     result->p_fileidtoname_map = fileidtoname_map_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nametofileid_map
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nametofileid_map
  // case: toBeCopied == COPY_DATA for nametofileid_map
     static std::map<std::string, int> nametofileid_map_copy = p_nametofileid_map; 
     result->p_nametofileid_map = nametofileid_map_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 2701 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFile::copy(SgCopyHelp& help) const {
    SgFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFile();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 3464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSourceFile::copy(SgCopyHelp& help) const {
    SgSourceFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSourceFile();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): globalScope_copy
     SgGlobal* globalScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalScope
          if (get_globalScope() != nullptr) 
             { 
               globalScope_copy = static_cast<SgGlobal*>(help.copyAst(get_globalScope())); 
             } 
            else 
             { 
               globalScope_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalScope != nullptr) delete result->p_globalScope; 
     result->p_globalScope = globalScope_copy; 
  // case: not a listType for (using conditionalToSetParent)globalScope
        if ((globalScope_copy != nullptr) && (globalScope_copy->get_parent() == nullptr) && (isSgType(globalScope_copy) == nullptr)) {
            globalScope_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_module_list()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_list
  // case: toBeCopied == COPY_DATA for module_list
     SgModuleStatementPtrList module_list_copy = p_module_list; 
     result->p_module_list = module_list_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_token_list()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for token_list
  // case: toBeCopied == COPY_DATA for token_list
     SgTokenPtrList token_list_copy = p_token_list; 
     result->p_token_list = token_list_copy; 
  // Copy non-constructor parameter data member (access function): temp_holding_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for temp_holding_scope
  // case: toBeCopied == COPY_DATA for temp_holding_scope
     SgGlobal* temp_holding_scope_copy = p_temp_holding_scope; 
     result->p_temp_holding_scope = temp_holding_scope_copy; 
  // Copy non-constructor parameter data member (access function): isHeaderFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isHeaderFile
  // case: toBeCopied == COPY_DATA for isHeaderFile
     bool isHeaderFile_copy = p_isHeaderFile; 
     result->p_isHeaderFile = isHeaderFile_copy; 
  // Copy non-constructor parameter data member (access function): isHeaderFileIncludedMoreThanOnce_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isHeaderFileIncludedMoreThanOnce
  // case: toBeCopied == COPY_DATA for isHeaderFileIncludedMoreThanOnce
     bool isHeaderFileIncludedMoreThanOnce_copy = p_isHeaderFileIncludedMoreThanOnce; 
     result->p_isHeaderFileIncludedMoreThanOnce = isHeaderFileIncludedMoreThanOnce_copy; 
  // Copy non-constructor parameter data member (access function): headerFileReport_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for headerFileReport
  // case: toBeCopied == COPY_DATA for headerFileReport
     SgHeaderFileReport* headerFileReport_copy = p_headerFileReport; 
     result->p_headerFileReport = headerFileReport_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierBeforeList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierBeforeList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierBeforeList
     SgStringList extraIncludeDirectorySpecifierBeforeList_copy = p_extraIncludeDirectorySpecifierBeforeList; 
     result->p_extraIncludeDirectorySpecifierBeforeList = extraIncludeDirectorySpecifierBeforeList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierAfterList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierAfterList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierAfterList
     SgStringList extraIncludeDirectorySpecifierAfterList_copy = p_extraIncludeDirectorySpecifierAfterList; 
     result->p_extraIncludeDirectorySpecifierAfterList = extraIncludeDirectorySpecifierAfterList_copy; 
  // Copy non-constructor parameter data member (access function): associated_include_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associated_include_file
  // case: toBeCopied == COPY_DATA for associated_include_file
     SgIncludeFile* associated_include_file_copy = p_associated_include_file; 
     result->p_associated_include_file = associated_include_file_copy; 
  // Copy non-constructor parameter data member (access function): processedToIncludeCppDirectivesAndComments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for processedToIncludeCppDirectivesAndComments
  // case: toBeCopied == COPY_DATA for processedToIncludeCppDirectivesAndComments
     bool processedToIncludeCppDirectivesAndComments_copy = p_processedToIncludeCppDirectivesAndComments; 
     result->p_processedToIncludeCppDirectivesAndComments = processedToIncludeCppDirectivesAndComments_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_extra_nodes_for_namequal_init()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra_nodes_for_namequal_init
  // case: toBeCopied == COPY_DATA for extra_nodes_for_namequal_init
     SgNodePtrList extra_nodes_for_namequal_init_copy = p_extra_nodes_for_namequal_init; 
     result->p_extra_nodes_for_namequal_init = extra_nodes_for_namequal_init_copy; 
  // Copy non-constructor parameter data member (access function): isDynamicLibrary_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDynamicLibrary
  // case: toBeCopied == COPY_DATA for isDynamicLibrary
     bool isDynamicLibrary_copy = p_isDynamicLibrary; 
     result->p_isDynamicLibrary = isDynamicLibrary_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 4305 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnknownFile::copy(SgCopyHelp& help) const {
    SgUnknownFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnknownFile();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): globalScope_copy
     SgGlobal* globalScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalScope
          if (get_globalScope() != nullptr) 
             { 
               globalScope_copy = static_cast<SgGlobal*>(help.copyAst(get_globalScope())); 
             } 
            else 
             { 
               globalScope_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalScope != nullptr) delete result->p_globalScope; 
     result->p_globalScope = globalScope_copy; 
  // case: not a listType for (using conditionalToSetParent)globalScope
        if ((globalScope_copy != nullptr) && (globalScope_copy->get_parent() == nullptr) && (isSgType(globalScope_copy) == nullptr)) {
            globalScope_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 5086 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBinaryComposite::copy(SgCopyHelp& help) const {
    SgBinaryComposite* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBinaryComposite();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): genericFileList_copy
     SgAsmGenericFileList* genericFileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)genericFileList
          if (get_genericFileList() != nullptr) 
             { 
               genericFileList_copy = static_cast<SgAsmGenericFileList*>(help.copyAst(get_genericFileList())); 
             } 
            else 
             { 
               genericFileList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_genericFileList != nullptr) delete result->p_genericFileList; 
     result->p_genericFileList = genericFileList_copy; 
  // case: not a listType for (using conditionalToSetParent)genericFileList
        if ((genericFileList_copy != nullptr) && (genericFileList_copy->get_parent() == nullptr) && (isSgType(genericFileList_copy) == nullptr)) {
            genericFileList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): interpretations_copy
     SgAsmInterpretationList* interpretations_copy; 
  // case: not a listType for (using conditionalToCopyVariable)interpretations
          if (get_interpretations() != nullptr) 
             { 
               interpretations_copy = static_cast<SgAsmInterpretationList*>(help.copyAst(get_interpretations())); 
             } 
            else 
             { 
               interpretations_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_interpretations != nullptr) delete result->p_interpretations; 
     result->p_interpretations = interpretations_copy; 
  // case: not a listType for (using conditionalToSetParent)interpretations
        if ((interpretations_copy != nullptr) && (interpretations_copy->get_parent() == nullptr) && (isSgType(interpretations_copy) == nullptr)) {
            interpretations_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 5885 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJvmComposite::copy(SgCopyHelp& help) const {
    SgJvmComposite* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJvmComposite();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for verbose
  // case: toBeCopied == COPY_DATA for verbose
     int verbose_copy = p_verbose; 
     result->p_verbose = verbose_copy; 
  // Copy non-constructor parameter data member (access function): output_warnings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_warnings
  // case: toBeCopied == COPY_DATA for output_warnings
     bool output_warnings_copy = p_output_warnings; 
     result->p_output_warnings = output_warnings_copy; 
  // Copy non-constructor parameter data member (access function): C_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
  // Copy non-constructor parameter data member (access function): Cxx_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): CoArrayFortran_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for CoArrayFortran_only
  // case: toBeCopied == COPY_DATA for CoArrayFortran_only
     bool CoArrayFortran_only_copy = p_CoArrayFortran_only; 
     result->p_CoArrayFortran_only = CoArrayFortran_only_copy; 
  // Copy non-constructor parameter data member (access function): upc_threads_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for upc_threads
  // case: toBeCopied == COPY_DATA for upc_threads
     int upc_threads_copy = p_upc_threads; 
     result->p_upc_threads = upc_threads_copy; 
  // Copy non-constructor parameter data member (access function): Java_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
  // Copy non-constructor parameter data member (access function): Jvm_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
  // Copy non-constructor parameter data member (access function): Jovial_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
  // Copy non-constructor parameter data member (access function): PHP_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PHP_only
  // case: toBeCopied == COPY_DATA for PHP_only
     bool PHP_only_copy = p_PHP_only; 
     result->p_PHP_only = PHP_only_copy; 
  // Copy non-constructor parameter data member (access function): Python_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Python_only
  // case: toBeCopied == COPY_DATA for Python_only
     bool Python_only_copy = p_Python_only; 
     result->p_Python_only = Python_only_copy; 
  // Copy non-constructor parameter data member (access function): Cuda_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cuda_only
  // case: toBeCopied == COPY_DATA for Cuda_only
     bool Cuda_only_copy = p_Cuda_only; 
     result->p_Cuda_only = Cuda_only_copy; 
  // Copy non-constructor parameter data member (access function): OpenCL_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OpenCL_only
  // case: toBeCopied == COPY_DATA for OpenCL_only
     bool OpenCL_only_copy = p_OpenCL_only; 
     result->p_OpenCL_only = OpenCL_only_copy; 
  // Copy non-constructor parameter data member (access function): Csharp_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Csharp_only
  // case: toBeCopied == COPY_DATA for Csharp_only
     bool Csharp_only_copy = p_Csharp_only; 
     result->p_Csharp_only = Csharp_only_copy; 
  // Copy non-constructor parameter data member (access function): Ada_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): requires_C_preprocessor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_C_preprocessor
  // case: toBeCopied == COPY_DATA for requires_C_preprocessor
     bool requires_C_preprocessor_copy = p_requires_C_preprocessor; 
     result->p_requires_C_preprocessor = requires_C_preprocessor_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): inputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputFormat
  // case: toBeCopied == COPY_DATA for inputFormat
     SgFile::outputFormatOption_enum inputFormat_copy = p_inputFormat; 
     result->p_inputFormat = inputFormat_copy; 
  // Copy non-constructor parameter data member (access function): outputFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFormat
  // case: toBeCopied == COPY_DATA for outputFormat
     SgFile::outputFormatOption_enum outputFormat_copy = p_outputFormat; 
     result->p_outputFormat = outputFormat_copy; 
  // Copy non-constructor parameter data member (access function): backendCompileFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompileFormat
  // case: toBeCopied == COPY_DATA for backendCompileFormat
     SgFile::outputFormatOption_enum backendCompileFormat_copy = p_backendCompileFormat; 
     result->p_backendCompileFormat = backendCompileFormat_copy; 
  // Copy non-constructor parameter data member (access function): fortran_implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_implicit_none
  // case: toBeCopied == COPY_DATA for fortran_implicit_none
     bool fortran_implicit_none_copy = p_fortran_implicit_none; 
     result->p_fortran_implicit_none = fortran_implicit_none_copy; 
  // Copy non-constructor parameter data member (access function): openmp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp
  // case: toBeCopied == COPY_DATA for openmp
     bool openmp_copy = p_openmp; 
     result->p_openmp = openmp_copy; 
  // Copy non-constructor parameter data member (access function): openmp_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_parse_only
  // case: toBeCopied == COPY_DATA for openmp_parse_only
     bool openmp_parse_only_copy = p_openmp_parse_only; 
     result->p_openmp_parse_only = openmp_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_ast_only
  // case: toBeCopied == COPY_DATA for openmp_ast_only
     bool openmp_ast_only_copy = p_openmp_ast_only; 
     result->p_openmp_ast_only = openmp_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_lowering_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_lowering
  // case: toBeCopied == COPY_DATA for openmp_lowering
     bool openmp_lowering_copy = p_openmp_lowering; 
     result->p_openmp_lowering = openmp_lowering_copy; 
  // Copy non-constructor parameter data member (access function): openacc_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc
  // case: toBeCopied == COPY_DATA for openacc
     bool openacc_copy = p_openacc; 
     result->p_openacc = openacc_copy; 
  // Copy non-constructor parameter data member (access function): openacc_parse_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_parse_only
  // case: toBeCopied == COPY_DATA for openacc_parse_only
     bool openacc_parse_only_copy = p_openacc_parse_only; 
     result->p_openacc_parse_only = openacc_parse_only_copy; 
  // Copy non-constructor parameter data member (access function): openacc_ast_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openacc_ast_only
  // case: toBeCopied == COPY_DATA for openacc_ast_only
     bool openacc_ast_only_copy = p_openacc_ast_only; 
     result->p_openacc_ast_only = openacc_ast_only_copy; 
  // Copy non-constructor parameter data member (access function): cray_pointer_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cray_pointer_support
  // case: toBeCopied == COPY_DATA for cray_pointer_support
     bool cray_pointer_support_copy = p_cray_pointer_support; 
     result->p_cray_pointer_support = cray_pointer_support_copy; 
  // Copy non-constructor parameter data member (access function): failsafe_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for failsafe
  // case: toBeCopied == COPY_DATA for failsafe
     bool failsafe_copy = p_failsafe; 
     result->p_failsafe = failsafe_copy; 
  // Copy non-constructor parameter data member (access function): output_parser_actions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for output_parser_actions
  // case: toBeCopied == COPY_DATA for output_parser_actions
     bool output_parser_actions_copy = p_output_parser_actions; 
     result->p_output_parser_actions = output_parser_actions_copy; 
  // Copy non-constructor parameter data member (access function): exit_after_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exit_after_parser
  // case: toBeCopied == COPY_DATA for exit_after_parser
     bool exit_after_parser_copy = p_exit_after_parser; 
     result->p_exit_after_parser = exit_after_parser_copy; 
  // Copy non-constructor parameter data member (access function): skip_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_syntax_check
  // case: toBeCopied == COPY_DATA for skip_syntax_check
     bool skip_syntax_check_copy = p_skip_syntax_check; 
     result->p_skip_syntax_check = skip_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_parser_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_parser
  // case: toBeCopied == COPY_DATA for skip_parser
     bool skip_parser_copy = p_skip_parser; 
     result->p_skip_parser = skip_parser_copy; 
  // Copy non-constructor parameter data member (access function): relax_syntax_check_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relax_syntax_check
  // case: toBeCopied == COPY_DATA for relax_syntax_check
     bool relax_syntax_check_copy = p_relax_syntax_check; 
     result->p_relax_syntax_check = relax_syntax_check_copy; 
  // Copy non-constructor parameter data member (access function): skip_translation_from_edg_ast_to_rose_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_translation_from_edg_ast_to_rose_ast
  // case: toBeCopied == COPY_DATA for skip_translation_from_edg_ast_to_rose_ast
     bool skip_translation_from_edg_ast_to_rose_ast_copy = p_skip_translation_from_edg_ast_to_rose_ast; 
     result->p_skip_translation_from_edg_ast_to_rose_ast = skip_translation_from_edg_ast_to_rose_ast_copy; 
  // Copy non-constructor parameter data member (access function): skip_transformation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_transformation
  // case: toBeCopied == COPY_DATA for skip_transformation
     bool skip_transformation_copy = p_skip_transformation; 
     result->p_skip_transformation = skip_transformation_copy; 
  // Copy non-constructor parameter data member (access function): skip_unparse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse
  // case: toBeCopied == COPY_DATA for skip_unparse
     bool skip_unparse_copy = p_skip_unparse; 
     result->p_skip_unparse = skip_unparse_copy; 
  // Copy non-constructor parameter data member (access function): skipfinalCompileStep_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipfinalCompileStep
  // case: toBeCopied == COPY_DATA for skipfinalCompileStep
     bool skipfinalCompileStep_copy = p_skipfinalCompileStep; 
     result->p_skipfinalCompileStep = skipfinalCompileStep_copy; 
  // Copy non-constructor parameter data member (access function): unparse_includes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_includes
  // case: toBeCopied == COPY_DATA for unparse_includes
     bool unparse_includes_copy = p_unparse_includes; 
     result->p_unparse_includes = unparse_includes_copy; 
  // Copy non-constructor parameter data member (access function): unparse_line_directives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_line_directives
  // case: toBeCopied == COPY_DATA for unparse_line_directives
     bool unparse_line_directives_copy = p_unparse_line_directives; 
     result->p_unparse_line_directives = unparse_line_directives_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_syntax
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_syntax
     bool unparse_function_calls_using_operator_syntax_copy = p_unparse_function_calls_using_operator_syntax; 
     result->p_unparse_function_calls_using_operator_syntax = unparse_function_calls_using_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): unparse_function_calls_using_operator_names_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_function_calls_using_operator_names
  // case: toBeCopied == COPY_DATA for unparse_function_calls_using_operator_names
     bool unparse_function_calls_using_operator_names_copy = p_unparse_function_calls_using_operator_names; 
     result->p_unparse_function_calls_using_operator_names = unparse_function_calls_using_operator_names_copy; 
  // Copy non-constructor parameter data member (access function): unparse_instruction_addresses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_instruction_addresses
  // case: toBeCopied == COPY_DATA for unparse_instruction_addresses
     bool unparse_instruction_addresses_copy = p_unparse_instruction_addresses; 
     result->p_unparse_instruction_addresses = unparse_instruction_addresses_copy; 
  // Copy non-constructor parameter data member (access function): unparse_raw_memory_contents_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_raw_memory_contents
  // case: toBeCopied == COPY_DATA for unparse_raw_memory_contents
     bool unparse_raw_memory_contents_copy = p_unparse_raw_memory_contents; 
     result->p_unparse_raw_memory_contents = unparse_raw_memory_contents_copy; 
  // Copy non-constructor parameter data member (access function): unparse_binary_file_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_binary_file_format
  // case: toBeCopied == COPY_DATA for unparse_binary_file_format
     bool unparse_binary_file_format_copy = p_unparse_binary_file_format; 
     result->p_unparse_binary_file_format = unparse_binary_file_format_copy; 
  // Copy non-constructor parameter data member (access function): outputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputLanguage
  // case: toBeCopied == COPY_DATA for outputLanguage
     SgFile::languageOption_enum outputLanguage_copy = p_outputLanguage; 
     result->p_outputLanguage = outputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): inputLanguage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for inputLanguage
  // case: toBeCopied == COPY_DATA for inputLanguage
     SgFile::languageOption_enum inputLanguage_copy = p_inputLanguage; 
     result->p_inputLanguage = inputLanguage_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithPath
     std::string sourceFileNameWithPath_copy = p_sourceFileNameWithPath; 
     result->p_sourceFileNameWithPath = sourceFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for sourceFileNameWithoutPath
     std::string sourceFileNameWithoutPath_copy = p_sourceFileNameWithoutPath; 
     result->p_sourceFileNameWithoutPath = sourceFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): unparse_output_filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_output_filename
  // case: toBeCopied == COPY_DATA for unparse_output_filename
     std::string unparse_output_filename_copy = p_unparse_output_filename; 
     result->p_unparse_output_filename = unparse_output_filename_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithPath
     std::string objectFileNameWithPath_copy = p_objectFileNameWithPath; 
     result->p_objectFileNameWithPath = objectFileNameWithPath_copy; 
  // Copy non-constructor parameter data member (access function): objectFileNameWithoutPath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameWithoutPath
  // case: toBeCopied == COPY_DATA for objectFileNameWithoutPath
     std::string objectFileNameWithoutPath_copy = p_objectFileNameWithoutPath; 
     result->p_objectFileNameWithoutPath = objectFileNameWithoutPath_copy; 
  // Copy non-constructor parameter data member (access function): useBackendOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useBackendOnly
  // case: toBeCopied == COPY_DATA for useBackendOnly
     bool useBackendOnly_copy = p_useBackendOnly; 
     result->p_useBackendOnly = useBackendOnly_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): savedFrontendCommandLine_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for savedFrontendCommandLine
  // case: toBeCopied == COPY_DATA for savedFrontendCommandLine
     std::string savedFrontendCommandLine_copy = p_savedFrontendCommandLine; 
     result->p_savedFrontendCommandLine = savedFrontendCommandLine_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_templates
  // case: toBeCopied == COPY_DATA for no_implicit_templates
     bool no_implicit_templates_copy = p_no_implicit_templates; 
     result->p_no_implicit_templates = no_implicit_templates_copy; 
  // Copy non-constructor parameter data member (access function): no_implicit_inline_templates_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_implicit_inline_templates
  // case: toBeCopied == COPY_DATA for no_implicit_inline_templates
     bool no_implicit_inline_templates_copy = p_no_implicit_inline_templates; 
     result->p_no_implicit_inline_templates = no_implicit_inline_templates_copy; 
  // Copy non-constructor parameter data member (access function): skip_commentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_commentsAndDirectives
  // case: toBeCopied == COPY_DATA for skip_commentsAndDirectives
     bool skip_commentsAndDirectives_copy = p_skip_commentsAndDirectives; 
     result->p_skip_commentsAndDirectives = skip_commentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): collectAllCommentsAndDirectives_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for collectAllCommentsAndDirectives
  // case: toBeCopied == COPY_DATA for collectAllCommentsAndDirectives
     bool collectAllCommentsAndDirectives_copy = p_collectAllCommentsAndDirectives; 
     result->p_collectAllCommentsAndDirectives = collectAllCommentsAndDirectives_copy; 
  // Copy non-constructor parameter data member (access function): translateCommentsAndDirectivesIntoAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for translateCommentsAndDirectivesIntoAST
  // case: toBeCopied == COPY_DATA for translateCommentsAndDirectivesIntoAST
     bool translateCommentsAndDirectivesIntoAST_copy = p_translateCommentsAndDirectivesIntoAST; 
     result->p_translateCommentsAndDirectivesIntoAST = translateCommentsAndDirectivesIntoAST_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFiles
  // case: toBeCopied == COPY_DATA for unparseHeaderFiles
     bool unparseHeaderFiles_copy = p_unparseHeaderFiles; 
     result->p_unparseHeaderFiles = unparseHeaderFiles_copy; 
  // Copy non-constructor parameter data member (access function): preprocessorDirectivesAndCommentsList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preprocessorDirectivesAndCommentsList
  // case: toBeCopied == COPY_DATA for preprocessorDirectivesAndCommentsList
     ROSEAttributesListContainerPtr preprocessorDirectivesAndCommentsList_copy = p_preprocessorDirectivesAndCommentsList; 
     result->p_preprocessorDirectivesAndCommentsList = preprocessorDirectivesAndCommentsList_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): KCC_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for KCC_frontend
  // case: toBeCopied == COPY_DATA for KCC_frontend
     bool KCC_frontend_copy = p_KCC_frontend; 
     result->p_KCC_frontend = KCC_frontend_copy; 
  // Copy non-constructor parameter data member (access function): new_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_frontend
  // case: toBeCopied == COPY_DATA for new_frontend
     bool new_frontend_copy = p_new_frontend; 
     result->p_new_frontend = new_frontend_copy; 
  // Copy non-constructor parameter data member (access function): disable_edg_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_edg_backend
  // case: toBeCopied == COPY_DATA for disable_edg_backend
     bool disable_edg_backend_copy = p_disable_edg_backend; 
     result->p_disable_edg_backend = disable_edg_backend_copy; 
  // Copy non-constructor parameter data member (access function): disable_sage_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for disable_sage_backend
  // case: toBeCopied == COPY_DATA for disable_sage_backend
     bool disable_sage_backend_copy = p_disable_sage_backend; 
     result->p_disable_sage_backend = disable_sage_backend_copy; 
  // Copy non-constructor parameter data member (access function): testingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for testingLevel
  // case: toBeCopied == COPY_DATA for testingLevel
     int testingLevel_copy = p_testingLevel; 
     result->p_testingLevel = testingLevel_copy; 
  // Copy non-constructor parameter data member (access function): preinit_il_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinit_il
  // case: toBeCopied == COPY_DATA for preinit_il
     bool preinit_il_copy = p_preinit_il; 
     result->p_preinit_il = preinit_il_copy; 
  // Copy non-constructor parameter data member (access function): enable_cp_backend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enable_cp_backend
  // case: toBeCopied == COPY_DATA for enable_cp_backend
     bool enable_cp_backend_copy = p_enable_cp_backend; 
     result->p_enable_cp_backend = enable_cp_backend_copy; 
  // Copy non-constructor parameter data member (access function): markGeneratedFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for markGeneratedFiles
  // case: toBeCopied == COPY_DATA for markGeneratedFiles
     bool markGeneratedFiles_copy = p_markGeneratedFiles; 
     result->p_markGeneratedFiles = markGeneratedFiles_copy; 
  // Copy non-constructor parameter data member (access function): negative_test_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for negative_test
  // case: toBeCopied == COPY_DATA for negative_test
     bool negative_test_copy = p_negative_test; 
     result->p_negative_test = negative_test_copy; 
  // Copy non-constructor parameter data member (access function): strict_language_handling_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strict_language_handling
  // case: toBeCopied == COPY_DATA for strict_language_handling
     bool strict_language_handling_copy = p_strict_language_handling; 
     result->p_strict_language_handling = strict_language_handling_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): embedColorCodesInGeneratedCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedColorCodesInGeneratedCode
  // case: toBeCopied == COPY_DATA for embedColorCodesInGeneratedCode
     int embedColorCodesInGeneratedCode_copy = p_embedColorCodesInGeneratedCode; 
     result->p_embedColorCodesInGeneratedCode = embedColorCodesInGeneratedCode_copy; 
  // Copy non-constructor parameter data member (access function): generateSourcePositionCodes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generateSourcePositionCodes
  // case: toBeCopied == COPY_DATA for generateSourcePositionCodes
     int generateSourcePositionCodes_copy = p_generateSourcePositionCodes; 
     result->p_generateSourcePositionCodes = generateSourcePositionCodes_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCppFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCppFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCppFileExtension
     bool sourceFileUsesCppFileExtension_copy = p_sourceFileUsesCppFileExtension; 
     result->p_sourceFileUsesCppFileExtension = sourceFileUsesCppFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortranFileExtension
     bool sourceFileUsesFortranFileExtension_copy = p_sourceFileUsesFortranFileExtension; 
     result->p_sourceFileUsesFortranFileExtension = sourceFileUsesFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran77FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran77FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran77FileExtension
     bool sourceFileUsesFortran77FileExtension_copy = p_sourceFileUsesFortran77FileExtension; 
     result->p_sourceFileUsesFortran77FileExtension = sourceFileUsesFortran77FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran90FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran90FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran90FileExtension
     bool sourceFileUsesFortran90FileExtension_copy = p_sourceFileUsesFortran90FileExtension; 
     result->p_sourceFileUsesFortran90FileExtension = sourceFileUsesFortran90FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran95FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran95FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran95FileExtension
     bool sourceFileUsesFortran95FileExtension_copy = p_sourceFileUsesFortran95FileExtension; 
     result->p_sourceFileUsesFortran95FileExtension = sourceFileUsesFortran95FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2003FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2003FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2003FileExtension
     bool sourceFileUsesFortran2003FileExtension_copy = p_sourceFileUsesFortran2003FileExtension; 
     result->p_sourceFileUsesFortran2003FileExtension = sourceFileUsesFortran2003FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesFortran2008FileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesFortran2008FileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesFortran2008FileExtension
     bool sourceFileUsesFortran2008FileExtension_copy = p_sourceFileUsesFortran2008FileExtension; 
     result->p_sourceFileUsesFortran2008FileExtension = sourceFileUsesFortran2008FileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCoArrayFortranFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCoArrayFortranFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCoArrayFortranFileExtension
     bool sourceFileUsesCoArrayFortranFileExtension_copy = p_sourceFileUsesCoArrayFortranFileExtension; 
     result->p_sourceFileUsesCoArrayFortranFileExtension = sourceFileUsesCoArrayFortranFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPHPFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPHPFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPHPFileExtension
     bool sourceFileUsesPHPFileExtension_copy = p_sourceFileUsesPHPFileExtension; 
     result->p_sourceFileUsesPHPFileExtension = sourceFileUsesPHPFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesPythonFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesPythonFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesPythonFileExtension
     bool sourceFileUsesPythonFileExtension_copy = p_sourceFileUsesPythonFileExtension; 
     result->p_sourceFileUsesPythonFileExtension = sourceFileUsesPythonFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJavaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJavaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJavaFileExtension
     bool sourceFileUsesJavaFileExtension_copy = p_sourceFileUsesJavaFileExtension; 
     result->p_sourceFileUsesJavaFileExtension = sourceFileUsesJavaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJvmFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJvmFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJvmFileExtension
     bool sourceFileUsesJvmFileExtension_copy = p_sourceFileUsesJvmFileExtension; 
     result->p_sourceFileUsesJvmFileExtension = sourceFileUsesJvmFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesBinaryFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesBinaryFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesBinaryFileExtension
     bool sourceFileUsesBinaryFileExtension_copy = p_sourceFileUsesBinaryFileExtension; 
     result->p_sourceFileUsesBinaryFileExtension = sourceFileUsesBinaryFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileTypeIsUnknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileTypeIsUnknown
  // case: toBeCopied == COPY_DATA for sourceFileTypeIsUnknown
     bool sourceFileTypeIsUnknown_copy = p_sourceFileTypeIsUnknown; 
     result->p_sourceFileTypeIsUnknown = sourceFileTypeIsUnknown_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesCsharpFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesCsharpFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesCsharpFileExtension
     bool sourceFileUsesCsharpFileExtension_copy = p_sourceFileUsesCsharpFileExtension; 
     result->p_sourceFileUsesCsharpFileExtension = sourceFileUsesCsharpFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesAdaFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesAdaFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesAdaFileExtension
     bool sourceFileUsesAdaFileExtension_copy = p_sourceFileUsesAdaFileExtension; 
     result->p_sourceFileUsesAdaFileExtension = sourceFileUsesAdaFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): sourceFileUsesJovialFileExtension_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileUsesJovialFileExtension
  // case: toBeCopied == COPY_DATA for sourceFileUsesJovialFileExtension
     bool sourceFileUsesJovialFileExtension_copy = p_sourceFileUsesJovialFileExtension; 
     result->p_sourceFileUsesJovialFileExtension = sourceFileUsesJovialFileExtension_copy; 
  // Copy non-constructor parameter data member (access function): detect_dangling_pointers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for detect_dangling_pointers
  // case: toBeCopied == COPY_DATA for detect_dangling_pointers
     int detect_dangling_pointers_copy = p_detect_dangling_pointers; 
     result->p_detect_dangling_pointers = detect_dangling_pointers_copy; 
  // Copy non-constructor parameter data member (access function): experimental_flang_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for experimental_flang_frontend
  // case: toBeCopied == COPY_DATA for experimental_flang_frontend
     bool experimental_flang_frontend_copy = p_experimental_flang_frontend; 
     result->p_experimental_flang_frontend = experimental_flang_frontend_copy; 
  // Copy non-constructor parameter data member (access function): read_executable_file_format_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_executable_file_format_only
  // case: toBeCopied == COPY_DATA for read_executable_file_format_only
     bool read_executable_file_format_only_copy = p_read_executable_file_format_only; 
     result->p_read_executable_file_format_only = read_executable_file_format_only_copy; 
  // Copy non-constructor parameter data member (access function): visualize_executable_file_format_skip_symbols_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_executable_file_format_skip_symbols
  // case: toBeCopied == COPY_DATA for visualize_executable_file_format_skip_symbols
     bool visualize_executable_file_format_skip_symbols_copy = p_visualize_executable_file_format_skip_symbols; 
     result->p_visualize_executable_file_format_skip_symbols = visualize_executable_file_format_skip_symbols_copy; 
  // Copy non-constructor parameter data member (access function): visualize_dwarf_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for visualize_dwarf_only
  // case: toBeCopied == COPY_DATA for visualize_dwarf_only
     bool visualize_dwarf_only_copy = p_visualize_dwarf_only; 
     result->p_visualize_dwarf_only = visualize_dwarf_only_copy; 
  // Copy non-constructor parameter data member (access function): read_instructions_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read_instructions_only
  // case: toBeCopied == COPY_DATA for read_instructions_only
     bool read_instructions_only_copy = p_read_instructions_only; 
     result->p_read_instructions_only = read_instructions_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_skip_unparse_asm_commands
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_unparse_asm_commands
  // case: toBeCopied == COPY_DATA for skip_unparse_asm_commands
     static bool skip_unparse_asm_commands_copy = p_skip_unparse_asm_commands; 
     result->p_skip_unparse_asm_commands = skip_unparse_asm_commands_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryArchiveObjectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryArchiveObjectFileNameList
  // case: toBeCopied == COPY_DATA for libraryArchiveObjectFileNameList
     SgStringList libraryArchiveObjectFileNameList_copy = p_libraryArchiveObjectFileNameList; 
     result->p_libraryArchiveObjectFileNameList = libraryArchiveObjectFileNameList_copy; 
  // Copy non-constructor parameter data member (access function): isLibraryArchive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isLibraryArchive
  // case: toBeCopied == COPY_DATA for isLibraryArchive
     bool isLibraryArchive_copy = p_isLibraryArchive; 
     result->p_isLibraryArchive = isLibraryArchive_copy; 
  // Copy non-constructor parameter data member (access function): isObjectFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isObjectFile
  // case: toBeCopied == COPY_DATA for isObjectFile
     bool isObjectFile_copy = p_isObjectFile; 
     result->p_isObjectFile = isObjectFile_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens
  // case: toBeCopied == COPY_DATA for unparse_tokens
     bool unparse_tokens_copy = p_unparse_tokens; 
     result->p_unparse_tokens = unparse_tokens_copy; 
  // Copy non-constructor parameter data member (access function): unparse_tokens_testing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_tokens_testing
  // case: toBeCopied == COPY_DATA for unparse_tokens_testing
     int unparse_tokens_testing_copy = p_unparse_tokens_testing; 
     result->p_unparse_tokens_testing = unparse_tokens_testing_copy; 
  // Copy non-constructor parameter data member (access function): unparse_using_leading_and_trailing_token_mappings_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_using_leading_and_trailing_token_mappings
  // case: toBeCopied == COPY_DATA for unparse_using_leading_and_trailing_token_mappings
     bool unparse_using_leading_and_trailing_token_mappings_copy = p_unparse_using_leading_and_trailing_token_mappings; 
     result->p_unparse_using_leading_and_trailing_token_mappings = unparse_using_leading_and_trailing_token_mappings_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): skipAstConsistancyTests_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipAstConsistancyTests
  // case: toBeCopied == COPY_DATA for skipAstConsistancyTests
     bool skipAstConsistancyTests_copy = p_skipAstConsistancyTests; 
     result->p_skipAstConsistancyTests = skipAstConsistancyTests_copy; 
  // Copy non-constructor parameter data member (access function): multifile_support_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for multifile_support
  // case: toBeCopied == COPY_DATA for multifile_support
     bool multifile_support_copy = p_multifile_support; 
     result->p_multifile_support = multifile_support_copy; 
  // Copy non-constructor parameter data member (access function): optimization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for optimization
  // case: toBeCopied == COPY_DATA for optimization
     bool optimization_copy = p_optimization; 
     result->p_optimization = optimization_copy; 
  // Copy non-constructor parameter data member (access function): use_token_stream_to_improve_source_position_info_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_token_stream_to_improve_source_position_info
  // case: toBeCopied == COPY_DATA for use_token_stream_to_improve_source_position_info
     bool use_token_stream_to_improve_source_position_info_copy = p_use_token_stream_to_improve_source_position_info; 
     result->p_use_token_stream_to_improve_source_position_info = use_token_stream_to_improve_source_position_info_copy; 
  // Copy non-constructor parameter data member (access function): suppress_variable_declaration_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppress_variable_declaration_normalization
  // case: toBeCopied == COPY_DATA for suppress_variable_declaration_normalization
     bool suppress_variable_declaration_normalization_copy = p_suppress_variable_declaration_normalization; 
     result->p_suppress_variable_declaration_normalization = suppress_variable_declaration_normalization_copy; 
  // Copy non-constructor parameter data member (access function): edg_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edg_il_to_graphviz
  // case: toBeCopied == COPY_DATA for edg_il_to_graphviz
     bool edg_il_to_graphviz_copy = p_edg_il_to_graphviz; 
     result->p_edg_il_to_graphviz = edg_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): clang_il_to_graphviz_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clang_il_to_graphviz
  // case: toBeCopied == COPY_DATA for clang_il_to_graphviz
     bool clang_il_to_graphviz_copy = p_clang_il_to_graphviz; 
     result->p_clang_il_to_graphviz = clang_il_to_graphviz_copy; 
  // Copy non-constructor parameter data member (access function): no_optimize_flag_for_frontend_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for no_optimize_flag_for_frontend
  // case: toBeCopied == COPY_DATA for no_optimize_flag_for_frontend
     bool no_optimize_flag_for_frontend_copy = p_no_optimize_flag_for_frontend; 
     result->p_no_optimize_flag_for_frontend = no_optimize_flag_for_frontend_copy; 
  // Copy non-constructor parameter data member (access function): unparse_edg_normalized_method_ROSE_1392_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_edg_normalized_method_ROSE_1392
  // case: toBeCopied == COPY_DATA for unparse_edg_normalized_method_ROSE_1392
     bool unparse_edg_normalized_method_ROSE_1392_copy = p_unparse_edg_normalized_method_ROSE_1392; 
     result->p_unparse_edg_normalized_method_ROSE_1392 = unparse_edg_normalized_method_ROSE_1392_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header_file_unparsing_optimization
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header_file_unparsing_optimization
  // case: toBeCopied == COPY_DATA for header_file_unparsing_optimization
     static bool header_file_unparsing_optimization_copy = p_header_file_unparsing_optimization; 
     result->p_header_file_unparsing_optimization = header_file_unparsing_optimization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standard
  // case: toBeCopied == COPY_DATA for standard
     SgFile::standard_enum standard_copy = p_standard; 
     result->p_standard = standard_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_gnu_standard
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_standard
  // case: toBeCopied == COPY_DATA for gnu_standard
     bool gnu_standard_copy = p_gnu_standard; 
     result->p_gnu_standard = gnu_standard_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparserErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparserErrorCode
  // case: toBeCopied == COPY_DATA for unparserErrorCode
     int unparserErrorCode_copy = p_unparserErrorCode; 
     result->p_unparserErrorCode = unparserErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendCompilerErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendCompilerErrorCode
  // case: toBeCopied == COPY_DATA for backendCompilerErrorCode
     int backendCompilerErrorCode_copy = p_backendCompilerErrorCode; 
     result->p_backendCompilerErrorCode = backendCompilerErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): unparsedFileFailedCompilation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsedFileFailedCompilation
  // case: toBeCopied == COPY_DATA for unparsedFileFailedCompilation
     bool unparsedFileFailedCompilation_copy = p_unparsedFileFailedCompilation; 
     result->p_unparsedFileFailedCompilation = unparsedFileFailedCompilation_copy; 
  // Copy non-constructor parameter data member (access function): genericFileList_copy
     SgAsmGenericFileList* genericFileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)genericFileList
          if (get_genericFileList() != nullptr) 
             { 
               genericFileList_copy = static_cast<SgAsmGenericFileList*>(help.copyAst(get_genericFileList())); 
             } 
            else 
             { 
               genericFileList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_genericFileList != nullptr) delete result->p_genericFileList; 
     result->p_genericFileList = genericFileList_copy; 
  // case: not a listType for (using conditionalToSetParent)genericFileList
        if ((genericFileList_copy != nullptr) && (genericFileList_copy->get_parent() == nullptr) && (isSgType(genericFileList_copy) == nullptr)) {
            genericFileList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): interpretations_copy
     SgAsmInterpretationList* interpretations_copy; 
  // case: not a listType for (using conditionalToCopyVariable)interpretations
          if (get_interpretations() != nullptr) 
             { 
               interpretations_copy = static_cast<SgAsmInterpretationList*>(help.copyAst(get_interpretations())); 
             } 
            else 
             { 
               interpretations_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_interpretations != nullptr) delete result->p_interpretations; 
     result->p_interpretations = interpretations_copy; 
  // case: not a listType for (using conditionalToSetParent)interpretations
        if ((interpretations_copy != nullptr) && (interpretations_copy->get_parent() == nullptr) && (isSgType(interpretations_copy) == nullptr)) {
            interpretations_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 6684 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProject::copy(SgCopyHelp& help) const {
    SgProject* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgProject();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): fileList_ptr_copy
     SgFileList* fileList_ptr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileList_ptr
          if (get_fileList_ptr() != nullptr) 
             { 
               fileList_ptr_copy = static_cast<SgFileList*>(help.copyAst(get_fileList_ptr())); 
             } 
            else 
             { 
               fileList_ptr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileList_ptr != nullptr) delete result->p_fileList_ptr; 
     result->p_fileList_ptr = fileList_ptr_copy; 
  // case: not a listType for (using conditionalToSetParent)fileList_ptr
        if ((fileList_ptr_copy != nullptr) && (fileList_ptr_copy->get_parent() == nullptr) && (isSgType(fileList_ptr_copy) == nullptr)) {
            fileList_ptr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalCommandLineArgumentList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalCommandLineArgumentList
  // case: toBeCopied == COPY_DATA for originalCommandLineArgumentList
     SgStringList originalCommandLineArgumentList_copy = p_originalCommandLineArgumentList; 
     result->p_originalCommandLineArgumentList = originalCommandLineArgumentList_copy; 
  // Copy non-constructor parameter data member (access function): frontendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendErrorCode
  // case: toBeCopied == COPY_DATA for frontendErrorCode
     int frontendErrorCode_copy = p_frontendErrorCode; 
     result->p_frontendErrorCode = frontendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): javacErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for javacErrorCode
  // case: toBeCopied == COPY_DATA for javacErrorCode
     int javacErrorCode_copy = p_javacErrorCode; 
     result->p_javacErrorCode = javacErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): ecjErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ecjErrorCode
  // case: toBeCopied == COPY_DATA for ecjErrorCode
     int ecjErrorCode_copy = p_ecjErrorCode; 
     result->p_ecjErrorCode = ecjErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): midendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for midendErrorCode
  // case: toBeCopied == COPY_DATA for midendErrorCode
     int midendErrorCode_copy = p_midendErrorCode; 
     result->p_midendErrorCode = midendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): backendErrorCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for backendErrorCode
  // case: toBeCopied == COPY_DATA for backendErrorCode
     int backendErrorCode_copy = p_backendErrorCode; 
     result->p_backendErrorCode = backendErrorCode_copy; 
  // Copy non-constructor parameter data member (access function): keep_going_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for keep_going
  // case: toBeCopied == COPY_DATA for keep_going
     bool keep_going_copy = p_keep_going; 
     result->p_keep_going = keep_going_copy; 
  // Copy non-constructor parameter data member (access function): unparser__clobber_input_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparser__clobber_input_file
  // case: toBeCopied == COPY_DATA for unparser__clobber_input_file
     bool unparser__clobber_input_file_copy = p_unparser__clobber_input_file; 
     result->p_unparser__clobber_input_file = unparser__clobber_input_file_copy; 
  // Copy non-constructor parameter data member (access function): outputFileName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputFileName
  // case: toBeCopied == COPY_DATA for outputFileName
     std::string outputFileName_copy = p_outputFileName; 
     result->p_outputFileName = outputFileName_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_sourceFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sourceFileNameList
  // case: toBeCopied == COPY_DATA for sourceFileNameList
     SgStringList sourceFileNameList_copy = p_sourceFileNameList; 
     result->p_sourceFileNameList = sourceFileNameList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_objectFileNameList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objectFileNameList
  // case: toBeCopied == COPY_DATA for objectFileNameList
     SgStringList objectFileNameList_copy = p_objectFileNameList; 
     result->p_objectFileNameList = objectFileNameList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryFileList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryFileList
  // case: toBeCopied == COPY_DATA for libraryFileList
     SgStringList libraryFileList_copy = p_libraryFileList; 
     result->p_libraryFileList = libraryFileList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_librarySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for librarySpecifierList
  // case: toBeCopied == COPY_DATA for librarySpecifierList
     SgStringList librarySpecifierList_copy = p_librarySpecifierList; 
     result->p_librarySpecifierList = librarySpecifierList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_libraryDirectorySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for libraryDirectorySpecifierList
  // case: toBeCopied == COPY_DATA for libraryDirectorySpecifierList
     SgStringList libraryDirectorySpecifierList_copy = p_libraryDirectorySpecifierList; 
     result->p_libraryDirectorySpecifierList = libraryDirectorySpecifierList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_includeDirectorySpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includeDirectorySpecifierList
  // case: toBeCopied == COPY_DATA for includeDirectorySpecifierList
     SgStringList includeDirectorySpecifierList_copy = p_includeDirectorySpecifierList; 
     result->p_includeDirectorySpecifierList = includeDirectorySpecifierList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_macroSpecifierList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for macroSpecifierList
  // case: toBeCopied == COPY_DATA for macroSpecifierList
     SgStringList macroSpecifierList_copy = p_macroSpecifierList; 
     result->p_macroSpecifierList = macroSpecifierList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_preincludeFileList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preincludeFileList
  // case: toBeCopied == COPY_DATA for preincludeFileList
     SgStringList preincludeFileList_copy = p_preincludeFileList; 
     result->p_preincludeFileList = preincludeFileList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_preincludeDirectoryList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preincludeDirectoryList
  // case: toBeCopied == COPY_DATA for preincludeDirectoryList
     SgStringList preincludeDirectoryList_copy = p_preincludeDirectoryList; 
     result->p_preincludeDirectoryList = preincludeDirectoryList_copy; 
  // Copy non-constructor parameter data member (access function): compileOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compileOnly
  // case: toBeCopied == COPY_DATA for compileOnly
     bool compileOnly_copy = p_compileOnly; 
     result->p_compileOnly = compileOnly_copy; 
  // Copy non-constructor parameter data member (access function): wave_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wave
  // case: toBeCopied == COPY_DATA for wave
     bool wave_copy = p_wave; 
     result->p_wave = wave_copy; 
  // Copy non-constructor parameter data member (access function): prelink_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prelink
  // case: toBeCopied == COPY_DATA for prelink
     bool prelink_copy = p_prelink; 
     result->p_prelink = prelink_copy; 
  // Copy non-constructor parameter data member (access function): template_instantiation_mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_instantiation_mode
  // case: toBeCopied == COPY_DATA for template_instantiation_mode
     SgProject::template_instantiation_enum template_instantiation_mode_copy = p_template_instantiation_mode; 
     result->p_template_instantiation_mode = template_instantiation_mode_copy; 
  // Copy non-constructor parameter data member (access function): ast_merge_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ast_merge
  // case: toBeCopied == COPY_DATA for ast_merge
     bool ast_merge_copy = p_ast_merge; 
     result->p_ast_merge = ast_merge_copy; 
  // Copy non-constructor parameter data member (access function): projectSpecificDatabaseFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for projectSpecificDatabaseFile
  // case: toBeCopied == COPY_DATA for projectSpecificDatabaseFile
     std::string projectSpecificDatabaseFile_copy = p_projectSpecificDatabaseFile; 
     result->p_projectSpecificDatabaseFile = projectSpecificDatabaseFile_copy; 
  // Copy non-constructor parameter data member (access function): C_PreprocessorOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_PreprocessorOnly
  // case: toBeCopied == COPY_DATA for C_PreprocessorOnly
     bool C_PreprocessorOnly_copy = p_C_PreprocessorOnly; 
     result->p_C_PreprocessorOnly = C_PreprocessorOnly_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): compilationPerformanceFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for compilationPerformanceFile
  // case: toBeCopied == COPY_DATA for compilationPerformanceFile
     std::string compilationPerformanceFile_copy = p_compilationPerformanceFile; 
     result->p_compilationPerformanceFile = compilationPerformanceFile_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_includePathList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includePathList
  // case: toBeCopied == COPY_DATA for includePathList
     SgStringList includePathList_copy = p_includePathList; 
     result->p_includePathList = includePathList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_excludePathList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excludePathList
  // case: toBeCopied == COPY_DATA for excludePathList
     SgStringList excludePathList_copy = p_excludePathList; 
     result->p_excludePathList = excludePathList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_includeFileList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includeFileList
  // case: toBeCopied == COPY_DATA for includeFileList
     SgStringList includeFileList_copy = p_includeFileList; 
     result->p_includeFileList = includeFileList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_excludeFileList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excludeFileList
  // case: toBeCopied == COPY_DATA for excludeFileList
     SgStringList excludeFileList_copy = p_excludeFileList; 
     result->p_excludeFileList = excludeFileList_copy; 
  // Copy non-constructor parameter data member (access function): binary_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binary_only
  // case: toBeCopied == COPY_DATA for binary_only
     bool binary_only_copy = p_binary_only; 
     result->p_binary_only = binary_only_copy; 
  // Copy non-constructor parameter data member (access function): dataBaseFilename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dataBaseFilename
  // case: toBeCopied == COPY_DATA for dataBaseFilename
     std::string dataBaseFilename_copy = p_dataBaseFilename; 
     result->p_dataBaseFilename = dataBaseFilename_copy; 
  // Copy non-constructor parameter data member (access function): directoryList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directoryList
  // case: toBeCopied == COPY_DATA for directoryList
     SgDirectoryList* directoryList_copy = p_directoryList; 
     result->p_directoryList = directoryList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_C_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C_only
  // case: toBeCopied == COPY_DATA for C_only
     bool C_only_copy = p_C_only; 
     result->p_C_only = C_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Cxx_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx_only
  // case: toBeCopied == COPY_DATA for Cxx_only
     bool Cxx_only_copy = p_Cxx_only; 
     result->p_Cxx_only = Cxx_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_C11_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C11_only
  // case: toBeCopied == COPY_DATA for C11_only
     bool C11_only_copy = p_C11_only; 
     result->p_C11_only = C11_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Cxx0x_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx0x_only
  // case: toBeCopied == COPY_DATA for Cxx0x_only
     bool Cxx0x_only_copy = p_Cxx0x_only; 
     result->p_Cxx0x_only = Cxx0x_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Cxx11_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx11_only
  // case: toBeCopied == COPY_DATA for Cxx11_only
     bool Cxx11_only_copy = p_Cxx11_only; 
     result->p_Cxx11_only = Cxx11_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_C14_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for C14_only
  // case: toBeCopied == COPY_DATA for C14_only
     bool C14_only_copy = p_C14_only; 
     result->p_C14_only = C14_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Cxx14_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Cxx14_only
  // case: toBeCopied == COPY_DATA for Cxx14_only
     bool Cxx14_only_copy = p_Cxx14_only; 
     result->p_Cxx14_only = Cxx14_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Fortran_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_only
  // case: toBeCopied == COPY_DATA for Fortran_only
     bool Fortran_only_copy = p_Fortran_only; 
     result->p_Fortran_only = Fortran_only_copy; 
  // Copy non-constructor parameter data member (access function): Fortran_ofp_jvm_options_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Fortran_ofp_jvm_options
  // case: toBeCopied == COPY_DATA for Fortran_ofp_jvm_options
     std::list<std::string> Fortran_ofp_jvm_options_copy = p_Fortran_ofp_jvm_options; 
     result->p_Fortran_ofp_jvm_options = Fortran_ofp_jvm_options_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Java_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_only
  // case: toBeCopied == COPY_DATA for Java_only
     bool Java_only_copy = p_Java_only; 
     result->p_Java_only = Java_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Jvm_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jvm_only
  // case: toBeCopied == COPY_DATA for Jvm_only
     bool Jvm_only_copy = p_Jvm_only; 
     result->p_Jvm_only = Jvm_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Jovial_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Jovial_only
  // case: toBeCopied == COPY_DATA for Jovial_only
     bool Jovial_only_copy = p_Jovial_only; 
     result->p_Jovial_only = Jovial_only_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Ada_only
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Ada_only
  // case: toBeCopied == COPY_DATA for Ada_only
     bool Ada_only_copy = p_Ada_only; 
     result->p_Ada_only = Ada_only_copy; 
  // Copy non-constructor parameter data member (access function): openmp_linking_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for openmp_linking
  // case: toBeCopied == COPY_DATA for openmp_linking
     bool openmp_linking_copy = p_openmp_linking; 
     result->p_openmp_linking = openmp_linking_copy; 
  // Copy non-constructor parameter data member (access function): Java_ecj_jvm_options_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_ecj_jvm_options
  // case: toBeCopied == COPY_DATA for Java_ecj_jvm_options
     std::list<std::string> Java_ecj_jvm_options_copy = p_Java_ecj_jvm_options; 
     result->p_Java_ecj_jvm_options = Java_ecj_jvm_options_copy; 
  // Copy non-constructor parameter data member (access function): Java_batch_mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_batch_mode
  // case: toBeCopied == COPY_DATA for Java_batch_mode
     bool Java_batch_mode_copy = p_Java_batch_mode; 
     result->p_Java_batch_mode = Java_batch_mode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Java_classpath
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_classpath
  // case: toBeCopied == COPY_DATA for Java_classpath
     std::list<std::string> Java_classpath_copy = p_Java_classpath; 
     result->p_Java_classpath = Java_classpath_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Java_sourcepath
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_sourcepath
  // case: toBeCopied == COPY_DATA for Java_sourcepath
     std::list<std::string> Java_sourcepath_copy = p_Java_sourcepath; 
     result->p_Java_sourcepath = Java_sourcepath_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Java_destdir
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_destdir
  // case: toBeCopied == COPY_DATA for Java_destdir
     std::string Java_destdir_copy = p_Java_destdir; 
     result->p_Java_destdir = Java_destdir_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Java_source_destdir
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_source_destdir
  // case: toBeCopied == COPY_DATA for Java_source_destdir
     std::string Java_source_destdir_copy = p_Java_source_destdir; 
     result->p_Java_source_destdir = Java_source_destdir_copy; 
  // Copy non-constructor parameter data member (access function): Java_s_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_s
  // case: toBeCopied == COPY_DATA for Java_s
     std::string Java_s_copy = p_Java_s; 
     result->p_Java_s = Java_s_copy; 
  // Copy non-constructor parameter data member (access function): Java_source_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_source
  // case: toBeCopied == COPY_DATA for Java_source
     std::string Java_source_copy = p_Java_source; 
     result->p_Java_source = Java_source_copy; 
  // Copy non-constructor parameter data member (access function): Java_target_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_target
  // case: toBeCopied == COPY_DATA for Java_target
     std::string Java_target_copy = p_Java_target; 
     result->p_Java_target = Java_target_copy; 
  // Copy non-constructor parameter data member (access function): Java_encoding_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_encoding
  // case: toBeCopied == COPY_DATA for Java_encoding
     std::string Java_encoding_copy = p_Java_encoding; 
     result->p_Java_encoding = Java_encoding_copy; 
  // Copy non-constructor parameter data member (access function): Java_g_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_g
  // case: toBeCopied == COPY_DATA for Java_g
     std::string Java_g_copy = p_Java_g; 
     result->p_Java_g = Java_g_copy; 
  // Copy non-constructor parameter data member (access function): Java_nowarn_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_nowarn
  // case: toBeCopied == COPY_DATA for Java_nowarn
     bool Java_nowarn_copy = p_Java_nowarn; 
     result->p_Java_nowarn = Java_nowarn_copy; 
  // Copy non-constructor parameter data member (access function): Java_verbose_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_verbose
  // case: toBeCopied == COPY_DATA for Java_verbose
     bool Java_verbose_copy = p_Java_verbose; 
     result->p_Java_verbose = Java_verbose_copy; 
  // Copy non-constructor parameter data member (access function): Java_deprecation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_deprecation
  // case: toBeCopied == COPY_DATA for Java_deprecation
     bool Java_deprecation_copy = p_Java_deprecation; 
     result->p_Java_deprecation = Java_deprecation_copy; 
  // Copy non-constructor parameter data member (access function): Java_bootclasspath_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Java_bootclasspath
  // case: toBeCopied == COPY_DATA for Java_bootclasspath
     std::list<std::string> Java_bootclasspath_copy = p_Java_bootclasspath; 
     result->p_Java_bootclasspath = Java_bootclasspath_copy; 
  // Copy non-constructor parameter data member (access function): addCppDirectivesToAST_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addCppDirectivesToAST
  // case: toBeCopied == COPY_DATA for addCppDirectivesToAST
     bool addCppDirectivesToAST_copy = p_addCppDirectivesToAST; 
     result->p_addCppDirectivesToAST = addCppDirectivesToAST_copy; 
  // Copy non-constructor parameter data member (access function): includingPreprocessingInfosMap_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for includingPreprocessingInfosMap
  // case: toBeCopied == COPY_DATA for includingPreprocessingInfosMap
     std::map<std::string, std::set<PreprocessingInfo*> > includingPreprocessingInfosMap_copy = p_includingPreprocessingInfosMap; 
     result->p_includingPreprocessingInfosMap = includingPreprocessingInfosMap_copy; 
  // Copy non-constructor parameter data member (access function): quotedIncludesSearchPaths_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for quotedIncludesSearchPaths
  // case: toBeCopied == COPY_DATA for quotedIncludesSearchPaths
     std::list<std::string> quotedIncludesSearchPaths_copy = p_quotedIncludesSearchPaths; 
     result->p_quotedIncludesSearchPaths = quotedIncludesSearchPaths_copy; 
  // Copy non-constructor parameter data member (access function): bracketedIncludesSearchPaths_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bracketedIncludesSearchPaths
  // case: toBeCopied == COPY_DATA for bracketedIncludesSearchPaths
     std::list<std::string> bracketedIncludesSearchPaths_copy = p_bracketedIncludesSearchPaths; 
     result->p_bracketedIncludesSearchPaths = bracketedIncludesSearchPaths_copy; 
  // Copy non-constructor parameter data member (access function): unparseHeaderFilesRootFolder_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparseHeaderFilesRootFolder
  // case: toBeCopied == COPY_DATA for unparseHeaderFilesRootFolder
     std::string unparseHeaderFilesRootFolder_copy = p_unparseHeaderFilesRootFolder; 
     result->p_unparseHeaderFilesRootFolder = unparseHeaderFilesRootFolder_copy; 
  // Copy non-constructor parameter data member (access function): frontendConstantFolding_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frontendConstantFolding
  // case: toBeCopied == COPY_DATA for frontendConstantFolding
     SgProject::constant_folding_enum frontendConstantFolding_copy = p_frontendConstantFolding; 
     result->p_frontendConstantFolding = frontendConstantFolding_copy; 
  // Copy non-constructor parameter data member (access function): globalScopeAcrossFiles_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for globalScopeAcrossFiles
  // case: toBeCopied == COPY_DATA for globalScopeAcrossFiles
     SgGlobal* globalScopeAcrossFiles_copy = p_globalScopeAcrossFiles; 
     result->p_globalScopeAcrossFiles = globalScopeAcrossFiles_copy; 
  // Copy non-constructor parameter data member (access function): unparse_in_same_directory_as_input_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_in_same_directory_as_input_file
  // case: toBeCopied == COPY_DATA for unparse_in_same_directory_as_input_file
     bool unparse_in_same_directory_as_input_file_copy = p_unparse_in_same_directory_as_input_file; 
     result->p_unparse_in_same_directory_as_input_file = unparse_in_same_directory_as_input_file_copy; 
  // Copy non-constructor parameter data member (access function): stop_after_compilation_do_not_assemble_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stop_after_compilation_do_not_assemble_file
  // case: toBeCopied == COPY_DATA for stop_after_compilation_do_not_assemble_file
     bool stop_after_compilation_do_not_assemble_file_copy = p_stop_after_compilation_do_not_assemble_file; 
     result->p_stop_after_compilation_do_not_assemble_file = stop_after_compilation_do_not_assemble_file_copy; 
  // Copy non-constructor parameter data member (access function): gnuOptionForUndefinedSymbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnuOptionForUndefinedSymbol
  // case: toBeCopied == COPY_DATA for gnuOptionForUndefinedSymbol
     std::string gnuOptionForUndefinedSymbol_copy = p_gnuOptionForUndefinedSymbol; 
     result->p_gnuOptionForUndefinedSymbol = gnuOptionForUndefinedSymbol_copy; 
  // Copy non-constructor parameter data member (access function): mode_32_bit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode_32_bit
  // case: toBeCopied == COPY_DATA for mode_32_bit
     bool mode_32_bit_copy = p_mode_32_bit; 
     result->p_mode_32_bit = mode_32_bit_copy; 
  // Copy non-constructor parameter data member (access function): noclobber_output_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for noclobber_output_file
  // case: toBeCopied == COPY_DATA for noclobber_output_file
     bool noclobber_output_file_copy = p_noclobber_output_file; 
     result->p_noclobber_output_file = noclobber_output_file_copy; 
  // Copy non-constructor parameter data member (access function): noclobber_if_different_output_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for noclobber_if_different_output_file
  // case: toBeCopied == COPY_DATA for noclobber_if_different_output_file
     bool noclobber_if_different_output_file_copy = p_noclobber_if_different_output_file; 
     result->p_noclobber_if_different_output_file = noclobber_if_different_output_file_copy; 
  // Copy non-constructor parameter data member (access function): suppressConstantFoldingPostProcessing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppressConstantFoldingPostProcessing
  // case: toBeCopied == COPY_DATA for suppressConstantFoldingPostProcessing
     bool suppressConstantFoldingPostProcessing_copy = p_suppressConstantFoldingPostProcessing; 
     result->p_suppressConstantFoldingPostProcessing = suppressConstantFoldingPostProcessing_copy; 
  // Copy non-constructor parameter data member (access function): suppressNameQualificationAcrossWholeTranslationUnit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for suppressNameQualificationAcrossWholeTranslationUnit
  // case: toBeCopied == COPY_DATA for suppressNameQualificationAcrossWholeTranslationUnit
     bool suppressNameQualificationAcrossWholeTranslationUnit_copy = p_suppressNameQualificationAcrossWholeTranslationUnit; 
     result->p_suppressNameQualificationAcrossWholeTranslationUnit = suppressNameQualificationAcrossWholeTranslationUnit_copy; 
  // Copy non-constructor parameter data member (access function): appendPID_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for appendPID
  // case: toBeCopied == COPY_DATA for appendPID
     bool appendPID_copy = p_appendPID; 
     result->p_appendPID = appendPID_copy; 
  // Copy non-constructor parameter data member (access function): reportOnHeaderFileUnparsing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reportOnHeaderFileUnparsing
  // case: toBeCopied == COPY_DATA for reportOnHeaderFileUnparsing
     bool reportOnHeaderFileUnparsing_copy = p_reportOnHeaderFileUnparsing; 
     result->p_reportOnHeaderFileUnparsing = reportOnHeaderFileUnparsing_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_applicationRootDirectory
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for applicationRootDirectory
  // case: toBeCopied == COPY_DATA for applicationRootDirectory
     std::string applicationRootDirectory_copy = p_applicationRootDirectory; 
     result->p_applicationRootDirectory = applicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): usingApplicationRootDirectory_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usingApplicationRootDirectory
  // case: toBeCopied == COPY_DATA for usingApplicationRootDirectory
     bool usingApplicationRootDirectory_copy = p_usingApplicationRootDirectory; 
     result->p_usingApplicationRootDirectory = usingApplicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): usingDeferredTransformations_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usingDeferredTransformations
  // case: toBeCopied == COPY_DATA for usingDeferredTransformations
     bool usingDeferredTransformations_copy = p_usingDeferredTransformations; 
     result->p_usingDeferredTransformations = usingDeferredTransformations_copy; 
  // Copy non-constructor parameter data member (access function): astfile_out_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for astfile_out
  // case: toBeCopied == COPY_DATA for astfile_out
     std::string astfile_out_copy = p_astfile_out; 
     result->p_astfile_out = astfile_out_copy; 
  // Copy non-constructor parameter data member (access function): astfiles_in_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for astfiles_in
  // case: toBeCopied == COPY_DATA for astfiles_in
     std::list<std::string> astfiles_in_copy = p_astfiles_in; 
     result->p_astfiles_in = astfiles_in_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierBeforeList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierBeforeList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierBeforeList
     SgStringList extraIncludeDirectorySpecifierBeforeList_copy = p_extraIncludeDirectorySpecifierBeforeList; 
     result->p_extraIncludeDirectorySpecifierBeforeList = extraIncludeDirectorySpecifierBeforeList_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_extraIncludeDirectorySpecifierAfterList()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extraIncludeDirectorySpecifierAfterList
  // case: toBeCopied == COPY_DATA for extraIncludeDirectorySpecifierAfterList
     SgStringList extraIncludeDirectorySpecifierAfterList_copy = p_extraIncludeDirectorySpecifierAfterList; 
     result->p_extraIncludeDirectorySpecifierAfterList = extraIncludeDirectorySpecifierAfterList_copy; 
  // Copy non-constructor parameter data member (access function): skip_post_processing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skip_post_processing
  // case: toBeCopied == COPY_DATA for skip_post_processing
     bool skip_post_processing_copy = p_skip_post_processing; 
     result->p_skip_post_processing = skip_post_processing_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 7244 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOptions::copy(SgCopyHelp& help) const {
    SgOptions* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOptions();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_default_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_output
  // case: toBeCopied == COPY_DATA for default_output
     std::ostream* default_output_copy = p_default_output; 
     result->p_default_output = default_output_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_debug_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for debug_output
  // case: toBeCopied == COPY_DATA for debug_output
     std::ostream* debug_output_copy = p_debug_output; 
     result->p_debug_output = debug_output_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_error_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for error_output
  // case: toBeCopied == COPY_DATA for error_output
     std::ostream* error_output_copy = p_error_output; 
     result->p_error_output = error_output_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_logging_output
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for logging_output
  // case: toBeCopied == COPY_DATA for logging_output
     std::ostream* logging_output_copy = p_logging_output; 
     result->p_logging_output = logging_output_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_debug_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for debug_level
  // case: toBeCopied == COPY_DATA for debug_level
     int debug_level_copy = p_debug_level; 
     result->p_debug_level = debug_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_logging_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for logging_level
  // case: toBeCopied == COPY_DATA for logging_level
     int logging_level_copy = p_logging_level; 
     result->p_logging_level = logging_level_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 7365 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnparse_Info::copy(SgCopyHelp& help) const {
    SgUnparse_Info* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnparse_Info();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_unparse_attribute
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_attribute
  // case: toBeCopied == COPY_DATA for unparse_attribute
     SgBitVector unparse_attribute_copy = p_unparse_attribute; 
     result->p_unparse_attribute = unparse_attribute_copy; 
  // Copy non-constructor parameter data member (access function): access_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_attribute
  // case: toBeCopied == COPY_DATA for access_attribute
     int access_attribute_copy = p_access_attribute; 
     result->p_access_attribute = access_attribute_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nested_expression
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nested_expression
  // case: toBeCopied == COPY_DATA for nested_expression
     int nested_expression_copy = p_nested_expression; 
     result->p_nested_expression = nested_expression_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operator_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     std::string operator_name_copy = p_operator_name; 
     result->p_operator_name = operator_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_var_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for var_name
  // case: toBeCopied == COPY_DATA for var_name
     SgName var_name_copy = p_var_name; 
     result->p_var_name = var_name_copy; 
  // Copy non-constructor parameter data member (access function): declstatement_ptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declstatement_ptr
  // case: toBeCopied == COPY_DATA for declstatement_ptr
     SgDeclarationStatement* declstatement_ptr_copy = p_declstatement_ptr; 
     result->p_declstatement_ptr = declstatement_ptr_copy; 
  // Copy non-constructor parameter data member (access function): declaration_of_context_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration_of_context
  // case: toBeCopied == COPY_DATA for declaration_of_context
     SgDeclarationStatement* declaration_of_context_copy = p_declaration_of_context; 
     result->p_declaration_of_context = declaration_of_context_copy; 
  // Copy non-constructor parameter data member (access function): current_context_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_context
  // case: toBeCopied == COPY_DATA for current_context
     SgNamedType* current_context_copy = p_current_context; 
     result->p_current_context = current_context_copy; 
  // Copy non-constructor parameter data member (access function): array_index_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_index_list
  // case: toBeCopied == COPY_DATA for array_index_list
     SgName array_index_list_copy = p_array_index_list; 
     result->p_array_index_list = array_index_list_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_structureTagProcessingList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structureTagProcessingList
  // case: toBeCopied == COPY_DATA for structureTagProcessingList
     static SgTypePtrList structureTagProcessingList_copy = p_structureTagProcessingList; 
     result->p_structureTagProcessingList = structureTagProcessingList_copy; 
  // Copy non-constructor parameter data member (access function): current_namespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_namespace
  // case: toBeCopied == COPY_DATA for current_namespace
     SgNamespaceDeclarationStatement* current_namespace_copy = p_current_namespace; 
     result->p_current_namespace = current_namespace_copy; 
  // Copy non-constructor parameter data member (access function): outputCodeGenerationFormatDelimiters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for outputCodeGenerationFormatDelimiters
  // case: toBeCopied == COPY_DATA for outputCodeGenerationFormatDelimiters
     bool outputCodeGenerationFormatDelimiters_copy = p_outputCodeGenerationFormatDelimiters; 
     result->p_outputCodeGenerationFormatDelimiters = outputCodeGenerationFormatDelimiters_copy; 
  // Copy non-constructor parameter data member (access function): qualifiedNameList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): current_function_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_function_call
  // case: toBeCopied == COPY_DATA for current_function_call
     SgFunctionCallExp* current_function_call_copy = p_current_function_call; 
     result->p_current_function_call = current_function_call_copy; 
  // Copy non-constructor parameter data member (access function): current_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_scope
  // case: toBeCopied == COPY_DATA for current_scope
     SgScopeStatement* current_scope_copy = p_current_scope; 
     result->p_current_scope = current_scope_copy; 
  // Copy non-constructor parameter data member (access function): reference_node_for_qualification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_node_for_qualification
  // case: toBeCopied == COPY_DATA for reference_node_for_qualification
     SgNode* reference_node_for_qualification_copy = p_reference_node_for_qualification; 
     result->p_reference_node_for_qualification = reference_node_for_qualification_copy; 
  // Copy non-constructor parameter data member (access function): name_qualification_length_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
  // Copy non-constructor parameter data member (access function): type_elaboration_required_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
  // Copy non-constructor parameter data member (access function): global_qualification_required_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): nestingLevel_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nestingLevel
  // case: toBeCopied == COPY_DATA for nestingLevel
     int nestingLevel_copy = p_nestingLevel; 
     result->p_nestingLevel = nestingLevel_copy; 
  // Copy non-constructor parameter data member (access function): language_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for language
  // case: toBeCopied == COPY_DATA for language
     SgFile::languageOption_enum language_copy = p_language; 
     result->p_language = language_copy; 
  // Copy non-constructor parameter data member (access function): current_source_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for current_source_file
  // case: toBeCopied == COPY_DATA for current_source_file
     SgSourceFile* current_source_file_copy = p_current_source_file; 
     result->p_current_source_file = current_source_file_copy; 
  // Copy non-constructor parameter data member (access function): use_generated_name_for_template_arguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_generated_name_for_template_arguments
  // case: toBeCopied == COPY_DATA for use_generated_name_for_template_arguments
     bool use_generated_name_for_template_arguments_copy = p_use_generated_name_for_template_arguments; 
     result->p_use_generated_name_for_template_arguments = use_generated_name_for_template_arguments_copy; 
  // Copy non-constructor parameter data member (access function): user_defined_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for user_defined_literal
  // case: toBeCopied == COPY_DATA for user_defined_literal
     bool user_defined_literal_copy = p_user_defined_literal; 
     result->p_user_defined_literal = user_defined_literal_copy; 
  // Copy non-constructor parameter data member (access function): declstatement_associated_with_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declstatement_associated_with_type
  // case: toBeCopied == COPY_DATA for declstatement_associated_with_type
     SgDeclarationStatement* declstatement_associated_with_type_copy = p_declstatement_associated_with_type; 
     result->p_declstatement_associated_with_type = declstatement_associated_with_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_extern_C_with_braces
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extern_C_with_braces
  // case: toBeCopied == COPY_DATA for extern_C_with_braces
     static bool extern_C_with_braces_copy = p_extern_C_with_braces; 
     result->p_extern_C_with_braces = extern_C_with_braces_copy; 
  // Copy non-constructor parameter data member (access function): context_for_added_parentheses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for context_for_added_parentheses
  // case: toBeCopied == COPY_DATA for context_for_added_parentheses
     bool context_for_added_parentheses_copy = p_context_for_added_parentheses; 
     result->p_context_for_added_parentheses = context_for_added_parentheses_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 7591 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBaseClass::copy(SgCopyHelp& help) const {
    SgBaseClass* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != nullptr) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBaseClass( base_class_copy, isDirectBaseClass_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = nullptr; 
     if ( p_baseClassModifier != nullptr ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != nullptr) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class
        if ((base_class_copy != nullptr) && (base_class_copy->get_parent() == nullptr) && (isSgType(base_class_copy) == nullptr)) {
            base_class_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 7733 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpBaseClass::copy(SgCopyHelp& help) const {
    SgExpBaseClass* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != nullptr) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
    // Copy constructor parameter data member: base_class_exp_copy
     SgExpression* base_class_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class_exp
          if (get_base_class_exp() != nullptr) 
             { 
               base_class_exp_copy = static_cast<SgExpression*>(help.copyAst(get_base_class_exp())); 
             } 
            else 
             { 
               base_class_exp_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExpBaseClass( base_class_copy, isDirectBaseClass_copy, base_class_exp_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = nullptr; 
     if ( p_baseClassModifier != nullptr ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != nullptr) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class_exp
        if ((base_class_exp_copy != nullptr) && (base_class_exp_copy->get_parent() == nullptr) && (isSgType(base_class_exp_copy) == nullptr)) {
            base_class_exp_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)base_class
        if ((base_class_copy != nullptr) && (base_class_copy->get_parent() == nullptr) && (isSgType(base_class_copy) == nullptr)) {
            base_class_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 7890 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealBaseClass::copy(SgCopyHelp& help) const {
    SgNonrealBaseClass* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_class_copy
     SgClassDeclaration* base_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class
          if (get_base_class() != nullptr) 
             { 
               base_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_base_class())); 
             } 
            else 
             { 
               base_class_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isDirectBaseClass_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDirectBaseClass
  // case: toBeCopied == COPY_DATA for isDirectBaseClass
     bool isDirectBaseClass_copy = p_isDirectBaseClass; 
    // Copy constructor parameter data member: base_class_nonreal_copy
     SgNonrealDecl* base_class_nonreal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_class_nonreal
          if (get_base_class_nonreal() != nullptr) 
             { 
               base_class_nonreal_copy = static_cast<SgNonrealDecl*>(help.copyAst(get_base_class_nonreal())); 
             } 
            else 
             { 
               base_class_nonreal_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonrealBaseClass( base_class_copy, isDirectBaseClass_copy, base_class_nonreal_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): baseClassModifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseClassModifier
  // case: toBeCopied == CLONE_PTR for baseClassModifier
     SgBaseClassModifier* baseClassModifier_copy = nullptr; 
     if ( p_baseClassModifier != nullptr ) 
        { 
          baseClassModifier_copy = new SgBaseClassModifier( *p_baseClassModifier); 
        } 
       else 
        { 
          baseClassModifier_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseClassModifier != nullptr) delete result->p_baseClassModifier; 
  /* add assignment to result here */ 
     result->p_baseClassModifier = baseClassModifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)base_class_nonreal
        if ((base_class_nonreal_copy != nullptr) && (base_class_nonreal_copy->get_parent() == nullptr) && (isSgType(base_class_nonreal_copy) == nullptr)) {
            base_class_nonreal_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)base_class
        if ((base_class_copy != nullptr) && (base_class_copy->get_parent() == nullptr) && (isSgType(base_class_copy) == nullptr)) {
            base_class_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8047 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefSeq::copy(SgCopyHelp& help) const {
    SgTypedefSeq* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypedefSeq();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_typedefs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypePtrList typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8143 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameter::copy(SgCopyHelp& help) const {
    SgTemplateParameter* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: parameterType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterType
  // case: toBeCopied == COPY_DATA for parameterType
     SgTemplateParameter::template_parameter_enum parameterType_copy = p_parameterType; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
    // Copy constructor parameter data member: defaultTypeParameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for defaultTypeParameter
  // case: toBeCopied == COPY_DATA for defaultTypeParameter
     SgType* defaultTypeParameter_copy = p_defaultTypeParameter; 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: defaultExpressionParameter_copy
     SgExpression* defaultExpressionParameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)defaultExpressionParameter
          if (get_defaultExpressionParameter() != nullptr) 
             { 
               defaultExpressionParameter_copy = static_cast<SgExpression*>(help.copyAst(get_defaultExpressionParameter())); 
             } 
            else 
             { 
               defaultExpressionParameter_copy = nullptr; 
             } 
    // Copy constructor parameter data member: templateDeclaration_copy
     SgDeclarationStatement* templateDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)templateDeclaration
          if (get_templateDeclaration() != nullptr) 
             { 
               templateDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_templateDeclaration())); 
             } 
            else 
             { 
               templateDeclaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: defaultTemplateDeclarationParameter_copy
     SgDeclarationStatement* defaultTemplateDeclarationParameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)defaultTemplateDeclarationParameter
          if (get_defaultTemplateDeclarationParameter() != nullptr) 
             { 
               defaultTemplateDeclarationParameter_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_defaultTemplateDeclarationParameter())); 
             } 
            else 
             { 
               defaultTemplateDeclarationParameter_copy = nullptr; 
             } 
    // Copy constructor parameter data member: initializedName_copy
     SgInitializedName* initializedName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializedName
          if (get_initializedName() != nullptr) 
             { 
               initializedName_copy = static_cast<SgInitializedName*>(help.copyAst(get_initializedName())); 
             } 
            else 
             { 
               initializedName_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateParameter( parameterType_copy, type_copy, defaultTypeParameter_copy, expression_copy, defaultExpressionParameter_copy, templateDeclaration_copy, defaultTemplateDeclarationParameter_copy, initializedName_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)defaultExpressionParameter
        if ((defaultExpressionParameter_copy != nullptr) && (defaultExpressionParameter_copy->get_parent() == nullptr) && (isSgType(defaultExpressionParameter_copy) == nullptr)) {
            defaultExpressionParameter_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)templateDeclaration
        if ((templateDeclaration_copy != nullptr) && (templateDeclaration_copy->get_parent() == nullptr) && (isSgType(templateDeclaration_copy) == nullptr)) {
            templateDeclaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)defaultTemplateDeclarationParameter
        if ((defaultTemplateDeclarationParameter_copy != nullptr) && (defaultTemplateDeclarationParameter_copy->get_parent() == nullptr) && (isSgType(defaultTemplateDeclarationParameter_copy) == nullptr)) {
            defaultTemplateDeclarationParameter_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)initializedName
        if ((initializedName_copy != nullptr) && (initializedName_copy->get_parent() == nullptr) && (isSgType(initializedName_copy) == nullptr)) {
            initializedName_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8322 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateArgument::copy(SgCopyHelp& help) const {
    SgTemplateArgument* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: argumentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argumentType
  // case: toBeCopied == COPY_DATA for argumentType
     SgTemplateArgument::template_argument_enum argumentType_copy = p_argumentType; 
    // Copy constructor parameter data member: isArrayBoundUnknownType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isArrayBoundUnknownType
  // case: toBeCopied == COPY_DATA for isArrayBoundUnknownType
     bool isArrayBoundUnknownType_copy = p_isArrayBoundUnknownType; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: templateDeclaration_copy
     SgDeclarationStatement* templateDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)templateDeclaration
          if (get_templateDeclaration() != nullptr) 
             { 
               templateDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_templateDeclaration())); 
             } 
            else 
             { 
               templateDeclaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: explicitlySpecified_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicitlySpecified
  // case: toBeCopied == COPY_DATA for explicitlySpecified
     bool explicitlySpecified_copy = p_explicitlySpecified; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateArgument( argumentType_copy, isArrayBoundUnknownType_copy, type_copy, expression_copy, templateDeclaration_copy, explicitlySpecified_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): unparsable_type_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparsable_type_alias
  // case: toBeCopied == COPY_DATA for unparsable_type_alias
     SgType* unparsable_type_alias_copy = p_unparsable_type_alias; 
     result->p_unparsable_type_alias = unparsable_type_alias_copy; 
  // Copy non-constructor parameter data member (access function): initializedName_copy
     SgInitializedName* initializedName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializedName
          if (get_initializedName() != nullptr) 
             { 
               initializedName_copy = static_cast<SgInitializedName*>(help.copyAst(get_initializedName())); 
             } 
            else 
             { 
               initializedName_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_initializedName != nullptr) delete result->p_initializedName; 
     result->p_initializedName = initializedName_copy; 
  // case: not a listType for (using conditionalToSetParent)initializedName
        if ((initializedName_copy != nullptr) && (initializedName_copy->get_parent() == nullptr) && (isSgType(initializedName_copy) == nullptr)) {
            initializedName_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_type
     bool type_elaboration_required_for_type_copy = p_type_elaboration_required_for_type; 
     result->p_type_elaboration_required_for_type = type_elaboration_required_for_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_type
     bool global_qualification_required_for_type_copy = p_global_qualification_required_for_type; 
     result->p_global_qualification_required_for_type = global_qualification_required_for_type_copy; 
  // Copy non-constructor parameter data member (access function): previous_instance_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for previous_instance
  // case: toBeCopied == COPY_DATA for previous_instance
     SgTemplateArgument* previous_instance_copy = p_previous_instance; 
     result->p_previous_instance = previous_instance_copy; 
  // Copy non-constructor parameter data member (access function): next_instance_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for next_instance
  // case: toBeCopied == COPY_DATA for next_instance
     SgTemplateArgument* next_instance_copy = p_next_instance; 
     result->p_next_instance = next_instance_copy; 
  // Copy non-constructor parameter data member (access function): is_pack_element_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_pack_element
  // case: toBeCopied == COPY_DATA for is_pack_element
     bool is_pack_element_copy = p_is_pack_element; 
     result->p_is_pack_element = is_pack_element_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)templateDeclaration
        if ((templateDeclaration_copy != nullptr) && (templateDeclaration_copy->get_parent() == nullptr) && (isSgType(templateDeclaration_copy) == nullptr)) {
            templateDeclaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8533 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectory::copy(SgCopyHelp& help) const {
    SgDirectory* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDirectory( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): fileList_copy
     SgFileList* fileList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileList
          if (get_fileList() != nullptr) 
             { 
               fileList_copy = static_cast<SgFileList*>(help.copyAst(get_fileList())); 
             } 
            else 
             { 
               fileList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileList != nullptr) delete result->p_fileList; 
     result->p_fileList = fileList_copy; 
  // case: not a listType for (using conditionalToSetParent)fileList
        if ((fileList_copy != nullptr) && (fileList_copy->get_parent() == nullptr) && (isSgType(fileList_copy) == nullptr)) {
            fileList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): directoryList_copy
     SgDirectoryList* directoryList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)directoryList
          if (get_directoryList() != nullptr) 
             { 
               directoryList_copy = static_cast<SgDirectoryList*>(help.copyAst(get_directoryList())); 
             } 
            else 
             { 
               directoryList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_directoryList != nullptr) delete result->p_directoryList; 
     result->p_directoryList = directoryList_copy; 
  // case: not a listType for (using conditionalToSetParent)directoryList
        if ((directoryList_copy != nullptr) && (directoryList_copy->get_parent() == nullptr) && (isSgType(directoryList_copy) == nullptr)) {
            directoryList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8681 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFileList::copy(SgCopyHelp& help) const {
    SgFileList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFileList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_listOfFiles()
     SgFilePtrList listOfFiles_copy; 
  // case: listType for listOfFiles
  // case: listType (typeIsPointerToList == false) for listOfFiles
    auto source_listOfFiles_iterator = get_listOfFiles().begin();
    for (/* empty by design */; source_listOfFiles_iterator != get_listOfFiles().end(); ++source_listOfFiles_iterator) {
          SgFile* source_list_element = *source_listOfFiles_iterator; 
          SgFile* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgFile*>(help.copyAst(*source_listOfFiles_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          listOfFiles_copy.push_back(copy_list_element); 
        }
     result->p_listOfFiles = listOfFiles_copy; 
  // case: listType for listOfFiles
  // case: listType (typeIsPointerToList == false) for listOfFiles
    auto listOfFiles_iterator = result->get_listOfFiles().begin();
    for (/* empty by design */; listOfFiles_iterator != result->get_listOfFiles().end(); ++listOfFiles_iterator) {
          SgFile* list_element = *listOfFiles_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectoryList::copy(SgCopyHelp& help) const {
    SgDirectoryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDirectoryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_listOfDirectories()
     SgDirectoryPtrList listOfDirectories_copy; 
  // case: listType for listOfDirectories
  // case: listType (typeIsPointerToList == false) for listOfDirectories
    auto source_listOfDirectories_iterator = get_listOfDirectories().begin();
    for (/* empty by design */; source_listOfDirectories_iterator != get_listOfDirectories().end(); ++source_listOfDirectories_iterator) {
          SgDirectory* source_list_element = *source_listOfDirectories_iterator; 
          SgDirectory* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDirectory*>(help.copyAst(*source_listOfDirectories_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          listOfDirectories_copy.push_back(copy_list_element); 
        }
     result->p_listOfDirectories = listOfDirectories_copy; 
  // case: listType for listOfDirectories
  // case: listType (typeIsPointerToList == false) for listOfDirectories
    auto listOfDirectories_iterator = result->get_listOfDirectories().begin();
    for (/* empty by design */; listOfDirectories_iterator != result->get_listOfDirectories().end(); ++listOfDirectories_iterator) {
          SgDirectory* list_element = *listOfDirectories_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 8935 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterTypeList::copy(SgCopyHelp& help) const {
    SgFunctionParameterTypeList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionParameterTypeList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_arguments
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for arguments
  // case: toBeCopied == COPY_DATA for arguments
     SgTypePtrList arguments_copy = p_arguments; 
     result->p_arguments = arguments_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9031 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgQualifiedName::copy(SgCopyHelp& help) const {
    SgQualifiedName* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgQualifiedName( scope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9126 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateArgumentList::copy(SgCopyHelp& help) const {
    SgTemplateArgumentList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateArgumentList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): args_copy
     SgTemplateArgumentPtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto get_args_copy = get_args();
    auto source_args_iterator = get_args_copy.begin();
    auto iterator_end = get_args_copy.end();
    for (/* empty by design */; source_args_iterator != iterator_end; ++source_args_iterator) {
          SgTemplateArgument* source_list_element = *source_args_iterator; 
          SgTemplateArgument* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgTemplateArgument*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          args_copy.push_back(copy_list_element); 
        }
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto result_get_args_copy = result->get_args();
    auto args_iterator = result_get_args_copy.begin();
    auto result_it_end = result_get_args_copy.end();
    for (/* empty by design */; args_iterator != result_it_end; ++args_iterator) {
          SgTemplateArgument* list_element = *args_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9249 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameterList::copy(SgCopyHelp& help) const {
    SgTemplateParameterList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateParameterList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): args_copy
     SgTemplateParameterPtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto get_args_copy = get_args();
    auto source_args_iterator = get_args_copy.begin();
    auto iterator_end = get_args_copy.end();
    for (/* empty by design */; source_args_iterator != iterator_end; ++source_args_iterator) {
          SgTemplateParameter* source_list_element = *source_args_iterator; 
          SgTemplateParameter* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgTemplateParameter*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          args_copy.push_back(copy_list_element); 
        }
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto result_get_args_copy = result->get_args();
    auto args_iterator = result_get_args_copy.begin();
    auto result_it_end = result_get_args_copy.end();
    for (/* empty by design */; args_iterator != result_it_end; ++args_iterator) {
          SgTemplateParameter* list_element = *args_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9372 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraph::copy(SgCopyHelp& help) const {
    SgGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9539 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncidenceDirectedGraph::copy(SgCopyHelp& help) const {
    SgIncidenceDirectedGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIncidenceDirectedGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9716 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBidirectionalGraph::copy(SgCopyHelp& help) const {
    SgBidirectionalGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBidirectionalGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 9893 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringKeyedBidirectionalGraph::copy(SgCopyHelp& help) const {
    SgStringKeyedBidirectionalGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStringKeyedBidirectionalGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10070 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntKeyedBidirectionalGraph::copy(SgCopyHelp& help) const {
    SgIntKeyedBidirectionalGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntKeyedBidirectionalGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesOut()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesOut
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesOut
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesOut_copy = p_node_index_to_edge_multimap_edgesOut; 
     result->p_node_index_to_edge_multimap_edgesOut = node_index_to_edge_multimap_edgesOut_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap_edgesIn()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap_edgesIn
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap_edgesIn
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_edgesIn_copy = p_node_index_to_edge_multimap_edgesIn; 
     result->p_node_index_to_edge_multimap_edgesIn = node_index_to_edge_multimap_edgesIn_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10247 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncidenceUndirectedGraph::copy(SgCopyHelp& help) const {
    SgIncidenceUndirectedGraph* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIncidenceUndirectedGraph( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_node_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_node_map
  // case: toBeCopied == COPY_DATA for node_index_to_node_map
     rose_graph_integer_node_hash_map node_index_to_node_map_copy = p_node_index_to_node_map; 
     result->p_node_index_to_node_map = node_index_to_node_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_edge_index_to_edge_map()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for edge_index_to_edge_map
  // case: toBeCopied == COPY_DATA for edge_index_to_edge_map
     rose_graph_integer_edge_hash_map edge_index_to_edge_map_copy = p_edge_index_to_edge_map; 
     result->p_edge_index_to_edge_map = edge_index_to_edge_map_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_pair_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_pair_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_pair_to_edge_multimap
     rose_graph_integerpair_edge_hash_multimap node_index_pair_to_edge_multimap_copy = p_node_index_pair_to_edge_multimap; 
     result->p_node_index_pair_to_edge_multimap = node_index_pair_to_edge_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_node_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_node_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_node_index_multimap
     rose_graph_string_integer_hash_multimap string_to_node_index_multimap_copy = p_string_to_node_index_multimap; 
     result->p_string_to_node_index_multimap = string_to_node_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_string_to_edge_index_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_to_edge_index_multimap
  // case: toBeCopied == COPY_DATA for string_to_edge_index_multimap
     rose_graph_string_integer_hash_multimap string_to_edge_index_multimap_copy = p_string_to_edge_index_multimap; 
     result->p_string_to_edge_index_multimap = string_to_edge_index_multimap_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_node_index_to_edge_multimap()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_index_to_edge_multimap
  // case: toBeCopied == COPY_DATA for node_index_to_edge_multimap
     rose_graph_integer_edge_hash_multimap node_index_to_edge_multimap_copy = p_node_index_to_edge_multimap; 
     result->p_node_index_to_edge_multimap = node_index_to_edge_multimap_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edges()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edges
  // case: toBeCopied == COPY_DATA for boost_edges
     SgBoostEdgeList boost_edges_copy = p_boost_edges; 
     result->p_boost_edges = boost_edges_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_boost_edge_weights()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for boost_edge_weights
  // case: toBeCopied == COPY_DATA for boost_edge_weights
     SgBoostEdgeWeightList boost_edge_weights_copy = p_boost_edge_weights; 
     result->p_boost_edge_weights = boost_edge_weights_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphNode::copy(SgCopyHelp& help) const {
    SgGraphNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGraphNode( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): SgNode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for SgNode
  // case: toBeCopied == COPY_DATA for SgNode
     SgNode* SgNode_copy = p_SgNode; 
     result->p_SgNode = SgNode_copy; 
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10546 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphEdge::copy(SgCopyHelp& help) const {
    SgGraphEdge* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
    // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGraphEdge( node_A_copy, node_B_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10681 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDirectedGraphEdge::copy(SgCopyHelp& help) const {
    SgDirectedGraphEdge* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
    // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDirectedGraphEdge( node_A_copy, node_B_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUndirectedGraphEdge::copy(SgCopyHelp& help) const {
    SgUndirectedGraphEdge* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: node_A_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_A
  // case: toBeCopied == COPY_DATA for node_A
     SgGraphNode* node_A_copy = p_node_A; 
    // Copy constructor parameter data member: node_B_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for node_B
  // case: toBeCopied == COPY_DATA for node_B
     SgGraphNode* node_B_copy = p_node_B; 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUndirectedGraphEdge( node_A_copy, node_B_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): index_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     int index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index_counter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index_counter
  // case: toBeCopied == COPY_DATA for index_counter
     static int index_counter_copy = p_index_counter; 
     result->p_index_counter = index_counter_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_properties()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for properties
  // case: toBeCopied == COPY_DATA for properties
     std::map<int, std::string> properties_copy = p_properties; 
     result->p_properties = properties_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 10951 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphNodeList::copy(SgCopyHelp& help) const {
    SgGraphNodeList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGraphNodeList();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11041 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGraphEdgeList::copy(SgCopyHelp& help) const {
    SgGraphEdgeList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGraphEdgeList();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11131 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTable::copy(SgCopyHelp& help) const {
    SgTypeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): type_table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_table
  // case: toBeCopied == COPY_DATA for type_table
     SgSymbolTable* type_table_copy = p_type_table; 
     result->p_type_table = type_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11227 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNameGroup::copy(SgCopyHelp& help) const {
    SgNameGroup* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNameGroup();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): group_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for group_name
  // case: toBeCopied == COPY_DATA for group_name
     std::string group_name_copy = p_group_name; 
     result->p_group_name = group_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_list
  // case: toBeCopied == COPY_DATA for name_list
     SgStringList name_list_copy = p_name_list; 
     result->p_name_list = name_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDimensionObject::copy(SgCopyHelp& help) const {
    SgDimensionObject* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDimensionObject();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array
  // case: toBeCopied == COPY_DATA for array
     SgInitializedName* array_copy = p_array; 
     result->p_array = array_copy; 
  // Copy non-constructor parameter data member (access function): shape_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shape
  // case: toBeCopied == COPY_DATA for shape
     SgExprListExp* shape_copy = p_shape; 
     result->p_shape = shape_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11429 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatItem::copy(SgCopyHelp& help) const {
    SgFormatItem* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFormatItem();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): repeat_specification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeat_specification
  // case: toBeCopied == COPY_DATA for repeat_specification
     int repeat_specification_copy = p_repeat_specification; 
     result->p_repeat_specification = repeat_specification_copy; 
  // Copy non-constructor parameter data member (access function): data_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgExpression* data_copy = p_data; 
     result->p_data = data_copy; 
  // Copy non-constructor parameter data member (access function): format_item_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemList* format_item_list_copy = p_format_item_list; 
     result->p_format_item_list = format_item_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11535 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatItemList::copy(SgCopyHelp& help) const {
    SgFormatItemList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFormatItemList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_format_item_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemPtrList format_item_list_copy = p_format_item_list; 
     result->p_format_item_list = format_item_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11631 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementGroup::copy(SgCopyHelp& help) const {
    SgDataStatementGroup* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDataStatementGroup();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_object_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for object_list
  // case: toBeCopied == COPY_DATA for object_list
     SgDataStatementObjectPtrList object_list_copy = p_object_list; 
     result->p_object_list = object_list_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_value_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value_list
  // case: toBeCopied == COPY_DATA for value_list
     SgDataStatementValuePtrList value_list_copy = p_value_list; 
     result->p_value_list = value_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11732 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementObject::copy(SgCopyHelp& help) const {
    SgDataStatementObject* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDataStatementObject();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): variableReference_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableReference_list
  // case: toBeCopied == COPY_DATA for variableReference_list
     SgExprListExp* variableReference_list_copy = p_variableReference_list; 
     result->p_variableReference_list = variableReference_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 11828 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeFile::copy(SgCopyHelp& help) const {
    SgIncludeFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     SgName filename_copy = p_filename; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIncludeFile( filename_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isIncludedMoreThanOnce_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isIncludedMoreThanOnce
  // case: toBeCopied == COPY_DATA for isIncludedMoreThanOnce
     bool isIncludedMoreThanOnce_copy = p_isIncludedMoreThanOnce; 
     result->p_isIncludedMoreThanOnce = isIncludedMoreThanOnce_copy; 
  // Copy non-constructor parameter data member (access function): isPrimaryUse_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrimaryUse
  // case: toBeCopied == COPY_DATA for isPrimaryUse
     bool isPrimaryUse_copy = p_isPrimaryUse; 
     result->p_isPrimaryUse = isPrimaryUse_copy; 
  // Copy non-constructor parameter data member (access function): file_hash_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_hash
  // case: toBeCopied == COPY_DATA for file_hash
     std::string file_hash_copy = p_file_hash; 
     result->p_file_hash = file_hash_copy; 
  // Copy non-constructor parameter data member (access function): name_used_in_include_directive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_used_in_include_directive
  // case: toBeCopied == COPY_DATA for name_used_in_include_directive
     SgName name_used_in_include_directive_copy = p_name_used_in_include_directive; 
     result->p_name_used_in_include_directive = name_used_in_include_directive_copy; 
  // Copy non-constructor parameter data member (access function): isSystemInclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSystemInclude
  // case: toBeCopied == COPY_DATA for isSystemInclude
     bool isSystemInclude_copy = p_isSystemInclude; 
     result->p_isSystemInclude = isSystemInclude_copy; 
  // Copy non-constructor parameter data member (access function): isPreinclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPreinclude
  // case: toBeCopied == COPY_DATA for isPreinclude
     bool isPreinclude_copy = p_isPreinclude; 
     result->p_isPreinclude = isPreinclude_copy; 
  // Copy non-constructor parameter data member (access function): requires_explict_path_for_unparsed_headers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires_explict_path_for_unparsed_headers
  // case: toBeCopied == COPY_DATA for requires_explict_path_for_unparsed_headers
     bool requires_explict_path_for_unparsed_headers_copy = p_requires_explict_path_for_unparsed_headers; 
     result->p_requires_explict_path_for_unparsed_headers = requires_explict_path_for_unparsed_headers_copy; 
  // Copy non-constructor parameter data member (access function): can_be_supported_using_token_based_unparsing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for can_be_supported_using_token_based_unparsing
  // case: toBeCopied == COPY_DATA for can_be_supported_using_token_based_unparsing
     bool can_be_supported_using_token_based_unparsing_copy = p_can_be_supported_using_token_based_unparsing; 
     result->p_can_be_supported_using_token_based_unparsing = can_be_supported_using_token_based_unparsing_copy; 
  // Copy non-constructor parameter data member (access function): directory_prefix_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directory_prefix
  // case: toBeCopied == COPY_DATA for directory_prefix
     SgName directory_prefix_copy = p_directory_prefix; 
     result->p_directory_prefix = directory_prefix_copy; 
  // Copy non-constructor parameter data member (access function): name_without_path_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_without_path
  // case: toBeCopied == COPY_DATA for name_without_path
     SgName name_without_path_copy = p_name_without_path; 
     result->p_name_without_path = name_without_path_copy; 
  // Copy non-constructor parameter data member (access function): applicationRootDirectory_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for applicationRootDirectory
  // case: toBeCopied == COPY_DATA for applicationRootDirectory
     SgName applicationRootDirectory_copy = p_applicationRootDirectory; 
     result->p_applicationRootDirectory = applicationRootDirectory_copy; 
  // Copy non-constructor parameter data member (access function): will_be_unparsed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for will_be_unparsed
  // case: toBeCopied == COPY_DATA for will_be_unparsed
     bool will_be_unparsed_copy = p_will_be_unparsed; 
     result->p_will_be_unparsed = will_be_unparsed_copy; 
  // Copy non-constructor parameter data member (access function): isRoseSystemInclude_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRoseSystemInclude
  // case: toBeCopied == COPY_DATA for isRoseSystemInclude
     bool isRoseSystemInclude_copy = p_isRoseSystemInclude; 
     result->p_isRoseSystemInclude = isRoseSystemInclude_copy; 
  // Copy non-constructor parameter data member (access function): from_system_include_dir_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_system_include_dir
  // case: toBeCopied == COPY_DATA for from_system_include_dir
     bool from_system_include_dir_copy = p_from_system_include_dir; 
     result->p_from_system_include_dir = from_system_include_dir_copy; 
  // Copy non-constructor parameter data member (access function): preinclude_macros_only_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinclude_macros_only
  // case: toBeCopied == COPY_DATA for preinclude_macros_only
     bool preinclude_macros_only_copy = p_preinclude_macros_only; 
     result->p_preinclude_macros_only = preinclude_macros_only_copy; 
  // Copy non-constructor parameter data member (access function): isApplicationFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isApplicationFile
  // case: toBeCopied == COPY_DATA for isApplicationFile
     bool isApplicationFile_copy = p_isApplicationFile; 
     result->p_isApplicationFile = isApplicationFile_copy; 
  // Copy non-constructor parameter data member (access function): isRootSourceFile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRootSourceFile
  // case: toBeCopied == COPY_DATA for isRootSourceFile
     bool isRootSourceFile_copy = p_isRootSourceFile; 
     result->p_isRootSourceFile = isRootSourceFile_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12009 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDataStatementValue::copy(SgCopyHelp& help) const {
    SgDataStatementValue* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: data_initialization_format_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_initialization_format
  // case: toBeCopied == COPY_DATA for data_initialization_format
     SgDataStatementValue::data_statement_value_enum data_initialization_format_copy = p_data_initialization_format; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDataStatementValue( data_initialization_format_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): initializer_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initializer_list
  // case: toBeCopied == COPY_DATA for initializer_list
     SgExprListExp* initializer_list_copy = p_initializer_list; 
     result->p_initializer_list = initializer_list_copy; 
  // Copy non-constructor parameter data member (access function): repeat_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeat_expression
  // case: toBeCopied == COPY_DATA for repeat_expression
     SgExpression* repeat_expression_copy = p_repeat_expression; 
     result->p_repeat_expression = repeat_expression_copy; 
  // Copy non-constructor parameter data member (access function): constant_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_expression
  // case: toBeCopied == COPY_DATA for constant_expression
     SgExpression* constant_expression_copy = p_constant_expression; 
     result->p_constant_expression = constant_expression_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaImportStatementList::copy(SgCopyHelp& help) const {
    SgJavaImportStatementList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaImportStatementList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_java_import_list()
     SgJavaImportStatementPtrList java_import_list_copy; 
  // case: listType for java_import_list
  // case: listType (typeIsPointerToList == false) for java_import_list
    auto source_java_import_list_iterator = get_java_import_list().begin();
    for (/* empty by design */; source_java_import_list_iterator != get_java_import_list().end(); ++source_java_import_list_iterator) {
          SgJavaImportStatement* source_list_element = *source_java_import_list_iterator; 
          SgJavaImportStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgJavaImportStatement*>(help.copyAst(*source_java_import_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          java_import_list_copy.push_back(copy_list_element); 
        }
     result->p_java_import_list = java_import_list_copy; 
  // case: listType for java_import_list
  // case: listType (typeIsPointerToList == false) for java_import_list
    auto java_import_list_iterator = result->get_java_import_list().begin();
    for (/* empty by design */; java_import_list_iterator != result->get_java_import_list().end(); ++java_import_list_iterator) {
          SgJavaImportStatement* list_element = *java_import_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12239 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaClassDeclarationList::copy(SgCopyHelp& help) const {
    SgJavaClassDeclarationList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaClassDeclarationList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (list access function): result->get_java_class_list()
     SgClassDeclarationPtrList java_class_list_copy; 
  // case: listType for java_class_list
  // case: listType (typeIsPointerToList == false) for java_class_list
    auto source_java_class_list_iterator = get_java_class_list().begin();
    for (/* empty by design */; source_java_class_list_iterator != get_java_class_list().end(); ++source_java_class_list_iterator) {
          SgClassDeclaration* source_list_element = *source_java_class_list_iterator; 
          SgClassDeclaration* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgClassDeclaration*>(help.copyAst(*source_java_class_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          java_class_list_copy.push_back(copy_list_element); 
        }
     result->p_java_class_list = java_class_list_copy; 
  // case: listType for java_class_list
  // case: listType (typeIsPointerToList == false) for java_class_list
    auto java_class_list_iterator = result->get_java_class_list().begin();
    for (/* empty by design */; java_class_list_iterator != result->get_java_class_list().end(); ++java_class_list_iterator) {
          SgClassDeclaration* list_element = *java_class_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12358 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHeaderFileReport::copy(SgCopyHelp& help) const {
    SgHeaderFileReport* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgHeaderFileReport();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgType::copy(SgCopyHelp& help) const {
    SgType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12623 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnknown::copy(SgCopyHelp& help) const {
    SgTypeUnknown* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnknown();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_name
  // case: toBeCopied == COPY_DATA for type_name
     std::string type_name_copy = p_type_name; 
     result->p_type_name = type_name_copy; 
  // Copy non-constructor parameter data member (access function): has_type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_type_name
  // case: toBeCopied == COPY_DATA for has_type_name
     bool has_type_name_copy = p_has_type_name; 
     result->p_has_type_name = has_type_name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeVoid::copy(SgCopyHelp& help) const {
    SgTypeVoid* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeVoid();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 12983 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeGlobalVoid::copy(SgCopyHelp& help) const {
    SgTypeGlobalVoid* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeGlobalVoid();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 13158 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTuple::copy(SgCopyHelp& help) const {
    SgTypeTuple* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeTuple();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_types
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for types
  // case: toBeCopied == COPY_DATA for types
     SgTypePtrList types_copy = p_types; 
     result->p_types = types_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 13338 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeString::copy(SgCopyHelp& help) const {
    SgTypeString* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: lengthExpression_copy
     SgExpression* lengthExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lengthExpression
          if (get_lengthExpression() != nullptr) 
             { 
               lengthExpression_copy = static_cast<SgExpression*>(help.copyAst(get_lengthExpression())); 
             } 
            else 
             { 
               lengthExpression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeString( lengthExpression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)lengthExpression
        if ((lengthExpression_copy != nullptr) && (lengthExpression_copy->get_parent() == nullptr) && (isSgType(lengthExpression_copy) == nullptr)) {
            lengthExpression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 13529 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeType::copy(SgCopyHelp& help) const {
    SgRangeType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRangeType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 13709 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerType::copy(SgCopyHelp& help) const {
    SgPointerType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPointerType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 13900 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerMemberType::copy(SgCopyHelp& help) const {
    SgPointerMemberType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPointerMemberType( base_type_copy, class_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 14095 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialBitType::copy(SgCopyHelp& help) const {
    SgJovialBitType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     SgExpression* size_copy = p_size; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialBitType( size_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 14275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReferenceType::copy(SgCopyHelp& help) const {
    SgReferenceType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgReferenceType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 14455 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamedType::copy(SgCopyHelp& help) const {
    SgNamedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamedType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 14656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassType::copy(SgCopyHelp& help) const {
    SgClassType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 14862 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaParameterType::copy(SgCopyHelp& help) const {
    SgJavaParameterType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaParameterType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 15068 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTableType::copy(SgCopyHelp& help) const {
    SgJovialTableType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialTableType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 
  // Copy non-constructor parameter data member (access function): base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
     result->p_base_type = base_type_copy; 
  // Copy non-constructor parameter data member (access function): dim_info_copy
     SgExprListExp* dim_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dim_info
          if (get_dim_info() != nullptr) 
             { 
               dim_info_copy = static_cast<SgExprListExp*>(help.copyAst(get_dim_info())); 
             } 
            else 
             { 
               dim_info_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dim_info != nullptr) delete result->p_dim_info; 
     result->p_dim_info = dim_info_copy; 
  // case: not a listType for (using conditionalToSetParent)dim_info
        if ((dim_info_copy != nullptr) && (dim_info_copy->get_parent() == nullptr) && (isSgType(dim_info_copy) == nullptr)) {
            dim_info_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): rank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rank
  // case: toBeCopied == COPY_DATA for rank
     int rank_copy = p_rank; 
     result->p_rank = rank_copy; 
  // Copy non-constructor parameter data member (access function): structure_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for structure_specifier
  // case: toBeCopied == COPY_DATA for structure_specifier
     SgJovialTableType::StructureSpecifier structure_specifier_copy = p_structure_specifier; 
     result->p_structure_specifier = structure_specifier_copy; 
  // Copy non-constructor parameter data member (access function): bits_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bits_per_entry
  // case: toBeCopied == COPY_DATA for bits_per_entry
     unsigned int bits_per_entry_copy = p_bits_per_entry; 
     result->p_bits_per_entry = bits_per_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 15312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumType::copy(SgCopyHelp& help) const {
    SgEnumType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEnumType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 15513 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefType::copy(SgCopyHelp& help) const {
    SgTypedefType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypedefType( declaration_copy, parent_scope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 15718 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealType::copy(SgCopyHelp& help) const {
    SgNonrealType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonrealType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 15919 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaParameterizedType::copy(SgCopyHelp& help) const {
    SgJavaParameterizedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: raw_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for raw_type
  // case: toBeCopied == COPY_DATA for raw_type
     SgNamedType* raw_type_copy = p_raw_type; 
    // Copy constructor parameter data member: type_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_list
  // case: toBeCopied == COPY_DATA for type_list
     SgTemplateParameterList* type_list_copy = p_type_list; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaParameterizedType( declaration_copy, raw_type_copy, type_list_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 16128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaQualifiedType::copy(SgCopyHelp& help) const {
    SgJavaQualifiedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: parent_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_type
  // case: toBeCopied == COPY_DATA for parent_type
     SgNamedType * parent_type_copy = p_parent_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgNamedType * type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaQualifiedType( declaration_copy, parent_type_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 16337 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaWildcardType::copy(SgCopyHelp& help) const {
    SgJavaWildcardType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: bound_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound_type
  // case: toBeCopied == COPY_DATA for bound_type
     SgType* bound_type_copy = p_bound_type; 
    // Copy constructor parameter data member: is_unbound_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_unbound
  // case: toBeCopied == COPY_DATA for is_unbound
     bool is_unbound_copy = p_is_unbound; 
    // Copy constructor parameter data member: has_extends_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_extends
  // case: toBeCopied == COPY_DATA for has_extends
     bool has_extends_copy = p_has_extends; 
    // Copy constructor parameter data member: has_super_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_super
  // case: toBeCopied == COPY_DATA for has_super
     bool has_super_copy = p_has_super; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaWildcardType( declaration_copy, bound_type_copy, is_unbound_copy, has_extends_copy, has_super_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 16554 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskType::copy(SgCopyHelp& help) const {
    SgAdaTaskType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 16755 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedType::copy(SgCopyHelp& help) const {
    SgAdaProtectedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 16956 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalType::copy(SgCopyHelp& help) const {
    SgAdaFormalType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: formal_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for formal_type
  // case: toBeCopied == COPY_DATA for formal_type
     SgType* formal_type_copy = p_formal_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFormalType( declaration_copy, formal_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // Copy non-constructor parameter data member (access function): is_private_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_private
  // case: toBeCopied == COPY_DATA for is_private
     bool is_private_copy = p_is_private; 
     result->p_is_private = is_private_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 17166 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminatedType::copy(SgCopyHelp& help) const {
    SgAdaDiscriminatedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDiscriminatedType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 17367 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopedType::copy(SgCopyHelp& help) const {
    SgScopedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgScopedType( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): autonomous_declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for autonomous_declaration
  // case: toBeCopied == COPY_DATA for autonomous_declaration
     bool autonomous_declaration_copy = p_autonomous_declaration; 
     result->p_autonomous_declaration = autonomous_declaration_copy; 
  // Copy non-constructor parameter data member (access function): is_from_template_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_from_template_parameter
  // case: toBeCopied == COPY_DATA for is_from_template_parameter
     bool is_from_template_parameter_copy = p_is_from_template_parameter; 
     result->p_is_from_template_parameter = is_from_template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 17568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModifierType::copy(SgCopyHelp& help) const {
    SgModifierType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModifierType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_typeModifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeModifier
  // case: toBeCopied == COPY_DATA for typeModifier
     SgTypeModifier typeModifier_copy = p_typeModifier; 
     result->p_typeModifier = typeModifier_copy; 
  // Copy non-constructor parameter data member (access function): frontend_type_reference_copy
    char* frontend_type_reference_copy; 
  // case: typeName == char* or char** for frontend_type_reference
     if (p_frontend_type_reference != nullptr) 
          frontend_type_reference_copy = strdup(p_frontend_type_reference); 
     frontend_type_reference_copy = p_frontend_type_reference; 
     result->p_frontend_type_reference = frontend_type_reference_copy;
  // case: typeName == char* or char** for frontend_type_reference
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 17772 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionType::copy(SgCopyHelp& help) const {
    SgFunctionType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != nullptr) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionType( return_type_copy, has_ellipses_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
        if ((return_type_copy != nullptr) && (return_type_copy->get_parent() == nullptr) && (isSgType(return_type_copy) == nullptr)) {
            return_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 17972 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionType::copy(SgCopyHelp& help) const {
    SgMemberFunctionType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != nullptr) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
    // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMemberFunctionType( return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
        if ((return_type_copy != nullptr) && (return_type_copy->get_parent() == nullptr) && (isSgType(return_type_copy) == nullptr)) {
            return_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 18180 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPartialFunctionType::copy(SgCopyHelp& help) const {
    SgPartialFunctionType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != nullptr) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
    // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPartialFunctionType( return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
        if ((return_type_copy != nullptr) && (return_type_copy->get_parent() == nullptr) && (isSgType(return_type_copy) == nullptr)) {
            return_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 18388 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPartialFunctionModifierType::copy(SgCopyHelp& help) const {
    SgPartialFunctionModifierType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != nullptr) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: has_ellipses_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_ellipses
  // case: toBeCopied == COPY_DATA for has_ellipses
     bool has_ellipses_copy = p_has_ellipses; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgType* class_type_copy = p_class_type; 
    // Copy constructor parameter data member: mfunc_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mfunc_specifier
  // case: toBeCopied == COPY_DATA for mfunc_specifier
     unsigned int mfunc_specifier_copy = p_mfunc_specifier; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPartialFunctionModifierType( return_type_copy, has_ellipses_copy, class_type_copy, mfunc_specifier_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): argument_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_list
  // case: toBeCopied == COPY_DATA for argument_list
     SgFunctionParameterTypeList* argument_list_copy = p_argument_list; 
     result->p_argument_list = argument_list_copy; 
  // case: not a listType for (using conditionalToSetParent)return_type
        if ((return_type_copy != nullptr) && (return_type_copy->get_parent() == nullptr) && (isSgType(return_type_copy) == nullptr)) {
            return_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 18596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrayType::copy(SgCopyHelp& help) const {
    SgArrayType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
    // Copy constructor parameter data member: index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != nullptr) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgArrayType( base_type_copy, index_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): dim_info_copy
     SgExprListExp* dim_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dim_info
          if (get_dim_info() != nullptr) 
             { 
               dim_info_copy = static_cast<SgExprListExp*>(help.copyAst(get_dim_info())); 
             } 
            else 
             { 
               dim_info_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dim_info != nullptr) delete result->p_dim_info; 
     result->p_dim_info = dim_info_copy; 
  // case: not a listType for (using conditionalToSetParent)dim_info
        if ((dim_info_copy != nullptr) && (dim_info_copy->get_parent() == nullptr) && (isSgType(dim_info_copy) == nullptr)) {
            dim_info_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): rank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rank
  // case: toBeCopied == COPY_DATA for rank
     int rank_copy = p_rank; 
     result->p_rank = rank_copy; 
  // Copy non-constructor parameter data member (access function): number_of_elements_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for number_of_elements
  // case: toBeCopied == COPY_DATA for number_of_elements
     int number_of_elements_copy = p_number_of_elements; 
     result->p_number_of_elements = number_of_elements_copy; 
  // Copy non-constructor parameter data member (access function): is_variable_length_array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_variable_length_array
  // case: toBeCopied == COPY_DATA for is_variable_length_array
     bool is_variable_length_array_copy = p_is_variable_length_array; 
     result->p_is_variable_length_array = is_variable_length_array_copy; 
  // case: not a listType for (using conditionalToSetParent)index
        if ((index_copy != nullptr) && (index_copy->get_parent() == nullptr) && (isSgType(index_copy) == nullptr)) {
            index_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 18824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeEllipse::copy(SgCopyHelp& help) const {
    SgTypeEllipse* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeEllipse();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 18999 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateType::copy(SgCopyHelp& help) const {
    SgTemplateType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateType( name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
     result->p_template_parameter_position = template_parameter_position_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_depth_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_depth
  // case: toBeCopied == COPY_DATA for template_parameter_depth
     int template_parameter_depth_copy = p_template_parameter_depth; 
     result->p_template_parameter_depth = template_parameter_depth_copy; 
  // Copy non-constructor parameter data member (access function): class_type_copy
     SgType * class_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)class_type
          if (get_class_type() != nullptr) 
             { 
               class_type_copy = static_cast<SgType *>(help.copyAst(get_class_type())); 
             } 
            else 
             { 
               class_type_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_class_type != nullptr) delete result->p_class_type; 
     result->p_class_type = class_type_copy; 
  // case: not a listType for (using conditionalToSetParent)class_type
        if ((class_type_copy != nullptr) && (class_type_copy->get_parent() == nullptr) && (isSgType(class_type_copy) == nullptr)) {
            class_type_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): parent_class_type_copy
     SgType * parent_class_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parent_class_type
          if (get_parent_class_type() != nullptr) 
             { 
               parent_class_type_copy = static_cast<SgType *>(help.copyAst(get_parent_class_type())); 
             } 
            else 
             { 
               parent_class_type_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parent_class_type != nullptr) delete result->p_parent_class_type; 
     result->p_parent_class_type = parent_class_type_copy; 
  // case: not a listType for (using conditionalToSetParent)parent_class_type
        if ((parent_class_type_copy != nullptr) && (parent_class_type_copy->get_parent() == nullptr) && (isSgType(parent_class_type_copy) == nullptr)) {
            parent_class_type_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): template_parameter_copy
     SgTemplateParameter * template_parameter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)template_parameter
          if (get_template_parameter() != nullptr) 
             { 
               template_parameter_copy = static_cast<SgTemplateParameter *>(help.copyAst(get_template_parameter())); 
             } 
            else 
             { 
               template_parameter_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_template_parameter != nullptr) delete result->p_template_parameter; 
     result->p_template_parameter = template_parameter_copy; 
  // case: not a listType for (using conditionalToSetParent)template_parameter
        if ((template_parameter_copy != nullptr) && (template_parameter_copy->get_parent() == nullptr) && (isSgType(template_parameter_copy) == nullptr)) {
            template_parameter_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_args
  // case: toBeCopied == COPY_DATA for tpl_args
     SgTemplateArgumentPtrList tpl_args_copy = p_tpl_args; 
     result->p_tpl_args = tpl_args_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_part_spec_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for part_spec_tpl_args
  // case: toBeCopied == COPY_DATA for part_spec_tpl_args
     SgTemplateArgumentPtrList part_spec_tpl_args_copy = p_part_spec_tpl_args; 
     result->p_part_spec_tpl_args = part_spec_tpl_args_copy; 
  // Copy non-constructor parameter data member (access function): packed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packed
  // case: toBeCopied == COPY_DATA for packed
     bool packed_copy = p_packed; 
     result->p_packed = packed_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 19258 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgQualifiedNameType::copy(SgCopyHelp& help) const {
    SgQualifiedNameType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgQualifiedNameType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 19443 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeComplex::copy(SgCopyHelp& help) const {
    SgTypeComplex* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeComplex( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 19623 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeImaginary::copy(SgCopyHelp& help) const {
    SgTypeImaginary* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeImaginary( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 19803 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeDefault::copy(SgCopyHelp& help) const {
    SgTypeDefault* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeDefault();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 19983 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeCAFTeam::copy(SgCopyHelp& help) const {
    SgTypeCAFTeam* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeCAFTeam();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 20158 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeCrayPointer::copy(SgCopyHelp& help) const {
    SgTypeCrayPointer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeCrayPointer();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 20333 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLabel::copy(SgCopyHelp& help) const {
    SgTypeLabel* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeLabel();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 20513 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnionType::copy(SgCopyHelp& help) const {
    SgJavaUnionType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaUnionType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): type_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_list
  // case: toBeCopied == COPY_DATA for type_list
     SgTypePtrList type_list_copy = p_type_list; 
     result->p_type_list = type_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 20693 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRvalueReferenceType::copy(SgCopyHelp& help) const {
    SgRvalueReferenceType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRvalueReferenceType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 20873 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeNullptr::copy(SgCopyHelp& help) const {
    SgTypeNullptr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeNullptr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclType::copy(SgCopyHelp& help) const {
    SgDeclType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_expression
  // case: toBeCopied == COPY_DATA for base_expression
     SgExpression* base_expression_copy = p_base_expression; 
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeclType( base_expression_copy, base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeOfType::copy(SgCopyHelp& help) const {
    SgTypeOfType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_expression
  // case: toBeCopied == COPY_DATA for base_expression
     SgExpression* base_expression_copy = p_base_expression; 
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeOfType( base_expression_copy, base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeMatrix::copy(SgCopyHelp& help) const {
    SgTypeMatrix* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeMatrix();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
     result->p_base_type = base_type_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFixed::copy(SgCopyHelp& help) const {
    SgTypeFixed* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: scale_copy
     SgExpression* scale_copy; 
  // case: not a listType for (using conditionalToCopyVariable)scale
          if (get_scale() != nullptr) 
             { 
               scale_copy = static_cast<SgExpression*>(help.copyAst(get_scale())); 
             } 
            else 
             { 
               scale_copy = nullptr; 
             } 
    // Copy constructor parameter data member: fraction_copy
     SgExpression* fraction_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fraction
          if (get_fraction() != nullptr) 
             { 
               fraction_copy = static_cast<SgExpression*>(help.copyAst(get_fraction())); 
             } 
            else 
             { 
               fraction_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFixed( scale_copy, fraction_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)scale
        if ((scale_copy != nullptr) && (scale_copy->get_parent() == nullptr) && (isSgType(scale_copy) == nullptr)) {
            scale_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)fraction
        if ((fraction_copy != nullptr) && (fraction_copy->get_parent() == nullptr) && (isSgType(fraction_copy) == nullptr)) {
            fraction_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21802 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAutoType::copy(SgCopyHelp& help) const {
    SgAutoType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAutoType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 21977 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAccessType::copy(SgCopyHelp& help) const {
    SgAdaAccessType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: is_general_access_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_general_access
  // case: toBeCopied == COPY_DATA for is_general_access
     bool is_general_access_copy = p_is_general_access; 
    // Copy constructor parameter data member: is_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_anonymous
  // case: toBeCopied == COPY_DATA for is_anonymous
     bool is_anonymous_copy = p_is_anonymous; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaAccessType( base_type_copy, is_general_access_copy, is_anonymous_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 22176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSubtype::copy(SgCopyHelp& help) const {
    SgAdaSubtype* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: constraint_copy
     SgAdaTypeConstraint* constraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)constraint
          if (get_constraint() != nullptr) 
             { 
               constraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_constraint())); 
             } 
            else 
             { 
               constraint_copy = nullptr; 
             } 
    // Copy constructor parameter data member: fromRootType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fromRootType
  // case: toBeCopied == COPY_DATA for fromRootType
     bool fromRootType_copy = p_fromRootType; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaSubtype( base_type_copy, constraint_copy, fromRootType_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)constraint
        if ((constraint_copy != nullptr) && (constraint_copy->get_parent() == nullptr) && (isSgType(constraint_copy) == nullptr)) {
            constraint_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 22386 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscreteType::copy(SgCopyHelp& help) const {
    SgAdaDiscreteType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDiscreteType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 22561 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDerivedType::copy(SgCopyHelp& help) const {
    SgAdaDerivedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: base_type_copy
     SgType* base_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)base_type
          if (get_base_type() != nullptr) 
             { 
               base_type_copy = static_cast<SgType*>(help.copyAst(get_base_type())); 
             } 
            else 
             { 
               base_type_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDerivedType( base_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)base_type
        if ((base_type_copy != nullptr) && (base_type_copy->get_parent() == nullptr) && (isSgType(base_type_copy) == nullptr)) {
            base_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 22752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSubroutineType::copy(SgCopyHelp& help) const {
    SgAdaSubroutineType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
    // Copy constructor parameter data member: functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
    // Copy constructor parameter data member: return_type_copy
     SgType* return_type_copy; 
  // case: not a listType for (using conditionalToCopyVariable)return_type
          if (get_return_type() != nullptr) 
             { 
               return_type_copy = static_cast<SgType*>(help.copyAst(get_return_type())); 
             } 
            else 
             { 
               return_type_copy = nullptr; 
             } 
    // Copy constructor parameter data member: is_protected_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_protected
  // case: toBeCopied == COPY_DATA for is_protected
     bool is_protected_copy = p_is_protected; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaSubroutineType( parameterList_copy, functionParameterScope_copy, return_type_copy, is_protected_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)return_type
        if ((return_type_copy != nullptr) && (return_type_copy->get_parent() == nullptr) && (isSgType(return_type_copy) == nullptr)) {
            return_type_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 22966 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntegralType::copy(SgCopyHelp& help) const {
    SgIntegralType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntegralType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 23141 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSignedIntegralType::copy(SgCopyHelp& help) const {
    SgSignedIntegralType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSignedIntegralType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 23316 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeInt::copy(SgCopyHelp& help) const {
    SgTypeInt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: field_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_size
  // case: toBeCopied == COPY_DATA for field_size
     int field_size_copy = p_field_size; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeInt( field_size_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 23496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLong::copy(SgCopyHelp& help) const {
    SgTypeLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 23671 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLongLong::copy(SgCopyHelp& help) const {
    SgTypeLongLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeLongLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 23846 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeShort::copy(SgCopyHelp& help) const {
    SgTypeShort* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeShort();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24021 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSigned128bitInteger::copy(SgCopyHelp& help) const {
    SgTypeSigned128bitInteger* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSigned128bitInteger();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24196 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedChar::copy(SgCopyHelp& help) const {
    SgTypeSignedChar* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSignedChar();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24371 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedInt::copy(SgCopyHelp& help) const {
    SgTypeSignedInt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSignedInt();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24546 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedLong::copy(SgCopyHelp& help) const {
    SgTypeSignedLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSignedLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24721 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedLongLong::copy(SgCopyHelp& help) const {
    SgTypeSignedLongLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSignedLongLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 24896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSignedShort::copy(SgCopyHelp& help) const {
    SgTypeSignedShort* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSignedShort();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25071 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedIntegralType::copy(SgCopyHelp& help) const {
    SgUnsignedIntegralType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedIntegralType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25246 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsigned128bitInteger::copy(SgCopyHelp& help) const {
    SgTypeUnsigned128bitInteger* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsigned128bitInteger();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25421 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedChar::copy(SgCopyHelp& help) const {
    SgTypeUnsignedChar* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsignedChar();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedInt::copy(SgCopyHelp& help) const {
    SgTypeUnsignedInt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsignedInt();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25771 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedLong::copy(SgCopyHelp& help) const {
    SgTypeUnsignedLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsignedLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 25946 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedLongLong::copy(SgCopyHelp& help) const {
    SgTypeUnsignedLongLong* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsignedLongLong();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26121 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeUnsignedShort::copy(SgCopyHelp& help) const {
    SgTypeUnsignedShort* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeUnsignedShort();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar::copy(SgCopyHelp& help) const {
    SgTypeChar* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeChar();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26471 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeWchar::copy(SgCopyHelp& help) const {
    SgTypeWchar* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeWchar();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26646 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar16::copy(SgCopyHelp& help) const {
    SgTypeChar16* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeChar16();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26821 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeChar32::copy(SgCopyHelp& help) const {
    SgTypeChar32* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeChar32();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 26996 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaModularType::copy(SgCopyHelp& help) const {
    SgAdaModularType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: modexpr_copy
     SgExpression* modexpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)modexpr
          if (get_modexpr() != nullptr) 
             { 
               modexpr_copy = static_cast<SgExpression*>(help.copyAst(get_modexpr())); 
             } 
            else 
             { 
               modexpr_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaModularType( modexpr_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)modexpr
        if ((modexpr_copy != nullptr) && (modexpr_copy->get_parent() == nullptr) && (isSgType(modexpr_copy) == nullptr)) {
            modexpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 27187 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeBool::copy(SgCopyHelp& help) const {
    SgTypeBool* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeBool();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 27362 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloatingPointType::copy(SgCopyHelp& help) const {
    SgFloatingPointType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloatingPointType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 27537 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat::copy(SgCopyHelp& help) const {
    SgTypeFloat* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 27712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat128::copy(SgCopyHelp& help) const {
    SgTypeFloat128* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat128();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 27887 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat80::copy(SgCopyHelp& help) const {
    SgTypeFloat80* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat80();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28062 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeDouble::copy(SgCopyHelp& help) const {
    SgTypeDouble* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeDouble();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28237 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeLongDouble::copy(SgCopyHelp& help) const {
    SgTypeLongDouble* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeLongDouble();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28412 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat16::copy(SgCopyHelp& help) const {
    SgTypeFloat16* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat16();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28587 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeBFloat16::copy(SgCopyHelp& help) const {
    SgTypeBFloat16* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeBFloat16();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28762 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFp16::copy(SgCopyHelp& help) const {
    SgTypeFp16* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFp16();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 28937 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat32x::copy(SgCopyHelp& help) const {
    SgTypeFloat32x* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat32x();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat64x::copy(SgCopyHelp& help) const {
    SgTypeFloat64x* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat64x();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29287 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat32::copy(SgCopyHelp& help) const {
    SgTypeFloat32* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat32();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29462 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeFloat64::copy(SgCopyHelp& help) const {
    SgTypeFloat64* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeFloat64();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): isCoArray_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCoArray
  // case: toBeCopied == COPY_DATA for isCoArray
     bool isCoArray_copy = p_isCoArray; 
     result->p_isCoArray = isCoArray_copy; 
  // Copy non-constructor parameter data member (access function): substitutedForTemplateParam_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for substitutedForTemplateParam
  // case: toBeCopied == COPY_DATA for substitutedForTemplateParam
     int substitutedForTemplateParam_copy = p_substitutedForTemplateParam; 
     result->p_substitutedForTemplateParam = substitutedForTemplateParam_copy; 
  // Copy non-constructor parameter data member (access function): ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ref_to
  // case: toBeCopied == COPY_DATA for ref_to
     SgReferenceType* ref_to_copy = p_ref_to; 
     result->p_ref_to = ref_to_copy; 
  // Copy non-constructor parameter data member (access function): ptr_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ptr_to
  // case: toBeCopied == COPY_DATA for ptr_to
     SgPointerType* ptr_to_copy = p_ptr_to; 
     result->p_ptr_to = ptr_to_copy; 
  // Copy non-constructor parameter data member (access function): modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgModifierNodes* modifiers_copy = p_modifiers; 
     result->p_modifiers = modifiers_copy; 
  // Copy non-constructor parameter data member (access function): typedefs_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefs
  // case: toBeCopied == COPY_DATA for typedefs
     SgTypedefSeq* typedefs_copy = p_typedefs; 
     result->p_typedefs = typedefs_copy; 
  // Copy non-constructor parameter data member (access function): rvalue_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rvalue_ref_to
  // case: toBeCopied == COPY_DATA for rvalue_ref_to
     SgRvalueReferenceType* rvalue_ref_to_copy = p_rvalue_ref_to; 
     result->p_rvalue_ref_to = rvalue_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): decltype_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decltype_ref_to
  // case: toBeCopied == COPY_DATA for decltype_ref_to
     SgDeclType* decltype_ref_to_copy = p_decltype_ref_to; 
     result->p_decltype_ref_to = decltype_ref_to_copy; 
  // Copy non-constructor parameter data member (access function): typeof_ref_to_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeof_ref_to
  // case: toBeCopied == COPY_DATA for typeof_ref_to
     SgTypeOfType* typeof_ref_to_copy = p_typeof_ref_to; 
     result->p_typeof_ref_to = typeof_ref_to_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_kind
     SgExpression* type_kind_copy; 
  // case: not a listType for (using conditionalToCopyVariable)type_kind
          if (get_type_kind() != nullptr) 
             { 
               type_kind_copy = static_cast<SgExpression*>(help.copyAst(get_type_kind())); 
             } 
            else 
             { 
               type_kind_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_type_kind != nullptr) delete result->p_type_kind; 
     result->p_type_kind = type_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)type_kind
        if ((type_kind_copy != nullptr) && (type_kind_copy->get_parent() == nullptr) && (isSgType(type_kind_copy) == nullptr)) {
            type_kind_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): hasTypeKindStar_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasTypeKindStar
  // case: toBeCopied == COPY_DATA for hasTypeKindStar
     bool hasTypeKindStar_copy = p_hasTypeKindStar; 
     result->p_hasTypeKindStar = hasTypeKindStar_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29637 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLocatedNode::copy(SgCopyHelp& help) const {
    SgLocatedNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLocatedNode( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29789 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgToken::copy(SgCopyHelp& help) const {
    SgToken* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgToken( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 29941 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLocatedNodeSupport::copy(SgCopyHelp& help) const {
    SgLocatedNodeSupport* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLocatedNodeSupport( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 30093 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonBlockObject::copy(SgCopyHelp& help) const {
    SgCommonBlockObject* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCommonBlockObject( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): block_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for block_name
  // case: toBeCopied == COPY_DATA for block_name
     std::string block_name_copy = p_block_name; 
     result->p_block_name = block_name_copy; 
  // Copy non-constructor parameter data member (access function): variable_reference_list_copy
     SgExprListExp* variable_reference_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variable_reference_list
          if (get_variable_reference_list() != nullptr) 
             { 
               variable_reference_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_variable_reference_list())); 
             } 
            else 
             { 
               variable_reference_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_variable_reference_list != nullptr) delete result->p_variable_reference_list; 
     result->p_variable_reference_list = variable_reference_list_copy; 
  // case: not a listType for (using conditionalToSetParent)variable_reference_list
        if ((variable_reference_list_copy != nullptr) && (variable_reference_list_copy->get_parent() == nullptr) && (isSgType(variable_reference_list_copy) == nullptr)) {
            variable_reference_list_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 30268 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInitializedName::copy(SgCopyHelp& help) const {
    SgInitializedName* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: typeptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typeptr
  // case: toBeCopied == COPY_DATA for typeptr
     SgType* typeptr_copy = p_typeptr; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInitializedName( startOfConstruct_copy, name_copy, typeptr_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): microsoft_uuid_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for microsoft_uuid_string
  // case: toBeCopied == COPY_DATA for microsoft_uuid_string
     std::string microsoft_uuid_string_copy = p_microsoft_uuid_string; 
     result->p_microsoft_uuid_string = microsoft_uuid_string_copy; 
  // Copy non-constructor parameter data member (access function): initptr_copy
     SgInitializer* initptr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initptr
          if (get_initptr() != nullptr) 
             { 
               initptr_copy = static_cast<SgInitializer*>(help.copyAst(get_initptr())); 
             } 
            else 
             { 
               initptr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_initptr != nullptr) delete result->p_initptr; 
     result->p_initptr = initptr_copy; 
  // case: not a listType for (using conditionalToSetParent)initptr
        if ((initptr_copy != nullptr) && (initptr_copy->get_parent() == nullptr) && (isSgType(initptr_copy) == nullptr)) {
            initptr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): prev_decl_item_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prev_decl_item
  // case: toBeCopied == COPY_DATA for prev_decl_item
     SgInitializedName* prev_decl_item_copy = p_prev_decl_item; 
     result->p_prev_decl_item = prev_decl_item_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_is_initializer
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_initializer
  // case: toBeCopied == COPY_DATA for is_initializer
     bool is_initializer_copy = p_is_initializer; 
     result->p_is_initializer = is_initializer_copy; 
  // Copy non-constructor parameter data member (access function): declptr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declptr
  // case: toBeCopied == COPY_DATA for declptr
     SgDeclarationStatement* declptr_copy = p_declptr; 
     result->p_declptr = declptr_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): preinitialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for preinitialization
  // case: toBeCopied == COPY_DATA for preinitialization
     SgInitializedName::preinitialization_enum preinitialization_copy = p_preinitialization; 
     result->p_preinitialization = preinitialization_copy; 
  // Copy non-constructor parameter data member (access function): register_name_code_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for register_name_code
  // case: toBeCopied == COPY_DATA for register_name_code
     SgInitializedName::asm_register_name_enum register_name_code_copy = p_register_name_code; 
     result->p_register_name_code = register_name_code_copy; 
  // Copy non-constructor parameter data member (access function): excess_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for excess_specifier
  // case: toBeCopied == COPY_DATA for excess_specifier
     SgInitializedName::excess_specifier_enum excess_specifier_copy = p_excess_specifier; 
     result->p_excess_specifier = excess_specifier_copy; 
  // Copy non-constructor parameter data member (access function): register_name_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for register_name_string
  // case: toBeCopied == COPY_DATA for register_name_string
     std::string register_name_string_copy = p_register_name_string; 
     result->p_register_name_string = register_name_string_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgInitializedName::e_flags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
  // Copy non-constructor parameter data member (access function): constant_or_type_argument_for_Alignas_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_or_type_argument_for_Alignas_keyword
  // case: toBeCopied == COPY_DATA for constant_or_type_argument_for_Alignas_keyword
     SgNode* constant_or_type_argument_for_Alignas_keyword_copy = p_constant_or_type_argument_for_Alignas_keyword; 
     result->p_constant_or_type_argument_for_Alignas_keyword = constant_or_type_argument_for_Alignas_keyword_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_modifierVector_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_modifierVector
  // case: toBeCopied == COPY_DATA for gnu_attribute_modifierVector
     SgBitVector gnu_attribute_modifierVector_copy = p_gnu_attribute_modifierVector; 
     result->p_gnu_attribute_modifierVector = gnu_attribute_modifierVector_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_attribute_initialization_priority
     unsigned long int gnu_attribute_initialization_priority_copy = p_gnu_attribute_initialization_priority; 
     result->p_gnu_attribute_initialization_priority = gnu_attribute_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_weak_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_weak_reference
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_weak_reference
     std::string gnu_attribute_named_weak_reference_copy = p_gnu_attribute_named_weak_reference; 
     result->p_gnu_attribute_named_weak_reference = gnu_attribute_named_weak_reference_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_named_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_named_alias
  // case: toBeCopied == COPY_DATA for gnu_attribute_named_alias
     std::string gnu_attribute_named_alias_copy = p_gnu_attribute_named_alias; 
     result->p_gnu_attribute_named_alias = gnu_attribute_named_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_cleanup_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_cleanup_function
  // case: toBeCopied == COPY_DATA for gnu_attribute_cleanup_function
     std::string gnu_attribute_cleanup_function_copy = p_gnu_attribute_cleanup_function; 
     result->p_gnu_attribute_cleanup_function = gnu_attribute_cleanup_function_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_section_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_section_name
  // case: toBeCopied == COPY_DATA for gnu_attribute_section_name
     std::string gnu_attribute_section_name_copy = p_gnu_attribute_section_name; 
     result->p_gnu_attribute_section_name = gnu_attribute_section_name_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_alignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_alignment
  // case: toBeCopied == COPY_DATA for gnu_attribute_alignment
     int gnu_attribute_alignment_copy = p_gnu_attribute_alignment; 
     result->p_gnu_attribute_alignment = gnu_attribute_alignment_copy; 
  // Copy non-constructor parameter data member (access function): gnu_attribute_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_attribute_visability
  // case: toBeCopied == COPY_DATA for gnu_attribute_visability
     SgDeclarationModifier::gnu_declaration_visability_enum gnu_attribute_visability_copy = p_gnu_attribute_visability; 
     result->p_gnu_attribute_visability = gnu_attribute_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
  // Copy non-constructor parameter data member (access function): auto_decltype_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for auto_decltype
  // case: toBeCopied == COPY_DATA for auto_decltype
     SgType * auto_decltype_copy = p_auto_decltype; 
     result->p_auto_decltype = auto_decltype_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 30556 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceBody::copy(SgCopyHelp& help) const {
    SgInterfaceBody* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_name
  // case: toBeCopied == COPY_DATA for function_name
     SgName function_name_copy = p_function_name; 
    // Copy constructor parameter data member: functionDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionDeclaration
  // case: toBeCopied == COPY_DATA for functionDeclaration
     SgFunctionDeclaration* functionDeclaration_copy = p_functionDeclaration; 
    // Copy constructor parameter data member: use_function_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_function_name
  // case: toBeCopied == COPY_DATA for use_function_name
     bool use_function_name_copy = p_use_function_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInterfaceBody( startOfConstruct_copy, function_name_copy, functionDeclaration_copy, use_function_name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 30720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHeaderFileBody::copy(SgCopyHelp& help) const {
    SgHeaderFileBody* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgHeaderFileBody( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): include_file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for include_file
  // case: toBeCopied == COPY_DATA for include_file
     SgSourceFile* include_file_copy = p_include_file; 
     result->p_include_file = include_file_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 30877 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRenamePair::copy(SgCopyHelp& help) const {
    SgRenamePair* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: local_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_name
  // case: toBeCopied == COPY_DATA for local_name
     SgName local_name_copy = p_local_name; 
    // Copy constructor parameter data member: use_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_name
  // case: toBeCopied == COPY_DATA for use_name
     SgName use_name_copy = p_use_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRenamePair( startOfConstruct_copy, local_name_copy, use_name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31037 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaMemberValuePair::copy(SgCopyHelp& help) const {
    SgJavaMemberValuePair* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaMemberValuePair( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_value != nullptr) delete result->p_value; 
     result->p_value = value_copy; 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31212 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpClause::copy(SgCopyHelp& help) const {
    SgOmpClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31364 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNowaitClause::copy(SgCopyHelp& help) const {
    SgOmpNowaitClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpNowaitClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31516 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBeginClause::copy(SgCopyHelp& help) const {
    SgOmpBeginClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpBeginClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31668 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpEndClause::copy(SgCopyHelp& help) const {
    SgOmpEndClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpEndClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31820 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpUntiedClause::copy(SgCopyHelp& help) const {
    SgOmpUntiedClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpUntiedClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 31972 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDefaultClause::copy(SgCopyHelp& help) const {
    SgOmpDefaultClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: data_sharing_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_sharing
  // case: toBeCopied == COPY_DATA for data_sharing
     SgOmpClause::omp_default_option_enum data_sharing_copy = p_data_sharing; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpDefaultClause( startOfConstruct_copy, data_sharing_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAtomicClause::copy(SgCopyHelp& help) const {
    SgOmpAtomicClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: atomicity_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for atomicity
  // case: toBeCopied == COPY_DATA for atomicity
     SgOmpClause::omp_atomic_clause_enum atomicity_copy = p_atomicity; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpAtomicClause( startOfConstruct_copy, atomicity_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32284 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpProcBindClause::copy(SgCopyHelp& help) const {
    SgOmpProcBindClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: policy_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for policy
  // case: toBeCopied == COPY_DATA for policy
     SgOmpClause::omp_proc_bind_policy_enum policy_copy = p_policy; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpProcBindClause( startOfConstruct_copy, policy_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpExpressionClause::copy(SgCopyHelp& help) const {
    SgOmpExpressionClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpExpressionClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32607 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpOrderedClause::copy(SgCopyHelp& help) const {
    SgOmpOrderedClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpOrderedClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32774 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCollapseClause::copy(SgCopyHelp& help) const {
    SgOmpCollapseClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpCollapseClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 32941 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpIfClause::copy(SgCopyHelp& help) const {
    SgOmpIfClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpIfClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33108 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNumThreadsClause::copy(SgCopyHelp& help) const {
    SgOmpNumThreadsClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpNumThreadsClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDeviceClause::copy(SgCopyHelp& help) const {
    SgOmpDeviceClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpDeviceClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33442 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSafelenClause::copy(SgCopyHelp& help) const {
    SgOmpSafelenClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSafelenClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33609 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSimdlenClause::copy(SgCopyHelp& help) const {
    SgOmpSimdlenClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSimdlenClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFinalClause::copy(SgCopyHelp& help) const {
    SgOmpFinalClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpFinalClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 33943 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpPriorityClause::copy(SgCopyHelp& help) const {
    SgOmpPriorityClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpPriorityClause( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34110 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpInbranchClause::copy(SgCopyHelp& help) const {
    SgOmpInbranchClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpInbranchClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34262 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpNotinbranchClause::copy(SgCopyHelp& help) const {
    SgOmpNotinbranchClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpNotinbranchClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpVariablesClause::copy(SgCopyHelp& help) const {
    SgOmpVariablesClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpVariablesClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34581 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCopyprivateClause::copy(SgCopyHelp& help) const {
    SgOmpCopyprivateClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpCopyprivateClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34748 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpPrivateClause::copy(SgCopyHelp& help) const {
    SgOmpPrivateClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpPrivateClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 34915 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFirstprivateClause::copy(SgCopyHelp& help) const {
    SgOmpFirstprivateClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpFirstprivateClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35082 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSharedClause::copy(SgCopyHelp& help) const {
    SgOmpSharedClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSharedClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35249 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCopyinClause::copy(SgCopyHelp& help) const {
    SgOmpCopyinClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpCopyinClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpLastprivateClause::copy(SgCopyHelp& help) const {
    SgOmpLastprivateClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpLastprivateClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35583 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpReductionClause::copy(SgCopyHelp& help) const {
    SgOmpReductionClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operation
  // case: toBeCopied == COPY_DATA for operation
     SgOmpClause::omp_reduction_operator_enum operation_copy = p_operation; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpReductionClause( startOfConstruct_copy, variables_copy, operation_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35754 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMapClause::copy(SgCopyHelp& help) const {
    SgOmpMapClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operation_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operation
  // case: toBeCopied == COPY_DATA for operation
     SgOmpClause::omp_map_operator_enum operation_copy = p_operation; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpMapClause( startOfConstruct_copy, variables_copy, operation_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): array_dimensions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_dimensions
  // case: toBeCopied == COPY_DATA for array_dimensions
     std::map<SgSymbol*,  std::vector < std::pair <SgExpression*, SgExpression*> > > array_dimensions_copy = p_array_dimensions; 
     result->p_array_dimensions = array_dimensions_copy; 
  // Copy non-constructor parameter data member (access function): dist_data_policies_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dist_data_policies
  // case: toBeCopied == COPY_DATA for dist_data_policies
     std::map<SgSymbol*,  std::vector < std::pair <SgOmpClause::omp_map_dist_data_enum, SgExpression*> > > dist_data_policies_copy = p_dist_data_policies; 
     result->p_dist_data_policies = dist_data_policies_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 35935 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpUniformClause::copy(SgCopyHelp& help) const {
    SgOmpUniformClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpUniformClause( startOfConstruct_copy, variables_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36102 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAlignedClause::copy(SgCopyHelp& help) const {
    SgOmpAlignedClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
    // Copy constructor parameter data member: alignment_copy
     SgExpression* alignment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alignment
          if (get_alignment() != nullptr) 
             { 
               alignment_copy = static_cast<SgExpression*>(help.copyAst(get_alignment())); 
             } 
            else 
             { 
               alignment_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpAlignedClause( startOfConstruct_copy, variables_copy, alignment_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)alignment
        if ((alignment_copy != nullptr) && (alignment_copy->get_parent() == nullptr) && (isSgType(alignment_copy) == nullptr)) {
            alignment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36284 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpLinearClause::copy(SgCopyHelp& help) const {
    SgOmpLinearClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
    // Copy constructor parameter data member: step_copy
     SgExpression* step_copy; 
  // case: not a listType for (using conditionalToCopyVariable)step
          if (get_step() != nullptr) 
             { 
               step_copy = static_cast<SgExpression*>(help.copyAst(get_step())); 
             } 
            else 
             { 
               step_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpLinearClause( startOfConstruct_copy, variables_copy, step_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)step
        if ((step_copy != nullptr) && (step_copy->get_parent() == nullptr) && (isSgType(step_copy) == nullptr)) {
            step_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36466 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDependClause::copy(SgCopyHelp& help) const {
    SgOmpDependClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variables_copy
     SgExprListExp* variables_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variables
          if (get_variables() != nullptr) 
             { 
               variables_copy = static_cast<SgExprListExp*>(help.copyAst(get_variables())); 
             } 
            else 
             { 
               variables_copy = nullptr; 
             } 
    // Copy constructor parameter data member: dependence_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dependence_type
  // case: toBeCopied == COPY_DATA for dependence_type
     SgOmpClause::omp_dependence_type_enum dependence_type_copy = p_dependence_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpDependClause( startOfConstruct_copy, variables_copy, dependence_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): array_dimensions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for array_dimensions
  // case: toBeCopied == COPY_DATA for array_dimensions
     std::map<SgSymbol*,  std::vector < std::pair <SgExpression*, SgExpression*> > > array_dimensions_copy = p_array_dimensions; 
     result->p_array_dimensions = array_dimensions_copy; 
  // case: not a listType for (using conditionalToSetParent)variables
        if ((variables_copy != nullptr) && (variables_copy->get_parent() == nullptr) && (isSgType(variables_copy) == nullptr)) {
            variables_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36642 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpScheduleClause::copy(SgCopyHelp& help) const {
    SgOmpScheduleClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     SgOmpClause::omp_schedule_kind_enum kind_copy = p_kind; 
    // Copy constructor parameter data member: chunk_size_copy
     SgExpression* chunk_size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)chunk_size
          if (get_chunk_size() != nullptr) 
             { 
               chunk_size_copy = static_cast<SgExpression*>(help.copyAst(get_chunk_size())); 
             } 
            else 
             { 
               chunk_size_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpScheduleClause( startOfConstruct_copy, kind_copy, chunk_size_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)chunk_size
        if ((chunk_size_copy != nullptr) && (chunk_size_copy->get_parent() == nullptr) && (isSgType(chunk_size_copy) == nullptr)) {
            chunk_size_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36813 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMergeableClause::copy(SgCopyHelp& help) const {
    SgOmpMergeableClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpMergeableClause( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 36965 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaCapture::copy(SgCopyHelp& help) const {
    SgLambdaCapture* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: capture_variable_copy
     SgExpression* capture_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)capture_variable
          if (get_capture_variable() != nullptr) 
             { 
               capture_variable_copy = static_cast<SgExpression*>(help.copyAst(get_capture_variable())); 
             } 
            else 
             { 
               capture_variable_copy = nullptr; 
             } 
    // Copy constructor parameter data member: source_closure_variable_copy
     SgExpression* source_closure_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)source_closure_variable
          if (get_source_closure_variable() != nullptr) 
             { 
               source_closure_variable_copy = static_cast<SgExpression*>(help.copyAst(get_source_closure_variable())); 
             } 
            else 
             { 
               source_closure_variable_copy = nullptr; 
             } 
    // Copy constructor parameter data member: closure_variable_copy
     SgExpression* closure_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)closure_variable
          if (get_closure_variable() != nullptr) 
             { 
               closure_variable_copy = static_cast<SgExpression*>(help.copyAst(get_closure_variable())); 
             } 
            else 
             { 
               closure_variable_copy = nullptr; 
             } 
    // Copy constructor parameter data member: capture_by_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for capture_by_reference
  // case: toBeCopied == COPY_DATA for capture_by_reference
     bool capture_by_reference_copy = p_capture_by_reference; 
    // Copy constructor parameter data member: implicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit
  // case: toBeCopied == COPY_DATA for implicit
     bool implicit_copy = p_implicit; 
    // Copy constructor parameter data member: pack_expansion_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pack_expansion
  // case: toBeCopied == COPY_DATA for pack_expansion
     bool pack_expansion_copy = p_pack_expansion; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLambdaCapture( startOfConstruct_copy, capture_variable_copy, source_closure_variable_copy, closure_variable_copy, capture_by_reference_copy, implicit_copy, pack_expansion_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)capture_variable
        if ((capture_variable_copy != nullptr) && (capture_variable_copy->get_parent() == nullptr) && (isSgType(capture_variable_copy) == nullptr)) {
            capture_variable_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)source_closure_variable
        if ((source_closure_variable_copy != nullptr) && (source_closure_variable_copy->get_parent() == nullptr) && (isSgType(source_closure_variable_copy) == nullptr)) {
            source_closure_variable_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)closure_variable
        if ((closure_variable_copy != nullptr) && (closure_variable_copy->get_parent() == nullptr) && (isSgType(closure_variable_copy) == nullptr)) {
            closure_variable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 37174 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaCaptureList::copy(SgCopyHelp& help) const {
    SgLambdaCaptureList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLambdaCaptureList( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_capture_list()
     SgLambdaCapturePtrList capture_list_copy; 
  // case: listType for capture_list
  // case: listType (typeIsPointerToList == false) for capture_list
    auto source_capture_list_iterator = get_capture_list().begin();
    for (/* empty by design */; source_capture_list_iterator != get_capture_list().end(); ++source_capture_list_iterator) {
          SgLambdaCapture* source_list_element = *source_capture_list_iterator; 
          SgLambdaCapture* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgLambdaCapture*>(help.copyAst(*source_capture_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          capture_list_copy.push_back(copy_list_element); 
        }
     result->p_capture_list = capture_list_copy; 
  // case: listType for capture_list
  // case: listType (typeIsPointerToList == false) for capture_list
    auto capture_list_iterator = result->get_capture_list().begin();
    for (/* empty by design */; capture_list_iterator != result->get_capture_list().end(); ++capture_list_iterator) {
          SgLambdaCapture* list_element = *capture_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 37354 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTypeConstraint::copy(SgCopyHelp& help) const {
    SgAdaTypeConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTypeConstraint( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 37506 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRangeConstraint::copy(SgCopyHelp& help) const {
    SgAdaRangeConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: range_copy
     SgExpression* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != nullptr) 
             { 
               range_copy = static_cast<SgExpression*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaRangeConstraint( startOfConstruct_copy, range_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)range
        if ((range_copy != nullptr) && (range_copy->get_parent() == nullptr) && (isSgType(range_copy) == nullptr)) {
            range_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 37673 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaIndexConstraint::copy(SgCopyHelp& help) const {
    SgAdaIndexConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaIndexConstraint( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_indexRanges()
     SgExpressionPtrList indexRanges_copy; 
  // case: listType for indexRanges
  // case: listType (typeIsPointerToList == false) for indexRanges
    auto source_indexRanges_iterator = get_indexRanges().begin();
    for (/* empty by design */; source_indexRanges_iterator != get_indexRanges().end(); ++source_indexRanges_iterator) {
          SgExpression* source_list_element = *source_indexRanges_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_indexRanges_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          indexRanges_copy.push_back(copy_list_element); 
        }
     result->p_indexRanges = indexRanges_copy; 
  // case: listType for indexRanges
  // case: listType (typeIsPointerToList == false) for indexRanges
    auto indexRanges_iterator = result->get_indexRanges().begin();
    for (/* empty by design */; indexRanges_iterator != result->get_indexRanges().end(); ++indexRanges_iterator) {
          SgExpression* list_element = *indexRanges_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 37853 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDigitsConstraint::copy(SgCopyHelp& help) const {
    SgAdaDigitsConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: digits_copy
     SgExpression* digits_copy; 
  // case: not a listType for (using conditionalToCopyVariable)digits
          if (get_digits() != nullptr) 
             { 
               digits_copy = static_cast<SgExpression*>(help.copyAst(get_digits())); 
             } 
            else 
             { 
               digits_copy = nullptr; 
             } 
    // Copy constructor parameter data member: subConstraint_copy
     SgAdaTypeConstraint* subConstraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subConstraint
          if (get_subConstraint() != nullptr) 
             { 
               subConstraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_subConstraint())); 
             } 
            else 
             { 
               subConstraint_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDigitsConstraint( startOfConstruct_copy, digits_copy, subConstraint_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)digits
        if ((digits_copy != nullptr) && (digits_copy->get_parent() == nullptr) && (isSgType(digits_copy) == nullptr)) {
            digits_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)subConstraint
        if ((subConstraint_copy != nullptr) && (subConstraint_copy->get_parent() == nullptr) && (isSgType(subConstraint_copy) == nullptr)) {
            subConstraint_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38035 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDeltaConstraint::copy(SgCopyHelp& help) const {
    SgAdaDeltaConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: delta_copy
     SgExpression* delta_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delta
          if (get_delta() != nullptr) 
             { 
               delta_copy = static_cast<SgExpression*>(help.copyAst(get_delta())); 
             } 
            else 
             { 
               delta_copy = nullptr; 
             } 
    // Copy constructor parameter data member: subConstraint_copy
     SgAdaTypeConstraint* subConstraint_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subConstraint
          if (get_subConstraint() != nullptr) 
             { 
               subConstraint_copy = static_cast<SgAdaTypeConstraint*>(help.copyAst(get_subConstraint())); 
             } 
            else 
             { 
               subConstraint_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isDecimal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDecimal
  // case: toBeCopied == COPY_DATA for isDecimal
     bool isDecimal_copy = p_isDecimal; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDeltaConstraint( startOfConstruct_copy, delta_copy, subConstraint_copy, isDecimal_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)delta
        if ((delta_copy != nullptr) && (delta_copy->get_parent() == nullptr) && (isSgType(delta_copy) == nullptr)) {
            delta_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)subConstraint
        if ((subConstraint_copy != nullptr) && (subConstraint_copy->get_parent() == nullptr) && (isSgType(subConstraint_copy) == nullptr)) {
            subConstraint_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38221 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminantConstraint::copy(SgCopyHelp& help) const {
    SgAdaDiscriminantConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDiscriminantConstraint( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_discriminants()
     SgExpressionPtrList discriminants_copy; 
  // case: listType for discriminants
  // case: listType (typeIsPointerToList == false) for discriminants
    auto source_discriminants_iterator = get_discriminants().begin();
    for (/* empty by design */; source_discriminants_iterator != get_discriminants().end(); ++source_discriminants_iterator) {
          SgExpression* source_list_element = *source_discriminants_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_discriminants_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          discriminants_copy.push_back(copy_list_element); 
        }
     result->p_discriminants = discriminants_copy; 
  // case: listType for discriminants
  // case: listType (typeIsPointerToList == false) for discriminants
    auto discriminants_iterator = result->get_discriminants().begin();
    for (/* empty by design */; discriminants_iterator != result->get_discriminants().end(); ++discriminants_iterator) {
          SgExpression* list_element = *discriminants_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38401 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaNullConstraint::copy(SgCopyHelp& help) const {
    SgAdaNullConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaNullConstraint( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38553 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatement::copy(SgCopyHelp& help) const {
    SgStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38715 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopeStatement::copy(SgCopyHelp& help) const {
    SgScopeStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgScopeStatement();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 38880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGlobal::copy(SgCopyHelp& help) const {
    SgGlobal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGlobal( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 39070 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBasicBlock::copy(SgCopyHelp& help) const {
    SgBasicBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBasicBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto source_statements_iterator = get_statements().begin();
    for (/* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) {
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statements_copy.push_back(copy_list_element); 
        }
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto statements_iterator = result->get_statements().begin();
    for (/* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) {
          SgStatement* list_element = *statements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): asm_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_function_body
  // case: toBeCopied == COPY_DATA for asm_function_body
     std::string asm_function_body_copy = p_asm_function_body; 
     result->p_asm_function_body = asm_function_body_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 39273 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfStmt::copy(SgCopyHelp& help) const {
    SgIfStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: conditional_copy
     SgStatement* conditional_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional
          if (get_conditional() != nullptr) 
             { 
               conditional_copy = static_cast<SgStatement*>(help.copyAst(get_conditional())); 
             } 
            else 
             { 
               conditional_copy = nullptr; 
             } 
    // Copy constructor parameter data member: true_body_copy
     SgStatement* true_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)true_body
          if (get_true_body() != nullptr) 
             { 
               true_body_copy = static_cast<SgStatement*>(help.copyAst(get_true_body())); 
             } 
            else 
             { 
               true_body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: false_body_copy
     SgStatement* false_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)false_body
          if (get_false_body() != nullptr) 
             { 
               false_body_copy = static_cast<SgStatement*>(help.copyAst(get_false_body())); 
             } 
            else 
             { 
               false_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIfStmt( startOfConstruct_copy, conditional_copy, true_body_copy, false_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): is_if_constexpr_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_if_constexpr_statement
  // case: toBeCopied == COPY_DATA for is_if_constexpr_statement
     bool is_if_constexpr_statement_copy = p_is_if_constexpr_statement; 
     result->p_is_if_constexpr_statement = is_if_constexpr_statement_copy; 
  // Copy non-constructor parameter data member (access function): if_constexpr_value_known_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for if_constexpr_value_known
  // case: toBeCopied == COPY_DATA for if_constexpr_value_known
     bool if_constexpr_value_known_copy = p_if_constexpr_value_known; 
     result->p_if_constexpr_value_known = if_constexpr_value_known_copy; 
  // Copy non-constructor parameter data member (access function): if_constexpr_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for if_constexpr_value
  // case: toBeCopied == COPY_DATA for if_constexpr_value
     bool if_constexpr_value_copy = p_if_constexpr_value; 
     result->p_if_constexpr_value = if_constexpr_value_copy; 
  // Copy non-constructor parameter data member (access function): else_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for else_numeric_label
  // case: toBeCopied == COPY_DATA for else_numeric_label
     SgLabelRefExp* else_numeric_label_copy = p_else_numeric_label; 
     result->p_else_numeric_label = else_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): use_then_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for use_then_keyword
  // case: toBeCopied == COPY_DATA for use_then_keyword
     bool use_then_keyword_copy = p_use_then_keyword; 
     result->p_use_then_keyword = use_then_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_else_if_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_else_if_statement
  // case: toBeCopied == COPY_DATA for is_else_if_statement
     bool is_else_if_statement_copy = p_is_else_if_statement; 
     result->p_is_else_if_statement = is_else_if_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)conditional
        if ((conditional_copy != nullptr) && (conditional_copy->get_parent() == nullptr) && (isSgType(conditional_copy) == nullptr)) {
            conditional_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)true_body
        if ((true_body_copy != nullptr) && (true_body_copy->get_parent() == nullptr) && (isSgType(true_body_copy) == nullptr)) {
            true_body_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)false_body
        if ((false_body_copy != nullptr) && (false_body_copy->get_parent() == nullptr) && (isSgType(false_body_copy) == nullptr)) {
            false_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 39525 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForStatement::copy(SgCopyHelp& help) const {
    SgForStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: test_copy
     SgStatement* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != nullptr) 
             { 
               test_copy = static_cast<SgStatement*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != nullptr) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != nullptr) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgForStatement( startOfConstruct_copy, test_copy, increment_copy, loop_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): for_init_stmt_copy
     SgForInitStatement* for_init_stmt_copy; 
  // case: not a listType for (using conditionalToCopyVariable)for_init_stmt
          if (get_for_init_stmt() != nullptr) 
             { 
               for_init_stmt_copy = static_cast<SgForInitStatement*>(help.copyAst(get_for_init_stmt())); 
             } 
            else 
             { 
               for_init_stmt_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_for_init_stmt != nullptr) delete result->p_for_init_stmt; 
     result->p_for_init_stmt = for_init_stmt_copy; 
  // case: not a listType for (using conditionalToSetParent)for_init_stmt
        if ((for_init_stmt_copy != nullptr) && (for_init_stmt_copy->get_parent() == nullptr) && (isSgType(for_init_stmt_copy) == nullptr)) {
            for_init_stmt_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != nullptr) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != nullptr) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
        if ((else_body_copy != nullptr) && (else_body_copy->get_parent() == nullptr) && (isSgType(else_body_copy) == nullptr)) {
            else_body_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // case: not a listType for (using conditionalToSetParent)test
        if ((test_copy != nullptr) && (test_copy->get_parent() == nullptr) && (isSgType(test_copy) == nullptr)) {
            test_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment
        if ((increment_copy != nullptr) && (increment_copy->get_parent() == nullptr) && (isSgType(increment_copy) == nullptr)) {
            increment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)loop_body
        if ((loop_body_copy != nullptr) && (loop_body_copy->get_parent() == nullptr) && (isSgType(loop_body_copy) == nullptr)) {
            loop_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 39773 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionDefinition::copy(SgCopyHelp& help) const {
    SgFunctionDefinition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionDefinition( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 39950 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionDefinition::copy(SgCopyHelp& help) const {
    SgTemplateFunctionDefinition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateFunctionDefinition( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 40127 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDefinition::copy(SgCopyHelp& help) const {
    SgClassDefinition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassDefinition( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto source_members_iterator = get_members().begin();
    for (/* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) {
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          members_copy.push_back(copy_list_element); 
        }
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto members_iterator = result->get_members().begin();
    for (/* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) {
          SgDeclarationStatement* list_element = *members_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto source_inheritances_iterator = get_inheritances().begin();
    for (/* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) {
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        }
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto inheritances_iterator = result->get_inheritances().begin();
    for (/* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) {
          SgBaseClass* list_element = *inheritances_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 40365 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDefn::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationDefn* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationDefn( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto source_members_iterator = get_members().begin();
    for (/* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) {
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          members_copy.push_back(copy_list_element); 
        }
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto members_iterator = result->get_members().begin();
    for (/* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) {
          SgDeclarationStatement* list_element = *members_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto source_inheritances_iterator = get_inheritances().begin();
    for (/* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) {
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        }
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto inheritances_iterator = result->get_inheritances().begin();
    for (/* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) {
          SgBaseClass* list_element = *inheritances_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 40603 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassDefinition::copy(SgCopyHelp& help) const {
    SgTemplateClassDefinition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateClassDefinition( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_members()
     SgDeclarationStatementPtrList members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto source_members_iterator = get_members().begin();
    for (/* empty by design */; source_members_iterator != get_members().end(); ++source_members_iterator) {
          SgDeclarationStatement* source_list_element = *source_members_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_members_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          members_copy.push_back(copy_list_element); 
        }
     result->p_members = members_copy; 
  // case: listType for members
  // case: listType (typeIsPointerToList == false) for members
    auto members_iterator = result->get_members().begin();
    for (/* empty by design */; members_iterator != result->get_members().end(); ++members_iterator) {
          SgDeclarationStatement* list_element = *members_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (list access function): result->get_inheritances()
     SgBaseClassPtrList inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto source_inheritances_iterator = get_inheritances().begin();
    for (/* empty by design */; source_inheritances_iterator != get_inheritances().end(); ++source_inheritances_iterator) {
          SgBaseClass* source_list_element = *source_inheritances_iterator; 
          SgBaseClass* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgBaseClass*>(help.copyAst(*source_inheritances_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          inheritances_copy.push_back(copy_list_element); 
        }
     result->p_inheritances = inheritances_copy; 
  // case: listType for inheritances
  // case: listType (typeIsPointerToList == false) for inheritances
    auto inheritances_iterator = result->get_inheritances().begin();
    for (/* empty by design */; inheritances_iterator != result->get_inheritances().end(); ++inheritances_iterator) {
          SgBaseClass* list_element = *inheritances_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): packingAlignment_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for packingAlignment
  // case: toBeCopied == COPY_DATA for packingAlignment
     unsigned int packingAlignment_copy = p_packingAlignment; 
     result->p_packingAlignment = packingAlignment_copy; 
  // Copy non-constructor parameter data member (access function): isSequence_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSequence
  // case: toBeCopied == COPY_DATA for isSequence
     bool isSequence_copy = p_isSequence; 
     result->p_isSequence = isSequence_copy; 
  // Copy non-constructor parameter data member (access function): isPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isPrivate
  // case: toBeCopied == COPY_DATA for isPrivate
     bool isPrivate_copy = p_isPrivate; 
     result->p_isPrivate = isPrivate_copy; 
  // Copy non-constructor parameter data member (access function): isAbstract_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAbstract
  // case: toBeCopied == COPY_DATA for isAbstract
     bool isAbstract_copy = p_isAbstract; 
     result->p_isAbstract = isAbstract_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 40841 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhileStmt::copy(SgCopyHelp& help) const {
    SgWhileStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: condition_copy
     SgStatement* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgStatement*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWhileStmt( startOfConstruct_copy, condition_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != nullptr) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != nullptr) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
        if ((else_body_copy != nullptr) && (else_body_copy->get_parent() == nullptr) && (isSgType(else_body_copy) == nullptr)) {
            else_body_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 41066 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDoWhileStmt::copy(SgCopyHelp& help) const {
    SgDoWhileStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: condition_copy
     SgStatement* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgStatement*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDoWhileStmt( startOfConstruct_copy, body_copy, condition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 41258 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSwitchStatement::copy(SgCopyHelp& help) const {
    SgSwitchStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: item_selector_copy
     SgStatement* item_selector_copy; 
  // case: not a listType for (using conditionalToCopyVariable)item_selector
          if (get_item_selector() != nullptr) 
             { 
               item_selector_copy = static_cast<SgStatement*>(help.copyAst(get_item_selector())); 
             } 
            else 
             { 
               item_selector_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSwitchStatement( startOfConstruct_copy, item_selector_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // case: not a listType for (using conditionalToSetParent)item_selector
        if ((item_selector_copy != nullptr) && (item_selector_copy->get_parent() == nullptr) && (isSgType(item_selector_copy) == nullptr)) {
            item_selector_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 41460 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCatchOptionStmt::copy(SgCopyHelp& help) const {
    SgCatchOptionStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: condition_copy
     SgVariableDeclaration* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: trystmt_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for trystmt
  // case: toBeCopied == COPY_DATA for trystmt
     SgTryStmt* trystmt_copy = p_trystmt; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCatchOptionStmt( startOfConstruct_copy, condition_copy, body_copy, trystmt_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 41656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceDefinitionStatement::copy(SgCopyHelp& help) const {
    SgNamespaceDefinitionStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamespaceDefinitionStatement( startOfConstruct_copy, namespaceDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): previousNamespaceDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for previousNamespaceDefinition
  // case: toBeCopied == COPY_DATA for previousNamespaceDefinition
     SgNamespaceDefinitionStatement* previousNamespaceDefinition_copy = p_previousNamespaceDefinition; 
     result->p_previousNamespaceDefinition = previousNamespaceDefinition_copy; 
  // Copy non-constructor parameter data member (access function): nextNamespaceDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nextNamespaceDefinition
  // case: toBeCopied == COPY_DATA for nextNamespaceDefinition
     SgNamespaceDefinitionStatement* nextNamespaceDefinition_copy = p_nextNamespaceDefinition; 
     result->p_nextNamespaceDefinition = nextNamespaceDefinition_copy; 
  // Copy non-constructor parameter data member (access function): global_definition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_definition
  // case: toBeCopied == COPY_DATA for global_definition
     SgNamespaceDefinitionStatement* global_definition_copy = p_global_definition; 
     result->p_global_definition = global_definition_copy; 
  // Copy non-constructor parameter data member (access function): isUnionOfReentrantNamespaceDefinitions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnionOfReentrantNamespaceDefinitions
  // case: toBeCopied == COPY_DATA for isUnionOfReentrantNamespaceDefinitions
     bool isUnionOfReentrantNamespaceDefinitions_copy = p_isUnionOfReentrantNamespaceDefinitions; 
     result->p_isUnionOfReentrantNamespaceDefinitions = isUnionOfReentrantNamespaceDefinitions_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 41870 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBlockDataStatement::copy(SgCopyHelp& help) const {
    SgBlockDataStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBlockDataStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgBasicBlock* body_copy = p_body; 
     result->p_body = body_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 42037 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssociateStatement::copy(SgCopyHelp& help) const {
    SgAssociateStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssociateStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_associates()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associates
  // case: toBeCopied == COPY_DATA for associates
     SgDeclarationStatementPtrList associates_copy = p_associates; 
     result->p_associates = associates_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 42222 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranDo::copy(SgCopyHelp& help) const {
    SgFortranDo* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != nullptr) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = nullptr; 
             } 
    // Copy constructor parameter data member: bound_copy
     SgExpression* bound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bound
          if (get_bound() != nullptr) 
             { 
               bound_copy = static_cast<SgExpression*>(help.copyAst(get_bound())); 
             } 
            else 
             { 
               bound_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != nullptr) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFortranDo( startOfConstruct_copy, initialization_copy, bound_copy, increment_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): old_style_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for old_style
  // case: toBeCopied == COPY_DATA for old_style
     bool old_style_copy = p_old_style; 
     result->p_old_style = old_style_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
        if ((initialization_copy != nullptr) && (initialization_copy->get_parent() == nullptr) && (isSgType(initialization_copy) == nullptr)) {
            initialization_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)bound
        if ((bound_copy != nullptr) && (bound_copy->get_parent() == nullptr) && (isSgType(bound_copy) == nullptr)) {
            bound_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment
        if ((increment_copy != nullptr) && (increment_copy->get_parent() == nullptr) && (isSgType(increment_copy) == nullptr)) {
            increment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 42464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranNonblockedDo::copy(SgCopyHelp& help) const {
    SgFortranNonblockedDo* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != nullptr) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = nullptr; 
             } 
    // Copy constructor parameter data member: bound_copy
     SgExpression* bound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bound
          if (get_bound() != nullptr) 
             { 
               bound_copy = static_cast<SgExpression*>(help.copyAst(get_bound())); 
             } 
            else 
             { 
               bound_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != nullptr) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_statement
  // case: toBeCopied == COPY_DATA for end_statement
     SgStatement* end_statement_copy = p_end_statement; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFortranNonblockedDo( startOfConstruct_copy, initialization_copy, bound_copy, increment_copy, body_copy, end_statement_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): old_style_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for old_style
  // case: toBeCopied == COPY_DATA for old_style
     bool old_style_copy = p_old_style; 
     result->p_old_style = old_style_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
        if ((initialization_copy != nullptr) && (initialization_copy->get_parent() == nullptr) && (isSgType(initialization_copy) == nullptr)) {
            initialization_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)bound
        if ((bound_copy != nullptr) && (bound_copy->get_parent() == nullptr) && (isSgType(bound_copy) == nullptr)) {
            bound_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment
        if ((increment_copy != nullptr) && (increment_copy->get_parent() == nullptr) && (isSgType(increment_copy) == nullptr)) {
            increment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 42710 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForAllStatement::copy(SgCopyHelp& help) const {
    SgForAllStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: forall_header_copy
     SgExprListExp* forall_header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)forall_header
          if (get_forall_header() != nullptr) 
             { 
               forall_header_copy = static_cast<SgExprListExp*>(help.copyAst(get_forall_header())); 
             } 
            else 
             { 
               forall_header_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgForAllStatement( startOfConstruct_copy, forall_header_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): forall_statement_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forall_statement_kind
  // case: toBeCopied == COPY_DATA for forall_statement_kind
     SgForAllStatement::forall_statement_kind_enum forall_statement_kind_copy = p_forall_statement_kind; 
     result->p_forall_statement_kind = forall_statement_kind_copy; 
  // case: not a listType for (using conditionalToSetParent)forall_header
        if ((forall_header_copy != nullptr) && (forall_header_copy->get_parent() == nullptr) && (isSgType(forall_header_copy) == nullptr)) {
            forall_header_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 42922 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcForAllStatement::copy(SgCopyHelp& help) const {
    SgUpcForAllStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: test_copy
     SgStatement* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != nullptr) 
             { 
               test_copy = static_cast<SgStatement*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != nullptr) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: affinity_copy
     SgExpression* affinity_copy; 
  // case: not a listType for (using conditionalToCopyVariable)affinity
          if (get_affinity() != nullptr) 
             { 
               affinity_copy = static_cast<SgExpression*>(help.copyAst(get_affinity())); 
             } 
            else 
             { 
               affinity_copy = nullptr; 
             } 
    // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != nullptr) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcForAllStatement( startOfConstruct_copy, test_copy, increment_copy, affinity_copy, loop_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): for_init_stmt_copy
     SgForInitStatement* for_init_stmt_copy; 
  // case: not a listType for (using conditionalToCopyVariable)for_init_stmt
          if (get_for_init_stmt() != nullptr) 
             { 
               for_init_stmt_copy = static_cast<SgForInitStatement*>(help.copyAst(get_for_init_stmt())); 
             } 
            else 
             { 
               for_init_stmt_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_for_init_stmt != nullptr) delete result->p_for_init_stmt; 
     result->p_for_init_stmt = for_init_stmt_copy; 
  // case: not a listType for (using conditionalToSetParent)for_init_stmt
        if ((for_init_stmt_copy != nullptr) && (for_init_stmt_copy->get_parent() == nullptr) && (isSgType(for_init_stmt_copy) == nullptr)) {
            for_init_stmt_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)test
        if ((test_copy != nullptr) && (test_copy->get_parent() == nullptr) && (isSgType(test_copy) == nullptr)) {
            test_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment
        if ((increment_copy != nullptr) && (increment_copy->get_parent() == nullptr) && (isSgType(increment_copy) == nullptr)) {
            increment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)affinity
        if ((affinity_copy != nullptr) && (affinity_copy->get_parent() == nullptr) && (isSgType(affinity_copy) == nullptr)) {
            affinity_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)loop_body
        if ((loop_body_copy != nullptr) && (loop_body_copy->get_parent() == nullptr) && (isSgType(loop_body_copy) == nullptr)) {
            loop_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 43162 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCAFWithTeamStatement::copy(SgCopyHelp& help) const {
    SgCAFWithTeamStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: teamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamId
  // case: toBeCopied == COPY_DATA for teamId
     SgVarRefExp* teamId_copy = p_teamId; 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCAFWithTeamStatement( startOfConstruct_copy, teamId_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): endHasTeamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endHasTeamId
  // case: toBeCopied == COPY_DATA for endHasTeamId
     bool endHasTeamId_copy = p_endHasTeamId; 
     result->p_endHasTeamId = endHasTeamId_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 43348 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaForEachStatement::copy(SgCopyHelp& help) const {
    SgJavaForEachStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: element_copy
     SgVariableDeclaration* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != nullptr) 
             { 
               element_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = nullptr; 
             } 
    // Copy constructor parameter data member: collection_copy
     SgExpression* collection_copy; 
  // case: not a listType for (using conditionalToCopyVariable)collection
          if (get_collection() != nullptr) 
             { 
               collection_copy = static_cast<SgExpression*>(help.copyAst(get_collection())); 
             } 
            else 
             { 
               collection_copy = nullptr; 
             } 
    // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != nullptr) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaForEachStatement( startOfConstruct_copy, element_copy, collection_copy, loop_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)element
        if ((element_copy != nullptr) && (element_copy->get_parent() == nullptr) && (isSgType(element_copy) == nullptr)) {
            element_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)collection
        if ((collection_copy != nullptr) && (collection_copy->get_parent() == nullptr) && (isSgType(collection_copy) == nullptr)) {
            collection_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)loop_body
        if ((loop_body_copy != nullptr) && (loop_body_copy->get_parent() == nullptr) && (isSgType(loop_body_copy) == nullptr)) {
            loop_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 43555 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaLabelStatement::copy(SgCopyHelp& help) const {
    SgJavaLabelStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgName label_copy = p_label; 
    // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != nullptr) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaLabelStatement( startOfConstruct_copy, label_copy, statement_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
        if ((statement_copy != nullptr) && (statement_copy->get_parent() == nullptr) && (isSgType(statement_copy) == nullptr)) {
            statement_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 43736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatlabForStatement::copy(SgCopyHelp& help) const {
    SgMatlabForStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != nullptr) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = nullptr; 
             } 
    // Copy constructor parameter data member: range_copy
     SgExpression* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != nullptr) 
             { 
               range_copy = static_cast<SgExpression*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMatlabForStatement( startOfConstruct_copy, index_copy, range_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)index
        if ((index_copy != nullptr) && (index_copy->get_parent() == nullptr) && (isSgType(index_copy) == nullptr)) {
            index_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)range
        if ((range_copy != nullptr) && (range_copy->get_parent() == nullptr) && (isSgType(range_copy) == nullptr)) {
            range_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 43943 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterScope::copy(SgCopyHelp& help) const {
    SgFunctionParameterScope* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionParameterScope( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 44133 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationScope::copy(SgCopyHelp& help) const {
    SgDeclarationScope* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeclarationScope( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 44295 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeBasedForStatement::copy(SgCopyHelp& help) const {
    SgRangeBasedForStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: iterator_declaration_copy
     SgVariableDeclaration* iterator_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iterator_declaration
          if (get_iterator_declaration() != nullptr) 
             { 
               iterator_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_iterator_declaration())); 
             } 
            else 
             { 
               iterator_declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: range_declaration_copy
     SgVariableDeclaration* range_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range_declaration
          if (get_range_declaration() != nullptr) 
             { 
               range_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_range_declaration())); 
             } 
            else 
             { 
               range_declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: begin_declaration_copy
     SgVariableDeclaration* begin_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)begin_declaration
          if (get_begin_declaration() != nullptr) 
             { 
               begin_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_begin_declaration())); 
             } 
            else 
             { 
               begin_declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: end_declaration_copy
     SgVariableDeclaration* end_declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end_declaration
          if (get_end_declaration() != nullptr) 
             { 
               end_declaration_copy = static_cast<SgVariableDeclaration*>(help.copyAst(get_end_declaration())); 
             } 
            else 
             { 
               end_declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: not_equal_expression_copy
     SgExpression* not_equal_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)not_equal_expression
          if (get_not_equal_expression() != nullptr) 
             { 
               not_equal_expression_copy = static_cast<SgExpression*>(help.copyAst(get_not_equal_expression())); 
             } 
            else 
             { 
               not_equal_expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_expression_copy
     SgExpression* increment_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment_expression
          if (get_increment_expression() != nullptr) 
             { 
               increment_expression_copy = static_cast<SgExpression*>(help.copyAst(get_increment_expression())); 
             } 
            else 
             { 
               increment_expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: loop_body_copy
     SgStatement* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != nullptr) 
             { 
               loop_body_copy = static_cast<SgStatement*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRangeBasedForStatement( startOfConstruct_copy, iterator_declaration_copy, range_declaration_copy, begin_declaration_copy, end_declaration_copy, not_equal_expression_copy, increment_expression_copy, loop_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)iterator_declaration
        if ((iterator_declaration_copy != nullptr) && (iterator_declaration_copy->get_parent() == nullptr) && (isSgType(iterator_declaration_copy) == nullptr)) {
            iterator_declaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)range_declaration
        if ((range_declaration_copy != nullptr) && (range_declaration_copy->get_parent() == nullptr) && (isSgType(range_declaration_copy) == nullptr)) {
            range_declaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)begin_declaration
        if ((begin_declaration_copy != nullptr) && (begin_declaration_copy->get_parent() == nullptr) && (isSgType(begin_declaration_copy) == nullptr)) {
            begin_declaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)end_declaration
        if ((end_declaration_copy != nullptr) && (end_declaration_copy->get_parent() == nullptr) && (isSgType(end_declaration_copy) == nullptr)) {
            end_declaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)not_equal_expression
        if ((not_equal_expression_copy != nullptr) && (not_equal_expression_copy->get_parent() == nullptr) && (isSgType(not_equal_expression_copy) == nullptr)) {
            not_equal_expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment_expression
        if ((increment_expression_copy != nullptr) && (increment_expression_copy->get_parent() == nullptr) && (isSgType(increment_expression_copy) == nullptr)) {
            increment_expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)loop_body
        if ((loop_body_copy != nullptr) && (loop_body_copy->get_parent() == nullptr) && (isSgType(loop_body_copy) == nullptr)) {
            loop_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 44562 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialForThenStatement::copy(SgCopyHelp& help) const {
    SgJovialForThenStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initialization_copy
     SgExpression* initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initialization
          if (get_initialization() != nullptr) 
             { 
               initialization_copy = static_cast<SgExpression*>(help.copyAst(get_initialization())); 
             } 
            else 
             { 
               initialization_copy = nullptr; 
             } 
    // Copy constructor parameter data member: while_expression_copy
     SgExpression* while_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)while_expression
          if (get_while_expression() != nullptr) 
             { 
               while_expression_copy = static_cast<SgExpression*>(help.copyAst(get_while_expression())); 
             } 
            else 
             { 
               while_expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: by_or_then_expression_copy
     SgExpression* by_or_then_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)by_or_then_expression
          if (get_by_or_then_expression() != nullptr) 
             { 
               by_or_then_expression_copy = static_cast<SgExpression*>(help.copyAst(get_by_or_then_expression())); 
             } 
            else 
             { 
               by_or_then_expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: loop_body_copy
     SgBasicBlock* loop_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)loop_body
          if (get_loop_body() != nullptr) 
             { 
               loop_body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_loop_body())); 
             } 
            else 
             { 
               loop_body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialForThenStatement( startOfConstruct_copy, initialization_copy, while_expression_copy, by_or_then_expression_copy, loop_body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): loop_statement_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for loop_statement_type
  // case: toBeCopied == COPY_DATA for loop_statement_type
     SgJovialForThenStatement::loop_statement_type_enum loop_statement_type_copy = p_loop_statement_type; 
     result->p_loop_statement_type = loop_statement_type_copy; 
  // case: not a listType for (using conditionalToSetParent)initialization
        if ((initialization_copy != nullptr) && (initialization_copy->get_parent() == nullptr) && (isSgType(initialization_copy) == nullptr)) {
            initialization_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)while_expression
        if ((while_expression_copy != nullptr) && (while_expression_copy->get_parent() == nullptr) && (isSgType(while_expression_copy) == nullptr)) {
            while_expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)by_or_then_expression
        if ((by_or_then_expression_copy != nullptr) && (by_or_then_expression_copy->get_parent() == nullptr) && (isSgType(by_or_then_expression_copy) == nullptr)) {
            by_or_then_expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)loop_body
        if ((loop_body_copy != nullptr) && (loop_body_copy->get_parent() == nullptr) && (isSgType(loop_body_copy) == nullptr)) {
            loop_body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 44789 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAcceptStmt::copy(SgCopyHelp& help) const {
    SgAdaAcceptStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaAcceptStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): parameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterScope
  // case: toBeCopied == COPY_DATA for parameterScope
     SgFunctionParameterScope* parameterScope_copy = p_parameterScope; 
     result->p_parameterScope = parameterScope_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): entry_copy
     SgExpression* entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entry
          if (get_entry() != nullptr) 
             { 
               entry_copy = static_cast<SgExpression*>(help.copyAst(get_entry())); 
             } 
            else 
             { 
               entry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entry != nullptr) delete result->p_entry; 
     result->p_entry = entry_copy; 
  // case: not a listType for (using conditionalToSetParent)entry
        if ((entry_copy != nullptr) && (entry_copy->get_parent() == nullptr) && (isSgType(entry_copy) == nullptr)) {
            entry_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): index_copy
     SgExpression* index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)index
          if (get_index() != nullptr) 
             { 
               index_copy = static_cast<SgExpression*>(help.copyAst(get_index())); 
             } 
            else 
             { 
               index_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_index != nullptr) delete result->p_index; 
     result->p_index = index_copy; 
  // case: not a listType for (using conditionalToSetParent)index
        if ((index_copy != nullptr) && (index_copy->get_parent() == nullptr) && (isSgType(index_copy) == nullptr)) {
            index_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 45028 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSpec::copy(SgCopyHelp& help) const {
    SgAdaPackageSpec* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaPackageSpec( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 45223 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageBody::copy(SgCopyHelp& help) const {
    SgAdaPackageBody* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaPackageBody( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto source_statements_iterator = get_statements().begin();
    for (/* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) {
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statements_copy.push_back(copy_list_element); 
        }
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto statements_iterator = result->get_statements().begin();
    for (/* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) {
          SgStatement* list_element = *statements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 45413 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSpec::copy(SgCopyHelp& help) const {
    SgAdaTaskSpec* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskSpec( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaTaskBody* body_copy = p_body; 
     result->p_body = body_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 45613 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskBody::copy(SgCopyHelp& help) const {
    SgAdaTaskBody* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskBody( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaTaskSpec* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto source_statements_iterator = get_statements().begin();
    for (/* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) {
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statements_copy.push_back(copy_list_element); 
        }
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto statements_iterator = result->get_statements().begin();
    for (/* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) {
          SgStatement* list_element = *statements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 45808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedBody::copy(SgCopyHelp& help) const {
    SgAdaProtectedBody* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedBody( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaProtectedSpec* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto source_statements_iterator = get_statements().begin();
    for (/* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) {
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statements_copy.push_back(copy_list_element); 
        }
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto statements_iterator = result->get_statements().begin();
    for (/* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) {
          SgStatement* list_element = *statements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 46003 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSpec::copy(SgCopyHelp& help) const {
    SgAdaProtectedSpec* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedSpec( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaProtectedBody* body_copy = p_body; 
     result->p_body = body_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): hasPrivate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasPrivate
  // case: toBeCopied == COPY_DATA for hasPrivate
     bool hasPrivate_copy = p_hasPrivate; 
     result->p_hasPrivate = hasPrivate_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 46203 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericDefn::copy(SgCopyHelp& help) const {
    SgAdaGenericDefn* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaGenericDefn( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarations()
     SgDeclarationStatementPtrList declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto source_declarations_iterator = get_declarations().begin();
    for (/* empty by design */; source_declarations_iterator != get_declarations().end(); ++source_declarations_iterator) {
          SgDeclarationStatement* source_list_element = *source_declarations_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_declarations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          declarations_copy.push_back(copy_list_element); 
        }
     result->p_declarations = declarations_copy; 
  // case: listType for declarations
  // case: listType (typeIsPointerToList == false) for declarations
    auto declarations_iterator = result->get_declarations().begin();
    for (/* empty by design */; declarations_iterator != result->get_declarations().end(); ++declarations_iterator) {
          SgDeclarationStatement* list_element = *declarations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 46393 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionTypeTable::copy(SgCopyHelp& help) const {
    SgFunctionTypeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionTypeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): function_type_table_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type_table
  // case: toBeCopied == COPY_DATA for function_type_table
     SgSymbolTable* function_type_table_copy = p_function_type_table; 
     result->p_function_type_table = function_type_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 46563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeclarationStatement::copy(SgCopyHelp& help) const {
    SgDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeclarationStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 46795 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterList::copy(SgCopyHelp& help) const {
    SgFunctionParameterList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionParameterList( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_args()
     SgInitializedNamePtrList args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto get_args_copy = get_args();
    auto source_args_iterator = get_args_copy.begin();
    auto iterator_end = get_args_copy.end();
    for (/* empty by design */; source_args_iterator != iterator_end; ++source_args_iterator) {
          SgInitializedName* source_list_element = *source_args_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_args_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          args_copy.push_back(copy_list_element); 
        }
     result->p_args = args_copy; 
  // case: listType for args
  // case: listType (typeIsPointerToList == false) for args
    auto result_get_args_copy = result->get_args();
    auto args_iterator = result_get_args_copy.begin();
    auto result_it_end = result_get_args_copy.end();
    for (/* empty by design */; args_iterator != result_it_end; ++args_iterator) {
          SgInitializedName* list_element = *args_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 47059 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableDeclaration::copy(SgCopyHelp& help) const {
    SgVariableDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVariableDeclaration( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != nullptr) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != nullptr) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
        if ((baseTypeDefiningDeclaration_copy != nullptr) && (baseTypeDefiningDeclaration_copy->get_parent() == nullptr) && (isSgType(baseTypeDefiningDeclaration_copy) == nullptr)) {
            baseTypeDefiningDeclaration_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgInitializedName* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 47417 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateVariableDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateVariableDeclaration( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != nullptr) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != nullptr) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
        if ((baseTypeDefiningDeclaration_copy != nullptr) && (baseTypeDefiningDeclaration_copy->get_parent() == nullptr) && (isSgType(baseTypeDefiningDeclaration_copy) == nullptr)) {
            baseTypeDefiningDeclaration_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgInitializedName* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 47795 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableInstantiation::copy(SgCopyHelp& help) const {
    SgTemplateVariableInstantiation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateVariableDeclaration* templateDeclaration_copy = p_templateDeclaration; 
    // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateVariableInstantiation( startOfConstruct_copy, templateDeclaration_copy, templateArguments_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseTypeDefiningDeclaration
     SgDeclarationStatement* baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)baseTypeDefiningDeclaration
          if (get_baseTypeDefiningDeclaration() != nullptr) 
             { 
               baseTypeDefiningDeclaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_baseTypeDefiningDeclaration())); 
             } 
            else 
             { 
               baseTypeDefiningDeclaration_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_baseTypeDefiningDeclaration != nullptr) delete result->p_baseTypeDefiningDeclaration; 
     result->p_baseTypeDefiningDeclaration = baseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)baseTypeDefiningDeclaration
        if ((baseTypeDefiningDeclaration_copy != nullptr) && (baseTypeDefiningDeclaration_copy->get_parent() == nullptr) && (isSgType(baseTypeDefiningDeclaration_copy) == nullptr)) {
            baseTypeDefiningDeclaration_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgInitializedName* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): variableDeclarationContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variableDeclarationContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for variableDeclarationContainsBaseTypeDefiningDeclaration
     bool variableDeclarationContainsBaseTypeDefiningDeclaration_copy = p_variableDeclarationContainsBaseTypeDefiningDeclaration; 
     result->p_variableDeclarationContainsBaseTypeDefiningDeclaration = variableDeclarationContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_initialization_priority_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_initialization_priority
  // case: toBeCopied == COPY_DATA for gnu_extension_initialization_priority
     unsigned short gnu_extension_initialization_priority_copy = p_gnu_extension_initialization_priority; 
     result->p_gnu_extension_initialization_priority = gnu_extension_initialization_priority_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_declaration_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_declaration_attribute
  // case: toBeCopied == COPY_DATA for gnu_extension_declaration_attribute
     SgVariableDeclaration::gnu_extension_declaration_attributes_enum gnu_extension_declaration_attribute_copy = p_gnu_extension_declaration_attribute; 
     result->p_gnu_extension_declaration_attribute = gnu_extension_declaration_attribute_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): is_thread_local_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_thread_local
  // case: toBeCopied == COPY_DATA for is_thread_local
     bool is_thread_local_copy = p_is_thread_local; 
     result->p_is_thread_local = is_thread_local_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): builtFromUseOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for builtFromUseOnly
  // case: toBeCopied == COPY_DATA for builtFromUseOnly
     bool builtFromUseOnly_copy = p_builtFromUseOnly; 
     result->p_builtFromUseOnly = builtFromUseOnly_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 48171 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableDefinition::copy(SgCopyHelp& help) const {
    SgVariableDefinition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: vardefn_copy
     SgInitializedName* vardefn_copy; 
  // case: not a listType for (using conditionalToCopyVariable)vardefn
          if (get_vardefn() != nullptr) 
             { 
               vardefn_copy = static_cast<SgInitializedName*>(help.copyAst(get_vardefn())); 
             } 
            else 
             { 
               vardefn_copy = nullptr; 
             } 
    // Copy constructor parameter data member: bitfield_copy
     SgExpression* bitfield_copy; 
  // case: not a listType for (using conditionalToCopyVariable)bitfield
          if (get_bitfield() != nullptr) 
             { 
               bitfield_copy = static_cast<SgExpression*>(help.copyAst(get_bitfield())); 
             } 
            else 
             { 
               bitfield_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVariableDefinition( startOfConstruct_copy, vardefn_copy, bitfield_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)vardefn
        if ((vardefn_copy != nullptr) && (vardefn_copy->get_parent() == nullptr) && (isSgType(vardefn_copy) == nullptr)) {
            vardefn_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)bitfield
        if ((bitfield_copy != nullptr) && (bitfield_copy->get_parent() == nullptr) && (isSgType(bitfield_copy) == nullptr)) {
            bitfield_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 48433 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageDeclarationStatement::copy(SgCopyHelp& help) const {
    SgClinkageDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClinkageDeclarationStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 48670 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageStartStatement::copy(SgCopyHelp& help) const {
    SgClinkageStartStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClinkageStartStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 48907 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClinkageEndStatement::copy(SgCopyHelp& help) const {
    SgClinkageEndStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClinkageEndStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_languageSpecifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for languageSpecifier
  // case: toBeCopied == COPY_DATA for languageSpecifier
     std::string languageSpecifier_copy = p_languageSpecifier; 
     result->p_languageSpecifier = languageSpecifier_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 49144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumDeclaration::copy(SgCopyHelp& help) const {
    SgEnumDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgEnumType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgEnumType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEnumDeclaration( startOfConstruct_copy, name_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): embedded_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for embedded
  // case: toBeCopied == COPY_DATA for embedded
     bool embedded_copy = p_embedded; 
     result->p_embedded = embedded_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgType* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_enumerators()
     SgInitializedNamePtrList enumerators_copy; 
  // case: listType for enumerators
  // case: listType (typeIsPointerToList == false) for enumerators
    auto source_enumerators_iterator = get_enumerators().begin();
    for (/* empty by design */; source_enumerators_iterator != get_enumerators().end(); ++source_enumerators_iterator) {
          SgInitializedName* source_list_element = *source_enumerators_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_enumerators_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          enumerators_copy.push_back(copy_list_element); 
        }
     result->p_enumerators = enumerators_copy; 
  // case: listType for enumerators
  // case: listType (typeIsPointerToList == false) for enumerators
    auto enumerators_iterator = result->get_enumerators().begin();
    for (/* empty by design */; enumerators_iterator != result->get_enumerators().end(); ++enumerators_iterator) {
          SgInitializedName* list_element = *enumerators_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): field_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_type
  // case: toBeCopied == CLONE_PTR for field_type
     SgType* field_type_copy = nullptr; 
     if ( p_field_type != nullptr ) 
        { 
          field_type_copy = new SgType( *p_field_type); 
        } 
       else 
        { 
          field_type_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_field_type != nullptr) delete result->p_field_type; 
  /* add assignment to result here */ 
     result->p_field_type = field_type_copy; 
  // Copy non-constructor parameter data member (access function): isScopedEnum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isScopedEnum
  // case: toBeCopied == COPY_DATA for isScopedEnum
     bool isScopedEnum_copy = p_isScopedEnum; 
     result->p_isScopedEnum = isScopedEnum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 49481 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStmt::copy(SgCopyHelp& help) const {
    SgAsmStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): assemblyCode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for assemblyCode
  // case: toBeCopied == COPY_DATA for assemblyCode
     std::string assemblyCode_copy = p_assemblyCode; 
     result->p_assemblyCode = assemblyCode_copy; 
  // Copy non-constructor parameter data member (access function): useGnuExtendedFormat_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for useGnuExtendedFormat
  // case: toBeCopied == COPY_DATA for useGnuExtendedFormat
     bool useGnuExtendedFormat_copy = p_useGnuExtendedFormat; 
     result->p_useGnuExtendedFormat = useGnuExtendedFormat_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto source_operands_iterator = get_operands().begin();
    for (/* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) {
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          operands_copy.push_back(copy_list_element); 
        }
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto operands_iterator = result->get_operands().begin();
    for (/* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) {
          SgExpression* list_element = *operands_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_clobberRegisterList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clobberRegisterList
  // case: toBeCopied == COPY_DATA for clobberRegisterList
     SgAsmStmt::AsmRegisterNameList clobberRegisterList_copy = p_clobberRegisterList; 
     result->p_clobberRegisterList = clobberRegisterList_copy; 
  // Copy non-constructor parameter data member (access function): isVolatile_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isVolatile
  // case: toBeCopied == COPY_DATA for isVolatile
     bool isVolatile_copy = p_isVolatile; 
     result->p_isVolatile = isVolatile_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 49761 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAttributeSpecificationStatement::copy(SgCopyHelp& help) const {
    SgAttributeSpecificationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAttributeSpecificationStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_list
  // case: toBeCopied == COPY_DATA for name_list
     SgStringList name_list_copy = p_name_list; 
     result->p_name_list = name_list_copy; 
  // Copy non-constructor parameter data member (access function): attribute_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_kind
  // case: toBeCopied == COPY_DATA for attribute_kind
     SgAttributeSpecificationStatement::attribute_spec_enum attribute_kind_copy = p_attribute_kind; 
     result->p_attribute_kind = attribute_kind_copy; 
  // Copy non-constructor parameter data member (access function): intent_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for intent
  // case: toBeCopied == COPY_DATA for intent
     int intent_copy = p_intent; 
     result->p_intent = intent_copy; 
  // Copy non-constructor parameter data member (access function): parameter_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_list
  // case: toBeCopied == COPY_DATA for parameter_list
     SgExprListExp* parameter_list_copy = p_parameter_list; 
     result->p_parameter_list = parameter_list_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data_statement_group_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_statement_group_list
  // case: toBeCopied == COPY_DATA for data_statement_group_list
     SgDataStatementGroupPtrList data_statement_group_list_copy = p_data_statement_group_list; 
     result->p_data_statement_group_list = data_statement_group_list_copy; 
  // Copy non-constructor parameter data member (access function): bind_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bind_list
  // case: toBeCopied == COPY_DATA for bind_list
     SgExprListExp* bind_list_copy = p_bind_list; 
     result->p_bind_list = bind_list_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dimension_object_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dimension_object_list
  // case: toBeCopied == COPY_DATA for dimension_object_list
     SgDimensionObjectPtrList dimension_object_list_copy = p_dimension_object_list; 
     result->p_dimension_object_list = dimension_object_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 50028 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFormatStatement::copy(SgCopyHelp& help) const {
    SgFormatStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: format_item_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for format_item_list
  // case: toBeCopied == COPY_DATA for format_item_list
     SgFormatItemList* format_item_list_copy = p_format_item_list; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFormatStatement( startOfConstruct_copy, format_item_list_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 50264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateDeclaration( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): template_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_kind
  // case: toBeCopied == COPY_DATA for template_kind
     SgTemplateDeclaration::template_type_enum template_kind_copy = p_template_kind; 
     result->p_template_kind = template_kind_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 50525 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDirectiveStatement::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationDirectiveStatement( startOfConstruct_copy, declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): do_not_instantiate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_not_instantiate
  // case: toBeCopied == COPY_DATA for do_not_instantiate
     bool do_not_instantiate_copy = p_do_not_instantiate; 
     result->p_do_not_instantiate = do_not_instantiate_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 50777 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUseStatement::copy(SgCopyHelp& help) const {
    SgUseStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: only_option_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for only_option
  // case: toBeCopied == COPY_DATA for only_option
     bool only_option_copy = p_only_option; 
    // Copy constructor parameter data member: module_nature_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_nature
  // case: toBeCopied == COPY_DATA for module_nature
     std::string module_nature_copy = p_module_nature; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUseStatement( startOfConstruct_copy, name_copy, only_option_copy, module_nature_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_rename_list()
     SgRenamePairPtrList rename_list_copy; 
  // case: listType for rename_list
  // case: listType (typeIsPointerToList == false) for rename_list
    auto source_rename_list_iterator = get_rename_list().begin();
    for (/* empty by design */; source_rename_list_iterator != get_rename_list().end(); ++source_rename_list_iterator) {
          SgRenamePair* source_list_element = *source_rename_list_iterator; 
          SgRenamePair* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgRenamePair*>(help.copyAst(*source_rename_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          rename_list_copy.push_back(copy_list_element); 
        }
     result->p_rename_list = rename_list_copy; 
  // case: listType for rename_list
  // case: listType (typeIsPointerToList == false) for rename_list
    auto rename_list_iterator = result->get_rename_list().begin();
    for (/* empty by design */; rename_list_iterator != result->get_rename_list().end(); ++rename_list_iterator) {
          SgRenamePair* list_element = *rename_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): module_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module
  // case: toBeCopied == COPY_DATA for module
     SgModuleStatement* module_copy = p_module; 
     result->p_module = module_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 51054 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgParameterStatement::copy(SgCopyHelp& help) const {
    SgParameterStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgParameterStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 51286 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceDeclarationStatement::copy(SgCopyHelp& help) const {
    SgNamespaceDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgNamespaceDefinitionStatement* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgNamespaceDefinitionStatement*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isUnnamedNamespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnnamedNamespace
  // case: toBeCopied == COPY_DATA for isUnnamedNamespace
     bool isUnnamedNamespace_copy = p_isUnnamedNamespace; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamespaceDeclarationStatement( startOfConstruct_copy, name_copy, definition_copy, isUnnamedNamespace_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): isInlinedNamespace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isInlinedNamespace
  // case: toBeCopied == COPY_DATA for isInlinedNamespace
     bool isInlinedNamespace_copy = p_isInlinedNamespace; 
     result->p_isInlinedNamespace = isInlinedNamespace_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 51546 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEquivalenceStatement::copy(SgCopyHelp& help) const {
    SgEquivalenceStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEquivalenceStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): equivalence_set_list_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for equivalence_set_list
  // case: toBeCopied == COPY_DATA for equivalence_set_list
     SgExprListExp* equivalence_set_list_copy = p_equivalence_set_list; 
     result->p_equivalence_set_list = equivalence_set_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 51783 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceStatement::copy(SgCopyHelp& help) const {
    SgInterfaceStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: generic_spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for generic_spec
  // case: toBeCopied == COPY_DATA for generic_spec
     SgInterfaceStatement::generic_spec_enum generic_spec_copy = p_generic_spec; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInterfaceStatement( startOfConstruct_copy, name_copy, generic_spec_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_interface_body_list()
     SgInterfaceBodyPtrList interface_body_list_copy; 
  // case: listType for interface_body_list
  // case: listType (typeIsPointerToList == false) for interface_body_list
    auto source_interface_body_list_iterator = get_interface_body_list().begin();
    for (/* empty by design */; source_interface_body_list_iterator != get_interface_body_list().end(); ++source_interface_body_list_iterator) {
          SgInterfaceBody* source_list_element = *source_interface_body_list_iterator; 
          SgInterfaceBody* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInterfaceBody*>(help.copyAst(*source_interface_body_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          interface_body_list_copy.push_back(copy_list_element); 
        }
     result->p_interface_body_list = interface_body_list_copy; 
  // case: listType for interface_body_list
  // case: listType (typeIsPointerToList == false) for interface_body_list
    auto interface_body_list_iterator = result->get_interface_body_list().begin();
    for (/* empty by design */; interface_body_list_iterator != result->get_interface_body_list().end(); ++interface_body_list_iterator) {
          SgInterfaceBody* list_element = *interface_body_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 52056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceAliasDeclarationStatement::copy(SgCopyHelp& help) const {
    SgNamespaceAliasDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamespaceAliasDeclarationStatement( startOfConstruct_copy, name_copy, namespaceDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_alias_for_another_namespace_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_alias_for_another_namespace_alias
  // case: toBeCopied == COPY_DATA for is_alias_for_another_namespace_alias
     bool is_alias_for_another_namespace_alias_copy = p_is_alias_for_another_namespace_alias; 
     result->p_is_alias_for_another_namespace_alias = is_alias_for_another_namespace_alias_copy; 
  // Copy non-constructor parameter data member (access function): namespaceAliasDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceAliasDeclaration
  // case: toBeCopied == COPY_DATA for namespaceAliasDeclaration
     SgNamespaceAliasDeclarationStatement* namespaceAliasDeclaration_copy = p_namespaceAliasDeclaration; 
     result->p_namespaceAliasDeclaration = namespaceAliasDeclaration_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 52321 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonBlock::copy(SgCopyHelp& help) const {
    SgCommonBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCommonBlock( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_block_list
     SgCommonBlockObjectPtrList block_list_copy; 
  // case: listType for block_list
  // case: listType (typeIsPointerToList == false) for block_list
    auto source_block_list_iterator = get_block_list().begin();
    for (/* empty by design */; source_block_list_iterator != get_block_list().end(); ++source_block_list_iterator) {
          SgCommonBlockObject* source_list_element = *source_block_list_iterator; 
          SgCommonBlockObject* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgCommonBlockObject*>(help.copyAst(*source_block_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          block_list_copy.push_back(copy_list_element); 
        }
     result->p_block_list = block_list_copy; 
  // case: listType for block_list
  // case: listType (typeIsPointerToList == false) for block_list
    auto block_list_iterator = result->get_block_list().begin();
    for (/* empty by design */; block_list_iterator != result->get_block_list().end(); ++block_list_iterator) {
          SgCommonBlockObject* list_element = *block_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 52581 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefDeclaration::copy(SgCopyHelp& help) const {
    SgTypedefDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypedefDeclaration( startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 52912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateTypedefDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateTypedefDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateTypedefDeclaration( startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 53263 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationTypedefDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationTypedefDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: base_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for base_type
  // case: toBeCopied == COPY_DATA for base_type
     SgType* base_type_copy = p_base_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgTypedefType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgTypedefType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: parent_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parent_scope
  // case: toBeCopied == COPY_DATA for parent_scope
     SgSymbol* parent_scope_copy = p_parent_scope; 
    // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateTypedefDeclaration* templateDeclaration_copy = p_templateDeclaration; 
    // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationTypedefDeclaration( startOfConstruct_copy, name_copy, base_type_copy, type_copy, declaration_copy, parent_scope_copy, templateDeclaration_copy, templateArguments_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): typedefBaseTypeContainsDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for typedefBaseTypeContainsDefiningDeclaration
  // case: toBeCopied == COPY_DATA for typedefBaseTypeContainsDefiningDeclaration
     bool typedefBaseTypeContainsDefiningDeclaration_copy = p_typedefBaseTypeContainsDefiningDeclaration; 
     result->p_typedefBaseTypeContainsDefiningDeclaration = typedefBaseTypeContainsDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_base_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_base_type
     int name_qualification_length_for_base_type_copy = p_name_qualification_length_for_base_type; 
     result->p_name_qualification_length_for_base_type = name_qualification_length_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_base_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_base_type
     bool type_elaboration_required_for_base_type_copy = p_type_elaboration_required_for_base_type; 
     result->p_type_elaboration_required_for_base_type = type_elaboration_required_for_base_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_base_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_base_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_base_type
     bool global_qualification_required_for_base_type_copy = p_global_qualification_required_for_base_type; 
     result->p_global_qualification_required_for_base_type = global_qualification_required_for_base_type_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isAssociatedWithDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAssociatedWithDeclarationList
  // case: toBeCopied == COPY_DATA for isAssociatedWithDeclarationList
     bool isAssociatedWithDeclarationList_copy = p_isAssociatedWithDeclarationList; 
     result->p_isAssociatedWithDeclarationList = isAssociatedWithDeclarationList_copy; 
  // Copy non-constructor parameter data member (access function): isFirstDeclarationOfDeclarationList_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isFirstDeclarationOfDeclarationList
  // case: toBeCopied == COPY_DATA for isFirstDeclarationOfDeclarationList
     bool isFirstDeclarationOfDeclarationList_copy = p_isFirstDeclarationOfDeclarationList; 
     result->p_isFirstDeclarationOfDeclarationList = isFirstDeclarationOfDeclarationList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 53617 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatementFunctionStatement::copy(SgCopyHelp& help) const {
    SgStatementFunctionStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: function_copy
     SgFunctionDeclaration* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != nullptr) 
             { 
               function_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStatementFunctionStatement( startOfConstruct_copy, function_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)function
        if ((function_copy != nullptr) && (function_copy->get_parent() == nullptr) && (isSgType(function_copy) == nullptr)) {
            function_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 53879 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCtorInitializerList::copy(SgCopyHelp& help) const {
    SgCtorInitializerList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCtorInitializerList( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_ctors()
     SgInitializedNamePtrList ctors_copy; 
  // case: listType for ctors
  // case: listType (typeIsPointerToList == false) for ctors
    auto source_ctors_iterator = get_ctors().begin();
    for (/* empty by design */; source_ctors_iterator != get_ctors().end(); ++source_ctors_iterator) {
          SgInitializedName* source_list_element = *source_ctors_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_ctors_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          ctors_copy.push_back(copy_list_element); 
        }
     result->p_ctors = ctors_copy; 
  // case: listType for ctors
  // case: listType (typeIsPointerToList == false) for ctors
    auto ctors_iterator = result->get_ctors().begin();
    for (/* empty by design */; ctors_iterator != result->get_ctors().end(); ++ctors_iterator) {
          SgInitializedName* list_element = *ctors_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 54139 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPragmaDeclaration::copy(SgCopyHelp& help) const {
    SgPragmaDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: pragma_copy
     SgPragma* pragma_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pragma
          if (get_pragma() != nullptr) 
             { 
               pragma_copy = static_cast<SgPragma*>(help.copyAst(get_pragma())); 
             } 
            else 
             { 
               pragma_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPragmaDeclaration( startOfConstruct_copy, pragma_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)pragma
        if ((pragma_copy != nullptr) && (pragma_copy->get_parent() == nullptr) && (isSgType(pragma_copy) == nullptr)) {
            pragma_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 54386 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUsingDirectiveStatement::copy(SgCopyHelp& help) const {
    SgUsingDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: namespaceDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for namespaceDeclaration
  // case: toBeCopied == COPY_DATA for namespaceDeclaration
     SgNamespaceDeclarationStatement* namespaceDeclaration_copy = p_namespaceDeclaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUsingDirectiveStatement( startOfConstruct_copy, namespaceDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 54637 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassDeclaration::copy(SgCopyHelp& help) const {
    SgClassDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassDeclaration( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 54997 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateClassDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateClassDeclaration( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 55382 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationDecl::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateClassDeclaration* templateDeclaration_copy = p_templateDeclaration; 
    // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationDecl( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): templateHeader_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateHeader
  // case: toBeCopied == COPY_DATA for templateHeader
     SgName templateHeader_copy = p_templateHeader; 
     result->p_templateHeader = templateHeader_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 55765 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDerivedTypeStatement::copy(SgCopyHelp& help) const {
    SgDerivedTypeStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDerivedTypeStatement( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 56130 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModuleStatement::copy(SgCopyHelp& help) const {
    SgModuleStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModuleStatement( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 56495 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaPackageDeclaration::copy(SgCopyHelp& help) const {
    SgJavaPackageDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaPackageDeclaration( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 56855 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTableStatement::copy(SgCopyHelp& help) const {
    SgJovialTableStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: class_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_type
  // case: toBeCopied == COPY_DATA for class_type
     SgClassDeclaration::class_types class_type_copy = p_class_type; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == CLONE_PTR for type
     SgClassType* type_copy = nullptr; 
     if ( p_type != nullptr ) 
        { 
          type_copy = new SgClassType( *p_type); 
        } 
       else 
        { 
          type_copy = nullptr; 
        } 
    // Copy constructor parameter data member: definition_copy
     SgClassDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgClassDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialTableStatement( startOfConstruct_copy, name_copy, class_type_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
  // Copy non-constructor parameter data member (access function): from_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for from_template
  // case: toBeCopied == COPY_DATA for from_template
     bool from_template_copy = p_from_template; 
     result->p_from_template = from_template_copy; 
  // Copy non-constructor parameter data member (access function): fixupScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fixupScope
  // case: toBeCopied == COPY_DATA for fixupScope
     bool fixupScope_copy = p_fixupScope; 
     result->p_fixupScope = fixupScope_copy; 
  // Copy non-constructor parameter data member (access function): isUnNamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isUnNamed
  // case: toBeCopied == COPY_DATA for isUnNamed
     bool isUnNamed_copy = p_isUnNamed; 
     result->p_isUnNamed = isUnNamed_copy; 
  // Copy non-constructor parameter data member (access function): explicit_annotation_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_annotation_interface
  // case: toBeCopied == COPY_DATA for explicit_annotation_interface
     bool explicit_annotation_interface_copy = p_explicit_annotation_interface; 
     result->p_explicit_annotation_interface = explicit_annotation_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_interface_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_interface
  // case: toBeCopied == COPY_DATA for explicit_interface
     bool explicit_interface_copy = p_explicit_interface; 
     result->p_explicit_interface = explicit_interface_copy; 
  // Copy non-constructor parameter data member (access function): explicit_enum_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_enum
  // case: toBeCopied == COPY_DATA for explicit_enum
     bool explicit_enum_copy = p_explicit_enum; 
     result->p_explicit_enum = explicit_enum_copy; 
  // Copy non-constructor parameter data member (access function): explicit_anonymous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_anonymous
  // case: toBeCopied == COPY_DATA for explicit_anonymous
     bool explicit_anonymous_copy = p_explicit_anonymous; 
     result->p_explicit_anonymous = explicit_anonymous_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): isAutonomousDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAutonomousDeclaration
  // case: toBeCopied == COPY_DATA for isAutonomousDeclaration
     bool isAutonomousDeclaration_copy = p_isAutonomousDeclaration; 
     result->p_isAutonomousDeclaration = isAutonomousDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): isRepresentingTemplateParameterInTemplateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRepresentingTemplateParameterInTemplateDeclaration
  // case: toBeCopied == COPY_DATA for isRepresentingTemplateParameterInTemplateDeclaration
     bool isRepresentingTemplateParameterInTemplateDeclaration_copy = p_isRepresentingTemplateParameterInTemplateDeclaration; 
     result->p_isRepresentingTemplateParameterInTemplateDeclaration = isRepresentingTemplateParameterInTemplateDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): adaParentType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaParentType
  // case: toBeCopied == COPY_DATA for adaParentType
     SgBaseClass* adaParentType_copy = p_adaParentType; 
     result->p_adaParentType = adaParentType_copy; 
  // Copy non-constructor parameter data member (access function): table_entry_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for table_entry_size
  // case: toBeCopied == COPY_DATA for table_entry_size
     SgExpression* table_entry_size_copy = p_table_entry_size; 
     result->p_table_entry_size = table_entry_size_copy; 
  // Copy non-constructor parameter data member (access function): has_table_entry_size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_table_entry_size
  // case: toBeCopied == COPY_DATA for has_table_entry_size
     bool has_table_entry_size_copy = p_has_table_entry_size; 
     result->p_has_table_entry_size = has_table_entry_size_copy; 
  // Copy non-constructor parameter data member (access function): words_per_entry_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for words_per_entry
  // case: toBeCopied == COPY_DATA for words_per_entry
     SgJovialTableStatement::WordsPerEntry words_per_entry_copy = p_words_per_entry; 
     result->p_words_per_entry = words_per_entry_copy; 
  // Copy non-constructor parameter data member (access function): like_table_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for like_table_name
  // case: toBeCopied == COPY_DATA for like_table_name
     std::string like_table_name_copy = p_like_table_name; 
     result->p_like_table_name = like_table_name_copy; 
  // Copy non-constructor parameter data member (access function): has_like_option_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_like_option
  // case: toBeCopied == COPY_DATA for has_like_option
     bool has_like_option_copy = p_has_like_option; 
     result->p_has_like_option = has_like_option_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 57240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImplicitStatement::copy(SgCopyHelp& help) const {
    SgImplicitStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: implicit_none_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit_none
  // case: toBeCopied == COPY_DATA for implicit_none
     bool implicit_none_copy = p_implicit_none; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgImplicitStatement( startOfConstruct_copy, implicit_none_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): implicit_spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implicit_spec
  // case: toBeCopied == COPY_DATA for implicit_spec
     SgImplicitStatement::implicit_spec_enum implicit_spec_copy = p_implicit_spec; 
     result->p_implicit_spec = implicit_spec_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgInitializedNamePtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgInitializedName* source_list_element = *source_variables_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgInitializedName* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 57509 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUsingDeclarationStatement::copy(SgCopyHelp& help) const {
    SgUsingDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgDeclarationStatement* declaration_copy = p_declaration; 
    // Copy constructor parameter data member: initializedName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initializedName
  // case: toBeCopied == COPY_DATA for initializedName
     SgInitializedName* initializedName_copy = p_initializedName; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUsingDeclarationStatement( startOfConstruct_copy, declaration_copy, initializedName_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_inheriting_constructor_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_inheriting_constructor
  // case: toBeCopied == COPY_DATA for is_inheriting_constructor
     bool is_inheriting_constructor_copy = p_is_inheriting_constructor; 
     result->p_is_inheriting_constructor = is_inheriting_constructor_copy; 
  // Copy non-constructor parameter data member (access function): adaTypeAttribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaTypeAttribute
  // case: toBeCopied == COPY_DATA for adaTypeAttribute
     SgName adaTypeAttribute_copy = p_adaTypeAttribute; 
     result->p_adaTypeAttribute = adaTypeAttribute_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 57774 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamelistStatement::copy(SgCopyHelp& help) const {
    SgNamelistStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamelistStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_group_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for group_list
  // case: toBeCopied == COPY_DATA for group_list
     SgNameGroupPtrList group_list_copy = p_group_list; 
     result->p_group_list = group_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 58011 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImportStatement::copy(SgCopyHelp& help) const {
    SgImportStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgImportStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_import_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for import_list
  // case: toBeCopied == COPY_DATA for import_list
     SgExpressionPtrList import_list_copy = p_import_list; 
     result->p_import_list = import_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 58248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionDeclaration::copy(SgCopyHelp& help) const {
    SgFunctionDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionDeclaration( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 58694 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateFunctionDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateFunctionDeclaration( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): string_represents_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_represents_function_body
  // case: toBeCopied == COPY_DATA for string_represents_function_body
     bool string_represents_function_body_copy = p_string_represents_function_body; 
     result->p_string_represents_function_body = string_represents_function_body_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 59165 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionDeclaration::copy(SgCopyHelp& help) const {
    SgMemberFunctionDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMemberFunctionDeclaration( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 59616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionDeclaration::copy(SgCopyHelp& help) const {
    SgTemplateMemberFunctionDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateMemberFunctionDeclaration( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateParameters()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateParameters
  // case: toBeCopied == COPY_DATA for templateParameters
     SgTemplateParameterPtrList templateParameters_copy = p_templateParameters; 
     result->p_templateParameters = templateParameters_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_templateSpecializationArguments()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateSpecializationArguments
  // case: toBeCopied == COPY_DATA for templateSpecializationArguments
     SgTemplateArgumentPtrList templateSpecializationArguments_copy = p_templateSpecializationArguments; 
     result->p_templateSpecializationArguments = templateSpecializationArguments_copy; 
  // Copy non-constructor parameter data member (access function): string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     SgName string_copy = p_string; 
     result->p_string = string_copy; 
  // Copy non-constructor parameter data member (access function): string_represents_function_body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_represents_function_body
  // case: toBeCopied == COPY_DATA for string_represents_function_body
     bool string_represents_function_body_copy = p_string_represents_function_body; 
     result->p_string_represents_function_body = string_represents_function_body_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 60092 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationMemberFunctionDecl::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationMemberFunctionDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateMemberFunctionDeclaration* templateDeclaration_copy = p_templateDeclaration; 
    // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationMemberFunctionDecl( startOfConstruct_copy, name_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_associatedClassDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedClassDeclaration
  // case: toBeCopied == COPY_DATA for associatedClassDeclaration
     SgDeclarationStatement* associatedClassDeclaration_copy = p_associatedClassDeclaration; 
     result->p_associatedClassDeclaration = associatedClassDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // Copy non-constructor parameter data member (access function): template_argument_list_is_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_argument_list_is_explicit
  // case: toBeCopied == COPY_DATA for template_argument_list_is_explicit
     bool template_argument_list_is_explicit_copy = p_template_argument_list_is_explicit; 
     result->p_template_argument_list_is_explicit = template_argument_list_is_explicit_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 60566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateInstantiationFunctionDecl::copy(SgCopyHelp& help) const {
    SgTemplateInstantiationFunctionDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgTemplateFunctionDeclaration* templateDeclaration_copy = p_templateDeclaration; 
    // Copy constructor parameter data member: templateArguments_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateArguments
  // case: toBeCopied == COPY_DATA for templateArguments
     SgTemplateArgumentPtrList templateArguments_copy = p_templateArguments; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateInstantiationFunctionDecl( startOfConstruct_copy, name_copy, type_copy, definition_copy, templateDeclaration_copy, templateArguments_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): templateName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateName
  // case: toBeCopied == COPY_DATA for templateName
     SgName templateName_copy = p_templateName; 
     result->p_templateName = templateName_copy; 
  // Copy non-constructor parameter data member (access function): nameResetFromMangledForm_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameResetFromMangledForm
  // case: toBeCopied == COPY_DATA for nameResetFromMangledForm
     bool nameResetFromMangledForm_copy = p_nameResetFromMangledForm; 
     result->p_nameResetFromMangledForm = nameResetFromMangledForm_copy; 
  // Copy non-constructor parameter data member (access function): template_argument_list_is_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_argument_list_is_explicit
  // case: toBeCopied == COPY_DATA for template_argument_list_is_explicit
     bool template_argument_list_is_explicit_copy = p_template_argument_list_is_explicit; 
     result->p_template_argument_list_is_explicit = template_argument_list_is_explicit_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 61035 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProgramHeaderStatement::copy(SgCopyHelp& help) const {
    SgProgramHeaderStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgProgramHeaderStatement( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): program_statement_explicit_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for program_statement_explicit
  // case: toBeCopied == COPY_DATA for program_statement_explicit
     bool program_statement_explicit_copy = p_program_statement_explicit; 
     result->p_program_statement_explicit = program_statement_explicit_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 61491 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProcedureHeaderStatement::copy(SgCopyHelp& help) const {
    SgProcedureHeaderStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgProcedureHeaderStatement( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): subprogram_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for subprogram_kind
  // case: toBeCopied == COPY_DATA for subprogram_kind
     SgProcedureHeaderStatement::subprogram_kind_enum subprogram_kind_copy = p_subprogram_kind; 
     result->p_subprogram_kind = subprogram_kind_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): result_name_copy
     SgInitializedName* result_name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)result_name
          if (get_result_name() != nullptr) 
             { 
               result_name_copy = static_cast<SgInitializedName*>(help.copyAst(get_result_name())); 
             } 
            else 
             { 
               result_name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_result_name != nullptr) delete result->p_result_name; 
     result->p_result_name = result_name_copy; 
  // case: not a listType for (using conditionalToSetParent)result_name
        if ((result_name_copy != nullptr) && (result_name_copy->get_parent() == nullptr) && (isSgType(result_name_copy) == nullptr)) {
            result_name_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 61965 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEntryStatement::copy(SgCopyHelp& help) const {
    SgEntryStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEntryStatement( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): result_name_copy
     SgInitializedName* result_name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)result_name
          if (get_result_name() != nullptr) 
             { 
               result_name_copy = static_cast<SgInitializedName*>(help.copyAst(get_result_name())); 
             } 
            else 
             { 
               result_name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_result_name != nullptr) delete result->p_result_name; 
     result->p_result_name = result_name_copy; 
  // case: not a listType for (using conditionalToSetParent)result_name
        if ((result_name_copy != nullptr) && (result_name_copy->get_parent() == nullptr) && (isSgType(result_name_copy) == nullptr)) {
            result_name_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 62429 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaEntryDecl::copy(SgCopyHelp& help) const {
    SgAdaEntryDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaEntryDecl( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): entryIndex_copy
     SgInitializedName* entryIndex_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryIndex
          if (get_entryIndex() != nullptr) 
             { 
               entryIndex_copy = static_cast<SgInitializedName*>(help.copyAst(get_entryIndex())); 
             } 
            else 
             { 
               entryIndex_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryIndex != nullptr) delete result->p_entryIndex; 
     result->p_entryIndex = entryIndex_copy; 
  // case: not a listType for (using conditionalToSetParent)entryIndex
        if ((entryIndex_copy != nullptr) && (entryIndex_copy->get_parent() == nullptr) && (isSgType(entryIndex_copy) == nullptr)) {
            entryIndex_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): entryBarrier_copy
     SgExpression* entryBarrier_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryBarrier
          if (get_entryBarrier() != nullptr) 
             { 
               entryBarrier_copy = static_cast<SgExpression*>(help.copyAst(get_entryBarrier())); 
             } 
            else 
             { 
               entryBarrier_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryBarrier != nullptr) delete result->p_entryBarrier; 
     result->p_entryBarrier = entryBarrier_copy; 
  // case: not a listType for (using conditionalToSetParent)entryBarrier
        if ((entryBarrier_copy != nullptr) && (entryBarrier_copy->get_parent() == nullptr) && (isSgType(entryBarrier_copy) == nullptr)) {
            entryBarrier_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 62911 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFunctionRenamingDecl::copy(SgCopyHelp& help) const {
    SgAdaFunctionRenamingDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgFunctionType* type_copy = p_type; 
    // Copy constructor parameter data member: definition_copy
     SgFunctionDefinition* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgFunctionDefinition*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFunctionRenamingDecl( startOfConstruct_copy, name_copy, type_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_copy
     SgFunctionParameterList* parameterList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameterList
          if (get_parameterList() != nullptr) 
             { 
               parameterList_copy = static_cast<SgFunctionParameterList*>(help.copyAst(get_parameterList())); 
             } 
            else 
             { 
               parameterList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameterList != nullptr) delete result->p_parameterList; 
     result->p_parameterList = parameterList_copy; 
  // case: not a listType for (using conditionalToSetParent)parameterList
        if ((parameterList_copy != nullptr) && (parameterList_copy->get_parent() == nullptr) && (isSgType(parameterList_copy) == nullptr)) {
            parameterList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_functionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionModifier
  // case: toBeCopied == COPY_DATA for functionModifier
     SgFunctionModifier functionModifier_copy = p_functionModifier; 
     result->p_functionModifier = functionModifier_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_specialFunctionModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialFunctionModifier
  // case: toBeCopied == COPY_DATA for specialFunctionModifier
     SgSpecialFunctionModifier specialFunctionModifier_copy = p_specialFunctionModifier; 
     result->p_specialFunctionModifier = specialFunctionModifier_copy; 
  // Copy non-constructor parameter data member (access function): exceptionSpecification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exceptionSpecification
  // case: toBeCopied == COPY_DATA for exceptionSpecification
     SgTypePtrList exceptionSpecification_copy = p_exceptionSpecification; 
     result->p_exceptionSpecification = exceptionSpecification_copy; 
  // Copy non-constructor parameter data member (access function): named_in_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_in_end_statement
  // case: toBeCopied == COPY_DATA for named_in_end_statement
     bool named_in_end_statement_copy = p_named_in_end_statement; 
     result->p_named_in_end_statement = named_in_end_statement_copy; 
  // Copy non-constructor parameter data member (access function): asm_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asm_name
  // case: toBeCopied == COPY_DATA for asm_name
     std::string asm_name_copy = p_asm_name; 
     result->p_asm_name = asm_name_copy; 
  // Copy non-constructor parameter data member (access function): decoratorList_copy
     SgExprListExp* decoratorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decoratorList
          if (get_decoratorList() != nullptr) 
             { 
               decoratorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_decoratorList())); 
             } 
            else 
             { 
               decoratorList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decoratorList != nullptr) delete result->p_decoratorList; 
     result->p_decoratorList = decoratorList_copy; 
  // case: not a listType for (using conditionalToSetParent)decoratorList
        if ((decoratorList_copy != nullptr) && (decoratorList_copy->get_parent() == nullptr) && (isSgType(decoratorList_copy) == nullptr)) {
            decoratorList_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): ada_formal_subprogram_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_subprogram_decl
  // case: toBeCopied == COPY_DATA for ada_formal_subprogram_decl
     bool ada_formal_subprogram_decl_copy = p_ada_formal_subprogram_decl; 
     result->p_ada_formal_subprogram_decl = ada_formal_subprogram_decl_copy; 
  // Copy non-constructor parameter data member (access function): ada_formal_decl_with_box_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ada_formal_decl_with_box
  // case: toBeCopied == COPY_DATA for ada_formal_decl_with_box
     bool ada_formal_decl_with_box_copy = p_ada_formal_decl_with_box; 
     result->p_ada_formal_decl_with_box = ada_formal_decl_with_box_copy; 
  // Copy non-constructor parameter data member (access function): oldStyleDefinition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for oldStyleDefinition
  // case: toBeCopied == COPY_DATA for oldStyleDefinition
     bool oldStyleDefinition_copy = p_oldStyleDefinition; 
     result->p_oldStyleDefinition = oldStyleDefinition_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): specialization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specialization
  // case: toBeCopied == COPY_DATA for specialization
     SgDeclarationStatement::template_specialization_enum specialization_copy = p_specialization; 
     result->p_specialization = specialization_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresNameQualificationOnReturnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualificationOnReturnType
  // case: toBeCopied == COPY_DATA for requiresNameQualificationOnReturnType
     bool requiresNameQualificationOnReturnType_copy = p_requiresNameQualificationOnReturnType; 
     result->p_requiresNameQualificationOnReturnType = requiresNameQualificationOnReturnType_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_section_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_section
  // case: toBeCopied == COPY_DATA for gnu_extension_section
     std::string gnu_extension_section_copy = p_gnu_extension_section; 
     result->p_gnu_extension_section = gnu_extension_section_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_alias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_alias
  // case: toBeCopied == COPY_DATA for gnu_extension_alias
     std::string gnu_extension_alias_copy = p_gnu_extension_alias; 
     result->p_gnu_extension_alias = gnu_extension_alias_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_visability_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_visability
  // case: toBeCopied == COPY_DATA for gnu_extension_visability
     SgDeclarationStatement::gnu_extension_visability_attribute_enum gnu_extension_visability_copy = p_gnu_extension_visability; 
     result->p_gnu_extension_visability = gnu_extension_visability_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_return_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_return_type
     int name_qualification_length_for_return_type_copy = p_name_qualification_length_for_return_type; 
     result->p_name_qualification_length_for_return_type = name_qualification_length_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_return_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_return_type
     bool type_elaboration_required_for_return_type_copy = p_type_elaboration_required_for_return_type; 
     result->p_type_elaboration_required_for_return_type = type_elaboration_required_for_return_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_return_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_return_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_return_type
     bool global_qualification_required_for_return_type_copy = p_global_qualification_required_for_return_type; 
     result->p_global_qualification_required_for_return_type = global_qualification_required_for_return_type_copy; 
  // Copy non-constructor parameter data member (access function): prototypeIsWithoutParameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeIsWithoutParameters
  // case: toBeCopied == COPY_DATA for prototypeIsWithoutParameters
     bool prototypeIsWithoutParameters_copy = p_prototypeIsWithoutParameters; 
     result->p_prototypeIsWithoutParameters = prototypeIsWithoutParameters_copy; 
  // Copy non-constructor parameter data member (access function): gnu_regparm_attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_regparm_attribute
  // case: toBeCopied == COPY_DATA for gnu_regparm_attribute
     int gnu_regparm_attribute_copy = p_gnu_regparm_attribute; 
     result->p_gnu_regparm_attribute = gnu_regparm_attribute_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax
  // case: toBeCopied == COPY_DATA for type_syntax
     SgFunctionType* type_syntax_copy = p_type_syntax; 
     result->p_type_syntax = type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): type_syntax_is_available_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_syntax_is_available
  // case: toBeCopied == COPY_DATA for type_syntax_is_available
     bool type_syntax_is_available_copy = p_type_syntax_is_available; 
     result->p_type_syntax_is_available = type_syntax_is_available_copy; 
  // Copy non-constructor parameter data member (access function): parameterList_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameterList_syntax
  // case: toBeCopied == COPY_DATA for parameterList_syntax
     SgFunctionParameterList* parameterList_syntax_copy = p_parameterList_syntax; 
     result->p_parameterList_syntax = parameterList_syntax_copy; 
  // Copy non-constructor parameter data member (access function): using_C11_Noreturn_keyword_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_C11_Noreturn_keyword
  // case: toBeCopied == COPY_DATA for using_C11_Noreturn_keyword
     bool using_C11_Noreturn_keyword_copy = p_using_C11_Noreturn_keyword; 
     result->p_using_C11_Noreturn_keyword = using_C11_Noreturn_keyword_copy; 
  // Copy non-constructor parameter data member (access function): is_constexpr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_constexpr
  // case: toBeCopied == COPY_DATA for is_constexpr
     bool is_constexpr_copy = p_is_constexpr; 
     result->p_is_constexpr = is_constexpr_copy; 
  // Copy non-constructor parameter data member (access function): using_new_function_return_type_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for using_new_function_return_type_syntax
  // case: toBeCopied == COPY_DATA for using_new_function_return_type_syntax
     bool using_new_function_return_type_syntax_copy = p_using_new_function_return_type_syntax; 
     result->p_using_new_function_return_type_syntax = using_new_function_return_type_syntax_copy; 
  // Copy non-constructor parameter data member (access function): functionParameterScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionParameterScope
  // case: toBeCopied == COPY_DATA for functionParameterScope
     SgFunctionParameterScope* functionParameterScope_copy = p_functionParameterScope; 
     result->p_functionParameterScope = functionParameterScope_copy; 
  // Copy non-constructor parameter data member (access function): marked_as_edg_normalization_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for marked_as_edg_normalization
  // case: toBeCopied == COPY_DATA for marked_as_edg_normalization
     bool marked_as_edg_normalization_copy = p_marked_as_edg_normalization; 
     result->p_marked_as_edg_normalization = marked_as_edg_normalization_copy; 
  // Copy non-constructor parameter data member (access function): is_implicit_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_implicit_function
  // case: toBeCopied == COPY_DATA for is_implicit_function
     bool is_implicit_function_copy = p_is_implicit_function; 
     result->p_is_implicit_function = is_implicit_function_copy; 
  // Copy non-constructor parameter data member (access function): renamed_function_copy
     SgExpression* renamed_function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)renamed_function
          if (get_renamed_function() != nullptr) 
             { 
               renamed_function_copy = static_cast<SgExpression*>(help.copyAst(get_renamed_function())); 
             } 
            else 
             { 
               renamed_function_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_renamed_function != nullptr) delete result->p_renamed_function; 
     result->p_renamed_function = renamed_function_copy; 
  // case: not a listType for (using conditionalToSetParent)renamed_function
        if ((renamed_function_copy != nullptr) && (renamed_function_copy->get_parent() == nullptr) && (isSgType(renamed_function_copy) == nullptr)) {
            renamed_function_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 63375 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgContainsStatement::copy(SgCopyHelp& help) const {
    SgContainsStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgContainsStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 63607 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgC_PreprocessorDirectiveStatement::copy(SgCopyHelp& help) const {
    SgC_PreprocessorDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgC_PreprocessorDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 63844 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIncludeDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIncludeDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 
  // Copy non-constructor parameter data member (access function): headerFileBody_copy
     SgHeaderFileBody* headerFileBody_copy; 
  // case: not a listType for (using conditionalToCopyVariable)headerFileBody
          if (get_headerFileBody() != nullptr) 
             { 
               headerFileBody_copy = static_cast<SgHeaderFileBody*>(help.copyAst(get_headerFileBody())); 
             } 
            else 
             { 
               headerFileBody_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_headerFileBody != nullptr) delete result->p_headerFileBody; 
     result->p_headerFileBody = headerFileBody_copy; 
  // case: not a listType for (using conditionalToSetParent)headerFileBody
        if ((headerFileBody_copy != nullptr) && (headerFileBody_copy->get_parent() == nullptr) && (isSgType(headerFileBody_copy) == nullptr)) {
            headerFileBody_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): include_file_heirarchy_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for include_file_heirarchy
  // case: toBeCopied == COPY_DATA for include_file_heirarchy
     SgIncludeFile* include_file_heirarchy_copy = p_include_file_heirarchy; 
     result->p_include_file_heirarchy = include_file_heirarchy_copy; 
  // Copy non-constructor parameter data member (access function): name_used_in_include_directive_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_used_in_include_directive
  // case: toBeCopied == COPY_DATA for name_used_in_include_directive
     std::string name_used_in_include_directive_copy = p_name_used_in_include_directive; 
     result->p_name_used_in_include_directive = name_used_in_include_directive_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 64109 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefineDirectiveStatement::copy(SgCopyHelp& help) const {
    SgDefineDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDefineDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 64346 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUndefDirectiveStatement::copy(SgCopyHelp& help) const {
    SgUndefDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUndefDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 64583 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfdefDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIfdefDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIfdefDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 64820 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfndefDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIfndefDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIfndefDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 65057 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIfDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIfDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIfDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 65294 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeadIfDirectiveStatement::copy(SgCopyHelp& help) const {
    SgDeadIfDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeadIfDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 65531 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseDirectiveStatement::copy(SgCopyHelp& help) const {
    SgElseDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElseDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 65768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseifDirectiveStatement::copy(SgCopyHelp& help) const {
    SgElseifDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElseifDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 66005 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEndifDirectiveStatement::copy(SgCopyHelp& help) const {
    SgEndifDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEndifDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 66242 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLineDirectiveStatement::copy(SgCopyHelp& help) const {
    SgLineDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLineDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 66479 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWarningDirectiveStatement::copy(SgCopyHelp& help) const {
    SgWarningDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWarningDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 66716 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgErrorDirectiveStatement::copy(SgCopyHelp& help) const {
    SgErrorDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgErrorDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 66953 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEmptyDirectiveStatement::copy(SgCopyHelp& help) const {
    SgEmptyDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEmptyDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 67190 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIncludeNextDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIncludeNextDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIncludeNextDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 67427 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIdentDirectiveStatement::copy(SgCopyHelp& help) const {
    SgIdentDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIdentDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 67664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLinemarkerDirectiveStatement::copy(SgCopyHelp& help) const {
    SgLinemarkerDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLinemarkerDirectiveStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): directiveString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directiveString
  // case: toBeCopied == COPY_DATA for directiveString
     std::string directiveString_copy = p_directiveString; 
     result->p_directiveString = directiveString_copy; 
  // Copy non-constructor parameter data member (access function): linenumber_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linenumber
  // case: toBeCopied == COPY_DATA for linenumber
     int linenumber_copy = p_linenumber; 
     result->p_linenumber = linenumber_copy; 
  // Copy non-constructor parameter data member (access function): filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     std::string filename_copy = p_filename; 
     result->p_filename = filename_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_flaglist()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flaglist
  // case: toBeCopied == COPY_DATA for flaglist
     SgUnsignedCharList flaglist_copy = p_flaglist; 
     result->p_flaglist = flaglist_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 67916 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpThreadprivateStatement::copy(SgCopyHelp& help) const {
    SgOmpThreadprivateStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpThreadprivateStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgVarRefExpPtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgVarRefExp* source_list_element = *source_variables_iterator; 
          SgVarRefExp* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgVarRefExp*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgVarRefExp* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 68176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranIncludeLine::copy(SgCopyHelp& help) const {
    SgFortranIncludeLine* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: filename_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filename
  // case: toBeCopied == COPY_DATA for filename
     std::string filename_copy = p_filename; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFortranIncludeLine( startOfConstruct_copy, filename_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 68412 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaImportStatement::copy(SgCopyHelp& help) const {
    SgJavaImportStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: path_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for path
  // case: toBeCopied == COPY_DATA for path
     SgName path_copy = p_path; 
    // Copy constructor parameter data member: containsWildCard_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsWildCard
  // case: toBeCopied == COPY_DATA for containsWildCard
     bool containsWildCard_copy = p_containsWildCard; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaImportStatement( startOfConstruct_copy, path_copy, containsWildCard_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 68652 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaPackageStatement::copy(SgCopyHelp& help) const {
    SgJavaPackageStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaPackageStatement( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 68888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStmtDeclarationStatement::copy(SgCopyHelp& help) const {
    SgStmtDeclarationStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != nullptr) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStmtDeclarationStatement( startOfConstruct_copy, statement_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
        if ((statement_copy != nullptr) && (statement_copy->get_parent() == nullptr) && (isSgType(statement_copy) == nullptr)) {
            statement_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 69135 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStaticAssertionDeclaration::copy(SgCopyHelp& help) const {
    SgStaticAssertionDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: string_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_literal
  // case: toBeCopied == COPY_DATA for string_literal
     SgName string_literal_copy = p_string_literal; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStaticAssertionDeclaration( startOfConstruct_copy, condition_copy, string_literal_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 69386 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDeclareSimdStatement::copy(SgCopyHelp& help) const {
    SgOmpDeclareSimdStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpDeclareSimdStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 69646 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMicrosoftAttributeDeclaration::copy(SgCopyHelp& help) const {
    SgMicrosoftAttributeDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: attribute_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_string
  // case: toBeCopied == COPY_DATA for attribute_string
     SgName attribute_string_copy = p_attribute_string; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMicrosoftAttributeDeclaration( startOfConstruct_copy, attribute_string_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 69882 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialCompoolStatement::copy(SgCopyHelp& help) const {
    SgJovialCompoolStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialCompoolStatement( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 70118 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialDirectiveStatement::copy(SgCopyHelp& help) const {
    SgJovialDirectiveStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: content_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for content_string
  // case: toBeCopied == COPY_DATA for content_string
     std::string content_string_copy = p_content_string; 
    // Copy constructor parameter data member: directive_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for directive_type
  // case: toBeCopied == COPY_DATA for directive_type
     SgJovialDirectiveStatement::directive_types directive_type_copy = p_directive_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialDirectiveStatement( startOfConstruct_copy, content_string_copy, directive_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 70358 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialDefineDeclaration::copy(SgCopyHelp& help) const {
    SgJovialDefineDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: define_string_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for define_string
  // case: toBeCopied == COPY_DATA for define_string
     std::string define_string_copy = p_define_string; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialDefineDeclaration( startOfConstruct_copy, define_string_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 70594 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialLabelDeclaration::copy(SgCopyHelp& help) const {
    SgJovialLabelDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     std::string label_copy = p_label; 
    // Copy constructor parameter data member: label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgJovialLabelDeclaration::label_type_enum label_type_copy = p_label_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialLabelDeclaration( startOfConstruct_copy, label_copy, label_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 70834 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialOverlayDeclaration::copy(SgCopyHelp& help) const {
    SgJovialOverlayDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: address_copy
     SgExpression* address_copy; 
  // case: not a listType for (using conditionalToCopyVariable)address
          if (get_address() != nullptr) 
             { 
               address_copy = static_cast<SgExpression*>(help.copyAst(get_address())); 
             } 
            else 
             { 
               address_copy = nullptr; 
             } 
    // Copy constructor parameter data member: overlay_copy
     SgExprListExp* overlay_copy; 
  // case: not a listType for (using conditionalToCopyVariable)overlay
          if (get_overlay() != nullptr) 
             { 
               overlay_copy = static_cast<SgExprListExp*>(help.copyAst(get_overlay())); 
             } 
            else 
             { 
               overlay_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialOverlayDeclaration( startOfConstruct_copy, address_copy, overlay_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)address
        if ((address_copy != nullptr) && (address_copy->get_parent() == nullptr) && (isSgType(address_copy) == nullptr)) {
            address_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)overlay
        if ((overlay_copy != nullptr) && (overlay_copy->get_parent() == nullptr) && (isSgType(overlay_copy) == nullptr)) {
            overlay_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 71096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealDecl::copy(SgCopyHelp& help) const {
    SgNonrealDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonrealDecl( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): nonreal_decl_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_decl_scope
  // case: toBeCopied == COPY_DATA for nonreal_decl_scope
     SgDeclarationScope* nonreal_decl_scope_copy = p_nonreal_decl_scope; 
     result->p_nonreal_decl_scope = nonreal_decl_scope_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgNonrealType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
     result->p_template_parameter_position = template_parameter_position_copy; 
  // Copy non-constructor parameter data member (access function): template_parameter_depth_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_depth
  // case: toBeCopied == COPY_DATA for template_parameter_depth
     int template_parameter_depth_copy = p_template_parameter_depth; 
     result->p_template_parameter_depth = template_parameter_depth_copy; 
  // Copy non-constructor parameter data member (access function): templateDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for templateDeclaration
  // case: toBeCopied == COPY_DATA for templateDeclaration
     SgDeclarationStatement* templateDeclaration_copy = p_templateDeclaration; 
     result->p_templateDeclaration = templateDeclaration_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_tpl_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_args
  // case: toBeCopied == COPY_DATA for tpl_args
     SgTemplateArgumentPtrList tpl_args_copy = p_tpl_args; 
     result->p_tpl_args = tpl_args_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_tpl_params()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tpl_params
  // case: toBeCopied == COPY_DATA for tpl_params
     SgTemplateParameterPtrList tpl_params_copy = p_tpl_params; 
     result->p_tpl_params = tpl_params_copy; 
  // Copy non-constructor parameter data member (access function): is_class_member_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_class_member
  // case: toBeCopied == COPY_DATA for is_class_member
     bool is_class_member_copy = p_is_class_member; 
     result->p_is_class_member = is_class_member_copy; 
  // Copy non-constructor parameter data member (access function): is_template_param_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_template_param
  // case: toBeCopied == COPY_DATA for is_template_param
     bool is_template_param_copy = p_is_template_param; 
     result->p_is_template_param = is_template_param_copy; 
  // Copy non-constructor parameter data member (access function): is_template_template_param_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_template_template_param
  // case: toBeCopied == COPY_DATA for is_template_template_param
     bool is_template_template_param_copy = p_is_template_template_param; 
     result->p_is_template_template_param = is_template_template_param_copy; 
  // Copy non-constructor parameter data member (access function): is_nonreal_template_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_nonreal_template
  // case: toBeCopied == COPY_DATA for is_nonreal_template
     bool is_nonreal_template_copy = p_is_nonreal_template; 
     result->p_is_nonreal_template = is_nonreal_template_copy; 
  // Copy non-constructor parameter data member (access function): is_nonreal_function_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_nonreal_function
  // case: toBeCopied == COPY_DATA for is_nonreal_function
     bool is_nonreal_function_copy = p_is_nonreal_function; 
     result->p_is_nonreal_function = is_nonreal_function_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 71392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEmptyDeclaration::copy(SgCopyHelp& help) const {
    SgEmptyDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEmptyDeclaration( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 71624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageBodyDecl::copy(SgCopyHelp& help) const {
    SgAdaPackageBodyDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaPackageBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaPackageBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaPackageBodyDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): spec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for spec
  // case: toBeCopied == COPY_DATA for spec
     SgAdaPackageSpecDecl* spec_copy = p_spec; 
     result->p_spec = spec_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 71885 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSpecDecl::copy(SgCopyHelp& help) const {
    SgAdaPackageSpecDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaPackageSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaPackageSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaPackageSpecDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAdaPackageBodyDecl* body_copy = p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 72146 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingDecl::copy(SgCopyHelp& help) const {
    SgAdaRenamingDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: renamed_copy
     SgExpression* renamed_copy; 
  // case: not a listType for (using conditionalToCopyVariable)renamed
          if (get_renamed() != nullptr) 
             { 
               renamed_copy = static_cast<SgExpression*>(help.copyAst(get_renamed())); 
             } 
            else 
             { 
               renamed_copy = nullptr; 
             } 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaRenamingDecl( startOfConstruct_copy, name_copy, renamed_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)renamed
        if ((renamed_copy != nullptr) && (renamed_copy->get_parent() == nullptr) && (isSgType(renamed_copy) == nullptr)) {
            renamed_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 72401 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSpecDecl::copy(SgCopyHelp& help) const {
    SgAdaTaskSpecDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaTaskSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaTaskSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskSpecDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaTaskType* type_copy = p_type; 
     result->p_type = type_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 72657 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskBodyDecl::copy(SgCopyHelp& help) const {
    SgAdaTaskBodyDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: specificationDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specificationDeclaration
  // case: toBeCopied == COPY_DATA for specificationDeclaration
     SgDeclarationStatement* specificationDeclaration_copy = p_specificationDeclaration; 
    // Copy constructor parameter data member: definition_copy
     SgAdaTaskBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaTaskBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskBodyDecl( startOfConstruct_copy, name_copy, specificationDeclaration_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 72912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskTypeDecl::copy(SgCopyHelp& help) const {
    SgAdaTaskTypeDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaTaskSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaTaskSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskTypeDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaTaskType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 73173 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSpecDecl::copy(SgCopyHelp& help) const {
    SgAdaProtectedSpecDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaProtectedSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaProtectedSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedSpecDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaProtectedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 73429 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedBodyDecl::copy(SgCopyHelp& help) const {
    SgAdaProtectedBodyDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: specificationDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specificationDeclaration
  // case: toBeCopied == COPY_DATA for specificationDeclaration
     SgDeclarationStatement* specificationDeclaration_copy = p_specificationDeclaration; 
    // Copy constructor parameter data member: definition_copy
     SgAdaProtectedBody* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaProtectedBody*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedBodyDecl( startOfConstruct_copy, name_copy, specificationDeclaration_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 73684 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedTypeDecl::copy(SgCopyHelp& help) const {
    SgAdaProtectedTypeDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaProtectedSpec* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaProtectedSpec*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedTypeDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaProtectedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 73945 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRepresentationClause::copy(SgCopyHelp& help) const {
    SgAdaRepresentationClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: recordType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recordType
  // case: toBeCopied == COPY_DATA for recordType
     SgType* recordType_copy = p_recordType; 
    // Copy constructor parameter data member: alignment_copy
     SgExpression* alignment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alignment
          if (get_alignment() != nullptr) 
             { 
               alignment_copy = static_cast<SgExpression*>(help.copyAst(get_alignment())); 
             } 
            else 
             { 
               alignment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: components_copy
     SgBasicBlock* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != nullptr) 
             { 
               components_copy = static_cast<SgBasicBlock*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaRepresentationClause( startOfConstruct_copy, recordType_copy, alignment_copy, components_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)alignment
        if ((alignment_copy != nullptr) && (alignment_copy->get_parent() == nullptr) && (isSgType(alignment_copy) == nullptr)) {
            alignment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)components
        if ((components_copy != nullptr) && (components_copy->get_parent() == nullptr) && (isSgType(components_copy) == nullptr)) {
            components_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 74211 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaComponentClause::copy(SgCopyHelp& help) const {
    SgAdaComponentClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: component_copy
     SgVarRefExp* component_copy; 
  // case: not a listType for (using conditionalToCopyVariable)component
          if (get_component() != nullptr) 
             { 
               component_copy = static_cast<SgVarRefExp*>(help.copyAst(get_component())); 
             } 
            else 
             { 
               component_copy = nullptr; 
             } 
    // Copy constructor parameter data member: offset_copy
     SgExpression* offset_copy; 
  // case: not a listType for (using conditionalToCopyVariable)offset
          if (get_offset() != nullptr) 
             { 
               offset_copy = static_cast<SgExpression*>(help.copyAst(get_offset())); 
             } 
            else 
             { 
               offset_copy = nullptr; 
             } 
    // Copy constructor parameter data member: range_copy
     SgRangeExp* range_copy; 
  // case: not a listType for (using conditionalToCopyVariable)range
          if (get_range() != nullptr) 
             { 
               range_copy = static_cast<SgRangeExp*>(help.copyAst(get_range())); 
             } 
            else 
             { 
               range_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaComponentClause( startOfConstruct_copy, component_copy, offset_copy, range_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)component
        if ((component_copy != nullptr) && (component_copy->get_parent() == nullptr) && (isSgType(component_copy) == nullptr)) {
            component_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)offset
        if ((offset_copy != nullptr) && (offset_copy->get_parent() == nullptr) && (isSgType(offset_copy) == nullptr)) {
            offset_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)range
        if ((range_copy != nullptr) && (range_copy->get_parent() == nullptr) && (isSgType(range_copy) == nullptr)) {
            range_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 74488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAttributeClause::copy(SgCopyHelp& help) const {
    SgAdaAttributeClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: attribute_copy
     SgAdaAttributeExp* attribute_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute
          if (get_attribute() != nullptr) 
             { 
               attribute_copy = static_cast<SgAdaAttributeExp*>(help.copyAst(get_attribute())); 
             } 
            else 
             { 
               attribute_copy = nullptr; 
             } 
    // Copy constructor parameter data member: size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != nullptr) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaAttributeClause( startOfConstruct_copy, attribute_copy, size_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute
        if ((attribute_copy != nullptr) && (attribute_copy->get_parent() == nullptr) && (isSgType(attribute_copy) == nullptr)) {
            attribute_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)size
        if ((size_copy != nullptr) && (size_copy->get_parent() == nullptr) && (isSgType(size_copy) == nullptr)) {
            size_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 74750 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaEnumRepresentationClause::copy(SgCopyHelp& help) const {
    SgAdaEnumRepresentationClause* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: enumType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for enumType
  // case: toBeCopied == COPY_DATA for enumType
     SgType* enumType_copy = p_enumType; 
    // Copy constructor parameter data member: components_copy
     SgExprListExp* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != nullptr) 
             { 
               components_copy = static_cast<SgExprListExp*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaEnumRepresentationClause( startOfConstruct_copy, enumType_copy, components_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)components
        if ((components_copy != nullptr) && (components_copy->get_parent() == nullptr) && (isSgType(components_copy) == nullptr)) {
            components_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 75001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericDecl::copy(SgCopyHelp& help) const {
    SgAdaGenericDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: definition_copy
     SgAdaGenericDefn* definition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)definition
          if (get_definition() != nullptr) 
             { 
               definition_copy = static_cast<SgAdaGenericDefn*>(help.copyAst(get_definition())); 
             } 
            else 
             { 
               definition_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaGenericDecl( startOfConstruct_copy, name_copy, definition_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_declaration != nullptr) delete result->p_declaration; 
     result->p_declaration = declaration_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)definition
        if ((definition_copy != nullptr) && (definition_copy->get_parent() == nullptr) && (isSgType(definition_copy) == nullptr)) {
            definition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 75275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalTypeDecl::copy(SgCopyHelp& help) const {
    SgAdaFormalTypeDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFormalTypeDecl( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaFormalType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): discriminants_copy
     SgAdaParameterList* discriminants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminants
          if (get_discriminants() != nullptr) 
             { 
               discriminants_copy = static_cast<SgAdaParameterList*>(help.copyAst(get_discriminants())); 
             } 
            else 
             { 
               discriminants_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_discriminants != nullptr) delete result->p_discriminants; 
     result->p_discriminants = discriminants_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminants
        if ((discriminants_copy != nullptr) && (discriminants_copy->get_parent() == nullptr) && (isSgType(discriminants_copy) == nullptr)) {
            discriminants_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 75534 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDiscriminatedTypeDecl::copy(SgCopyHelp& help) const {
    SgAdaDiscriminatedTypeDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: discriminantScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for discriminantScope
  // case: toBeCopied == COPY_DATA for discriminantScope
     SgDeclarationScope* discriminantScope_copy = p_discriminantScope; 
    // Copy constructor parameter data member: discriminants_copy
     SgAdaParameterList* discriminants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminants
          if (get_discriminants() != nullptr) 
             { 
               discriminants_copy = static_cast<SgAdaParameterList*>(help.copyAst(get_discriminants())); 
             } 
            else 
             { 
               discriminants_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDiscriminatedTypeDecl( startOfConstruct_copy, discriminantScope_copy, discriminants_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): discriminatedDecl_copy
     SgDeclarationStatement* discriminatedDecl_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminatedDecl
          if (get_discriminatedDecl() != nullptr) 
             { 
               discriminatedDecl_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_discriminatedDecl())); 
             } 
            else 
             { 
               discriminatedDecl_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_discriminatedDecl != nullptr) delete result->p_discriminatedDecl; 
     result->p_discriminatedDecl = discriminatedDecl_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminatedDecl
        if ((discriminatedDecl_copy != nullptr) && (discriminatedDecl_copy->get_parent() == nullptr) && (isSgType(discriminatedDecl_copy) == nullptr)) {
            discriminatedDecl_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAdaDiscriminatedType* type_copy = p_type; 
     result->p_type = type_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminants
        if ((discriminants_copy != nullptr) && (discriminants_copy->get_parent() == nullptr) && (isSgType(discriminants_copy) == nullptr)) {
            discriminants_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 75813 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericInstanceDecl::copy(SgCopyHelp& help) const {
    SgAdaGenericInstanceDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: genericDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for genericDeclaration
  // case: toBeCopied == COPY_DATA for genericDeclaration
     SgDeclarationStatement* genericDeclaration_copy = p_genericDeclaration; 
    // Copy constructor parameter data member: instantiatedScope_copy
     SgScopeStatement* instantiatedScope_copy; 
  // case: not a listType for (using conditionalToCopyVariable)instantiatedScope
          if (get_instantiatedScope() != nullptr) 
             { 
               instantiatedScope_copy = static_cast<SgScopeStatement*>(help.copyAst(get_instantiatedScope())); 
             } 
            else 
             { 
               instantiatedScope_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaGenericInstanceDecl( startOfConstruct_copy, name_copy, genericDeclaration_copy, instantiatedScope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): actual_parameters_copy
     SgExprListExp* actual_parameters_copy; 
  // case: not a listType for (using conditionalToCopyVariable)actual_parameters
          if (get_actual_parameters() != nullptr) 
             { 
               actual_parameters_copy = static_cast<SgExprListExp*>(help.copyAst(get_actual_parameters())); 
             } 
            else 
             { 
               actual_parameters_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_actual_parameters != nullptr) delete result->p_actual_parameters; 
     result->p_actual_parameters = actual_parameters_copy; 
  // case: not a listType for (using conditionalToSetParent)actual_parameters
        if ((actual_parameters_copy != nullptr) && (actual_parameters_copy->get_parent() == nullptr) && (isSgType(actual_parameters_copy) == nullptr)) {
            actual_parameters_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // case: not a listType for (using conditionalToSetParent)instantiatedScope
        if ((instantiatedScope_copy != nullptr) && (instantiatedScope_copy->get_parent() == nullptr) && (isSgType(instantiatedScope_copy) == nullptr)) {
            instantiatedScope_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 76091 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalPackageDecl::copy(SgCopyHelp& help) const {
    SgAdaFormalPackageDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgDeclarationStatement* declaration_copy = p_declaration; 
    // Copy constructor parameter data member: actual_parameters_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for actual_parameters
  // case: toBeCopied == COPY_DATA for actual_parameters
     SgExprListExp* actual_parameters_copy = p_actual_parameters; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFormalPackageDecl( startOfConstruct_copy, name_copy, declaration_copy, actual_parameters_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // Copy non-constructor parameter data member (access function): prototype_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototype
  // case: toBeCopied == COPY_DATA for prototype
     SgDeclarationStatement* prototype_copy = p_prototype; 
     result->p_prototype = prototype_copy; 
  // Copy non-constructor parameter data member (access function): prototypeScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prototypeScope
  // case: toBeCopied == COPY_DATA for prototypeScope
     SgScopeStatement* prototypeScope_copy = p_prototypeScope; 
     result->p_prototypeScope = prototypeScope_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 76345 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaParameterList::copy(SgCopyHelp& help) const {
    SgAdaParameterList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaParameterList( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_parameters()
     SgDeclarationStatementPtrList parameters_copy; 
  // case: listType for parameters
  // case: listType (typeIsPointerToList == false) for parameters
    auto source_parameters_iterator = get_parameters().begin();
    for (/* empty by design */; source_parameters_iterator != get_parameters().end(); ++source_parameters_iterator) {
          SgDeclarationStatement* source_list_element = *source_parameters_iterator; 
          SgDeclarationStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgDeclarationStatement*>(help.copyAst(*source_parameters_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          parameters_copy.push_back(copy_list_element); 
        }
     result->p_parameters = parameters_copy; 
  // case: listType for parameters
  // case: listType (typeIsPointerToList == false) for parameters
    auto parameters_iterator = result->get_parameters().begin();
    for (/* empty by design */; parameters_iterator != result->get_parameters().end(); ++parameters_iterator) {
          SgDeclarationStatement* list_element = *parameters_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 76605 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaVariantDecl::copy(SgCopyHelp& help) const {
    SgAdaVariantDecl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: discriminant_copy
     SgExpression* discriminant_copy; 
  // case: not a listType for (using conditionalToCopyVariable)discriminant
          if (get_discriminant() != nullptr) 
             { 
               discriminant_copy = static_cast<SgExpression*>(help.copyAst(get_discriminant())); 
             } 
            else 
             { 
               discriminant_copy = nullptr; 
             } 
    // Copy constructor parameter data member: variants_copy
     SgAdaUnscopedBlock* variants_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variants
          if (get_variants() != nullptr) 
             { 
               variants_copy = static_cast<SgAdaUnscopedBlock*>(help.copyAst(get_variants())); 
             } 
            else 
             { 
               variants_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaVariantDecl( startOfConstruct_copy, discriminant_copy, variants_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): decl_attributes_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_attributes
  // case: toBeCopied == COPY_DATA for decl_attributes
     unsigned int decl_attributes_copy = p_decl_attributes; 
     result->p_decl_attributes = decl_attributes_copy; 
  // Copy non-constructor parameter data member (access function): linkage_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage
  // case: toBeCopied == COPY_DATA for linkage
     std::string linkage_copy = p_linkage; 
     result->p_linkage = linkage_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_declarationModifier()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationModifier
  // case: toBeCopied == COPY_DATA for declarationModifier
     SgDeclarationModifier declarationModifier_copy = p_declarationModifier; 
     result->p_declarationModifier = declarationModifier_copy; 
  // Copy non-constructor parameter data member (access function): nameOnly_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOnly
  // case: toBeCopied == COPY_DATA for nameOnly
     bool nameOnly_copy = p_nameOnly; 
     result->p_nameOnly = nameOnly_copy; 
  // Copy non-constructor parameter data member (access function): forward_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forward
  // case: toBeCopied == COPY_DATA for forward
     bool forward_copy = p_forward; 
     result->p_forward = forward_copy; 
  // Copy non-constructor parameter data member (access function): externBrace_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for externBrace
  // case: toBeCopied == COPY_DATA for externBrace
     bool externBrace_copy = p_externBrace; 
     result->p_externBrace = externBrace_copy; 
  // Copy non-constructor parameter data member (access function): skipElaborateType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for skipElaborateType
  // case: toBeCopied == COPY_DATA for skipElaborateType
     bool skipElaborateType_copy = p_skipElaborateType; 
     result->p_skipElaborateType = skipElaborateType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_definingDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definingDeclaration
  // case: toBeCopied == COPY_DATA for definingDeclaration
     SgDeclarationStatement* definingDeclaration_copy = p_definingDeclaration; 
     result->p_definingDeclaration = definingDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_firstNondefiningDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for firstNondefiningDeclaration
  // case: toBeCopied == COPY_DATA for firstNondefiningDeclaration
     SgDeclarationStatement* firstNondefiningDeclaration_copy = p_firstNondefiningDeclaration; 
     result->p_firstNondefiningDeclaration = firstNondefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_qualifiedNameList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for qualifiedNameList
  // case: toBeCopied == COPY_DATA for qualifiedNameList
     SgQualifiedNamePtrList qualifiedNameList_copy = p_qualifiedNameList; 
     result->p_qualifiedNameList = qualifiedNameList_copy; 
  // Copy non-constructor parameter data member (access function): binding_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding_label
  // case: toBeCopied == COPY_DATA for binding_label
     std::string binding_label_copy = p_binding_label; 
     result->p_binding_label = binding_label_copy; 
  // Copy non-constructor parameter data member (access function): declarationScope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declarationScope
  // case: toBeCopied == COPY_DATA for declarationScope
     SgDeclarationScope* declarationScope_copy = p_declarationScope; 
     result->p_declarationScope = declarationScope_copy; 
  // Copy non-constructor parameter data member (access function): unparse_template_ast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unparse_template_ast
  // case: toBeCopied == COPY_DATA for unparse_template_ast
     bool unparse_template_ast_copy = p_unparse_template_ast; 
     result->p_unparse_template_ast = unparse_template_ast_copy; 
  // Copy non-constructor parameter data member (access function): adaAspects_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adaAspects
  // case: toBeCopied == COPY_DATA for adaAspects
     SgExprListExp* adaAspects_copy = p_adaAspects; 
     result->p_adaAspects = adaAspects_copy; 
  // case: not a listType for (using conditionalToSetParent)discriminant
        if ((discriminant_copy != nullptr) && (discriminant_copy->get_parent() == nullptr) && (isSgType(discriminant_copy) == nullptr)) {
            discriminant_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)variants
        if ((variants_copy != nullptr) && (variants_copy->get_parent() == nullptr) && (isSgType(variants_copy) == nullptr)) {
            variants_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 76867 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExprStatement::copy(SgCopyHelp& help) const {
    SgExprStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExprStatement( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 77044 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelStatement::copy(SgCopyHelp& help) const {
    SgLabelStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgName label_copy = p_label; 
    // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != nullptr) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLabelStatement( startOfConstruct_copy, label_copy, statement_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for scope
  // case: toBeCopied == COPY_DATA for scope
     SgScopeStatement* scope_copy = p_scope; 
     result->p_scope = scope_copy; 
  // Copy non-constructor parameter data member (access function): label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgLabelStatement::label_type_enum label_type_copy = p_label_type; 
     result->p_label_type = label_type_copy; 
  // Copy non-constructor parameter data member (access function): gnu_extension_unused_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for gnu_extension_unused
  // case: toBeCopied == COPY_DATA for gnu_extension_unused
     bool gnu_extension_unused_copy = p_gnu_extension_unused; 
     result->p_gnu_extension_unused = gnu_extension_unused_copy; 
  // case: not a listType for (using conditionalToSetParent)statement
        if ((statement_copy != nullptr) && (statement_copy->get_parent() == nullptr) && (isSgType(statement_copy) == nullptr)) {
            statement_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 77240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCaseOptionStmt::copy(SgCopyHelp& help) const {
    SgCaseOptionStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: key_copy
     SgExpression* key_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key
          if (get_key() != nullptr) 
             { 
               key_copy = static_cast<SgExpression*>(help.copyAst(get_key())); 
             } 
            else 
             { 
               key_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCaseOptionStmt( startOfConstruct_copy, key_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): key_range_end_copy
     SgExpression* key_range_end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key_range_end
          if (get_key_range_end() != nullptr) 
             { 
               key_range_end_copy = static_cast<SgExpression*>(help.copyAst(get_key_range_end())); 
             } 
            else 
             { 
               key_range_end_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_key_range_end != nullptr) delete result->p_key_range_end; 
     result->p_key_range_end = key_range_end_copy; 
  // case: not a listType for (using conditionalToSetParent)key_range_end
        if ((key_range_end_copy != nullptr) && (key_range_end_copy->get_parent() == nullptr) && (isSgType(key_range_end_copy) == nullptr)) {
            key_range_end_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): case_construct_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for case_construct_name
  // case: toBeCopied == COPY_DATA for case_construct_name
     std::string case_construct_name_copy = p_case_construct_name; 
     result->p_case_construct_name = case_construct_name_copy; 
  // Copy non-constructor parameter data member (access function): has_fall_through_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_fall_through
  // case: toBeCopied == COPY_DATA for has_fall_through
     bool has_fall_through_copy = p_has_fall_through; 
     result->p_has_fall_through = has_fall_through_copy; 
  // case: not a listType for (using conditionalToSetParent)key
        if ((key_copy != nullptr) && (key_copy->get_parent() == nullptr) && (isSgType(key_copy) == nullptr)) {
            key_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 77460 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTryStmt::copy(SgCopyHelp& help) const {
    SgTryStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTryStmt( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): catch_statement_seq_root_copy
     SgCatchStatementSeq* catch_statement_seq_root_copy; 
  // case: not a listType for (using conditionalToCopyVariable)catch_statement_seq_root
          if (get_catch_statement_seq_root() != nullptr) 
             { 
               catch_statement_seq_root_copy = static_cast<SgCatchStatementSeq*>(help.copyAst(get_catch_statement_seq_root())); 
             } 
            else 
             { 
               catch_statement_seq_root_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_catch_statement_seq_root != nullptr) delete result->p_catch_statement_seq_root; 
     result->p_catch_statement_seq_root = catch_statement_seq_root_copy; 
  // case: not a listType for (using conditionalToSetParent)catch_statement_seq_root
        if ((catch_statement_seq_root_copy != nullptr) && (catch_statement_seq_root_copy->get_parent() == nullptr) && (isSgType(catch_statement_seq_root_copy) == nullptr)) {
            catch_statement_seq_root_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): else_body_copy
     SgStatement* else_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_body
          if (get_else_body() != nullptr) 
             { 
               else_body_copy = static_cast<SgStatement*>(help.copyAst(get_else_body())); 
             } 
            else 
             { 
               else_body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_body != nullptr) delete result->p_else_body; 
     result->p_else_body = else_body_copy; 
  // case: not a listType for (using conditionalToSetParent)else_body
        if ((else_body_copy != nullptr) && (else_body_copy->get_parent() == nullptr) && (isSgType(else_body_copy) == nullptr)) {
            else_body_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): finally_body_copy
     SgStatement* finally_body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)finally_body
          if (get_finally_body() != nullptr) 
             { 
               finally_body_copy = static_cast<SgStatement*>(help.copyAst(get_finally_body())); 
             } 
            else 
             { 
               finally_body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_finally_body != nullptr) delete result->p_finally_body; 
     result->p_finally_body = finally_body_copy; 
  // case: not a listType for (using conditionalToSetParent)finally_body
        if ((finally_body_copy != nullptr) && (finally_body_copy->get_parent() == nullptr) && (isSgType(finally_body_copy) == nullptr)) {
            finally_body_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 77691 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefaultOptionStmt::copy(SgCopyHelp& help) const {
    SgDefaultOptionStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDefaultOptionStmt( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): default_construct_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_construct_name
  // case: toBeCopied == COPY_DATA for default_construct_name
     std::string default_construct_name_copy = p_default_construct_name; 
     result->p_default_construct_name = default_construct_name_copy; 
  // Copy non-constructor parameter data member (access function): has_fall_through_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_fall_through
  // case: toBeCopied == COPY_DATA for has_fall_through
     bool has_fall_through_copy = p_has_fall_through; 
     result->p_has_fall_through = has_fall_through_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 77878 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBreakStmt::copy(SgCopyHelp& help) const {
    SgBreakStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBreakStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): do_string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_string_label
  // case: toBeCopied == COPY_DATA for do_string_label
     std::string do_string_label_copy = p_do_string_label; 
     result->p_do_string_label = do_string_label_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78045 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgContinueStmt::copy(SgCopyHelp& help) const {
    SgContinueStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgContinueStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): do_string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for do_string_label
  // case: toBeCopied == COPY_DATA for do_string_label
     std::string do_string_label_copy = p_do_string_label; 
     result->p_do_string_label = do_string_label_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78212 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReturnStmt::copy(SgCopyHelp& help) const {
    SgReturnStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgReturnStmt( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78389 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGotoStatement::copy(SgCopyHelp& help) const {
    SgGotoStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgLabelStatement* label_copy = p_label; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGotoStatement( startOfConstruct_copy, label_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): label_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_expression
  // case: toBeCopied == COPY_DATA for label_expression
     SgLabelRefExp* label_expression_copy = p_label_expression; 
     result->p_label_expression = label_expression_copy; 
  // Copy non-constructor parameter data member (access function): selector_expression_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for selector_expression
  // case: toBeCopied == COPY_DATA for selector_expression
     SgExpression* selector_expression_copy = p_selector_expression; 
     result->p_selector_expression = selector_expression_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78565 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpawnStmt::copy(SgCopyHelp& help) const {
    SgSpawnStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: the_func_copy
     SgFunctionCallExp* the_func_copy; 
  // case: not a listType for (using conditionalToCopyVariable)the_func
          if (get_the_func() != nullptr) 
             { 
               the_func_copy = static_cast<SgFunctionCallExp*>(help.copyAst(get_the_func())); 
             } 
            else 
             { 
               the_func_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSpawnStmt( startOfConstruct_copy, the_func_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)the_func
        if ((the_func_copy != nullptr) && (the_func_copy->get_parent() == nullptr) && (isSgType(the_func_copy) == nullptr)) {
            the_func_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78742 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullStatement::copy(SgCopyHelp& help) const {
    SgNullStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNullStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 78904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariantStatement::copy(SgCopyHelp& help) const {
    SgVariantStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVariantStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 79066 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgForInitStatement::copy(SgCopyHelp& help) const {
    SgForInitStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgForInitStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_init_stmt()
     SgStatementPtrList init_stmt_copy; 
  // case: listType for init_stmt
  // case: listType (typeIsPointerToList == false) for init_stmt
    auto source_init_stmt_iterator = get_init_stmt().begin();
    for (/* empty by design */; source_init_stmt_iterator != get_init_stmt().end(); ++source_init_stmt_iterator) {
          SgStatement* source_list_element = *source_init_stmt_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_init_stmt_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          init_stmt_copy.push_back(copy_list_element); 
        }
     result->p_init_stmt = init_stmt_copy; 
  // case: listType for init_stmt
  // case: listType (typeIsPointerToList == false) for init_stmt
    auto init_stmt_iterator = result->get_init_stmt().begin();
    for (/* empty by design */; init_stmt_iterator != result->get_init_stmt().end(); ++init_stmt_iterator) {
          SgStatement* list_element = *init_stmt_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 79256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCatchStatementSeq::copy(SgCopyHelp& help) const {
    SgCatchStatementSeq* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCatchStatementSeq( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_catch_statement_seq()
     SgStatementPtrList catch_statement_seq_copy; 
  // case: listType for catch_statement_seq
  // case: listType (typeIsPointerToList == false) for catch_statement_seq
    auto source_catch_statement_seq_iterator = get_catch_statement_seq().begin();
    for (/* empty by design */; source_catch_statement_seq_iterator != get_catch_statement_seq().end(); ++source_catch_statement_seq_iterator) {
          SgStatement* source_list_element = *source_catch_statement_seq_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_catch_statement_seq_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          catch_statement_seq_copy.push_back(copy_list_element); 
        }
     result->p_catch_statement_seq = catch_statement_seq_copy; 
  // case: listType for catch_statement_seq
  // case: listType (typeIsPointerToList == false) for catch_statement_seq
    auto catch_statement_seq_iterator = result->get_catch_statement_seq().begin();
    for (/* empty by design */; catch_statement_seq_iterator != result->get_catch_statement_seq().end(); ++catch_statement_seq_iterator) {
          SgStatement* list_element = *catch_statement_seq_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 79446 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgProcessControlStatement::copy(SgCopyHelp& help) const {
    SgProcessControlStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: code_copy
     SgExpression* code_copy; 
  // case: not a listType for (using conditionalToCopyVariable)code
          if (get_code() != nullptr) 
             { 
               code_copy = static_cast<SgExpression*>(help.copyAst(get_code())); 
             } 
            else 
             { 
               code_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgProcessControlStatement( startOfConstruct_copy, code_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): control_kind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for control_kind
  // case: toBeCopied == COPY_DATA for control_kind
     SgProcessControlStatement::control_enum control_kind_copy = p_control_kind; 
     result->p_control_kind = control_kind_copy; 
  // Copy non-constructor parameter data member (access function): quiet_copy
     SgExpression* quiet_copy; 
  // case: not a listType for (using conditionalToCopyVariable)quiet
          if (get_quiet() != nullptr) 
             { 
               quiet_copy = static_cast<SgExpression*>(help.copyAst(get_quiet())); 
             } 
            else 
             { 
               quiet_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_quiet != nullptr) delete result->p_quiet; 
     result->p_quiet = quiet_copy; 
  // case: not a listType for (using conditionalToSetParent)quiet
        if ((quiet_copy != nullptr) && (quiet_copy->get_parent() == nullptr) && (isSgType(quiet_copy) == nullptr)) {
            quiet_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)code
        if ((code_copy != nullptr) && (code_copy->get_parent() == nullptr) && (isSgType(code_copy) == nullptr)) {
            code_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 79646 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIOStatement::copy(SgCopyHelp& help) const {
    SgIOStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIOStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 79903 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPrintStatement::copy(SgCopyHelp& help) const {
    SgPrintStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPrintStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != nullptr) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != nullptr) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
        if ((format_copy != nullptr) && (format_copy->get_parent() == nullptr) && (isSgType(format_copy) == nullptr)) {
            format_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 80178 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReadStatement::copy(SgCopyHelp& help) const {
    SgReadStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgReadStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != nullptr) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != nullptr) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
        if ((format_copy != nullptr) && (format_copy->get_parent() == nullptr) && (isSgType(format_copy) == nullptr)) {
            format_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): namelist_copy
     SgExpression* namelist_copy; 
  // case: not a listType for (using conditionalToCopyVariable)namelist
          if (get_namelist() != nullptr) 
             { 
               namelist_copy = static_cast<SgExpression*>(help.copyAst(get_namelist())); 
             } 
            else 
             { 
               namelist_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_namelist != nullptr) delete result->p_namelist; 
     result->p_namelist = namelist_copy; 
  // case: not a listType for (using conditionalToSetParent)namelist
        if ((namelist_copy != nullptr) && (namelist_copy->get_parent() == nullptr) && (isSgType(namelist_copy) == nullptr)) {
            namelist_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): advance_copy
     SgExpression* advance_copy; 
  // case: not a listType for (using conditionalToCopyVariable)advance
          if (get_advance() != nullptr) 
             { 
               advance_copy = static_cast<SgExpression*>(help.copyAst(get_advance())); 
             } 
            else 
             { 
               advance_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_advance != nullptr) delete result->p_advance; 
     result->p_advance = advance_copy; 
  // case: not a listType for (using conditionalToSetParent)advance
        if ((advance_copy != nullptr) && (advance_copy->get_parent() == nullptr) && (isSgType(advance_copy) == nullptr)) {
            advance_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != nullptr) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != nullptr) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
        if ((asynchronous_copy != nullptr) && (asynchronous_copy->get_parent() == nullptr) && (isSgType(asynchronous_copy) == nullptr)) {
            asynchronous_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != nullptr) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != nullptr) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
        if ((blank_copy != nullptr) && (blank_copy->get_parent() == nullptr) && (isSgType(blank_copy) == nullptr)) {
            blank_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): decimal_copy
     SgExpression* decimal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decimal
          if (get_decimal() != nullptr) 
             { 
               decimal_copy = static_cast<SgExpression*>(help.copyAst(get_decimal())); 
             } 
            else 
             { 
               decimal_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decimal != nullptr) delete result->p_decimal; 
     result->p_decimal = decimal_copy; 
  // case: not a listType for (using conditionalToSetParent)decimal
        if ((decimal_copy != nullptr) && (decimal_copy->get_parent() == nullptr) && (isSgType(decimal_copy) == nullptr)) {
            decimal_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != nullptr) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != nullptr) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
        if ((delim_copy != nullptr) && (delim_copy->get_parent() == nullptr) && (isSgType(delim_copy) == nullptr)) {
            delim_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != nullptr) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != nullptr) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
        if ((end_copy != nullptr) && (end_copy->get_parent() == nullptr) && (isSgType(end_copy) == nullptr)) {
            end_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): eor_copy
     SgExpression* eor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)eor
          if (get_eor() != nullptr) 
             { 
               eor_copy = static_cast<SgExpression*>(help.copyAst(get_eor())); 
             } 
            else 
             { 
               eor_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_eor != nullptr) delete result->p_eor; 
     result->p_eor = eor_copy; 
  // case: not a listType for (using conditionalToSetParent)eor
        if ((eor_copy != nullptr) && (eor_copy->get_parent() == nullptr) && (isSgType(eor_copy) == nullptr)) {
            eor_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): id_copy
     SgExpression* id_copy; 
  // case: not a listType for (using conditionalToCopyVariable)id
          if (get_id() != nullptr) 
             { 
               id_copy = static_cast<SgExpression*>(help.copyAst(get_id())); 
             } 
            else 
             { 
               id_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_id != nullptr) delete result->p_id; 
     result->p_id = id_copy; 
  // case: not a listType for (using conditionalToSetParent)id
        if ((id_copy != nullptr) && (id_copy->get_parent() == nullptr) && (isSgType(id_copy) == nullptr)) {
            id_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != nullptr) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != nullptr) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
        if ((pad_copy != nullptr) && (pad_copy->get_parent() == nullptr) && (isSgType(pad_copy) == nullptr)) {
            pad_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): pos_copy
     SgExpression* pos_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pos
          if (get_pos() != nullptr) 
             { 
               pos_copy = static_cast<SgExpression*>(help.copyAst(get_pos())); 
             } 
            else 
             { 
               pos_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pos != nullptr) delete result->p_pos; 
     result->p_pos = pos_copy; 
  // case: not a listType for (using conditionalToSetParent)pos
        if ((pos_copy != nullptr) && (pos_copy->get_parent() == nullptr) && (isSgType(pos_copy) == nullptr)) {
            pos_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): rec_copy
     SgExpression* rec_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rec
          if (get_rec() != nullptr) 
             { 
               rec_copy = static_cast<SgExpression*>(help.copyAst(get_rec())); 
             } 
            else 
             { 
               rec_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rec != nullptr) delete result->p_rec; 
     result->p_rec = rec_copy; 
  // case: not a listType for (using conditionalToSetParent)rec
        if ((rec_copy != nullptr) && (rec_copy->get_parent() == nullptr) && (isSgType(rec_copy) == nullptr)) {
            rec_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != nullptr) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != nullptr) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
        if ((round_copy != nullptr) && (round_copy->get_parent() == nullptr) && (isSgType(round_copy) == nullptr)) {
            round_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != nullptr) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != nullptr) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
        if ((sign_copy != nullptr) && (sign_copy->get_parent() == nullptr) && (isSgType(sign_copy) == nullptr)) {
            sign_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != nullptr) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_size != nullptr) delete result->p_size; 
     result->p_size = size_copy; 
  // case: not a listType for (using conditionalToSetParent)size
        if ((size_copy != nullptr) && (size_copy->get_parent() == nullptr) && (isSgType(size_copy) == nullptr)) {
            size_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 80723 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWriteStatement::copy(SgCopyHelp& help) const {
    SgWriteStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWriteStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): format_copy
     SgExpression* format_copy; 
  // case: not a listType for (using conditionalToCopyVariable)format
          if (get_format() != nullptr) 
             { 
               format_copy = static_cast<SgExpression*>(help.copyAst(get_format())); 
             } 
            else 
             { 
               format_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_format != nullptr) delete result->p_format; 
     result->p_format = format_copy; 
  // case: not a listType for (using conditionalToSetParent)format
        if ((format_copy != nullptr) && (format_copy->get_parent() == nullptr) && (isSgType(format_copy) == nullptr)) {
            format_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): namelist_copy
     SgExpression* namelist_copy; 
  // case: not a listType for (using conditionalToCopyVariable)namelist
          if (get_namelist() != nullptr) 
             { 
               namelist_copy = static_cast<SgExpression*>(help.copyAst(get_namelist())); 
             } 
            else 
             { 
               namelist_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_namelist != nullptr) delete result->p_namelist; 
     result->p_namelist = namelist_copy; 
  // case: not a listType for (using conditionalToSetParent)namelist
        if ((namelist_copy != nullptr) && (namelist_copy->get_parent() == nullptr) && (isSgType(namelist_copy) == nullptr)) {
            namelist_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): advance_copy
     SgExpression* advance_copy; 
  // case: not a listType for (using conditionalToCopyVariable)advance
          if (get_advance() != nullptr) 
             { 
               advance_copy = static_cast<SgExpression*>(help.copyAst(get_advance())); 
             } 
            else 
             { 
               advance_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_advance != nullptr) delete result->p_advance; 
     result->p_advance = advance_copy; 
  // case: not a listType for (using conditionalToSetParent)advance
        if ((advance_copy != nullptr) && (advance_copy->get_parent() == nullptr) && (isSgType(advance_copy) == nullptr)) {
            advance_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != nullptr) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != nullptr) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
        if ((asynchronous_copy != nullptr) && (asynchronous_copy->get_parent() == nullptr) && (isSgType(asynchronous_copy) == nullptr)) {
            asynchronous_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != nullptr) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != nullptr) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
        if ((blank_copy != nullptr) && (blank_copy->get_parent() == nullptr) && (isSgType(blank_copy) == nullptr)) {
            blank_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): decimal_copy
     SgExpression* decimal_copy; 
  // case: not a listType for (using conditionalToCopyVariable)decimal
          if (get_decimal() != nullptr) 
             { 
               decimal_copy = static_cast<SgExpression*>(help.copyAst(get_decimal())); 
             } 
            else 
             { 
               decimal_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_decimal != nullptr) delete result->p_decimal; 
     result->p_decimal = decimal_copy; 
  // case: not a listType for (using conditionalToSetParent)decimal
        if ((decimal_copy != nullptr) && (decimal_copy->get_parent() == nullptr) && (isSgType(decimal_copy) == nullptr)) {
            decimal_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != nullptr) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != nullptr) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
        if ((delim_copy != nullptr) && (delim_copy->get_parent() == nullptr) && (isSgType(delim_copy) == nullptr)) {
            delim_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != nullptr) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != nullptr) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
        if ((end_copy != nullptr) && (end_copy->get_parent() == nullptr) && (isSgType(end_copy) == nullptr)) {
            end_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): eor_copy
     SgExpression* eor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)eor
          if (get_eor() != nullptr) 
             { 
               eor_copy = static_cast<SgExpression*>(help.copyAst(get_eor())); 
             } 
            else 
             { 
               eor_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_eor != nullptr) delete result->p_eor; 
     result->p_eor = eor_copy; 
  // case: not a listType for (using conditionalToSetParent)eor
        if ((eor_copy != nullptr) && (eor_copy->get_parent() == nullptr) && (isSgType(eor_copy) == nullptr)) {
            eor_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): id_copy
     SgExpression* id_copy; 
  // case: not a listType for (using conditionalToCopyVariable)id
          if (get_id() != nullptr) 
             { 
               id_copy = static_cast<SgExpression*>(help.copyAst(get_id())); 
             } 
            else 
             { 
               id_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_id != nullptr) delete result->p_id; 
     result->p_id = id_copy; 
  // case: not a listType for (using conditionalToSetParent)id
        if ((id_copy != nullptr) && (id_copy->get_parent() == nullptr) && (isSgType(id_copy) == nullptr)) {
            id_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != nullptr) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != nullptr) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
        if ((pad_copy != nullptr) && (pad_copy->get_parent() == nullptr) && (isSgType(pad_copy) == nullptr)) {
            pad_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): pos_copy
     SgExpression* pos_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pos
          if (get_pos() != nullptr) 
             { 
               pos_copy = static_cast<SgExpression*>(help.copyAst(get_pos())); 
             } 
            else 
             { 
               pos_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pos != nullptr) delete result->p_pos; 
     result->p_pos = pos_copy; 
  // case: not a listType for (using conditionalToSetParent)pos
        if ((pos_copy != nullptr) && (pos_copy->get_parent() == nullptr) && (isSgType(pos_copy) == nullptr)) {
            pos_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): rec_copy
     SgExpression* rec_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rec
          if (get_rec() != nullptr) 
             { 
               rec_copy = static_cast<SgExpression*>(help.copyAst(get_rec())); 
             } 
            else 
             { 
               rec_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rec != nullptr) delete result->p_rec; 
     result->p_rec = rec_copy; 
  // case: not a listType for (using conditionalToSetParent)rec
        if ((rec_copy != nullptr) && (rec_copy->get_parent() == nullptr) && (isSgType(rec_copy) == nullptr)) {
            rec_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != nullptr) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != nullptr) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
        if ((round_copy != nullptr) && (round_copy->get_parent() == nullptr) && (isSgType(round_copy) == nullptr)) {
            round_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != nullptr) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != nullptr) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
        if ((sign_copy != nullptr) && (sign_copy->get_parent() == nullptr) && (isSgType(sign_copy) == nullptr)) {
            sign_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): size_copy
     SgExpression* size_copy; 
  // case: not a listType for (using conditionalToCopyVariable)size
          if (get_size() != nullptr) 
             { 
               size_copy = static_cast<SgExpression*>(help.copyAst(get_size())); 
             } 
            else 
             { 
               size_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_size != nullptr) delete result->p_size; 
     result->p_size = size_copy; 
  // case: not a listType for (using conditionalToSetParent)size
        if ((size_copy != nullptr) && (size_copy->get_parent() == nullptr) && (isSgType(size_copy) == nullptr)) {
            size_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 81268 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOpenStatement::copy(SgCopyHelp& help) const {
    SgOpenStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOpenStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): file_copy
     SgExpression* file_copy; 
  // case: not a listType for (using conditionalToCopyVariable)file
          if (get_file() != nullptr) 
             { 
               file_copy = static_cast<SgExpression*>(help.copyAst(get_file())); 
             } 
            else 
             { 
               file_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_file != nullptr) delete result->p_file; 
     result->p_file = file_copy; 
  // case: not a listType for (using conditionalToSetParent)file
        if ((file_copy != nullptr) && (file_copy->get_parent() == nullptr) && (isSgType(file_copy) == nullptr)) {
            file_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): status_copy
     SgExpression* status_copy; 
  // case: not a listType for (using conditionalToCopyVariable)status
          if (get_status() != nullptr) 
             { 
               status_copy = static_cast<SgExpression*>(help.copyAst(get_status())); 
             } 
            else 
             { 
               status_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_status != nullptr) delete result->p_status; 
     result->p_status = status_copy; 
  // case: not a listType for (using conditionalToSetParent)status
        if ((status_copy != nullptr) && (status_copy->get_parent() == nullptr) && (isSgType(status_copy) == nullptr)) {
            status_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): access_copy
     SgExpression* access_copy; 
  // case: not a listType for (using conditionalToCopyVariable)access
          if (get_access() != nullptr) 
             { 
               access_copy = static_cast<SgExpression*>(help.copyAst(get_access())); 
             } 
            else 
             { 
               access_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_access != nullptr) delete result->p_access; 
     result->p_access = access_copy; 
  // case: not a listType for (using conditionalToSetParent)access
        if ((access_copy != nullptr) && (access_copy->get_parent() == nullptr) && (isSgType(access_copy) == nullptr)) {
            access_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): form_copy
     SgExpression* form_copy; 
  // case: not a listType for (using conditionalToCopyVariable)form
          if (get_form() != nullptr) 
             { 
               form_copy = static_cast<SgExpression*>(help.copyAst(get_form())); 
             } 
            else 
             { 
               form_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_form != nullptr) delete result->p_form; 
     result->p_form = form_copy; 
  // case: not a listType for (using conditionalToSetParent)form
        if ((form_copy != nullptr) && (form_copy->get_parent() == nullptr) && (isSgType(form_copy) == nullptr)) {
            form_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): recl_copy
     SgExpression* recl_copy; 
  // case: not a listType for (using conditionalToCopyVariable)recl
          if (get_recl() != nullptr) 
             { 
               recl_copy = static_cast<SgExpression*>(help.copyAst(get_recl())); 
             } 
            else 
             { 
               recl_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_recl != nullptr) delete result->p_recl; 
     result->p_recl = recl_copy; 
  // case: not a listType for (using conditionalToSetParent)recl
        if ((recl_copy != nullptr) && (recl_copy->get_parent() == nullptr) && (isSgType(recl_copy) == nullptr)) {
            recl_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): blank_copy
     SgExpression* blank_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blank
          if (get_blank() != nullptr) 
             { 
               blank_copy = static_cast<SgExpression*>(help.copyAst(get_blank())); 
             } 
            else 
             { 
               blank_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_blank != nullptr) delete result->p_blank; 
     result->p_blank = blank_copy; 
  // case: not a listType for (using conditionalToSetParent)blank
        if ((blank_copy != nullptr) && (blank_copy->get_parent() == nullptr) && (isSgType(blank_copy) == nullptr)) {
            blank_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): position_copy
     SgExpression* position_copy; 
  // case: not a listType for (using conditionalToCopyVariable)position
          if (get_position() != nullptr) 
             { 
               position_copy = static_cast<SgExpression*>(help.copyAst(get_position())); 
             } 
            else 
             { 
               position_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_position != nullptr) delete result->p_position; 
     result->p_position = position_copy; 
  // case: not a listType for (using conditionalToSetParent)position
        if ((position_copy != nullptr) && (position_copy->get_parent() == nullptr) && (isSgType(position_copy) == nullptr)) {
            position_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): action_copy
     SgExpression* action_copy; 
  // case: not a listType for (using conditionalToCopyVariable)action
          if (get_action() != nullptr) 
             { 
               action_copy = static_cast<SgExpression*>(help.copyAst(get_action())); 
             } 
            else 
             { 
               action_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_action != nullptr) delete result->p_action; 
     result->p_action = action_copy; 
  // case: not a listType for (using conditionalToSetParent)action
        if ((action_copy != nullptr) && (action_copy->get_parent() == nullptr) && (isSgType(action_copy) == nullptr)) {
            action_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): delim_copy
     SgExpression* delim_copy; 
  // case: not a listType for (using conditionalToCopyVariable)delim
          if (get_delim() != nullptr) 
             { 
               delim_copy = static_cast<SgExpression*>(help.copyAst(get_delim())); 
             } 
            else 
             { 
               delim_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_delim != nullptr) delete result->p_delim; 
     result->p_delim = delim_copy; 
  // case: not a listType for (using conditionalToSetParent)delim
        if ((delim_copy != nullptr) && (delim_copy->get_parent() == nullptr) && (isSgType(delim_copy) == nullptr)) {
            delim_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): pad_copy
     SgExpression* pad_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pad
          if (get_pad() != nullptr) 
             { 
               pad_copy = static_cast<SgExpression*>(help.copyAst(get_pad())); 
             } 
            else 
             { 
               pad_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pad != nullptr) delete result->p_pad; 
     result->p_pad = pad_copy; 
  // case: not a listType for (using conditionalToSetParent)pad
        if ((pad_copy != nullptr) && (pad_copy->get_parent() == nullptr) && (isSgType(pad_copy) == nullptr)) {
            pad_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): round_copy
     SgExpression* round_copy; 
  // case: not a listType for (using conditionalToCopyVariable)round
          if (get_round() != nullptr) 
             { 
               round_copy = static_cast<SgExpression*>(help.copyAst(get_round())); 
             } 
            else 
             { 
               round_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_round != nullptr) delete result->p_round; 
     result->p_round = round_copy; 
  // case: not a listType for (using conditionalToSetParent)round
        if ((round_copy != nullptr) && (round_copy->get_parent() == nullptr) && (isSgType(round_copy) == nullptr)) {
            round_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): sign_copy
     SgExpression* sign_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sign
          if (get_sign() != nullptr) 
             { 
               sign_copy = static_cast<SgExpression*>(help.copyAst(get_sign())); 
             } 
            else 
             { 
               sign_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sign != nullptr) delete result->p_sign; 
     result->p_sign = sign_copy; 
  // case: not a listType for (using conditionalToSetParent)sign
        if ((sign_copy != nullptr) && (sign_copy->get_parent() == nullptr) && (isSgType(sign_copy) == nullptr)) {
            sign_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
     SgExpression* asynchronous_copy; 
  // case: not a listType for (using conditionalToCopyVariable)asynchronous
          if (get_asynchronous() != nullptr) 
             { 
               asynchronous_copy = static_cast<SgExpression*>(help.copyAst(get_asynchronous())); 
             } 
            else 
             { 
               asynchronous_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_asynchronous != nullptr) delete result->p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // case: not a listType for (using conditionalToSetParent)asynchronous
        if ((asynchronous_copy != nullptr) && (asynchronous_copy->get_parent() == nullptr) && (isSgType(asynchronous_copy) == nullptr)) {
            asynchronous_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 81759 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCloseStatement::copy(SgCopyHelp& help) const {
    SgCloseStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCloseStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): status_copy
     SgExpression* status_copy; 
  // case: not a listType for (using conditionalToCopyVariable)status
          if (get_status() != nullptr) 
             { 
               status_copy = static_cast<SgExpression*>(help.copyAst(get_status())); 
             } 
            else 
             { 
               status_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_status != nullptr) delete result->p_status; 
     result->p_status = status_copy; 
  // case: not a listType for (using conditionalToSetParent)status
        if ((status_copy != nullptr) && (status_copy->get_parent() == nullptr) && (isSgType(status_copy) == nullptr)) {
            status_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 82034 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInquireStatement::copy(SgCopyHelp& help) const {
    SgInquireStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInquireStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): file_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgExpression* file_copy = p_file; 
     result->p_file = file_copy; 
  // Copy non-constructor parameter data member (access function): access_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access
  // case: toBeCopied == COPY_DATA for access
     SgExpression* access_copy = p_access; 
     result->p_access = access_copy; 
  // Copy non-constructor parameter data member (access function): form_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for form
  // case: toBeCopied == COPY_DATA for form
     SgExpression* form_copy = p_form; 
     result->p_form = form_copy; 
  // Copy non-constructor parameter data member (access function): recl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recl
  // case: toBeCopied == COPY_DATA for recl
     SgExpression* recl_copy = p_recl; 
     result->p_recl = recl_copy; 
  // Copy non-constructor parameter data member (access function): blank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for blank
  // case: toBeCopied == COPY_DATA for blank
     SgExpression* blank_copy = p_blank; 
     result->p_blank = blank_copy; 
  // Copy non-constructor parameter data member (access function): exist_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exist
  // case: toBeCopied == COPY_DATA for exist
     SgExpression* exist_copy = p_exist; 
     result->p_exist = exist_copy; 
  // Copy non-constructor parameter data member (access function): opened_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opened
  // case: toBeCopied == COPY_DATA for opened
     SgExpression* opened_copy = p_opened; 
     result->p_opened = opened_copy; 
  // Copy non-constructor parameter data member (access function): number_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for number
  // case: toBeCopied == COPY_DATA for number
     SgExpression* number_copy = p_number; 
     result->p_number = number_copy; 
  // Copy non-constructor parameter data member (access function): named_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named
  // case: toBeCopied == COPY_DATA for named
     SgExpression* named_copy = p_named; 
     result->p_named = named_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgExpression* name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): sequential_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sequential
  // case: toBeCopied == COPY_DATA for sequential
     SgExpression* sequential_copy = p_sequential; 
     result->p_sequential = sequential_copy; 
  // Copy non-constructor parameter data member (access function): direct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for direct
  // case: toBeCopied == COPY_DATA for direct
     SgExpression* direct_copy = p_direct; 
     result->p_direct = direct_copy; 
  // Copy non-constructor parameter data member (access function): formatted_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for formatted
  // case: toBeCopied == COPY_DATA for formatted
     SgExpression* formatted_copy = p_formatted; 
     result->p_formatted = formatted_copy; 
  // Copy non-constructor parameter data member (access function): unformatted_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for unformatted
  // case: toBeCopied == COPY_DATA for unformatted
     SgExpression* unformatted_copy = p_unformatted; 
     result->p_unformatted = unformatted_copy; 
  // Copy non-constructor parameter data member (access function): nextrec_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nextrec
  // case: toBeCopied == COPY_DATA for nextrec
     SgExpression* nextrec_copy = p_nextrec; 
     result->p_nextrec = nextrec_copy; 
  // Copy non-constructor parameter data member (access function): position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for position
  // case: toBeCopied == COPY_DATA for position
     SgExpression* position_copy = p_position; 
     result->p_position = position_copy; 
  // Copy non-constructor parameter data member (access function): action_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for action
  // case: toBeCopied == COPY_DATA for action
     SgExpression* action_copy = p_action; 
     result->p_action = action_copy; 
  // Copy non-constructor parameter data member (access function): read_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for read
  // case: toBeCopied == COPY_DATA for read
     SgExpression* read_copy = p_read; 
     result->p_read = read_copy; 
  // Copy non-constructor parameter data member (access function): write_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for write
  // case: toBeCopied == COPY_DATA for write
     SgExpression* write_copy = p_write; 
     result->p_write = write_copy; 
  // Copy non-constructor parameter data member (access function): readwrite_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for readwrite
  // case: toBeCopied == COPY_DATA for readwrite
     SgExpression* readwrite_copy = p_readwrite; 
     result->p_readwrite = readwrite_copy; 
  // Copy non-constructor parameter data member (access function): delim_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delim
  // case: toBeCopied == COPY_DATA for delim
     SgExpression* delim_copy = p_delim; 
     result->p_delim = delim_copy; 
  // Copy non-constructor parameter data member (access function): pad_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pad
  // case: toBeCopied == COPY_DATA for pad
     SgExpression* pad_copy = p_pad; 
     result->p_pad = pad_copy; 
  // Copy non-constructor parameter data member (access function): asynchronous_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for asynchronous
  // case: toBeCopied == COPY_DATA for asynchronous
     SgExpression* asynchronous_copy = p_asynchronous; 
     result->p_asynchronous = asynchronous_copy; 
  // Copy non-constructor parameter data member (access function): decimal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decimal
  // case: toBeCopied == COPY_DATA for decimal
     SgExpression* decimal_copy = p_decimal; 
     result->p_decimal = decimal_copy; 
  // Copy non-constructor parameter data member (access function): stream_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stream
  // case: toBeCopied == COPY_DATA for stream
     SgExpression* stream_copy = p_stream; 
     result->p_stream = stream_copy; 
  // Copy non-constructor parameter data member (access function): size_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     SgExpression* size_copy = p_size; 
     result->p_size = size_copy; 
  // Copy non-constructor parameter data member (access function): pending_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pending
  // case: toBeCopied == COPY_DATA for pending
     SgExpression* pending_copy = p_pending; 
     result->p_pending = pending_copy; 
  // Copy non-constructor parameter data member (access function): iolengthExp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iolengthExp
  // case: toBeCopied == COPY_DATA for iolengthExp
     SgVarRefExp* iolengthExp_copy = p_iolengthExp; 
     result->p_iolengthExp = iolengthExp_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 82431 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFlushStatement::copy(SgCopyHelp& help) const {
    SgFlushStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFlushStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 82688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBackspaceStatement::copy(SgCopyHelp& help) const {
    SgBackspaceStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBackspaceStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 82945 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRewindStatement::copy(SgCopyHelp& help) const {
    SgRewindStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRewindStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 83202 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEndfileStatement::copy(SgCopyHelp& help) const {
    SgEndfileStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEndfileStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 83459 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWaitStatement::copy(SgCopyHelp& help) const {
    SgWaitStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWaitStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): io_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for io_statement
  // case: toBeCopied == COPY_DATA for io_statement
     SgIOStatement::io_statement_enum io_statement_copy = p_io_statement; 
     result->p_io_statement = io_statement_copy; 
  // Copy non-constructor parameter data member (access function): io_stmt_list_copy
     SgExprListExp* io_stmt_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_stmt_list
          if (get_io_stmt_list() != nullptr) 
             { 
               io_stmt_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_io_stmt_list())); 
             } 
            else 
             { 
               io_stmt_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_io_stmt_list != nullptr) delete result->p_io_stmt_list; 
     result->p_io_stmt_list = io_stmt_list_copy; 
  // case: not a listType for (using conditionalToSetParent)io_stmt_list
        if ((io_stmt_list_copy != nullptr) && (io_stmt_list_copy->get_parent() == nullptr) && (isSgType(io_stmt_list_copy) == nullptr)) {
            io_stmt_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): unit_copy
     SgExpression* unit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unit
          if (get_unit() != nullptr) 
             { 
               unit_copy = static_cast<SgExpression*>(help.copyAst(get_unit())); 
             } 
            else 
             { 
               unit_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unit != nullptr) delete result->p_unit; 
     result->p_unit = unit_copy; 
  // case: not a listType for (using conditionalToSetParent)unit
        if ((unit_copy != nullptr) && (unit_copy->get_parent() == nullptr) && (isSgType(unit_copy) == nullptr)) {
            unit_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iostat_copy
     SgExpression* iostat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iostat
          if (get_iostat() != nullptr) 
             { 
               iostat_copy = static_cast<SgExpression*>(help.copyAst(get_iostat())); 
             } 
            else 
             { 
               iostat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iostat != nullptr) delete result->p_iostat; 
     result->p_iostat = iostat_copy; 
  // case: not a listType for (using conditionalToSetParent)iostat
        if ((iostat_copy != nullptr) && (iostat_copy->get_parent() == nullptr) && (isSgType(iostat_copy) == nullptr)) {
            iostat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_copy
     SgExpression* err_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err
          if (get_err() != nullptr) 
             { 
               err_copy = static_cast<SgExpression*>(help.copyAst(get_err())); 
             } 
            else 
             { 
               err_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err != nullptr) delete result->p_err; 
     result->p_err = err_copy; 
  // case: not a listType for (using conditionalToSetParent)err
        if ((err_copy != nullptr) && (err_copy->get_parent() == nullptr) && (isSgType(err_copy) == nullptr)) {
            err_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): iomsg_copy
     SgExpression* iomsg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iomsg
          if (get_iomsg() != nullptr) 
             { 
               iomsg_copy = static_cast<SgExpression*>(help.copyAst(get_iomsg())); 
             } 
            else 
             { 
               iomsg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_iomsg != nullptr) delete result->p_iomsg; 
     result->p_iomsg = iomsg_copy; 
  // case: not a listType for (using conditionalToSetParent)iomsg
        if ((iomsg_copy != nullptr) && (iomsg_copy->get_parent() == nullptr) && (isSgType(iomsg_copy) == nullptr)) {
            iomsg_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 83716 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFortranContinueStmt::copy(SgCopyHelp& help) const {
    SgFortranContinueStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFortranContinueStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 83878 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhereStatement::copy(SgCopyHelp& help) const {
    SgWhereStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: elsewhere_copy
     SgElseWhereStatement* elsewhere_copy; 
  // case: not a listType for (using conditionalToCopyVariable)elsewhere
          if (get_elsewhere() != nullptr) 
             { 
               elsewhere_copy = static_cast<SgElseWhereStatement*>(help.copyAst(get_elsewhere())); 
             } 
            else 
             { 
               elsewhere_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWhereStatement( startOfConstruct_copy, condition_copy, body_copy, elsewhere_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): end_numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_numeric_label
  // case: toBeCopied == COPY_DATA for end_numeric_label
     SgLabelRefExp* end_numeric_label_copy = p_end_numeric_label; 
     result->p_end_numeric_label = end_numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 
  // Copy non-constructor parameter data member (access function): has_end_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_end_statement
  // case: toBeCopied == COPY_DATA for has_end_statement
     bool has_end_statement_copy = p_has_end_statement; 
     result->p_has_end_statement = has_end_statement_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)elsewhere
        if ((elsewhere_copy != nullptr) && (elsewhere_copy->get_parent() == nullptr) && (isSgType(elsewhere_copy) == nullptr)) {
            elsewhere_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 84100 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElseWhereStatement::copy(SgCopyHelp& help) const {
    SgElseWhereStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: elsewhere_copy
     SgElseWhereStatement* elsewhere_copy; 
  // case: not a listType for (using conditionalToCopyVariable)elsewhere
          if (get_elsewhere() != nullptr) 
             { 
               elsewhere_copy = static_cast<SgElseWhereStatement*>(help.copyAst(get_elsewhere())); 
             } 
            else 
             { 
               elsewhere_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElseWhereStatement( startOfConstruct_copy, condition_copy, body_copy, elsewhere_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)elsewhere
        if ((elsewhere_copy != nullptr) && (elsewhere_copy->get_parent() == nullptr) && (isSgType(elsewhere_copy) == nullptr)) {
            elsewhere_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 84307 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullifyStatement::copy(SgCopyHelp& help) const {
    SgNullifyStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNullifyStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): pointer_list_copy
     SgExprListExp* pointer_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pointer_list
          if (get_pointer_list() != nullptr) 
             { 
               pointer_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_pointer_list())); 
             } 
            else 
             { 
               pointer_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pointer_list != nullptr) delete result->p_pointer_list; 
     result->p_pointer_list = pointer_list_copy; 
  // case: not a listType for (using conditionalToSetParent)pointer_list
        if ((pointer_list_copy != nullptr) && (pointer_list_copy->get_parent() == nullptr) && (isSgType(pointer_list_copy) == nullptr)) {
            pointer_list_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 84487 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArithmeticIfStatement::copy(SgCopyHelp& help) const {
    SgArithmeticIfStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: conditional_copy
     SgExpression* conditional_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional
          if (get_conditional() != nullptr) 
             { 
               conditional_copy = static_cast<SgExpression*>(help.copyAst(get_conditional())); 
             } 
            else 
             { 
               conditional_copy = nullptr; 
             } 
    // Copy constructor parameter data member: less_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for less_label
  // case: toBeCopied == COPY_DATA for less_label
     SgLabelRefExp* less_label_copy = p_less_label; 
    // Copy constructor parameter data member: equal_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for equal_label
  // case: toBeCopied == COPY_DATA for equal_label
     SgLabelRefExp* equal_label_copy = p_equal_label; 
    // Copy constructor parameter data member: greater_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for greater_label
  // case: toBeCopied == COPY_DATA for greater_label
     SgLabelRefExp* greater_label_copy = p_greater_label; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgArithmeticIfStatement( startOfConstruct_copy, conditional_copy, less_label_copy, equal_label_copy, greater_label_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)conditional
        if ((conditional_copy != nullptr) && (conditional_copy->get_parent() == nullptr) && (isSgType(conditional_copy) == nullptr)) {
            conditional_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 84676 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignStatement::copy(SgCopyHelp& help) const {
    SgAssignStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label
  // case: toBeCopied == COPY_DATA for label
     SgLabelRefExp* label_copy = p_label; 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssignStatement( startOfConstruct_copy, label_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 84857 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComputedGotoStatement::copy(SgCopyHelp& help) const {
    SgComputedGotoStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: labelList_copy
     SgExprListExp* labelList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)labelList
          if (get_labelList() != nullptr) 
             { 
               labelList_copy = static_cast<SgExprListExp*>(help.copyAst(get_labelList())); 
             } 
            else 
             { 
               labelList_copy = nullptr; 
             } 
    // Copy constructor parameter data member: label_index_copy
     SgExpression* label_index_copy; 
  // case: not a listType for (using conditionalToCopyVariable)label_index
          if (get_label_index() != nullptr) 
             { 
               label_index_copy = static_cast<SgExpression*>(help.copyAst(get_label_index())); 
             } 
            else 
             { 
               label_index_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgComputedGotoStatement( startOfConstruct_copy, labelList_copy, label_index_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)labelList
        if ((labelList_copy != nullptr) && (labelList_copy->get_parent() == nullptr) && (isSgType(labelList_copy) == nullptr)) {
            labelList_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)label_index
        if ((label_index_copy != nullptr) && (label_index_copy->get_parent() == nullptr) && (isSgType(label_index_copy) == nullptr)) {
            label_index_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 85049 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignedGotoStatement::copy(SgCopyHelp& help) const {
    SgAssignedGotoStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: targets_copy
     SgExprListExp* targets_copy; 
  // case: not a listType for (using conditionalToCopyVariable)targets
          if (get_targets() != nullptr) 
             { 
               targets_copy = static_cast<SgExprListExp*>(help.copyAst(get_targets())); 
             } 
            else 
             { 
               targets_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssignedGotoStatement( startOfConstruct_copy, targets_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)targets
        if ((targets_copy != nullptr) && (targets_copy->get_parent() == nullptr) && (isSgType(targets_copy) == nullptr)) {
            targets_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 85226 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAllocateStatement::copy(SgCopyHelp& help) const {
    SgAllocateStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAllocateStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): expr_list_copy
     SgExprListExp* expr_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expr_list
          if (get_expr_list() != nullptr) 
             { 
               expr_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expr_list())); 
             } 
            else 
             { 
               expr_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expr_list != nullptr) delete result->p_expr_list; 
     result->p_expr_list = expr_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expr_list
        if ((expr_list_copy != nullptr) && (expr_list_copy->get_parent() == nullptr) && (isSgType(expr_list_copy) == nullptr)) {
            expr_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): stat_expression_copy
     SgExpression* stat_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat_expression
          if (get_stat_expression() != nullptr) 
             { 
               stat_expression_copy = static_cast<SgExpression*>(help.copyAst(get_stat_expression())); 
             } 
            else 
             { 
               stat_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat_expression != nullptr) delete result->p_stat_expression; 
     result->p_stat_expression = stat_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)stat_expression
        if ((stat_expression_copy != nullptr) && (stat_expression_copy->get_parent() == nullptr) && (isSgType(stat_expression_copy) == nullptr)) {
            stat_expression_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): errmsg_expression_copy
     SgExpression* errmsg_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)errmsg_expression
          if (get_errmsg_expression() != nullptr) 
             { 
               errmsg_expression_copy = static_cast<SgExpression*>(help.copyAst(get_errmsg_expression())); 
             } 
            else 
             { 
               errmsg_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_errmsg_expression != nullptr) delete result->p_errmsg_expression; 
     result->p_errmsg_expression = errmsg_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)errmsg_expression
        if ((errmsg_expression_copy != nullptr) && (errmsg_expression_copy->get_parent() == nullptr) && (isSgType(errmsg_expression_copy) == nullptr)) {
            errmsg_expression_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): source_expression_copy
     SgExpression* source_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)source_expression
          if (get_source_expression() != nullptr) 
             { 
               source_expression_copy = static_cast<SgExpression*>(help.copyAst(get_source_expression())); 
             } 
            else 
             { 
               source_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_source_expression != nullptr) delete result->p_source_expression; 
     result->p_source_expression = source_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)source_expression
        if ((source_expression_copy != nullptr) && (source_expression_copy->get_parent() == nullptr) && (isSgType(source_expression_copy) == nullptr)) {
            source_expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 85460 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeallocateStatement::copy(SgCopyHelp& help) const {
    SgDeallocateStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeallocateStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): expr_list_copy
     SgExprListExp* expr_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expr_list
          if (get_expr_list() != nullptr) 
             { 
               expr_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expr_list())); 
             } 
            else 
             { 
               expr_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expr_list != nullptr) delete result->p_expr_list; 
     result->p_expr_list = expr_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expr_list
        if ((expr_list_copy != nullptr) && (expr_list_copy->get_parent() == nullptr) && (isSgType(expr_list_copy) == nullptr)) {
            expr_list_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): stat_expression_copy
     SgExpression* stat_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat_expression
          if (get_stat_expression() != nullptr) 
             { 
               stat_expression_copy = static_cast<SgExpression*>(help.copyAst(get_stat_expression())); 
             } 
            else 
             { 
               stat_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat_expression != nullptr) delete result->p_stat_expression; 
     result->p_stat_expression = stat_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)stat_expression
        if ((stat_expression_copy != nullptr) && (stat_expression_copy->get_parent() == nullptr) && (isSgType(stat_expression_copy) == nullptr)) {
            stat_expression_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): errmsg_expression_copy
     SgExpression* errmsg_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)errmsg_expression
          if (get_errmsg_expression() != nullptr) 
             { 
               errmsg_expression_copy = static_cast<SgExpression*>(help.copyAst(get_errmsg_expression())); 
             } 
            else 
             { 
               errmsg_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_errmsg_expression != nullptr) delete result->p_errmsg_expression; 
     result->p_errmsg_expression = errmsg_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)errmsg_expression
        if ((errmsg_expression_copy != nullptr) && (errmsg_expression_copy->get_parent() == nullptr) && (isSgType(errmsg_expression_copy) == nullptr)) {
            errmsg_expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 85676 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcNotifyStatement::copy(SgCopyHelp& help) const {
    SgUpcNotifyStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: notify_expression_copy
     SgExpression* notify_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)notify_expression
          if (get_notify_expression() != nullptr) 
             { 
               notify_expression_copy = static_cast<SgExpression*>(help.copyAst(get_notify_expression())); 
             } 
            else 
             { 
               notify_expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcNotifyStatement( startOfConstruct_copy, notify_expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)notify_expression
        if ((notify_expression_copy != nullptr) && (notify_expression_copy->get_parent() == nullptr) && (isSgType(notify_expression_copy) == nullptr)) {
            notify_expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 85853 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcWaitStatement::copy(SgCopyHelp& help) const {
    SgUpcWaitStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: wait_expression_copy
     SgExpression* wait_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)wait_expression
          if (get_wait_expression() != nullptr) 
             { 
               wait_expression_copy = static_cast<SgExpression*>(help.copyAst(get_wait_expression())); 
             } 
            else 
             { 
               wait_expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcWaitStatement( startOfConstruct_copy, wait_expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)wait_expression
        if ((wait_expression_copy != nullptr) && (wait_expression_copy->get_parent() == nullptr) && (isSgType(wait_expression_copy) == nullptr)) {
            wait_expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86030 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcBarrierStatement::copy(SgCopyHelp& help) const {
    SgUpcBarrierStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: barrier_expression_copy
     SgExpression* barrier_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)barrier_expression
          if (get_barrier_expression() != nullptr) 
             { 
               barrier_expression_copy = static_cast<SgExpression*>(help.copyAst(get_barrier_expression())); 
             } 
            else 
             { 
               barrier_expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcBarrierStatement( startOfConstruct_copy, barrier_expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)barrier_expression
        if ((barrier_expression_copy != nullptr) && (barrier_expression_copy->get_parent() == nullptr) && (isSgType(barrier_expression_copy) == nullptr)) {
            barrier_expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcFenceStatement::copy(SgCopyHelp& help) const {
    SgUpcFenceStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcFenceStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86369 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBarrierStatement::copy(SgCopyHelp& help) const {
    SgOmpBarrierStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpBarrierStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86531 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTaskwaitStatement::copy(SgCopyHelp& help) const {
    SgOmpTaskwaitStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpTaskwaitStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86693 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpFlushStatement::copy(SgCopyHelp& help) const {
    SgOmpFlushStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpFlushStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_variables()
     SgVarRefExpPtrList variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto source_variables_iterator = get_variables().begin();
    for (/* empty by design */; source_variables_iterator != get_variables().end(); ++source_variables_iterator) {
          SgVarRefExp* source_list_element = *source_variables_iterator; 
          SgVarRefExp* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgVarRefExp*>(help.copyAst(*source_variables_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          variables_copy.push_back(copy_list_element); 
        }
     result->p_variables = variables_copy; 
  // case: listType for variables
  // case: listType (typeIsPointerToList == false) for variables
    auto variables_iterator = result->get_variables().begin();
    for (/* empty by design */; variables_iterator != result->get_variables().end(); ++variables_iterator) {
          SgVarRefExp* list_element = *variables_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 86883 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpBodyStatement::copy(SgCopyHelp& help) const {
    SgOmpBodyStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpBodyStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87060 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpMasterStatement::copy(SgCopyHelp& help) const {
    SgOmpMasterStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpMasterStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87237 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpOrderedStatement::copy(SgCopyHelp& help) const {
    SgOmpOrderedStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpOrderedStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpCriticalStatement::copy(SgCopyHelp& help) const {
    SgOmpCriticalStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpCriticalStatement( startOfConstruct_copy, body_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87595 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSectionStatement::copy(SgCopyHelp& help) const {
    SgOmpSectionStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSectionStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87772 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpWorkshareStatement::copy(SgCopyHelp& help) const {
    SgOmpWorkshareStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpWorkshareStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 87949 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpClauseBodyStatement::copy(SgCopyHelp& help) const {
    SgOmpClauseBodyStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpClauseBodyStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 88154 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpParallelStatement::copy(SgCopyHelp& help) const {
    SgOmpParallelStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpParallelStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 88359 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSingleStatement::copy(SgCopyHelp& help) const {
    SgOmpSingleStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSingleStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 88564 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpAtomicStatement::copy(SgCopyHelp& help) const {
    SgOmpAtomicStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpAtomicStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 88769 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTaskStatement::copy(SgCopyHelp& help) const {
    SgOmpTaskStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpTaskStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 88974 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpForStatement::copy(SgCopyHelp& help) const {
    SgOmpForStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpForStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 89179 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpDoStatement::copy(SgCopyHelp& help) const {
    SgOmpDoStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpDoStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 89384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSectionsStatement::copy(SgCopyHelp& help) const {
    SgOmpSectionsStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSectionsStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 89589 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTargetStatement::copy(SgCopyHelp& help) const {
    SgOmpTargetStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpTargetStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 89794 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpTargetDataStatement::copy(SgCopyHelp& help) const {
    SgOmpTargetDataStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpTargetDataStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 89999 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpSimdStatement::copy(SgCopyHelp& help) const {
    SgOmpSimdStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpSimdStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 90204 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOmpForSimdStatement::copy(SgCopyHelp& help) const {
    SgOmpForSimdStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOmpForSimdStatement( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_clauses()
     SgOmpClausePtrList clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto source_clauses_iterator = get_clauses().begin();
    for (/* empty by design */; source_clauses_iterator != get_clauses().end(); ++source_clauses_iterator) {
          SgOmpClause* source_list_element = *source_clauses_iterator; 
          SgOmpClause* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgOmpClause*>(help.copyAst(*source_clauses_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          clauses_copy.push_back(copy_list_element); 
        }
     result->p_clauses = clauses_copy; 
  // case: listType for clauses
  // case: listType (typeIsPointerToList == false) for clauses
    auto clauses_iterator = result->get_clauses().begin();
    for (/* empty by design */; clauses_iterator != result->get_clauses().end(); ++clauses_iterator) {
          SgOmpClause* list_element = *clauses_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 90409 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSequenceStatement::copy(SgCopyHelp& help) const {
    SgSequenceStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSequenceStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 90571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWithStatement::copy(SgCopyHelp& help) const {
    SgWithStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWithStatement( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 90763 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPythonPrintStmt::copy(SgCopyHelp& help) const {
    SgPythonPrintStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: destination_copy
     SgExpression* destination_copy; 
  // case: not a listType for (using conditionalToCopyVariable)destination
          if (get_destination() != nullptr) 
             { 
               destination_copy = static_cast<SgExpression*>(help.copyAst(get_destination())); 
             } 
            else 
             { 
               destination_copy = nullptr; 
             } 
    // Copy constructor parameter data member: values_copy
     SgExprListExp* values_copy; 
  // case: not a listType for (using conditionalToCopyVariable)values
          if (get_values() != nullptr) 
             { 
               values_copy = static_cast<SgExprListExp*>(help.copyAst(get_values())); 
             } 
            else 
             { 
               values_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPythonPrintStmt( startOfConstruct_copy, destination_copy, values_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)destination
        if ((destination_copy != nullptr) && (destination_copy->get_parent() == nullptr) && (isSgType(destination_copy) == nullptr)) {
            destination_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)values
        if ((values_copy != nullptr) && (values_copy->get_parent() == nullptr) && (isSgType(values_copy) == nullptr)) {
            values_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 90955 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPassStatement::copy(SgCopyHelp& help) const {
    SgPassStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPassStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 91117 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssertStmt::copy(SgCopyHelp& help) const {
    SgAssertStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: test_copy
     SgExpression* test_copy; 
  // case: not a listType for (using conditionalToCopyVariable)test
          if (get_test() != nullptr) 
             { 
               test_copy = static_cast<SgExpression*>(help.copyAst(get_test())); 
             } 
            else 
             { 
               test_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssertStmt( startOfConstruct_copy, test_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): exception_argument_copy
     SgExpression* exception_argument_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exception_argument
          if (get_exception_argument() != nullptr) 
             { 
               exception_argument_copy = static_cast<SgExpression*>(help.copyAst(get_exception_argument())); 
             } 
            else 
             { 
               exception_argument_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exception_argument != nullptr) delete result->p_exception_argument; 
     result->p_exception_argument = exception_argument_copy; 
  // case: not a listType for (using conditionalToSetParent)exception_argument
        if ((exception_argument_copy != nullptr) && (exception_argument_copy->get_parent() == nullptr) && (isSgType(exception_argument_copy) == nullptr)) {
            exception_argument_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)test
        if ((test_copy != nullptr) && (test_copy->get_parent() == nullptr) && (isSgType(test_copy) == nullptr)) {
            test_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 91312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExecStatement::copy(SgCopyHelp& help) const {
    SgExecStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: executable_copy
     SgExpression* executable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executable
          if (get_executable() != nullptr) 
             { 
               executable_copy = static_cast<SgExpression*>(help.copyAst(get_executable())); 
             } 
            else 
             { 
               executable_copy = nullptr; 
             } 
    // Copy constructor parameter data member: globals_copy
     SgExpression* globals_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globals
          if (get_globals() != nullptr) 
             { 
               globals_copy = static_cast<SgExpression*>(help.copyAst(get_globals())); 
             } 
            else 
             { 
               globals_copy = nullptr; 
             } 
    // Copy constructor parameter data member: locals_copy
     SgExpression* locals_copy; 
  // case: not a listType for (using conditionalToCopyVariable)locals
          if (get_locals() != nullptr) 
             { 
               locals_copy = static_cast<SgExpression*>(help.copyAst(get_locals())); 
             } 
            else 
             { 
               locals_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExecStatement( startOfConstruct_copy, executable_copy, globals_copy, locals_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)executable
        if ((executable_copy != nullptr) && (executable_copy->get_parent() == nullptr) && (isSgType(executable_copy) == nullptr)) {
            executable_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)globals
        if ((globals_copy != nullptr) && (globals_copy->get_parent() == nullptr) && (isSgType(globals_copy) == nullptr)) {
            globals_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)locals
        if ((locals_copy != nullptr) && (locals_copy->get_parent() == nullptr) && (isSgType(locals_copy) == nullptr)) {
            locals_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 91519 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPythonGlobalStmt::copy(SgCopyHelp& help) const {
    SgPythonGlobalStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPythonGlobalStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_names()
     SgInitializedNamePtrList names_copy; 
  // case: listType for names
  // case: listType (typeIsPointerToList == false) for names
    auto source_names_iterator = get_names().begin();
    for (/* empty by design */; source_names_iterator != get_names().end(); ++source_names_iterator) {
          SgInitializedName* source_list_element = *source_names_iterator; 
          SgInitializedName* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgInitializedName*>(help.copyAst(*source_names_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          names_copy.push_back(copy_list_element); 
        }
     result->p_names = names_copy; 
  // case: listType for names
  // case: listType (typeIsPointerToList == false) for names
    auto names_iterator = result->get_names().begin();
    for (/* empty by design */; names_iterator != result->get_names().end(); ++names_iterator) {
          SgInitializedName* list_element = *names_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 91709 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaThrowStatement::copy(SgCopyHelp& help) const {
    SgJavaThrowStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: throwOp_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for throwOp
  // case: toBeCopied == COPY_DATA for throwOp
     SgThrowOp* throwOp_copy = p_throwOp; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaThrowStatement( startOfConstruct_copy, throwOp_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 91875 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaSynchronizedStatement::copy(SgCopyHelp& help) const {
    SgJavaSynchronizedStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaSynchronizedStatement( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92067 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsyncStmt::copy(SgCopyHelp& help) const {
    SgAsyncStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsyncStmt( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92244 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFinishStmt::copy(SgCopyHelp& help) const {
    SgFinishStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFinishStmt( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92421 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtStmt::copy(SgCopyHelp& help) const {
    SgAtStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAtStmt( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92613 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtomicStmt::copy(SgCopyHelp& help) const {
    SgAtomicStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAtomicStmt( startOfConstruct_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92790 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWhenStmt::copy(SgCopyHelp& help) const {
    SgWhenStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWhenStmt( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 92982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImageControlStatement::copy(SgCopyHelp& help) const {
    SgImageControlStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgImageControlStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 93203 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncAllStatement::copy(SgCopyHelp& help) const {
    SgSyncAllStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSyncAllStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 93424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncImagesStatement::copy(SgCopyHelp& help) const {
    SgSyncImagesStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: image_set_copy
     SgExpression* image_set_copy; 
  // case: not a listType for (using conditionalToCopyVariable)image_set
          if (get_image_set() != nullptr) 
             { 
               image_set_copy = static_cast<SgExpression*>(help.copyAst(get_image_set())); 
             } 
            else 
             { 
               image_set_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSyncImagesStatement( startOfConstruct_copy, image_set_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)image_set
        if ((image_set_copy != nullptr) && (image_set_copy->get_parent() == nullptr) && (isSgType(image_set_copy) == nullptr)) {
            image_set_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 93660 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncMemoryStatement::copy(SgCopyHelp& help) const {
    SgSyncMemoryStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSyncMemoryStatement( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 93881 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSyncTeamStatement::copy(SgCopyHelp& help) const {
    SgSyncTeamStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: team_value_copy
     SgExpression* team_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)team_value
          if (get_team_value() != nullptr) 
             { 
               team_value_copy = static_cast<SgExpression*>(help.copyAst(get_team_value())); 
             } 
            else 
             { 
               team_value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSyncTeamStatement( startOfConstruct_copy, team_value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)team_value
        if ((team_value_copy != nullptr) && (team_value_copy->get_parent() == nullptr) && (isSgType(team_value_copy) == nullptr)) {
            team_value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 94117 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLockStatement::copy(SgCopyHelp& help) const {
    SgLockStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lock_variable_copy
     SgExpression* lock_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lock_variable
          if (get_lock_variable() != nullptr) 
             { 
               lock_variable_copy = static_cast<SgExpression*>(help.copyAst(get_lock_variable())); 
             } 
            else 
             { 
               lock_variable_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLockStatement( startOfConstruct_copy, lock_variable_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lock_variable
        if ((lock_variable_copy != nullptr) && (lock_variable_copy->get_parent() == nullptr) && (isSgType(lock_variable_copy) == nullptr)) {
            lock_variable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 94353 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnlockStatement::copy(SgCopyHelp& help) const {
    SgUnlockStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lock_variable_copy
     SgExpression* lock_variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lock_variable
          if (get_lock_variable() != nullptr) 
             { 
               lock_variable_copy = static_cast<SgExpression*>(help.copyAst(get_lock_variable())); 
             } 
            else 
             { 
               lock_variable_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnlockStatement( startOfConstruct_copy, lock_variable_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): image_control_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for image_control_statement
  // case: toBeCopied == COPY_DATA for image_control_statement
     SgImageControlStatement::image_control_statement_enum image_control_statement_copy = p_image_control_statement; 
     result->p_image_control_statement = image_control_statement_copy; 
  // Copy non-constructor parameter data member (access function): stat_copy
     SgExpression* stat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stat
          if (get_stat() != nullptr) 
             { 
               stat_copy = static_cast<SgExpression*>(help.copyAst(get_stat())); 
             } 
            else 
             { 
               stat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stat != nullptr) delete result->p_stat; 
     result->p_stat = stat_copy; 
  // case: not a listType for (using conditionalToSetParent)stat
        if ((stat_copy != nullptr) && (stat_copy->get_parent() == nullptr) && (isSgType(stat_copy) == nullptr)) {
            stat_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): err_msg_copy
     SgExpression* err_msg_copy; 
  // case: not a listType for (using conditionalToCopyVariable)err_msg
          if (get_err_msg() != nullptr) 
             { 
               err_msg_copy = static_cast<SgExpression*>(help.copyAst(get_err_msg())); 
             } 
            else 
             { 
               err_msg_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_err_msg != nullptr) delete result->p_err_msg; 
     result->p_err_msg = err_msg_copy; 
  // case: not a listType for (using conditionalToSetParent)err_msg
        if ((err_msg_copy != nullptr) && (err_msg_copy->get_parent() == nullptr) && (isSgType(err_msg_copy) == nullptr)) {
            err_msg_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): acquired_lock_copy
     SgExpression* acquired_lock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)acquired_lock
          if (get_acquired_lock() != nullptr) 
             { 
               acquired_lock_copy = static_cast<SgExpression*>(help.copyAst(get_acquired_lock())); 
             } 
            else 
             { 
               acquired_lock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_acquired_lock != nullptr) delete result->p_acquired_lock; 
     result->p_acquired_lock = acquired_lock_copy; 
  // case: not a listType for (using conditionalToSetParent)acquired_lock
        if ((acquired_lock_copy != nullptr) && (acquired_lock_copy->get_parent() == nullptr) && (isSgType(acquired_lock_copy) == nullptr)) {
            acquired_lock_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lock_variable
        if ((lock_variable_copy != nullptr) && (lock_variable_copy->get_parent() == nullptr) && (isSgType(lock_variable_copy) == nullptr)) {
            lock_variable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 94589 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaExitStmt::copy(SgCopyHelp& help) const {
    SgAdaExitStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: loop_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for loop
  // case: toBeCopied == COPY_DATA for loop
     SgStatement* loop_copy = p_loop; 
    // Copy constructor parameter data member: condition_copy
     SgExpression* condition_copy; 
  // case: not a listType for (using conditionalToCopyVariable)condition
          if (get_condition() != nullptr) 
             { 
               condition_copy = static_cast<SgExpression*>(help.copyAst(get_condition())); 
             } 
            else 
             { 
               condition_copy = nullptr; 
             } 
    // Copy constructor parameter data member: explicitLoopName_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicitLoopName
  // case: toBeCopied == COPY_DATA for explicitLoopName
     bool explicitLoopName_copy = p_explicitLoopName; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaExitStmt( startOfConstruct_copy, loop_copy, condition_copy, explicitLoopName_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)condition
        if ((condition_copy != nullptr) && (condition_copy->get_parent() == nullptr) && (isSgType(condition_copy) == nullptr)) {
            condition_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 94774 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaDelayStmt::copy(SgCopyHelp& help) const {
    SgAdaDelayStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: time_copy
     SgExpression* time_copy; 
  // case: not a listType for (using conditionalToCopyVariable)time
          if (get_time() != nullptr) 
             { 
               time_copy = static_cast<SgExpression*>(help.copyAst(get_time())); 
             } 
            else 
             { 
               time_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isRelative_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRelative
  // case: toBeCopied == COPY_DATA for isRelative
     bool isRelative_copy = p_isRelative; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaDelayStmt( startOfConstruct_copy, time_copy, isRelative_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)time
        if ((time_copy != nullptr) && (time_copy->get_parent() == nullptr) && (isSgType(time_copy) == nullptr)) {
            time_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 94955 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaLoopStmt::copy(SgCopyHelp& help) const {
    SgAdaLoopStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaLoopStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): string_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_label
  // case: toBeCopied == COPY_DATA for string_label
     std::string string_label_copy = p_string_label; 
     result->p_string_label = string_label_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 95140 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSelectStmt::copy(SgCopyHelp& help) const {
    SgAdaSelectStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaSelectStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): select_path_copy
     SgAdaSelectAlternativeStmt* select_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)select_path
          if (get_select_path() != nullptr) 
             { 
               select_path_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_select_path())); 
             } 
            else 
             { 
               select_path_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_select_path != nullptr) delete result->p_select_path; 
     result->p_select_path = select_path_copy; 
  // case: not a listType for (using conditionalToSetParent)select_path
        if ((select_path_copy != nullptr) && (select_path_copy->get_parent() == nullptr) && (isSgType(select_path_copy) == nullptr)) {
            select_path_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): or_path_copy
     SgAdaSelectAlternativeStmt* or_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)or_path
          if (get_or_path() != nullptr) 
             { 
               or_path_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_or_path())); 
             } 
            else 
             { 
               or_path_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_or_path != nullptr) delete result->p_or_path; 
     result->p_or_path = or_path_copy; 
  // case: not a listType for (using conditionalToSetParent)or_path
        if ((or_path_copy != nullptr) && (or_path_copy->get_parent() == nullptr) && (isSgType(or_path_copy) == nullptr)) {
            or_path_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): else_path_copy
     SgBasicBlock* else_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)else_path
          if (get_else_path() != nullptr) 
             { 
               else_path_copy = static_cast<SgBasicBlock*>(help.copyAst(get_else_path())); 
             } 
            else 
             { 
               else_path_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_else_path != nullptr) delete result->p_else_path; 
     result->p_else_path = else_path_copy; 
  // case: not a listType for (using conditionalToSetParent)else_path
        if ((else_path_copy != nullptr) && (else_path_copy->get_parent() == nullptr) && (isSgType(else_path_copy) == nullptr)) {
            else_path_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): abort_path_copy
     SgBasicBlock* abort_path_copy; 
  // case: not a listType for (using conditionalToCopyVariable)abort_path
          if (get_abort_path() != nullptr) 
             { 
               abort_path_copy = static_cast<SgBasicBlock*>(help.copyAst(get_abort_path())); 
             } 
            else 
             { 
               abort_path_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_abort_path != nullptr) delete result->p_abort_path; 
     result->p_abort_path = abort_path_copy; 
  // case: not a listType for (using conditionalToSetParent)abort_path
        if ((abort_path_copy != nullptr) && (abort_path_copy->get_parent() == nullptr) && (isSgType(abort_path_copy) == nullptr)) {
            abort_path_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): select_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for select_type
  // case: toBeCopied == COPY_DATA for select_type
     SgAdaSelectStmt::select_type_enum select_type_copy = p_select_type; 
     result->p_select_type = select_type_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 95379 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaSelectAlternativeStmt::copy(SgCopyHelp& help) const {
    SgAdaSelectAlternativeStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaSelectAlternativeStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // Copy non-constructor parameter data member (access function): body_copy
     SgBasicBlock* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgBasicBlock*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): guard_copy
     SgExpression* guard_copy; 
  // case: not a listType for (using conditionalToCopyVariable)guard
          if (get_guard() != nullptr) 
             { 
               guard_copy = static_cast<SgExpression*>(help.copyAst(get_guard())); 
             } 
            else 
             { 
               guard_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_guard != nullptr) delete result->p_guard; 
     result->p_guard = guard_copy; 
  // case: not a listType for (using conditionalToSetParent)guard
        if ((guard_copy != nullptr) && (guard_copy->get_parent() == nullptr) && (isSgType(guard_copy) == nullptr)) {
            guard_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): next_copy
     SgAdaSelectAlternativeStmt* next_copy; 
  // case: not a listType for (using conditionalToCopyVariable)next
          if (get_next() != nullptr) 
             { 
               next_copy = static_cast<SgAdaSelectAlternativeStmt*>(help.copyAst(get_next())); 
             } 
            else 
             { 
               next_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_next != nullptr) delete result->p_next; 
     result->p_next = next_copy; 
  // case: not a listType for (using conditionalToSetParent)next
        if ((next_copy != nullptr) && (next_copy->get_parent() == nullptr) && (isSgType(next_copy) == nullptr)) {
            next_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 95595 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTerminateStmt::copy(SgCopyHelp& help) const {
    SgAdaTerminateStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTerminateStmt( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 95757 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaUnscopedBlock::copy(SgCopyHelp& help) const {
    SgAdaUnscopedBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaUnscopedBlock( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_statements()
     SgStatementPtrList statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto source_statements_iterator = get_statements().begin();
    for (/* empty by design */; source_statements_iterator != get_statements().end(); ++source_statements_iterator) {
          SgStatement* source_list_element = *source_statements_iterator; 
          SgStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgStatement*>(help.copyAst(*source_statements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statements_copy.push_back(copy_list_element); 
        }
     result->p_statements = statements_copy; 
  // case: listType for statements
  // case: listType (typeIsPointerToList == false) for statements
    auto statements_iterator = result->get_statements().begin();
    for (/* empty by design */; statements_iterator != result->get_statements().end(); ++statements_iterator) {
          SgStatement* list_element = *statements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 95947 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaVariantWhenStmt::copy(SgCopyHelp& help) const {
    SgAdaVariantWhenStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: choices_copy
     SgExprListExp* choices_copy; 
  // case: not a listType for (using conditionalToCopyVariable)choices
          if (get_choices() != nullptr) 
             { 
               choices_copy = static_cast<SgExprListExp*>(help.copyAst(get_choices())); 
             } 
            else 
             { 
               choices_copy = nullptr; 
             } 
    // Copy constructor parameter data member: components_copy
     SgAdaUnscopedBlock* components_copy; 
  // case: not a listType for (using conditionalToCopyVariable)components
          if (get_components() != nullptr) 
             { 
               components_copy = static_cast<SgAdaUnscopedBlock*>(help.copyAst(get_components())); 
             } 
            else 
             { 
               components_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaVariantWhenStmt( startOfConstruct_copy, choices_copy, components_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label
  // case: toBeCopied == COPY_DATA for numeric_label
     SgLabelRefExp* numeric_label_copy = p_numeric_label; 
     result->p_numeric_label = numeric_label_copy; 
  // Copy non-constructor parameter data member (access function): source_sequence_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_sequence_value
  // case: toBeCopied == COPY_DATA for source_sequence_value
     int source_sequence_value_copy = p_source_sequence_value; 
     result->p_source_sequence_value = source_sequence_value_copy; 
  // case: not a listType for (using conditionalToSetParent)choices
        if ((choices_copy != nullptr) && (choices_copy->get_parent() == nullptr) && (isSgType(choices_copy) == nullptr)) {
            choices_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)components
        if ((components_copy != nullptr) && (components_copy->get_parent() == nullptr) && (isSgType(components_copy) == nullptr)) {
            components_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 96139 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpression::copy(SgCopyHelp& help) const {
    SgExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExpression( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 96340 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnaryOp::copy(SgCopyHelp& help) const {
    SgUnaryOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnaryOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 96565 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExpressionRoot::copy(SgCopyHelp& help) const {
    SgExpressionRoot* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExpressionRoot( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 96790 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusOp::copy(SgCopyHelp& help) const {
    SgMinusOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMinusOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 97015 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnaryAddOp::copy(SgCopyHelp& help) const {
    SgUnaryAddOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnaryAddOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 97240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNotOp::copy(SgCopyHelp& help) const {
    SgNotOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNotOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 97465 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerDerefExp::copy(SgCopyHelp& help) const {
    SgPointerDerefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPointerDerefExp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 97690 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAddressOfOp::copy(SgCopyHelp& help) const {
    SgAddressOfOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAddressOfOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 97920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusMinusOp::copy(SgCopyHelp& help) const {
    SgMinusMinusOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMinusMinusOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 98145 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPlusPlusOp::copy(SgCopyHelp& help) const {
    SgPlusPlusOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPlusPlusOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 98370 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitComplementOp::copy(SgCopyHelp& help) const {
    SgBitComplementOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitComplementOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 98595 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCastExp::copy(SgCopyHelp& help) const {
    SgCastExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: cast_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cast_type
  // case: toBeCopied == COPY_DATA for cast_type
     SgCastExp::cast_type_enum cast_type_copy = p_cast_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCastExp( startOfConstruct_copy, operand_i_copy, expression_type_copy, cast_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): castContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for castContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for castContainsBaseTypeDefiningDeclaration
     bool castContainsBaseTypeDefiningDeclaration_copy = p_castContainsBaseTypeDefiningDeclaration; 
     result->p_castContainsBaseTypeDefiningDeclaration = castContainsBaseTypeDefiningDeclaration_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 98864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgThrowOp::copy(SgCopyHelp& help) const {
    SgThrowOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: throwKind_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for throwKind
  // case: toBeCopied == COPY_DATA for throwKind
     SgThrowOp::e_throw_kind throwKind_copy = p_throwKind; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgThrowOp( startOfConstruct_copy, operand_i_copy, expression_type_copy, throwKind_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 99093 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRealPartOp::copy(SgCopyHelp& help) const {
    SgRealPartOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRealPartOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 99318 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImagPartOp::copy(SgCopyHelp& help) const {
    SgImagPartOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgImagPartOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 99543 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConjugateOp::copy(SgCopyHelp& help) const {
    SgConjugateOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgConjugateOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 99768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUserDefinedUnaryOp::copy(SgCopyHelp& help) const {
    SgUserDefinedUnaryOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: operator_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     SgName operator_name_copy = p_operator_name; 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgFunctionSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUserDefinedUnaryOp( startOfConstruct_copy, operand_i_copy, expression_type_copy, operator_name_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 100001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatrixTransposeOp::copy(SgCopyHelp& help) const {
    SgMatrixTransposeOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMatrixTransposeOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // Copy non-constructor parameter data member (access function): is_conjugate_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_conjugate
  // case: toBeCopied == COPY_DATA for is_conjugate
     bool is_conjugate_copy = p_is_conjugate; 
     result->p_is_conjugate = is_conjugate_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 100231 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAbsOp::copy(SgCopyHelp& help) const {
    SgAbsOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAbsOp( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): mode_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mode
  // case: toBeCopied == COPY_DATA for mode
     SgUnaryOp::Sgop_mode mode_copy = p_mode; 
     result->p_mode = mode_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 100456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBinaryOp::copy(SgCopyHelp& help) const {
    SgBinaryOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBinaryOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 100696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrowExp::copy(SgCopyHelp& help) const {
    SgArrowExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgArrowExp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 100936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotExp::copy(SgCopyHelp& help) const {
    SgDotExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDotExp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 101176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotStarOp::copy(SgCopyHelp& help) const {
    SgDotStarOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDotStarOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 101416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgArrowStarOp::copy(SgCopyHelp& help) const {
    SgArrowStarOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgArrowStarOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 101656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEqualityOp::copy(SgCopyHelp& help) const {
    SgEqualityOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEqualityOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 101896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLessThanOp::copy(SgCopyHelp& help) const {
    SgLessThanOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLessThanOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 102136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGreaterThanOp::copy(SgCopyHelp& help) const {
    SgGreaterThanOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGreaterThanOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 102376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNotEqualOp::copy(SgCopyHelp& help) const {
    SgNotEqualOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNotEqualOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 102616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLessOrEqualOp::copy(SgCopyHelp& help) const {
    SgLessOrEqualOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLessOrEqualOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 102856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgGreaterOrEqualOp::copy(SgCopyHelp& help) const {
    SgGreaterOrEqualOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgGreaterOrEqualOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 103096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAddOp::copy(SgCopyHelp& help) const {
    SgAddOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAddOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 103336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSubtractOp::copy(SgCopyHelp& help) const {
    SgSubtractOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSubtractOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 103576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMultiplyOp::copy(SgCopyHelp& help) const {
    SgMultiplyOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMultiplyOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 103816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDivideOp::copy(SgCopyHelp& help) const {
    SgDivideOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDivideOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 104056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntegerDivideOp::copy(SgCopyHelp& help) const {
    SgIntegerDivideOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntegerDivideOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 104296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModOp::copy(SgCopyHelp& help) const {
    SgModOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 104536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAndOp::copy(SgCopyHelp& help) const {
    SgAndOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAndOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 104776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgOrOp::copy(SgCopyHelp& help) const {
    SgOrOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgOrOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 105016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitXorOp::copy(SgCopyHelp& help) const {
    SgBitXorOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitXorOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 105256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitAndOp::copy(SgCopyHelp& help) const {
    SgBitAndOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitAndOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 105496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitOrOp::copy(SgCopyHelp& help) const {
    SgBitOrOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitOrOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 105736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBitEqvOp::copy(SgCopyHelp& help) const {
    SgBitEqvOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBitEqvOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 105976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommaOpExp::copy(SgCopyHelp& help) const {
    SgCommaOpExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCommaOpExp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 106216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLshiftOp::copy(SgCopyHelp& help) const {
    SgLshiftOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLshiftOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 106456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRshiftOp::copy(SgCopyHelp& help) const {
    SgRshiftOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRshiftOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 106696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPntrArrRefExp::copy(SgCopyHelp& help) const {
    SgPntrArrRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPntrArrRefExp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 106936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopeOp::copy(SgCopyHelp& help) const {
    SgScopeOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgScopeOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 107176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignOp::copy(SgCopyHelp& help) const {
    SgAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 107416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExponentiationOp::copy(SgCopyHelp& help) const {
    SgExponentiationOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExponentiationOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 107656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnsignedRshiftOp::copy(SgCopyHelp& help) const {
    SgJavaUnsignedRshiftOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaUnsignedRshiftOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 107896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConcatenationOp::copy(SgCopyHelp& help) const {
    SgConcatenationOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgConcatenationOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 108136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPointerAssignOp::copy(SgCopyHelp& help) const {
    SgPointerAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPointerAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 108376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUserDefinedBinaryOp::copy(SgCopyHelp& help) const {
    SgUserDefinedBinaryOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: operator_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_name
  // case: toBeCopied == COPY_DATA for operator_name
     SgName operator_name_copy = p_operator_name; 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgFunctionSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUserDefinedBinaryOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy, operator_name_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 108624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundAssignOp::copy(SgCopyHelp& help) const {
    SgCompoundAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCompoundAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 108864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPlusAssignOp::copy(SgCopyHelp& help) const {
    SgPlusAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPlusAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 109104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMinusAssignOp::copy(SgCopyHelp& help) const {
    SgMinusAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMinusAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 109344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAndAssignOp::copy(SgCopyHelp& help) const {
    SgAndAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAndAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 109584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIorAssignOp::copy(SgCopyHelp& help) const {
    SgIorAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIorAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 109824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMultAssignOp::copy(SgCopyHelp& help) const {
    SgMultAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMultAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 110064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDivAssignOp::copy(SgCopyHelp& help) const {
    SgDivAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDivAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 110304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModAssignOp::copy(SgCopyHelp& help) const {
    SgModAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 110544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgXorAssignOp::copy(SgCopyHelp& help) const {
    SgXorAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgXorAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 110784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLshiftAssignOp::copy(SgCopyHelp& help) const {
    SgLshiftAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLshiftAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 111024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRshiftAssignOp::copy(SgCopyHelp& help) const {
    SgRshiftAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRshiftAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 111264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaUnsignedRshiftAssignOp::copy(SgCopyHelp& help) const {
    SgJavaUnsignedRshiftAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaUnsignedRshiftAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 111504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntegerDivideAssignOp::copy(SgCopyHelp& help) const {
    SgIntegerDivideAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntegerDivideAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 111744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExponentiationAssignOp::copy(SgCopyHelp& help) const {
    SgExponentiationAssignOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExponentiationAssignOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 111984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMembershipOp::copy(SgCopyHelp& help) const {
    SgMembershipOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMembershipOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 112224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSpaceshipOp::copy(SgCopyHelp& help) const {
    SgSpaceshipOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSpaceshipOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 112464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonMembershipOp::copy(SgCopyHelp& help) const {
    SgNonMembershipOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonMembershipOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 112704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIsOp::copy(SgCopyHelp& help) const {
    SgIsOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIsOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 112944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIsNotOp::copy(SgCopyHelp& help) const {
    SgIsNotOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIsNotOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 113184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDotDotExp::copy(SgCopyHelp& help) const {
    SgDotDotExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDotDotExp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 113424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseOp::copy(SgCopyHelp& help) const {
    SgElementwiseOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 113664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseMultiplyOp::copy(SgCopyHelp& help) const {
    SgElementwiseMultiplyOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseMultiplyOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 113904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwisePowerOp::copy(SgCopyHelp& help) const {
    SgElementwisePowerOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwisePowerOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 114144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseLeftDivideOp::copy(SgCopyHelp& help) const {
    SgElementwiseLeftDivideOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseLeftDivideOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 114384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseDivideOp::copy(SgCopyHelp& help) const {
    SgElementwiseDivideOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseDivideOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 114624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseAddOp::copy(SgCopyHelp& help) const {
    SgElementwiseAddOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseAddOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 114864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgElementwiseSubtractOp::copy(SgCopyHelp& help) const {
    SgElementwiseSubtractOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgElementwiseSubtractOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 115104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPowerOp::copy(SgCopyHelp& help) const {
    SgPowerOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPowerOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 115344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLeftDivideOp::copy(SgCopyHelp& help) const {
    SgLeftDivideOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLeftDivideOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 115584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRemOp::copy(SgCopyHelp& help) const {
    SgRemOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRemOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 115824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReplicationOp::copy(SgCopyHelp& help) const {
    SgReplicationOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgReplicationOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 116064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtOp::copy(SgCopyHelp& help) const {
    SgAtOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_i_copy
     SgExpression* lhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand_i
          if (get_lhs_operand_i() != nullptr) 
             { 
               lhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand_i())); 
             } 
            else 
             { 
               lhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_i_copy
     SgExpression* rhs_operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand_i
          if (get_rhs_operand_i() != nullptr) 
             { 
               rhs_operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand_i())); 
             } 
            else 
             { 
               rhs_operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAtOp( startOfConstruct_copy, lhs_operand_i_copy, rhs_operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs_operand_i
        if ((lhs_operand_i_copy != nullptr) && (lhs_operand_i_copy->get_parent() == nullptr) && (isSgType(lhs_operand_i_copy) == nullptr)) {
            lhs_operand_i_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand_i
        if ((rhs_operand_i_copy != nullptr) && (rhs_operand_i_copy->get_parent() == nullptr) && (isSgType(rhs_operand_i_copy) == nullptr)) {
            rhs_operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 116304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgExprListExp::copy(SgCopyHelp& help) const {
    SgExprListExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgExprListExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto source_expressions_iterator = get_expressions().begin();
    for (/* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) {
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          expressions_copy.push_back(copy_list_element); 
        }
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto expressions_iterator = result->get_expressions().begin();
    for (/* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) {
          SgExpression* list_element = *expressions_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 116533 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgListExp::copy(SgCopyHelp& help) const {
    SgListExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgListExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto source_expressions_iterator = get_expressions().begin();
    for (/* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) {
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          expressions_copy.push_back(copy_list_element); 
        }
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto expressions_iterator = result->get_expressions().begin();
    for (/* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) {
          SgExpression* list_element = *expressions_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 116762 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTupleExp::copy(SgCopyHelp& help) const {
    SgTupleExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTupleExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto source_expressions_iterator = get_expressions().begin();
    for (/* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) {
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          expressions_copy.push_back(copy_list_element); 
        }
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto expressions_iterator = result->get_expressions().begin();
    for (/* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) {
          SgExpression* list_element = *expressions_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 116991 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMatrixExp::copy(SgCopyHelp& help) const {
    SgMatrixExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMatrixExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto source_expressions_iterator = get_expressions().begin();
    for (/* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) {
          SgExpression* source_list_element = *source_expressions_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          expressions_copy.push_back(copy_list_element); 
        }
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto expressions_iterator = result->get_expressions().begin();
    for (/* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) {
          SgExpression* list_element = *expressions_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 117220 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgValueExp::copy(SgCopyHelp& help) const {
    SgValueExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgValueExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 117426 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBoolValExp::copy(SgCopyHelp& help) const {
    SgBoolValExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBoolValExp( startOfConstruct_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 117636 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringVal::copy(SgCopyHelp& help) const {
    SgStringVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     std::string value_copy = p_value; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStringVal( startOfConstruct_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): wcharString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wcharString
  // case: toBeCopied == COPY_DATA for wcharString
     bool wcharString_copy = p_wcharString; 
     result->p_wcharString = wcharString_copy; 
  // Copy non-constructor parameter data member (access function): stringDelimiter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stringDelimiter
  // case: toBeCopied == COPY_DATA for stringDelimiter
     char stringDelimiter_copy = p_stringDelimiter; 
     result->p_stringDelimiter = stringDelimiter_copy; 
  // Copy non-constructor parameter data member (access function): is16bitString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is16bitString
  // case: toBeCopied == COPY_DATA for is16bitString
     bool is16bitString_copy = p_is16bitString; 
     result->p_is16bitString = is16bitString_copy; 
  // Copy non-constructor parameter data member (access function): is32bitString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is32bitString
  // case: toBeCopied == COPY_DATA for is32bitString
     bool is32bitString_copy = p_is32bitString; 
     result->p_is32bitString = is32bitString_copy; 
  // Copy non-constructor parameter data member (access function): isRawString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRawString
  // case: toBeCopied == COPY_DATA for isRawString
     bool isRawString_copy = p_isRawString; 
     result->p_isRawString = isRawString_copy; 
  // Copy non-constructor parameter data member (access function): raw_string_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for raw_string_value
  // case: toBeCopied == COPY_DATA for raw_string_value
     std::string raw_string_value_copy = p_raw_string_value; 
     result->p_raw_string_value = raw_string_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 117876 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgShortVal::copy(SgCopyHelp& help) const {
    SgShortVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     short value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgShortVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 118090 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCharVal::copy(SgCopyHelp& help) const {
    SgCharVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     char value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCharVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 118304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedCharVal::copy(SgCopyHelp& help) const {
    SgUnsignedCharVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned char value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedCharVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 118518 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgWcharVal::copy(SgCopyHelp& help) const {
    SgWcharVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned long valueUL_copy = p_valueUL; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgWcharVal( startOfConstruct_copy, valueUL_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 118732 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedShortVal::copy(SgCopyHelp& help) const {
    SgUnsignedShortVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned short value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedShortVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 118946 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntVal::copy(SgCopyHelp& help) const {
    SgIntVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 119160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumVal::copy(SgCopyHelp& help) const {
    SgEnumVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long long int value_copy = p_value; 
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgEnumDeclaration* declaration_copy = p_declaration; 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEnumVal( startOfConstruct_copy, value_copy, declaration_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): requiresNameQualification_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresNameQualification
  // case: toBeCopied == COPY_DATA for requiresNameQualification
     bool requiresNameQualification_copy = p_requiresNameQualification; 
     result->p_requiresNameQualification = requiresNameQualification_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 119398 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedIntVal::copy(SgCopyHelp& help) const {
    SgUnsignedIntVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedIntVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 119612 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongIntVal::copy(SgCopyHelp& help) const {
    SgLongIntVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLongIntVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 119826 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongLongIntVal::copy(SgCopyHelp& help) const {
    SgLongLongIntVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long long int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLongLongIntVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 120040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedLongLongIntVal::copy(SgCopyHelp& help) const {
    SgUnsignedLongLongIntVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned long long int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedLongLongIntVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 120254 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnsignedLongVal::copy(SgCopyHelp& help) const {
    SgUnsignedLongVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     unsigned long value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnsignedLongVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 120468 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloatVal::copy(SgCopyHelp& help) const {
    SgFloatVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     float value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloatVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 120682 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDoubleVal::copy(SgCopyHelp& help) const {
    SgDoubleVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     double value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDoubleVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 120896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLongDoubleVal::copy(SgCopyHelp& help) const {
    SgLongDoubleVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLongDoubleVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 121110 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComplexVal::copy(SgCopyHelp& help) const {
    SgComplexVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: real_value_copy
     SgValueExp* real_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)real_value
          if (get_real_value() != nullptr) 
             { 
               real_value_copy = static_cast<SgValueExp*>(help.copyAst(get_real_value())); 
             } 
            else 
             { 
               real_value_copy = nullptr; 
             } 
    // Copy constructor parameter data member: imaginary_value_copy
     SgValueExp* imaginary_value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)imaginary_value
          if (get_imaginary_value() != nullptr) 
             { 
               imaginary_value_copy = static_cast<SgValueExp*>(help.copyAst(get_imaginary_value())); 
             } 
            else 
             { 
               imaginary_value_copy = nullptr; 
             } 
    // Copy constructor parameter data member: precisionType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for precisionType
  // case: toBeCopied == COPY_DATA for precisionType
     SgType* precisionType_copy = p_precisionType; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgComplexVal( startOfConstruct_copy, real_value_copy, imaginary_value_copy, precisionType_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)real_value
        if ((real_value_copy != nullptr) && (real_value_copy->get_parent() == nullptr) && (isSgType(real_value_copy) == nullptr)) {
            real_value_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)imaginary_value
        if ((imaginary_value_copy != nullptr) && (imaginary_value_copy->get_parent() == nullptr) && (isSgType(imaginary_value_copy) == nullptr)) {
            imaginary_value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 121354 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcThreads::copy(SgCopyHelp& help) const {
    SgUpcThreads* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcThreads( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 121568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcMythread::copy(SgCopyHelp& help) const {
    SgUpcMythread* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     int value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcMythread( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 121782 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateParameterVal::copy(SgCopyHelp& help) const {
    SgTemplateParameterVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: template_parameter_position_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for template_parameter_position
  // case: toBeCopied == COPY_DATA for template_parameter_position
     int template_parameter_position_copy = p_template_parameter_position; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateParameterVal( startOfConstruct_copy, template_parameter_position_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // Copy non-constructor parameter data member (access function): valueType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueType
  // case: toBeCopied == COPY_DATA for valueType
     SgType* valueType_copy = p_valueType; 
     result->p_valueType = valueType_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 122001 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullptrValExp::copy(SgCopyHelp& help) const {
    SgNullptrValExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNullptrValExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 122207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChar16Val::copy(SgCopyHelp& help) const {
    SgChar16Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned short valueUL_copy = p_valueUL; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgChar16Val( startOfConstruct_copy, valueUL_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 122421 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChar32Val::copy(SgCopyHelp& help) const {
    SgChar32Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: valueUL_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueUL
  // case: toBeCopied == COPY_DATA for valueUL
     unsigned int valueUL_copy = p_valueUL; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgChar32Val( startOfConstruct_copy, valueUL_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 122635 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat80Val::copy(SgCopyHelp& help) const {
    SgFloat80Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloat80Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 122849 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat128Val::copy(SgCopyHelp& help) const {
    SgFloat128Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     long double value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloat128Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 123063 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVoidVal::copy(SgCopyHelp& help) const {
    SgVoidVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVoidVal( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 123269 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFloatVal::copy(SgCopyHelp& help) const {
    SgAdaFloatVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFloatVal( startOfConstruct_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 123479 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSignedCharVal::copy(SgCopyHelp& help) const {
    SgSignedCharVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     signed char value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSignedCharVal( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 123693 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBFloat16Val::copy(SgCopyHelp& help) const {
    SgBFloat16Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     float value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBFloat16Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 123907 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat16Val::copy(SgCopyHelp& help) const {
    SgFloat16Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     float value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloat16Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 124121 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat32Val::copy(SgCopyHelp& help) const {
    SgFloat32Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     float value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloat32Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 124335 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFloat64Val::copy(SgCopyHelp& help) const {
    SgFloat64Val* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     double value_copy = p_value; 
    // Copy constructor parameter data member: valueString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFloat64Val( startOfConstruct_copy, value_copy, valueString_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 124549 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialBitVal::copy(SgCopyHelp& help) const {
    SgJovialBitVal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialBitVal();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_startOfConstruct != nullptr) delete result->p_startOfConstruct; 
  /* add assignment to result here */ 
     result->p_startOfConstruct = startOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_valueString
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for valueString
  // case: toBeCopied == COPY_DATA for valueString
     std::string valueString_copy = p_valueString; 
     result->p_valueString = valueString_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 124763 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCallExpression::copy(SgCopyHelp& help) const {
    SgCallExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != nullptr) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = nullptr; 
             } 
    // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != nullptr) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCallExpression( startOfConstruct_copy, function_copy, args_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)function
        if ((function_copy != nullptr) && (function_copy->get_parent() == nullptr) && (isSgType(function_copy) == nullptr)) {
            function_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)args
        if ((args_copy != nullptr) && (args_copy->get_parent() == nullptr) && (isSgType(args_copy) == nullptr)) {
            args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 124998 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionCallExp::copy(SgCopyHelp& help) const {
    SgFunctionCallExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != nullptr) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = nullptr; 
             } 
    // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != nullptr) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionCallExp( startOfConstruct_copy, function_copy, args_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): uses_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_operator_syntax
  // case: toBeCopied == COPY_DATA for uses_operator_syntax
     bool uses_operator_syntax_copy = p_uses_operator_syntax; 
     result->p_uses_operator_syntax = uses_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): usesObjectCallSyntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesObjectCallSyntax
  // case: toBeCopied == COPY_DATA for usesObjectCallSyntax
     bool usesObjectCallSyntax_copy = p_usesObjectCallSyntax; 
     result->p_usesObjectCallSyntax = usesObjectCallSyntax_copy; 
  // case: not a listType for (using conditionalToSetParent)function
        if ((function_copy != nullptr) && (function_copy->get_parent() == nullptr) && (isSgType(function_copy) == nullptr)) {
            function_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)args
        if ((args_copy != nullptr) && (args_copy->get_parent() == nullptr) && (isSgType(args_copy) == nullptr)) {
            args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 125243 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCudaKernelCallExp::copy(SgCopyHelp& help) const {
    SgCudaKernelCallExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: function_copy
     SgExpression* function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)function
          if (get_function() != nullptr) 
             { 
               function_copy = static_cast<SgExpression*>(help.copyAst(get_function())); 
             } 
            else 
             { 
               function_copy = nullptr; 
             } 
    // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != nullptr) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: exec_config_copy
     SgCudaKernelExecConfig* exec_config_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exec_config
          if (get_exec_config() != nullptr) 
             { 
               exec_config_copy = static_cast<SgCudaKernelExecConfig*>(help.copyAst(get_exec_config())); 
             } 
            else 
             { 
               exec_config_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCudaKernelCallExp( startOfConstruct_copy, function_copy, args_copy, expression_type_copy, exec_config_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): uses_operator_syntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_operator_syntax
  // case: toBeCopied == COPY_DATA for uses_operator_syntax
     bool uses_operator_syntax_copy = p_uses_operator_syntax; 
     result->p_uses_operator_syntax = uses_operator_syntax_copy; 
  // Copy non-constructor parameter data member (access function): usesObjectCallSyntax_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesObjectCallSyntax
  // case: toBeCopied == COPY_DATA for usesObjectCallSyntax
     bool usesObjectCallSyntax_copy = p_usesObjectCallSyntax; 
     result->p_usesObjectCallSyntax = usesObjectCallSyntax_copy; 
  // case: not a listType for (using conditionalToSetParent)exec_config
        if ((exec_config_copy != nullptr) && (exec_config_copy->get_parent() == nullptr) && (isSgType(exec_config_copy) == nullptr)) {
            exec_config_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)function
        if ((function_copy != nullptr) && (function_copy->get_parent() == nullptr) && (isSgType(function_copy) == nullptr)) {
            function_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)args
        if ((args_copy != nullptr) && (args_copy->get_parent() == nullptr) && (isSgType(args_copy) == nullptr)) {
            args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 125503 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcLocalsizeofExpression::copy(SgCopyHelp& help) const {
    SgUpcLocalsizeofExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcLocalsizeofExpression( startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 125727 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcBlocksizeofExpression::copy(SgCopyHelp& help) const {
    SgUpcBlocksizeofExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcBlocksizeofExpression( startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 125951 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUpcElemsizeofExpression::copy(SgCopyHelp& help) const {
    SgUpcElemsizeofExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUpcElemsizeofExpression( startOfConstruct_copy, expression_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 126175 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaInstanceOfOp::copy(SgCopyHelp& help) const {
    SgJavaInstanceOfOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaInstanceOfOp( startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 126399 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSuperExp::copy(SgCopyHelp& help) const {
    SgSuperExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
    // Copy constructor parameter data member: pobj_super_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_super
  // case: toBeCopied == COPY_DATA for pobj_super
     int pobj_super_copy = p_pobj_super; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSuperExp( startOfConstruct_copy, class_symbol_copy, pobj_super_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 126608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeIdOp::copy(SgCopyHelp& help) const {
    SgTypeIdOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeIdOp( startOfConstruct_copy, operand_expr_copy, operand_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 126858 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConditionalExp::copy(SgCopyHelp& help) const {
    SgConditionalExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: conditional_exp_copy
     SgExpression* conditional_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)conditional_exp
          if (get_conditional_exp() != nullptr) 
             { 
               conditional_exp_copy = static_cast<SgExpression*>(help.copyAst(get_conditional_exp())); 
             } 
            else 
             { 
               conditional_exp_copy = nullptr; 
             } 
    // Copy constructor parameter data member: true_exp_copy
     SgExpression* true_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)true_exp
          if (get_true_exp() != nullptr) 
             { 
               true_exp_copy = static_cast<SgExpression*>(help.copyAst(get_true_exp())); 
             } 
            else 
             { 
               true_exp_copy = nullptr; 
             } 
    // Copy constructor parameter data member: false_exp_copy
     SgExpression* false_exp_copy; 
  // case: not a listType for (using conditionalToCopyVariable)false_exp
          if (get_false_exp() != nullptr) 
             { 
               false_exp_copy = static_cast<SgExpression*>(help.copyAst(get_false_exp())); 
             } 
            else 
             { 
               false_exp_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgConditionalExp( startOfConstruct_copy, conditional_exp_copy, true_exp_copy, false_exp_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)conditional_exp
        if ((conditional_exp_copy != nullptr) && (conditional_exp_copy->get_parent() == nullptr) && (isSgType(conditional_exp_copy) == nullptr)) {
            conditional_exp_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)true_exp
        if ((true_exp_copy != nullptr) && (true_exp_copy->get_parent() == nullptr) && (isSgType(true_exp_copy) == nullptr)) {
            true_exp_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)false_exp
        if ((false_exp_copy != nullptr) && (false_exp_copy->get_parent() == nullptr) && (isSgType(false_exp_copy) == nullptr)) {
            false_exp_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 127108 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNewExp::copy(SgCopyHelp& help) const {
    SgNewExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: specified_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for specified_type
  // case: toBeCopied == COPY_DATA for specified_type
     SgType* specified_type_copy = p_specified_type; 
    // Copy constructor parameter data member: placement_args_copy
     SgExprListExp* placement_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)placement_args
          if (get_placement_args() != nullptr) 
             { 
               placement_args_copy = static_cast<SgExprListExp*>(help.copyAst(get_placement_args())); 
             } 
            else 
             { 
               placement_args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: constructor_args_copy
     SgConstructorInitializer* constructor_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)constructor_args
          if (get_constructor_args() != nullptr) 
             { 
               constructor_args_copy = static_cast<SgConstructorInitializer*>(help.copyAst(get_constructor_args())); 
             } 
            else 
             { 
               constructor_args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: builtin_args_copy
     SgExpression* builtin_args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)builtin_args
          if (get_builtin_args() != nullptr) 
             { 
               builtin_args_copy = static_cast<SgExpression*>(help.copyAst(get_builtin_args())); 
             } 
            else 
             { 
               builtin_args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: need_global_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_global_specifier
  // case: toBeCopied == COPY_DATA for need_global_specifier
     short need_global_specifier_copy = p_need_global_specifier; 
    // Copy constructor parameter data member: newOperatorDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for newOperatorDeclaration
  // case: toBeCopied == COPY_DATA for newOperatorDeclaration
     SgFunctionDeclaration* newOperatorDeclaration_copy = p_newOperatorDeclaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNewExp( startOfConstruct_copy, specified_type_copy, placement_args_copy, constructor_args_copy, builtin_args_copy, need_global_specifier_copy, newOperatorDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)placement_args
        if ((placement_args_copy != nullptr) && (placement_args_copy->get_parent() == nullptr) && (isSgType(placement_args_copy) == nullptr)) {
            placement_args_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)constructor_args
        if ((constructor_args_copy != nullptr) && (constructor_args_copy->get_parent() == nullptr) && (isSgType(constructor_args_copy) == nullptr)) {
            constructor_args_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)builtin_args
        if ((builtin_args_copy != nullptr) && (builtin_args_copy->get_parent() == nullptr) && (isSgType(builtin_args_copy) == nullptr)) {
            builtin_args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 127396 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDeleteExp::copy(SgCopyHelp& help) const {
    SgDeleteExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: variable_copy
     SgExpression* variable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)variable
          if (get_variable() != nullptr) 
             { 
               variable_copy = static_cast<SgExpression*>(help.copyAst(get_variable())); 
             } 
            else 
             { 
               variable_copy = nullptr; 
             } 
    // Copy constructor parameter data member: is_array_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_array
  // case: toBeCopied == COPY_DATA for is_array
     short is_array_copy = p_is_array; 
    // Copy constructor parameter data member: need_global_specifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_global_specifier
  // case: toBeCopied == COPY_DATA for need_global_specifier
     short need_global_specifier_copy = p_need_global_specifier; 
    // Copy constructor parameter data member: deleteOperatorDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for deleteOperatorDeclaration
  // case: toBeCopied == COPY_DATA for deleteOperatorDeclaration
     SgFunctionDeclaration* deleteOperatorDeclaration_copy = p_deleteOperatorDeclaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDeleteExp( startOfConstruct_copy, variable_copy, is_array_copy, need_global_specifier_copy, deleteOperatorDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)variable
        if ((variable_copy != nullptr) && (variable_copy->get_parent() == nullptr) && (isSgType(variable_copy) == nullptr)) {
            variable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 127624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgThisExp::copy(SgCopyHelp& help) const {
    SgThisExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
    // Copy constructor parameter data member: nonreal_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nonreal_symbol
  // case: toBeCopied == COPY_DATA for nonreal_symbol
     SgNonrealSymbol* nonreal_symbol_copy = p_nonreal_symbol; 
    // Copy constructor parameter data member: pobj_this_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_this
  // case: toBeCopied == COPY_DATA for pobj_this
     int pobj_this_copy = p_pobj_this; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgThisExp( startOfConstruct_copy, class_symbol_copy, nonreal_symbol_copy, pobj_this_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 127837 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRefExp::copy(SgCopyHelp& help) const {
    SgRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: type_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_name
  // case: toBeCopied == COPY_DATA for type_name
     SgType* type_name_copy = p_type_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRefExp( startOfConstruct_copy, type_name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 128042 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInitializer::copy(SgCopyHelp& help) const {
    SgInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInitializer( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 128253 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAggregateInitializer::copy(SgCopyHelp& help) const {
    SgAggregateInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != nullptr) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAggregateInitializer( startOfConstruct_copy, initializers_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // Copy non-constructor parameter data member (access function): need_explicit_braces_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_explicit_braces
  // case: toBeCopied == COPY_DATA for need_explicit_braces
     bool need_explicit_braces_copy = p_need_explicit_braces; 
     result->p_need_explicit_braces = need_explicit_braces_copy; 
  // Copy non-constructor parameter data member (access function): uses_compound_literal_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_compound_literal
  // case: toBeCopied == COPY_DATA for uses_compound_literal
     bool uses_compound_literal_copy = p_uses_compound_literal; 
     result->p_uses_compound_literal = uses_compound_literal_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requiresGlobalNameQualificationOnType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requiresGlobalNameQualificationOnType
  // case: toBeCopied == COPY_DATA for requiresGlobalNameQualificationOnType
     bool requiresGlobalNameQualificationOnType_copy = p_requiresGlobalNameQualificationOnType; 
     result->p_requiresGlobalNameQualificationOnType = requiresGlobalNameQualificationOnType_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length_for_type
  // case: toBeCopied == COPY_DATA for name_qualification_length_for_type
     int name_qualification_length_for_type_copy = p_name_qualification_length_for_type; 
     result->p_name_qualification_length_for_type = name_qualification_length_for_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required_for_type
  // case: toBeCopied == COPY_DATA for type_elaboration_required_for_type
     bool type_elaboration_required_for_type_copy = p_type_elaboration_required_for_type; 
     result->p_type_elaboration_required_for_type = type_elaboration_required_for_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required_for_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required_for_type
  // case: toBeCopied == COPY_DATA for global_qualification_required_for_type
     bool global_qualification_required_for_type_copy = p_global_qualification_required_for_type; 
     result->p_global_qualification_required_for_type = global_qualification_required_for_type_copy; 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
        if ((initializers_copy != nullptr) && (initializers_copy->get_parent() == nullptr) && (isSgType(initializers_copy) == nullptr)) {
            initializers_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 128518 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundInitializer::copy(SgCopyHelp& help) const {
    SgCompoundInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != nullptr) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCompoundInitializer( startOfConstruct_copy, initializers_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
        if ((initializers_copy != nullptr) && (initializers_copy->get_parent() == nullptr) && (isSgType(initializers_copy) == nullptr)) {
            initializers_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 128748 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgConstructorInitializer::copy(SgCopyHelp& help) const {
    SgConstructorInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgMemberFunctionDeclaration* declaration_copy = p_declaration; 
    // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != nullptr) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: need_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_name
  // case: toBeCopied == COPY_DATA for need_name
     bool need_name_copy = p_need_name; 
    // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     bool need_qualifier_copy = p_need_qualifier; 
    // Copy constructor parameter data member: need_parenthesis_after_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_parenthesis_after_name
  // case: toBeCopied == COPY_DATA for need_parenthesis_after_name
     bool need_parenthesis_after_name_copy = p_need_parenthesis_after_name; 
    // Copy constructor parameter data member: associated_class_unknown_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associated_class_unknown
  // case: toBeCopied == COPY_DATA for associated_class_unknown
     bool associated_class_unknown_copy = p_associated_class_unknown; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgConstructorInitializer( startOfConstruct_copy, declaration_copy, args_copy, expression_type_copy, need_name_copy, need_qualifier_copy, need_parenthesis_after_name_copy, associated_class_unknown_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): is_used_in_conditional_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_used_in_conditional
  // case: toBeCopied == COPY_DATA for is_used_in_conditional
     bool is_used_in_conditional_copy = p_is_used_in_conditional; 
     result->p_is_used_in_conditional = is_used_in_conditional_copy; 
  // case: not a listType for (using conditionalToSetParent)args
        if ((args_copy != nullptr) && (args_copy->get_parent() == nullptr) && (isSgType(args_copy) == nullptr)) {
            args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 129018 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAssignInitializer::copy(SgCopyHelp& help) const {
    SgAssignInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_i_copy
     SgExpression* operand_i_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_i
          if (get_operand_i() != nullptr) 
             { 
               operand_i_copy = static_cast<SgExpression*>(help.copyAst(get_operand_i())); 
             } 
            else 
             { 
               operand_i_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAssignInitializer( startOfConstruct_copy, operand_i_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_i
        if ((operand_i_copy != nullptr) && (operand_i_copy->get_parent() == nullptr) && (isSgType(operand_i_copy) == nullptr)) {
            operand_i_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 129248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDesignatedInitializer::copy(SgCopyHelp& help) const {
    SgDesignatedInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: designatorList_copy
     SgExprListExp* designatorList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)designatorList
          if (get_designatorList() != nullptr) 
             { 
               designatorList_copy = static_cast<SgExprListExp*>(help.copyAst(get_designatorList())); 
             } 
            else 
             { 
               designatorList_copy = nullptr; 
             } 
    // Copy constructor parameter data member: memberInit_copy
     SgInitializer* memberInit_copy; 
  // case: not a listType for (using conditionalToCopyVariable)memberInit
          if (get_memberInit() != nullptr) 
             { 
               memberInit_copy = static_cast<SgInitializer*>(help.copyAst(get_memberInit())); 
             } 
            else 
             { 
               memberInit_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDesignatedInitializer( startOfConstruct_copy, designatorList_copy, memberInit_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)designatorList
        if ((designatorList_copy != nullptr) && (designatorList_copy->get_parent() == nullptr) && (isSgType(designatorList_copy) == nullptr)) {
            designatorList_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)memberInit
        if ((memberInit_copy != nullptr) && (memberInit_copy->get_parent() == nullptr) && (isSgType(memberInit_copy) == nullptr)) {
            memberInit_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 129489 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgBracedInitializer::copy(SgCopyHelp& help) const {
    SgBracedInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: initializers_copy
     SgExprListExp* initializers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)initializers
          if (get_initializers() != nullptr) 
             { 
               initializers_copy = static_cast<SgExprListExp*>(help.copyAst(get_initializers())); 
             } 
            else 
             { 
               initializers_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgBracedInitializer( startOfConstruct_copy, initializers_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)initializers
        if ((initializers_copy != nullptr) && (initializers_copy->get_parent() == nullptr) && (isSgType(initializers_copy) == nullptr)) {
            initializers_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 129719 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAncestorInitializer::copy(SgCopyHelp& help) const {
    SgAdaAncestorInitializer* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: ancestor_copy
     SgExpression* ancestor_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ancestor
          if (get_ancestor() != nullptr) 
             { 
               ancestor_copy = static_cast<SgExpression*>(help.copyAst(get_ancestor())); 
             } 
            else 
             { 
               ancestor_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaAncestorInitializer( startOfConstruct_copy, ancestor_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_explicit_cast_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_explicit_cast
  // case: toBeCopied == COPY_DATA for is_explicit_cast
     bool is_explicit_cast_copy = p_is_explicit_cast; 
     result->p_is_explicit_cast = is_explicit_cast_copy; 
  // Copy non-constructor parameter data member (access function): is_braced_initialized_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_braced_initialized
  // case: toBeCopied == COPY_DATA for is_braced_initialized
     bool is_braced_initialized_copy = p_is_braced_initialized; 
     result->p_is_braced_initialized = is_braced_initialized_copy; 
  // case: not a listType for (using conditionalToSetParent)ancestor
        if ((ancestor_copy != nullptr) && (ancestor_copy->get_parent() == nullptr) && (isSgType(ancestor_copy) == nullptr)) {
            ancestor_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 129945 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgStartOp::copy(SgCopyHelp& help) const {
    SgVarArgStartOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_copy
     SgExpression* lhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand
          if (get_lhs_operand() != nullptr) 
             { 
               lhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand())); 
             } 
            else 
             { 
               lhs_operand_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_copy
     SgExpression* rhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand
          if (get_rhs_operand() != nullptr) 
             { 
               rhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand())); 
             } 
            else 
             { 
               rhs_operand_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarArgStartOp( startOfConstruct_copy, lhs_operand_copy, rhs_operand_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lhs_operand
        if ((lhs_operand_copy != nullptr) && (lhs_operand_copy->get_parent() == nullptr) && (isSgType(lhs_operand_copy) == nullptr)) {
            lhs_operand_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand
        if ((rhs_operand_copy != nullptr) && (rhs_operand_copy->get_parent() == nullptr) && (isSgType(rhs_operand_copy) == nullptr)) {
            rhs_operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 130180 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgOp::copy(SgCopyHelp& help) const {
    SgVarArgOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarArgOp( startOfConstruct_copy, operand_expr_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 130400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgEndOp::copy(SgCopyHelp& help) const {
    SgVarArgEndOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarArgEndOp( startOfConstruct_copy, operand_expr_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 130620 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgCopyOp::copy(SgCopyHelp& help) const {
    SgVarArgCopyOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_operand_copy
     SgExpression* lhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs_operand
          if (get_lhs_operand() != nullptr) 
             { 
               lhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_lhs_operand())); 
             } 
            else 
             { 
               lhs_operand_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_operand_copy
     SgExpression* rhs_operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs_operand
          if (get_rhs_operand() != nullptr) 
             { 
               rhs_operand_copy = static_cast<SgExpression*>(help.copyAst(get_rhs_operand())); 
             } 
            else 
             { 
               rhs_operand_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarArgCopyOp( startOfConstruct_copy, lhs_operand_copy, rhs_operand_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lhs_operand
        if ((lhs_operand_copy != nullptr) && (lhs_operand_copy->get_parent() == nullptr) && (isSgType(lhs_operand_copy) == nullptr)) {
            lhs_operand_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs_operand
        if ((rhs_operand_copy != nullptr) && (rhs_operand_copy->get_parent() == nullptr) && (isSgType(rhs_operand_copy) == nullptr)) {
            rhs_operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 130855 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarArgStartOneOperandOp::copy(SgCopyHelp& help) const {
    SgVarArgStartOneOperandOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarArgStartOneOperandOp( startOfConstruct_copy, operand_expr_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 131075 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNullExpression::copy(SgCopyHelp& help) const {
    SgNullExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNullExpression( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 131276 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariantExpression::copy(SgCopyHelp& help) const {
    SgVariantExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVariantExpression( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 131477 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSubscriptExpression::copy(SgCopyHelp& help) const {
    SgSubscriptExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lowerBound_copy
     SgExpression* lowerBound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lowerBound
          if (get_lowerBound() != nullptr) 
             { 
               lowerBound_copy = static_cast<SgExpression*>(help.copyAst(get_lowerBound())); 
             } 
            else 
             { 
               lowerBound_copy = nullptr; 
             } 
    // Copy constructor parameter data member: upperBound_copy
     SgExpression* upperBound_copy; 
  // case: not a listType for (using conditionalToCopyVariable)upperBound
          if (get_upperBound() != nullptr) 
             { 
               upperBound_copy = static_cast<SgExpression*>(help.copyAst(get_upperBound())); 
             } 
            else 
             { 
               upperBound_copy = nullptr; 
             } 
    // Copy constructor parameter data member: stride_copy
     SgExpression* stride_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stride
          if (get_stride() != nullptr) 
             { 
               stride_copy = static_cast<SgExpression*>(help.copyAst(get_stride())); 
             } 
            else 
             { 
               stride_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSubscriptExpression( startOfConstruct_copy, lowerBound_copy, upperBound_copy, stride_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lowerBound
        if ((lowerBound_copy != nullptr) && (lowerBound_copy->get_parent() == nullptr) && (isSgType(lowerBound_copy) == nullptr)) {
            lowerBound_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)upperBound
        if ((upperBound_copy != nullptr) && (upperBound_copy->get_parent() == nullptr) && (isSgType(upperBound_copy) == nullptr)) {
            upperBound_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)stride
        if ((stride_copy != nullptr) && (stride_copy->get_parent() == nullptr) && (isSgType(stride_copy) == nullptr)) {
            stride_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 131723 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgColonShapeExp::copy(SgCopyHelp& help) const {
    SgColonShapeExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgColonShapeExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 131924 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsteriskShapeExp::copy(SgCopyHelp& help) const {
    SgAsteriskShapeExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsteriskShapeExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 132125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgImpliedDo::copy(SgCopyHelp& help) const {
    SgImpliedDo* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: do_var_initialization_copy
     SgExpression* do_var_initialization_copy; 
  // case: not a listType for (using conditionalToCopyVariable)do_var_initialization
          if (get_do_var_initialization() != nullptr) 
             { 
               do_var_initialization_copy = static_cast<SgExpression*>(help.copyAst(get_do_var_initialization())); 
             } 
            else 
             { 
               do_var_initialization_copy = nullptr; 
             } 
    // Copy constructor parameter data member: last_val_copy
     SgExpression* last_val_copy; 
  // case: not a listType for (using conditionalToCopyVariable)last_val
          if (get_last_val() != nullptr) 
             { 
               last_val_copy = static_cast<SgExpression*>(help.copyAst(get_last_val())); 
             } 
            else 
             { 
               last_val_copy = nullptr; 
             } 
    // Copy constructor parameter data member: increment_copy
     SgExpression* increment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)increment
          if (get_increment() != nullptr) 
             { 
               increment_copy = static_cast<SgExpression*>(help.copyAst(get_increment())); 
             } 
            else 
             { 
               increment_copy = nullptr; 
             } 
    // Copy constructor parameter data member: object_list_copy
     SgExprListExp* object_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)object_list
          if (get_object_list() != nullptr) 
             { 
               object_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_object_list())); 
             } 
            else 
             { 
               object_list_copy = nullptr; 
             } 
    // Copy constructor parameter data member: implied_do_scope_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for implied_do_scope
  // case: toBeCopied == COPY_DATA for implied_do_scope
     SgScopeStatement* implied_do_scope_copy = p_implied_do_scope; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgImpliedDo( startOfConstruct_copy, do_var_initialization_copy, last_val_copy, increment_copy, object_list_copy, implied_do_scope_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)do_var_initialization
        if ((do_var_initialization_copy != nullptr) && (do_var_initialization_copy->get_parent() == nullptr) && (isSgType(do_var_initialization_copy) == nullptr)) {
            do_var_initialization_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)last_val
        if ((last_val_copy != nullptr) && (last_val_copy->get_parent() == nullptr) && (isSgType(last_val_copy) == nullptr)) {
            last_val_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)increment
        if ((increment_copy != nullptr) && (increment_copy->get_parent() == nullptr) && (isSgType(increment_copy) == nullptr)) {
            increment_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)object_list
        if ((object_list_copy != nullptr) && (object_list_copy->get_parent() == nullptr) && (isSgType(object_list_copy) == nullptr)) {
            object_list_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 132390 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIOItemExpression::copy(SgCopyHelp& help) const {
    SgIOItemExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: io_item_copy
     SgExpression* io_item_copy; 
  // case: not a listType for (using conditionalToCopyVariable)io_item
          if (get_io_item() != nullptr) 
             { 
               io_item_copy = static_cast<SgExpression*>(help.copyAst(get_io_item())); 
             } 
            else 
             { 
               io_item_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIOItemExpression( startOfConstruct_copy, name_copy, io_item_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)io_item
        if ((io_item_copy != nullptr) && (io_item_copy->get_parent() == nullptr) && (isSgType(io_item_copy) == nullptr)) {
            io_item_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 132610 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgActualArgumentExpression::copy(SgCopyHelp& help) const {
    SgActualArgumentExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: argument_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for argument_name
  // case: toBeCopied == COPY_DATA for argument_name
     SgName argument_name_copy = p_argument_name; 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgActualArgumentExpression( startOfConstruct_copy, argument_name_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 132830 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStatementExpression::copy(SgCopyHelp& help) const {
    SgStatementExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: statement_copy
     SgStatement* statement_copy; 
  // case: not a listType for (using conditionalToCopyVariable)statement
          if (get_statement() != nullptr) 
             { 
               statement_copy = static_cast<SgStatement*>(help.copyAst(get_statement())); 
             } 
            else 
             { 
               statement_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStatementExpression( startOfConstruct_copy, statement_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)statement
        if ((statement_copy != nullptr) && (statement_copy->get_parent() == nullptr) && (isSgType(statement_copy) == nullptr)) {
            statement_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 133046 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmOp::copy(SgCopyHelp& help) const {
    SgAsmOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: constraint_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constraint
  // case: toBeCopied == COPY_DATA for constraint
     SgAsmOp::asm_operand_constraint_enum constraint_copy = p_constraint; 
    // Copy constructor parameter data member: modifiers_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifiers
  // case: toBeCopied == COPY_DATA for modifiers
     SgAsmOp::asm_operand_modifier_enum modifiers_copy = p_modifiers; 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmOp( startOfConstruct_copy, constraint_copy, modifiers_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): recordRawAsmOperandDescriptions_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for recordRawAsmOperandDescriptions
  // case: toBeCopied == COPY_DATA for recordRawAsmOperandDescriptions
     bool recordRawAsmOperandDescriptions_copy = p_recordRawAsmOperandDescriptions; 
     result->p_recordRawAsmOperandDescriptions = recordRawAsmOperandDescriptions_copy; 
  // Copy non-constructor parameter data member (access function): isOutputOperand_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isOutputOperand
  // case: toBeCopied == COPY_DATA for isOutputOperand
     bool isOutputOperand_copy = p_isOutputOperand; 
     result->p_isOutputOperand = isOutputOperand_copy; 
  // Copy non-constructor parameter data member (access function): constraintString_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constraintString
  // case: toBeCopied == COPY_DATA for constraintString
     std::string constraintString_copy = p_constraintString; 
     result->p_constraintString = constraintString_copy; 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 133290 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCudaKernelExecConfig::copy(SgCopyHelp& help) const {
    SgCudaKernelExecConfig* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: grid_copy
     SgExpression* grid_copy; 
  // case: not a listType for (using conditionalToCopyVariable)grid
          if (get_grid() != nullptr) 
             { 
               grid_copy = static_cast<SgExpression*>(help.copyAst(get_grid())); 
             } 
            else 
             { 
               grid_copy = nullptr; 
             } 
    // Copy constructor parameter data member: blocks_copy
     SgExpression* blocks_copy; 
  // case: not a listType for (using conditionalToCopyVariable)blocks
          if (get_blocks() != nullptr) 
             { 
               blocks_copy = static_cast<SgExpression*>(help.copyAst(get_blocks())); 
             } 
            else 
             { 
               blocks_copy = nullptr; 
             } 
    // Copy constructor parameter data member: shared_copy
     SgExpression* shared_copy; 
  // case: not a listType for (using conditionalToCopyVariable)shared
          if (get_shared() != nullptr) 
             { 
               shared_copy = static_cast<SgExpression*>(help.copyAst(get_shared())); 
             } 
            else 
             { 
               shared_copy = nullptr; 
             } 
    // Copy constructor parameter data member: stream_copy
     SgExpression* stream_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stream
          if (get_stream() != nullptr) 
             { 
               stream_copy = static_cast<SgExpression*>(help.copyAst(get_stream())); 
             } 
            else 
             { 
               stream_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCudaKernelExecConfig( startOfConstruct_copy, grid_copy, blocks_copy, shared_copy, stream_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)grid
        if ((grid_copy != nullptr) && (grid_copy->get_parent() == nullptr) && (isSgType(grid_copy) == nullptr)) {
            grid_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)blocks
        if ((blocks_copy != nullptr) && (blocks_copy->get_parent() == nullptr) && (isSgType(blocks_copy) == nullptr)) {
            blocks_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)shared
        if ((shared_copy != nullptr) && (shared_copy->get_parent() == nullptr) && (isSgType(shared_copy) == nullptr)) {
            shared_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)stream
        if ((stream_copy != nullptr) && (stream_copy->get_parent() == nullptr) && (isSgType(stream_copy) == nullptr)) {
            stream_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 133551 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgUnknownArrayOrFunctionReference::copy(SgCopyHelp& help) const {
    SgUnknownArrayOrFunctionReference* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgUnknownArrayOrFunctionReference( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
  // Copy non-constructor parameter data member (access function): named_reference_copy
     SgExpression* named_reference_copy; 
  // case: not a listType for (using conditionalToCopyVariable)named_reference
          if (get_named_reference() != nullptr) 
             { 
               named_reference_copy = static_cast<SgExpression*>(help.copyAst(get_named_reference())); 
             } 
            else 
             { 
               named_reference_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_named_reference != nullptr) delete result->p_named_reference; 
     result->p_named_reference = named_reference_copy; 
  // case: not a listType for (using conditionalToSetParent)named_reference
        if ((named_reference_copy != nullptr) && (named_reference_copy->get_parent() == nullptr) && (isSgType(named_reference_copy) == nullptr)) {
            named_reference_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): expression_list_copy
     SgExprListExp* expression_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression_list
          if (get_expression_list() != nullptr) 
             { 
               expression_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_expression_list())); 
             } 
            else 
             { 
               expression_list_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_expression_list != nullptr) delete result->p_expression_list; 
     result->p_expression_list = expression_list_copy; 
  // case: not a listType for (using conditionalToSetParent)expression_list
        if ((expression_list_copy != nullptr) && (expression_list_copy->get_parent() == nullptr) && (isSgType(expression_list_copy) == nullptr)) {
            expression_list_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 133793 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgPseudoDestructorRefExp::copy(SgCopyHelp& help) const {
    SgPseudoDestructorRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: object_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for object_type
  // case: toBeCopied == COPY_DATA for object_type
     SgType* object_type_copy = p_object_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgPseudoDestructorRefExp( startOfConstruct_copy, object_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
     result->p_expression_type = expression_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 134018 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCAFCoExpression::copy(SgCopyHelp& help) const {
    SgCAFCoExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: teamId_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamId
  // case: toBeCopied == COPY_DATA for teamId
     SgVarRefExp* teamId_copy = p_teamId; 
    // Copy constructor parameter data member: teamRank_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for teamRank
  // case: toBeCopied == COPY_DATA for teamRank
     SgExpression* teamRank_copy = p_teamRank; 
    // Copy constructor parameter data member: referData_copy
     SgExpression* referData_copy; 
  // case: not a listType for (using conditionalToCopyVariable)referData
          if (get_referData() != nullptr) 
             { 
               referData_copy = static_cast<SgExpression*>(help.copyAst(get_referData())); 
             } 
            else 
             { 
               referData_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCAFCoExpression( startOfConstruct_copy, teamId_copy, teamRank_copy, referData_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)referData
        if ((referData_copy != nullptr) && (referData_copy->get_parent() == nullptr) && (isSgType(referData_copy) == nullptr)) {
            referData_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 134242 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSizeOfOp::copy(SgCopyHelp& help) const {
    SgSizeOfOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSizeOfOp( startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): sizeOfContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sizeOfContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for sizeOfContainsBaseTypeDefiningDeclaration
     bool sizeOfContainsBaseTypeDefiningDeclaration_copy = p_sizeOfContainsBaseTypeDefiningDeclaration; 
     result->p_sizeOfContainsBaseTypeDefiningDeclaration = sizeOfContainsBaseTypeDefiningDeclaration_copy; 
  // Copy non-constructor parameter data member (access function): is_objectless_nonstatic_data_member_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_objectless_nonstatic_data_member_reference
  // case: toBeCopied == COPY_DATA for is_objectless_nonstatic_data_member_reference
     bool is_objectless_nonstatic_data_member_reference_copy = p_is_objectless_nonstatic_data_member_reference; 
     result->p_is_objectless_nonstatic_data_member_reference = is_objectless_nonstatic_data_member_reference_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_for_pointer_to_member_class_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_for_pointer_to_member_class_length
  // case: toBeCopied == COPY_DATA for name_qualification_for_pointer_to_member_class_length
     int name_qualification_for_pointer_to_member_class_length_copy = p_name_qualification_for_pointer_to_member_class_length; 
     result->p_name_qualification_for_pointer_to_member_class_length = name_qualification_for_pointer_to_member_class_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for type_elaboration_for_pointer_to_member_class_required
     bool type_elaboration_for_pointer_to_member_class_required_copy = p_type_elaboration_for_pointer_to_member_class_required; 
     result->p_type_elaboration_for_pointer_to_member_class_required = type_elaboration_for_pointer_to_member_class_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_for_pointer_to_member_class_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_for_pointer_to_member_class_required
  // case: toBeCopied == COPY_DATA for global_qualification_for_pointer_to_member_class_required
     bool global_qualification_for_pointer_to_member_class_required_copy = p_global_qualification_for_pointer_to_member_class_required; 
     result->p_global_qualification_for_pointer_to_member_class_required = global_qualification_for_pointer_to_member_class_required_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 134506 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaRefExp::copy(SgCopyHelp& help) const {
    SgLambdaRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: functionDeclaration_copy
     SgFunctionDeclaration* functionDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)functionDeclaration
          if (get_functionDeclaration() != nullptr) 
             { 
               functionDeclaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_functionDeclaration())); 
             } 
            else 
             { 
               functionDeclaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLambdaRefExp( startOfConstruct_copy, functionDeclaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)functionDeclaration
        if ((functionDeclaration_copy != nullptr) && (functionDeclaration_copy->get_parent() == nullptr) && (isSgType(functionDeclaration_copy) == nullptr)) {
            functionDeclaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 134722 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDictionaryExp::copy(SgCopyHelp& help) const {
    SgDictionaryExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDictionaryExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_key_datum_pairs
     SgKeyDatumPairPtrList key_datum_pairs_copy; 
  // case: listType for key_datum_pairs
  // case: listType (typeIsPointerToList == false) for key_datum_pairs
    auto source_key_datum_pairs_iterator = get_key_datum_pairs().begin();
    for (/* empty by design */; source_key_datum_pairs_iterator != get_key_datum_pairs().end(); ++source_key_datum_pairs_iterator) {
          SgKeyDatumPair* source_list_element = *source_key_datum_pairs_iterator; 
          SgKeyDatumPair* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgKeyDatumPair*>(help.copyAst(*source_key_datum_pairs_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          key_datum_pairs_copy.push_back(copy_list_element); 
        }
     result->p_key_datum_pairs = key_datum_pairs_copy; 
  // case: listType for key_datum_pairs
  // case: listType (typeIsPointerToList == false) for key_datum_pairs
    auto key_datum_pairs_iterator = result->get_key_datum_pairs().begin();
    for (/* empty by design */; key_datum_pairs_iterator != result->get_key_datum_pairs().end(); ++key_datum_pairs_iterator) {
          SgKeyDatumPair* list_element = *key_datum_pairs_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 134951 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgKeyDatumPair::copy(SgCopyHelp& help) const {
    SgKeyDatumPair* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: key_copy
     SgExpression* key_copy; 
  // case: not a listType for (using conditionalToCopyVariable)key
          if (get_key() != nullptr) 
             { 
               key_copy = static_cast<SgExpression*>(help.copyAst(get_key())); 
             } 
            else 
             { 
               key_copy = nullptr; 
             } 
    // Copy constructor parameter data member: datum_copy
     SgExpression* datum_copy; 
  // case: not a listType for (using conditionalToCopyVariable)datum
          if (get_datum() != nullptr) 
             { 
               datum_copy = static_cast<SgExpression*>(help.copyAst(get_datum())); 
             } 
            else 
             { 
               datum_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgKeyDatumPair( startOfConstruct_copy, key_copy, datum_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)key
        if ((key_copy != nullptr) && (key_copy->get_parent() == nullptr) && (isSgType(key_copy) == nullptr)) {
            key_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)datum
        if ((datum_copy != nullptr) && (datum_copy->get_parent() == nullptr) && (isSgType(datum_copy) == nullptr)) {
            datum_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 135182 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgReferenceExp::copy(SgCopyHelp& help) const {
    SgReferenceExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgReferenceExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 135383 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVarRefExp::copy(SgCopyHelp& help) const {
    SgVarRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgVariableSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVarRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 135608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelRefExp::copy(SgCopyHelp& help) const {
    SgLabelRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgLabelSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLabelRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 135813 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassNameRefExp::copy(SgCopyHelp& help) const {
    SgClassNameRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgClassSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassNameRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 136018 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealRefExp::copy(SgCopyHelp& help) const {
    SgNonrealRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgNonrealSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonrealRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 136238 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgScopedRefExp::copy(SgCopyHelp& help) const {
    SgScopedRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lhs_copy
     SgReferenceExp *  lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgReferenceExp * >(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
    // Copy constructor parameter data member: rhs_copy
     SgReferenceExp *  rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgReferenceExp * >(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgScopedRefExp( startOfConstruct_copy, lhs_copy, rhs_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 136469 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeRefExp::copy(SgCopyHelp& help) const {
    SgTypeRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: named_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for named_type
  // case: toBeCopied == COPY_DATA for named_type
     SgNamedType *  named_type_copy = p_named_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeRefExp( startOfConstruct_copy, named_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 136674 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionRefExp::copy(SgCopyHelp& help) const {
    SgFunctionRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgFunctionSymbol* symbol_i_copy = p_symbol_i; 
    // Copy constructor parameter data member: function_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type
  // case: toBeCopied == COPY_DATA for function_type
     SgFunctionType* function_type_copy = p_function_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionRefExp( startOfConstruct_copy, symbol_i_copy, function_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): originalExpressionTree_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for originalExpressionTree
  // case: toBeCopied == COPY_DATA for originalExpressionTree
     SgExpression* originalExpressionTree_copy = p_originalExpressionTree; 
     result->p_originalExpressionTree = originalExpressionTree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 136903 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionRefExp::copy(SgCopyHelp& help) const {
    SgMemberFunctionRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgMemberFunctionSymbol* symbol_i_copy = p_symbol_i; 
    // Copy constructor parameter data member: virtual_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_call
  // case: toBeCopied == COPY_DATA for virtual_call
     int virtual_call_copy = p_virtual_call; 
    // Copy constructor parameter data member: function_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for function_type
  // case: toBeCopied == COPY_DATA for function_type
     SgFunctionType* function_type_copy = p_function_type; 
    // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     int need_qualifier_copy = p_need_qualifier; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMemberFunctionRefExp( startOfConstruct_copy, symbol_i_copy, virtual_call_copy, function_type_copy, need_qualifier_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 137135 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionRefExp::copy(SgCopyHelp& help) const {
    SgTemplateFunctionRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgTemplateFunctionSymbol* symbol_i_copy = p_symbol_i; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateFunctionRefExp( startOfConstruct_copy, symbol_i_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 137355 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionRefExp::copy(SgCopyHelp& help) const {
    SgTemplateMemberFunctionRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_i_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol_i
  // case: toBeCopied == COPY_DATA for symbol_i
     SgTemplateMemberFunctionSymbol* symbol_i_copy = p_symbol_i; 
    // Copy constructor parameter data member: virtual_call_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_call
  // case: toBeCopied == COPY_DATA for virtual_call
     int virtual_call_copy = p_virtual_call; 
    // Copy constructor parameter data member: need_qualifier_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_qualifier
  // case: toBeCopied == COPY_DATA for need_qualifier
     int need_qualifier_copy = p_need_qualifier; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateMemberFunctionRefExp( startOfConstruct_copy, symbol_i_copy, virtual_call_copy, need_qualifier_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 137583 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgComprehension::copy(SgCopyHelp& help) const {
    SgComprehension* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: target_copy
     SgExpression* target_copy; 
  // case: not a listType for (using conditionalToCopyVariable)target
          if (get_target() != nullptr) 
             { 
               target_copy = static_cast<SgExpression*>(help.copyAst(get_target())); 
             } 
            else 
             { 
               target_copy = nullptr; 
             } 
    // Copy constructor parameter data member: iter_copy
     SgExpression* iter_copy; 
  // case: not a listType for (using conditionalToCopyVariable)iter
          if (get_iter() != nullptr) 
             { 
               iter_copy = static_cast<SgExpression*>(help.copyAst(get_iter())); 
             } 
            else 
             { 
               iter_copy = nullptr; 
             } 
    // Copy constructor parameter data member: filters_copy
     SgExprListExp* filters_copy; 
  // case: not a listType for (using conditionalToCopyVariable)filters
          if (get_filters() != nullptr) 
             { 
               filters_copy = static_cast<SgExprListExp*>(help.copyAst(get_filters())); 
             } 
            else 
             { 
               filters_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgComprehension( startOfConstruct_copy, target_copy, iter_copy, filters_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)target
        if ((target_copy != nullptr) && (target_copy->get_parent() == nullptr) && (isSgType(target_copy) == nullptr)) {
            target_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)iter
        if ((iter_copy != nullptr) && (iter_copy->get_parent() == nullptr) && (isSgType(iter_copy) == nullptr)) {
            iter_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)filters
        if ((filters_copy != nullptr) && (filters_copy->get_parent() == nullptr) && (isSgType(filters_copy) == nullptr)) {
            filters_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 137829 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgListComprehension::copy(SgCopyHelp& help) const {
    SgListComprehension* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: element_copy
     SgExpression* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != nullptr) 
             { 
               element_copy = static_cast<SgExpression*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = nullptr; 
             } 
    // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != nullptr) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgListComprehension( startOfConstruct_copy, element_copy, generators_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)element
        if ((element_copy != nullptr) && (element_copy->get_parent() == nullptr) && (isSgType(element_copy) == nullptr)) {
            element_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)generators
        if ((generators_copy != nullptr) && (generators_copy->get_parent() == nullptr) && (isSgType(generators_copy) == nullptr)) {
            generators_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 138060 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgSetComprehension::copy(SgCopyHelp& help) const {
    SgSetComprehension* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: element_copy
     SgExpression* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != nullptr) 
             { 
               element_copy = static_cast<SgExpression*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = nullptr; 
             } 
    // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != nullptr) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgSetComprehension( startOfConstruct_copy, element_copy, generators_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)element
        if ((element_copy != nullptr) && (element_copy->get_parent() == nullptr) && (isSgType(element_copy) == nullptr)) {
            element_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)generators
        if ((generators_copy != nullptr) && (generators_copy->get_parent() == nullptr) && (isSgType(generators_copy) == nullptr)) {
            generators_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 138291 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDictionaryComprehension::copy(SgCopyHelp& help) const {
    SgDictionaryComprehension* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: element_copy
     SgKeyDatumPair* element_copy; 
  // case: not a listType for (using conditionalToCopyVariable)element
          if (get_element() != nullptr) 
             { 
               element_copy = static_cast<SgKeyDatumPair*>(help.copyAst(get_element())); 
             } 
            else 
             { 
               element_copy = nullptr; 
             } 
    // Copy constructor parameter data member: generators_copy
     SgExprListExp* generators_copy; 
  // case: not a listType for (using conditionalToCopyVariable)generators
          if (get_generators() != nullptr) 
             { 
               generators_copy = static_cast<SgExprListExp*>(help.copyAst(get_generators())); 
             } 
            else 
             { 
               generators_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDictionaryComprehension( startOfConstruct_copy, element_copy, generators_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)element
        if ((element_copy != nullptr) && (element_copy->get_parent() == nullptr) && (isSgType(element_copy) == nullptr)) {
            element_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)generators
        if ((generators_copy != nullptr) && (generators_copy->get_parent() == nullptr) && (isSgType(generators_copy) == nullptr)) {
            generators_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 138522 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryOp::copy(SgCopyHelp& help) const {
    SgNaryOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNaryOp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto source_operands_iterator = get_operands().begin();
    for (/* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) {
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          operands_copy.push_back(copy_list_element); 
        }
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto operands_iterator = result->get_operands().begin();
    for (/* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) {
          SgExpression* list_element = *operands_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 138756 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryBooleanOp::copy(SgCopyHelp& help) const {
    SgNaryBooleanOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNaryBooleanOp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto source_operands_iterator = get_operands().begin();
    for (/* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) {
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          operands_copy.push_back(copy_list_element); 
        }
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto operands_iterator = result->get_operands().begin();
    for (/* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) {
          SgExpression* list_element = *operands_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 138990 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNaryComparisonOp::copy(SgCopyHelp& help) const {
    SgNaryComparisonOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNaryComparisonOp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto source_operands_iterator = get_operands().begin();
    for (/* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) {
          SgExpression* source_list_element = *source_operands_iterator; 
          SgExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          operands_copy.push_back(copy_list_element); 
        }
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto operands_iterator = result->get_operands().begin();
    for (/* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) {
          SgExpression* list_element = *operands_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_operators
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operators
  // case: toBeCopied == COPY_DATA for operators
     VariantTList operators_copy = p_operators; 
     result->p_operators = operators_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 139224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgStringConversion::copy(SgCopyHelp& help) const {
    SgStringConversion* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgStringConversion( startOfConstruct_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 139440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgYieldExpression::copy(SgCopyHelp& help) const {
    SgYieldExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgYieldExpression( startOfConstruct_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 139656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAlignOfOp::copy(SgCopyHelp& help) const {
    SgAlignOfOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operand_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operand_type
  // case: toBeCopied == COPY_DATA for operand_type
     SgType* operand_type_copy = p_operand_type; 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAlignOfOp( startOfConstruct_copy, operand_expr_copy, operand_type_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name_qualification_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_qualification_length
  // case: toBeCopied == COPY_DATA for name_qualification_length
     int name_qualification_length_copy = p_name_qualification_length; 
     result->p_name_qualification_length = name_qualification_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_elaboration_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_elaboration_required
  // case: toBeCopied == COPY_DATA for type_elaboration_required
     bool type_elaboration_required_copy = p_type_elaboration_required; 
     result->p_type_elaboration_required = type_elaboration_required_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_global_qualification_required
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualification_required
  // case: toBeCopied == COPY_DATA for global_qualification_required
     bool global_qualification_required_copy = p_global_qualification_required; 
     result->p_global_qualification_required = global_qualification_required_copy; 
  // Copy non-constructor parameter data member (access function): alignOfContainsBaseTypeDefiningDeclaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for alignOfContainsBaseTypeDefiningDeclaration
  // case: toBeCopied == COPY_DATA for alignOfContainsBaseTypeDefiningDeclaration
     bool alignOfContainsBaseTypeDefiningDeclaration_copy = p_alignOfContainsBaseTypeDefiningDeclaration; 
     result->p_alignOfContainsBaseTypeDefiningDeclaration = alignOfContainsBaseTypeDefiningDeclaration_copy; 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 139900 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRangeExp::copy(SgCopyHelp& help) const {
    SgRangeExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRangeExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): start_copy
     SgExpression* start_copy; 
  // case: not a listType for (using conditionalToCopyVariable)start
          if (get_start() != nullptr) 
             { 
               start_copy = static_cast<SgExpression*>(help.copyAst(get_start())); 
             } 
            else 
             { 
               start_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_start != nullptr) delete result->p_start; 
     result->p_start = start_copy; 
  // case: not a listType for (using conditionalToSetParent)start
        if ((start_copy != nullptr) && (start_copy->get_parent() == nullptr) && (isSgType(start_copy) == nullptr)) {
            start_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): end_copy
     SgExpression* end_copy; 
  // case: not a listType for (using conditionalToCopyVariable)end
          if (get_end() != nullptr) 
             { 
               end_copy = static_cast<SgExpression*>(help.copyAst(get_end())); 
             } 
            else 
             { 
               end_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_end != nullptr) delete result->p_end; 
     result->p_end = end_copy; 
  // case: not a listType for (using conditionalToSetParent)end
        if ((end_copy != nullptr) && (end_copy->get_parent() == nullptr) && (isSgType(end_copy) == nullptr)) {
            end_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): stride_copy
     SgExpression* stride_copy; 
  // case: not a listType for (using conditionalToCopyVariable)stride
          if (get_stride() != nullptr) 
             { 
               stride_copy = static_cast<SgExpression*>(help.copyAst(get_stride())); 
             } 
            else 
             { 
               stride_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_stride != nullptr) delete result->p_stride; 
     result->p_stride = stride_copy; 
  // case: not a listType for (using conditionalToSetParent)stride
        if ((stride_copy != nullptr) && (stride_copy->get_parent() == nullptr) && (isSgType(stride_copy) == nullptr)) {
            stride_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 140155 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMagicColonExp::copy(SgCopyHelp& help) const {
    SgMagicColonExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMagicColonExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 140356 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeTraitBuiltinOperator::copy(SgCopyHelp& help) const {
    SgTypeTraitBuiltinOperator* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeTraitBuiltinOperator( startOfConstruct_copy, name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_args()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for args
  // case: toBeCopied == COPY_DATA for args
     SgNodePtrList args_copy = p_args; 
     result->p_args = args_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 140566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCompoundLiteralExp::copy(SgCopyHelp& help) const {
    SgCompoundLiteralExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgVariableSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCompoundLiteralExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 140771 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaAnnotation::copy(SgCopyHelp& help) const {
    SgJavaAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaAnnotation( startOfConstruct_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 140976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaMarkerAnnotation::copy(SgCopyHelp& help) const {
    SgJavaMarkerAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaMarkerAnnotation( startOfConstruct_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 141181 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaSingleMemberAnnotation::copy(SgCopyHelp& help) const {
    SgJavaSingleMemberAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaSingleMemberAnnotation( startOfConstruct_copy, expression_type_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 141401 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaNormalAnnotation::copy(SgCopyHelp& help) const {
    SgJavaNormalAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaNormalAnnotation( startOfConstruct_copy, expression_type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (list access function): result->get_value_pair_list()
     SgJavaMemberValuePairPtrList value_pair_list_copy; 
  // case: listType for value_pair_list
  // case: listType (typeIsPointerToList == false) for value_pair_list
    auto source_value_pair_list_iterator = get_value_pair_list().begin();
    for (/* empty by design */; source_value_pair_list_iterator != get_value_pair_list().end(); ++source_value_pair_list_iterator) {
          SgJavaMemberValuePair* source_list_element = *source_value_pair_list_iterator; 
          SgJavaMemberValuePair* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgJavaMemberValuePair*>(help.copyAst(*source_value_pair_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          value_pair_list_copy.push_back(copy_list_element); 
        }
     result->p_value_pair_list = value_pair_list_copy; 
  // case: listType for value_pair_list
  // case: listType (typeIsPointerToList == false) for value_pair_list
    auto value_pair_list_iterator = result->get_value_pair_list().begin();
    for (/* empty by design */; value_pair_list_iterator != result->get_value_pair_list().end(); ++value_pair_list_iterator) {
          SgJavaMemberValuePair* list_element = *value_pair_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 141634 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaTypeExpression::copy(SgCopyHelp& help) const {
    SgJavaTypeExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaTypeExpression( startOfConstruct_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 141839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeExpression::copy(SgCopyHelp& help) const {
    SgTypeExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeExpression( startOfConstruct_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 142044 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassExp::copy(SgCopyHelp& help) const {
    SgClassExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: class_symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_symbol
  // case: toBeCopied == COPY_DATA for class_symbol
     SgClassSymbol* class_symbol_copy = p_class_symbol; 
    // Copy constructor parameter data member: pobj_class_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pobj_class
  // case: toBeCopied == COPY_DATA for pobj_class
     int pobj_class_copy = p_pobj_class; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassExp( startOfConstruct_copy, class_symbol_copy, pobj_class_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 142253 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionParameterRefExp::copy(SgCopyHelp& help) const {
    SgFunctionParameterRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: parameter_number_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_number
  // case: toBeCopied == COPY_DATA for parameter_number
     int parameter_number_copy = p_parameter_number; 
    // Copy constructor parameter data member: parameter_levels_up_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_levels_up
  // case: toBeCopied == COPY_DATA for parameter_levels_up
     int parameter_levels_up_copy = p_parameter_levels_up; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionParameterRefExp( startOfConstruct_copy, parameter_number_copy, parameter_levels_up_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): parameter_expression_copy
     SgExpression* parameter_expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)parameter_expression
          if (get_parameter_expression() != nullptr) 
             { 
               parameter_expression_copy = static_cast<SgExpression*>(help.copyAst(get_parameter_expression())); 
             } 
            else 
             { 
               parameter_expression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_parameter_expression != nullptr) delete result->p_parameter_expression; 
     result->p_parameter_expression = parameter_expression_copy; 
  // case: not a listType for (using conditionalToSetParent)parameter_expression
        if ((parameter_expression_copy != nullptr) && (parameter_expression_copy->get_parent() == nullptr) && (isSgType(parameter_expression_copy) == nullptr)) {
            parameter_expression_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): parameter_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameter_type
  // case: toBeCopied == COPY_DATA for parameter_type
     SgType* parameter_type_copy = p_parameter_type; 
     result->p_parameter_type = parameter_type_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 142485 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLambdaExp::copy(SgCopyHelp& help) const {
    SgLambdaExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: lambda_capture_list_copy
     SgLambdaCaptureList* lambda_capture_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_capture_list
          if (get_lambda_capture_list() != nullptr) 
             { 
               lambda_capture_list_copy = static_cast<SgLambdaCaptureList*>(help.copyAst(get_lambda_capture_list())); 
             } 
            else 
             { 
               lambda_capture_list_copy = nullptr; 
             } 
    // Copy constructor parameter data member: lambda_closure_class_copy
     SgClassDeclaration* lambda_closure_class_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_closure_class
          if (get_lambda_closure_class() != nullptr) 
             { 
               lambda_closure_class_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_lambda_closure_class())); 
             } 
            else 
             { 
               lambda_closure_class_copy = nullptr; 
             } 
    // Copy constructor parameter data member: lambda_function_copy
     SgFunctionDeclaration* lambda_function_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lambda_function
          if (get_lambda_function() != nullptr) 
             { 
               lambda_function_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_lambda_function())); 
             } 
            else 
             { 
               lambda_function_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLambdaExp( startOfConstruct_copy, lambda_capture_list_copy, lambda_closure_class_copy, lambda_function_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // Copy non-constructor parameter data member (access function): is_mutable_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_mutable
  // case: toBeCopied == COPY_DATA for is_mutable
     bool is_mutable_copy = p_is_mutable; 
     result->p_is_mutable = is_mutable_copy; 
  // Copy non-constructor parameter data member (access function): capture_default_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for capture_default
  // case: toBeCopied == COPY_DATA for capture_default
     bool capture_default_copy = p_capture_default; 
     result->p_capture_default = capture_default_copy; 
  // Copy non-constructor parameter data member (access function): default_is_by_reference_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_is_by_reference
  // case: toBeCopied == COPY_DATA for default_is_by_reference
     bool default_is_by_reference_copy = p_default_is_by_reference; 
     result->p_default_is_by_reference = default_is_by_reference_copy; 
  // Copy non-constructor parameter data member (access function): explicit_return_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for explicit_return_type
  // case: toBeCopied == COPY_DATA for explicit_return_type
     bool explicit_return_type_copy = p_explicit_return_type; 
     result->p_explicit_return_type = explicit_return_type_copy; 
  // Copy non-constructor parameter data member (access function): has_parameter_decl_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for has_parameter_decl
  // case: toBeCopied == COPY_DATA for has_parameter_decl
     bool has_parameter_decl_copy = p_has_parameter_decl; 
     result->p_has_parameter_decl = has_parameter_decl_copy; 
  // Copy non-constructor parameter data member (access function): is_device_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_device
  // case: toBeCopied == COPY_DATA for is_device
     bool is_device_copy = p_is_device; 
     result->p_is_device = is_device_copy; 
  // case: not a listType for (using conditionalToSetParent)lambda_capture_list
        if ((lambda_capture_list_copy != nullptr) && (lambda_capture_list_copy->get_parent() == nullptr) && (isSgType(lambda_capture_list_copy) == nullptr)) {
            lambda_capture_list_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lambda_closure_class
        if ((lambda_closure_class_copy != nullptr) && (lambda_closure_class_copy->get_parent() == nullptr) && (isSgType(lambda_closure_class_copy) == nullptr)) {
            lambda_closure_class_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)lambda_function
        if ((lambda_function_copy != nullptr) && (lambda_function_copy->get_parent() == nullptr) && (isSgType(lambda_function_copy) == nullptr)) {
            lambda_function_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 142761 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgHereExp::copy(SgCopyHelp& help) const {
    SgHereExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expression_type
  // case: toBeCopied == COPY_DATA for expression_type
     SgType* expression_type_copy = p_expression_type; 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgHereExp( startOfConstruct_copy, expression_type_copy, expression_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 142981 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAtExp::copy(SgCopyHelp& help) const {
    SgAtExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAtExp( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 143212 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFinishExp::copy(SgCopyHelp& help) const {
    SgFinishExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: expression_copy
     SgExpression* expression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)expression
          if (get_expression() != nullptr) 
             { 
               expression_copy = static_cast<SgExpression*>(help.copyAst(get_expression())); 
             } 
            else 
             { 
               expression_copy = nullptr; 
             } 
    // Copy constructor parameter data member: body_copy
     SgStatement* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgStatement*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFinishExp( startOfConstruct_copy, expression_copy, body_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)expression
        if ((expression_copy != nullptr) && (expression_copy->get_parent() == nullptr) && (isSgType(expression_copy) == nullptr)) {
            expression_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 143443 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNoexceptOp::copy(SgCopyHelp& help) const {
    SgNoexceptOp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operand_expr_copy
     SgExpression* operand_expr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand_expr
          if (get_operand_expr() != nullptr) 
             { 
               operand_expr_copy = static_cast<SgExpression*>(help.copyAst(get_operand_expr())); 
             } 
            else 
             { 
               operand_expr_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNoexceptOp( startOfConstruct_copy, operand_expr_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operand_expr
        if ((operand_expr_copy != nullptr) && (operand_expr_copy->get_parent() == nullptr) && (isSgType(operand_expr_copy) == nullptr)) {
            operand_expr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 143659 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskRefExp::copy(SgCopyHelp& help) const {
    SgAdaTaskRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaTaskSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 143864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedRefExp::copy(SgCopyHelp& help) const {
    SgAdaProtectedRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaProtectedSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 144069 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFoldExpression::copy(SgCopyHelp& help) const {
    SgFoldExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: operands_copy
     SgExpression* operands_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operands
          if (get_operands() != nullptr) 
             { 
               operands_copy = static_cast<SgExpression*>(help.copyAst(get_operands())); 
             } 
            else 
             { 
               operands_copy = nullptr; 
             } 
    // Copy constructor parameter data member: operator_token_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operator_token
  // case: toBeCopied == COPY_DATA for operator_token
     std::string operator_token_copy = p_operator_token; 
    // Copy constructor parameter data member: is_left_associative_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_left_associative
  // case: toBeCopied == COPY_DATA for is_left_associative
     bool is_left_associative_copy = p_is_left_associative; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFoldExpression( startOfConstruct_copy, operands_copy, operator_token_copy, is_left_associative_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)operands
        if ((operands_copy != nullptr) && (operands_copy->get_parent() == nullptr) && (isSgType(operands_copy) == nullptr)) {
            operands_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 144293 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAwaitExpression::copy(SgCopyHelp& help) const {
    SgAwaitExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAwaitExpression( startOfConstruct_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 144509 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgChooseExpression::copy(SgCopyHelp& help) const {
    SgChooseExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgChooseExpression( startOfConstruct_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 144725 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaAttributeExp::copy(SgCopyHelp& help) const {
    SgAdaAttributeExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: attribute_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute
  // case: toBeCopied == COPY_DATA for attribute
     SgName attribute_copy = p_attribute; 
    // Copy constructor parameter data member: object_copy
     SgExpression* object_copy; 
  // case: not a listType for (using conditionalToCopyVariable)object
          if (get_object() != nullptr) 
             { 
               object_copy = static_cast<SgExpression*>(help.copyAst(get_object())); 
             } 
            else 
             { 
               object_copy = nullptr; 
             } 
    // Copy constructor parameter data member: args_copy
     SgExprListExp* args_copy; 
  // case: not a listType for (using conditionalToCopyVariable)args
          if (get_args() != nullptr) 
             { 
               args_copy = static_cast<SgExprListExp*>(help.copyAst(get_args())); 
             } 
            else 
             { 
               args_copy = nullptr; 
             } 
    // Copy constructor parameter data member: attrType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attrType
  // case: toBeCopied == COPY_DATA for attrType
     SgType* attrType_copy = p_attrType; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaAttributeExp( startOfConstruct_copy, attribute_copy, object_copy, args_copy, attrType_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)object
        if ((object_copy != nullptr) && (object_copy->get_parent() == nullptr) && (isSgType(object_copy) == nullptr)) {
            object_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)args
        if ((args_copy != nullptr) && (args_copy->get_parent() == nullptr) && (isSgType(args_copy) == nullptr)) {
            args_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 144964 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialTablePresetExp::copy(SgCopyHelp& help) const {
    SgJovialTablePresetExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: preset_list_copy
     SgExprListExp* preset_list_copy; 
  // case: not a listType for (using conditionalToCopyVariable)preset_list
          if (get_preset_list() != nullptr) 
             { 
               preset_list_copy = static_cast<SgExprListExp*>(help.copyAst(get_preset_list())); 
             } 
            else 
             { 
               preset_list_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialTablePresetExp( startOfConstruct_copy, preset_list_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)preset_list
        if ((preset_list_copy != nullptr) && (preset_list_copy->get_parent() == nullptr) && (isSgType(preset_list_copy) == nullptr)) {
            preset_list_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 145180 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJovialPresetPositionExp::copy(SgCopyHelp& help) const {
    SgJovialPresetPositionExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: indices_copy
     SgExprListExp* indices_copy; 
  // case: not a listType for (using conditionalToCopyVariable)indices
          if (get_indices() != nullptr) 
             { 
               indices_copy = static_cast<SgExprListExp*>(help.copyAst(get_indices())); 
             } 
            else 
             { 
               indices_copy = nullptr; 
             } 
    // Copy constructor parameter data member: value_copy
     SgExpression* value_copy; 
  // case: not a listType for (using conditionalToCopyVariable)value
          if (get_value() != nullptr) 
             { 
               value_copy = static_cast<SgExpression*>(help.copyAst(get_value())); 
             } 
            else 
             { 
               value_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJovialPresetPositionExp( startOfConstruct_copy, indices_copy, value_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)indices
        if ((indices_copy != nullptr) && (indices_copy->get_parent() == nullptr) && (isSgType(indices_copy) == nullptr)) {
            indices_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)value
        if ((value_copy != nullptr) && (value_copy->get_parent() == nullptr) && (isSgType(value_copy) == nullptr)) {
            value_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 145411 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaOthersExp::copy(SgCopyHelp& help) const {
    SgAdaOthersExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaOthersExp( startOfConstruct_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 145612 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingRefExp::copy(SgCopyHelp& help) const {
    SgAdaRenamingRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgAdaRenamingSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaRenamingRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 145817 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaUnitRefExp::copy(SgCopyHelp& help) const {
    SgAdaUnitRefExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: startOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for startOfConstruct
  // case: toBeCopied == CLONE_PTR for startOfConstruct
     Sg_File_Info* startOfConstruct_copy = nullptr; 
     if ( p_startOfConstruct != nullptr ) 
        { 
          startOfConstruct_copy = new Sg_File_Info( *p_startOfConstruct); 
        } 
       else 
        { 
          startOfConstruct_copy = nullptr; 
        } 
    // Copy constructor parameter data member: symbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaUnitRefExp( startOfConstruct_copy, symbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): endOfConstruct_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for endOfConstruct
  // case: toBeCopied == CLONE_PTR for endOfConstruct
     Sg_File_Info* endOfConstruct_copy = nullptr; 
     if ( p_endOfConstruct != nullptr ) 
        { 
          endOfConstruct_copy = new Sg_File_Info( *p_endOfConstruct); 
        } 
       else 
        { 
          endOfConstruct_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_endOfConstruct != nullptr) delete result->p_endOfConstruct; 
  /* add assignment to result here */ 
     result->p_endOfConstruct = endOfConstruct_copy; 
  // Copy non-constructor parameter data member (access function): attachedPreprocessingInfoPtr_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attachedPreprocessingInfoPtr
  // case: toBeCopied == CLONE_PTR for attachedPreprocessingInfoPtr
     AttachedPreprocessingInfoType* attachedPreprocessingInfoPtr_copy = nullptr; 
     if ( p_attachedPreprocessingInfoPtr != nullptr ) 
        { 
          attachedPreprocessingInfoPtr_copy = new AttachedPreprocessingInfoType( *p_attachedPreprocessingInfoPtr); 
        } 
       else 
        { 
          attachedPreprocessingInfoPtr_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attachedPreprocessingInfoPtr != nullptr) delete result->p_attachedPreprocessingInfoPtr; 
  /* add assignment to result here */ 
     result->p_attachedPreprocessingInfoPtr = attachedPreprocessingInfoPtr_copy; 
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): need_paren_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for need_paren
  // case: toBeCopied == COPY_DATA for need_paren
     bool need_paren_copy = p_need_paren; 
     result->p_need_paren = need_paren_copy; 
  // Copy non-constructor parameter data member (access function): lvalue_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lvalue
  // case: toBeCopied == COPY_DATA for lvalue
     bool lvalue_copy = p_lvalue; 
     result->p_lvalue = lvalue_copy; 
  // Copy non-constructor parameter data member (access function): global_qualified_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for global_qualified_name
  // case: toBeCopied == COPY_DATA for global_qualified_name
     bool global_qualified_name_copy = p_global_qualified_name; 
     result->p_global_qualified_name = global_qualified_name_copy; 
  // Copy non-constructor parameter data member (access function): operatorPosition_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operatorPosition
  // case: toBeCopied == CLONE_PTR for operatorPosition
     Sg_File_Info* operatorPosition_copy = nullptr; 
     if ( p_operatorPosition != nullptr ) 
        { 
          operatorPosition_copy = new Sg_File_Info( *p_operatorPosition); 
        } 
       else 
        { 
          operatorPosition_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operatorPosition != nullptr) delete result->p_operatorPosition; 
  /* add assignment to result here */ 
     result->p_operatorPosition = operatorPosition_copy; 
  // Copy non-constructor parameter data member (access function): alternativeExpr_copy
     SgExpression* alternativeExpr_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alternativeExpr
          if (get_alternativeExpr() != nullptr) 
             { 
               alternativeExpr_copy = static_cast<SgExpression*>(help.copyAst(get_alternativeExpr())); 
             } 
            else 
             { 
               alternativeExpr_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_alternativeExpr != nullptr) delete result->p_alternativeExpr; 
     result->p_alternativeExpr = alternativeExpr_copy; 
  // case: not a listType for (using conditionalToSetParent)alternativeExpr
        if ((alternativeExpr_copy != nullptr) && (alternativeExpr_copy->get_parent() == nullptr) && (isSgType(alternativeExpr_copy) == nullptr)) {
            alternativeExpr_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146022 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgVariableSymbol::copy(SgCopyHelp& help) const {
    SgVariableSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgVariableSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146134 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateVariableSymbol::copy(SgCopyHelp& help) const {
    SgTemplateVariableSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateVariableSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146246 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNonrealSymbol::copy(SgCopyHelp& help) const {
    SgNonrealSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgNonrealDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgNonrealDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNonrealSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146369 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionSymbol::copy(SgCopyHelp& help) const {
    SgFunctionSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146492 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgMemberFunctionSymbol::copy(SgCopyHelp& help) const {
    SgMemberFunctionSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgMemberFunctionSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146615 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateMemberFunctionSymbol::copy(SgCopyHelp& help) const {
    SgTemplateMemberFunctionSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateMemberFunctionSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146738 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateFunctionSymbol::copy(SgCopyHelp& help) const {
    SgTemplateFunctionSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateFunctionSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 146861 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgRenameSymbol::copy(SgCopyHelp& help) const {
    SgRenameSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: original_symbol_copy
     SgSymbol* original_symbol_copy; 
  // case: not a listType for (using conditionalToCopyVariable)original_symbol
          if (get_original_symbol() != nullptr) 
             { 
               original_symbol_copy = static_cast<SgSymbol*>(help.copyAst(get_original_symbol())); 
             } 
            else 
             { 
               original_symbol_copy = nullptr; 
             } 
    // Copy constructor parameter data member: new_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_name
  // case: toBeCopied == COPY_DATA for new_name
     SgName new_name_copy = p_new_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgRenameSymbol( declaration_copy, original_symbol_copy, new_name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)original_symbol
        if ((original_symbol_copy != nullptr) && (original_symbol_copy->get_parent() == nullptr) && (isSgType(original_symbol_copy) == nullptr)) {
            original_symbol_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147003 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaInheritedFunctionSymbol::copy(SgCopyHelp& help) const {
    SgAdaInheritedFunctionSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgFunctionDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgFunctionDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: derivedFunctionType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for derivedFunctionType
  // case: toBeCopied == COPY_DATA for derivedFunctionType
     SgFunctionType* derivedFunctionType_copy = p_derivedFunctionType; 
    // Copy constructor parameter data member: associatedDerivedType_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for associatedDerivedType
  // case: toBeCopied == COPY_DATA for associatedDerivedType
     SgNamedType* associatedDerivedType_copy = p_associatedDerivedType; 
    // Copy constructor parameter data member: publiclyVisibleFunctionSymbol_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for publiclyVisibleFunctionSymbol
  // case: toBeCopied == COPY_DATA for publiclyVisibleFunctionSymbol
     SgFunctionSymbol* publiclyVisibleFunctionSymbol_copy = p_publiclyVisibleFunctionSymbol; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaInheritedFunctionSymbol( declaration_copy, derivedFunctionType_copy, associatedDerivedType_copy, publiclyVisibleFunctionSymbol_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147138 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgFunctionTypeSymbol::copy(SgCopyHelp& help) const {
    SgFunctionTypeSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgFunctionTypeSymbol( name_copy, type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147254 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateSymbol::copy(SgCopyHelp& help) const {
    SgTemplateSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgTemplateDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgTemplateDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147377 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumFieldSymbol::copy(SgCopyHelp& help) const {
    SgEnumFieldSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEnumFieldSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147489 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypeSymbol::copy(SgCopyHelp& help) const {
    SgTypeSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypeSymbol();
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147596 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgClassSymbol::copy(SgCopyHelp& help) const {
    SgClassSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgClassDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgClassSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147719 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateClassSymbol::copy(SgCopyHelp& help) const {
    SgTemplateClassSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgClassDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgClassDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateClassSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTypedefSymbol::copy(SgCopyHelp& help) const {
    SgTypedefSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgTypedefDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgTypedefDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTypedefSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 147965 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgTemplateTypedefSymbol::copy(SgCopyHelp& help) const {
    SgTemplateTypedefSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgTypedefDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgTypedefDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgTemplateTypedefSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgEnumSymbol::copy(SgCopyHelp& help) const {
    SgEnumSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgEnumDeclaration* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgEnumDeclaration*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgEnumSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148211 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgLabelSymbol::copy(SgCopyHelp& help) const {
    SgLabelSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgLabelStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgLabelStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgLabelSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // Copy non-constructor parameter data member (access function): fortran_statement_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_statement
  // case: toBeCopied == COPY_DATA for fortran_statement
     SgStatement* fortran_statement_copy = p_fortran_statement; 
     result->p_fortran_statement = fortran_statement_copy; 
  // Copy non-constructor parameter data member (access function): fortran_alternate_return_parameter_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fortran_alternate_return_parameter
  // case: toBeCopied == COPY_DATA for fortran_alternate_return_parameter
     SgInitializedName* fortran_alternate_return_parameter_copy = p_fortran_alternate_return_parameter; 
     result->p_fortran_alternate_return_parameter = fortran_alternate_return_parameter_copy; 
  // Copy non-constructor parameter data member (access function): numeric_label_value_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numeric_label_value
  // case: toBeCopied == COPY_DATA for numeric_label_value
     int numeric_label_value_copy = p_numeric_label_value; 
     result->p_numeric_label_value = numeric_label_value_copy; 
  // Copy non-constructor parameter data member (access function): label_type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for label_type
  // case: toBeCopied == COPY_DATA for label_type
     SgLabelSymbol::label_type_enum label_type_copy = p_label_type; 
     result->p_label_type = label_type_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148354 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgDefaultSymbol::copy(SgCopyHelp& help) const {
    SgDefaultSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: type_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgType* type_copy = p_type; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgDefaultSymbol( type_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148466 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgNamespaceSymbol::copy(SgCopyHelp& help) const {
    SgNamespaceSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgName name_copy = p_name; 
    // Copy constructor parameter data member: declaration_copy
     SgNamespaceDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgNamespaceDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: aliasDeclaration_copy
     SgNamespaceAliasDeclarationStatement* aliasDeclaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)aliasDeclaration
          if (get_aliasDeclaration() != nullptr) 
             { 
               aliasDeclaration_copy = static_cast<SgNamespaceAliasDeclarationStatement*>(help.copyAst(get_aliasDeclaration())); 
             } 
            else 
             { 
               aliasDeclaration_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isAlias_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isAlias
  // case: toBeCopied == COPY_DATA for isAlias
     bool isAlias_copy = p_isAlias; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgNamespaceSymbol( name_copy, declaration_copy, aliasDeclaration_copy, isAlias_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 
  // case: not a listType for (using conditionalToSetParent)aliasDeclaration
        if ((aliasDeclaration_copy != nullptr) && (aliasDeclaration_copy->get_parent() == nullptr) && (isSgType(aliasDeclaration_copy) == nullptr)) {
            aliasDeclaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148612 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgIntrinsicSymbol::copy(SgCopyHelp& help) const {
    SgIntrinsicSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgIntrinsicSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148724 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgModuleSymbol::copy(SgCopyHelp& help) const {
    SgModuleSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgModuleStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgModuleStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgModuleSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148847 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgInterfaceSymbol::copy(SgCopyHelp& help) const {
    SgInterfaceSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInterfaceStatement* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgInterfaceSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 148959 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgCommonSymbol::copy(SgCopyHelp& help) const {
    SgCommonSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for declaration
  // case: toBeCopied == COPY_DATA for declaration
     SgInitializedName* declaration_copy = p_declaration; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgCommonSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149071 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAliasSymbol::copy(SgCopyHelp& help) const {
    SgAliasSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: alias_copy
     SgSymbol* alias_copy; 
  // case: not a listType for (using conditionalToCopyVariable)alias
          if (get_alias() != nullptr) 
             { 
               alias_copy = static_cast<SgSymbol*>(help.copyAst(get_alias())); 
             } 
            else 
             { 
               alias_copy = nullptr; 
             } 
    // Copy constructor parameter data member: isRenamed_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isRenamed
  // case: toBeCopied == COPY_DATA for isRenamed
     bool isRenamed_copy = p_isRenamed; 
    // Copy constructor parameter data member: new_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for new_name
  // case: toBeCopied == COPY_DATA for new_name
     SgName new_name_copy = p_new_name; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAliasSymbol( alias_copy, isRenamed_copy, new_name_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
    // Copy non-constructor parameter data member (list access function): result->get_causal_nodes()
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for causal_nodes
  // case: toBeCopied == COPY_DATA for causal_nodes
     SgNodePtrList causal_nodes_copy = p_causal_nodes; 
     result->p_causal_nodes = causal_nodes_copy; 
  // case: not a listType for (using conditionalToSetParent)alias
        if ((alias_copy != nullptr) && (alias_copy->get_parent() == nullptr) && (isSgType(alias_copy) == nullptr)) {
            alias_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149207 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAddressSymbol::copy(SgCopyHelp& help) const {
    SgAsmBinaryAddressSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: address_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_name
  // case: toBeCopied == COPY_DATA for address_name
     SgName address_name_copy = p_address_name; 
    // Copy constructor parameter data member: address_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     SgAsmInstruction* address_copy = p_address; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryAddressSymbol( address_name_copy, address_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149323 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryDataSymbol::copy(SgCopyHelp& help) const {
    SgAsmBinaryDataSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: variable_name_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for variable_name
  // case: toBeCopied == COPY_DATA for variable_name
     SgName variable_name_copy = p_variable_name; 
    // Copy constructor parameter data member: address_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     SgAsmInstruction* address_copy = p_address; 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryDataSymbol( variable_name_copy, address_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149439 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgJavaLabelSymbol::copy(SgCopyHelp& help) const {
    SgJavaLabelSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgJavaLabelStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgJavaLabelStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgJavaLabelSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149562 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaPackageSymbol::copy(SgCopyHelp& help) const {
    SgAdaPackageSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaPackageSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149685 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaTaskSymbol::copy(SgCopyHelp& help) const {
    SgAdaTaskSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaTaskSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaProtectedSymbol::copy(SgCopyHelp& help) const {
    SgAdaProtectedSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaProtectedSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 149931 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaRenamingSymbol::copy(SgCopyHelp& help) const {
    SgAdaRenamingSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgAdaRenamingDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgAdaRenamingDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaRenamingSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150054 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericSymbol::copy(SgCopyHelp& help) const {
    SgAdaGenericSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgDeclarationStatement* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgDeclarationStatement*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaGenericSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150177 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaGenericInstanceSymbol::copy(SgCopyHelp& help) const {
    SgAdaGenericInstanceSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgAdaGenericInstanceDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgAdaGenericInstanceDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaGenericInstanceSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150300 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAdaFormalPackageSymbol::copy(SgCopyHelp& help) const {
    SgAdaFormalPackageSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
    // Copy constructor parameter data member: declaration_copy
     SgAdaFormalPackageDecl* declaration_copy; 
  // case: not a listType for (using conditionalToCopyVariable)declaration
          if (get_declaration() != nullptr) 
             { 
               declaration_copy = static_cast<SgAdaFormalPackageDecl*>(help.copyAst(get_declaration())); 
             } 
            else 
             { 
               declaration_copy = nullptr; 
             } 
 
/* #line 12 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAdaFormalPackageSymbol( declaration_copy);
    ASSERT_not_null(result);

    // Copy data members of "this" class
  // Copy non-constructor parameter data member (access function): attributeMechanism_copy
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attributeMechanism
  // case: toBeCopied == CLONE_PTR for attributeMechanism
     AstAttributeMechanism* attributeMechanism_copy = nullptr; 
     if ( p_attributeMechanism != nullptr ) 
        { 
          attributeMechanism_copy = new AstAttributeMechanism( *p_attributeMechanism); 
        } 
       else 
        { 
          attributeMechanism_copy = nullptr; 
        } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attributeMechanism != nullptr) delete result->p_attributeMechanism; 
  /* add assignment to result here */ 
     result->p_attributeMechanism = attributeMechanism_copy; 
  // case: not a listType for (using conditionalToSetParent)declaration
        if ((declaration_copy != nullptr) && (declaration_copy->get_parent() == nullptr) && (isSgType(declaration_copy) == nullptr)) {
            declaration_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150423 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNode::copy(SgCopyHelp& help) const {
    SgAsmNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNode();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150513 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNode::copy(SgCopyHelp& help) const {
    SgAsmCilNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilNode();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150603 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDataStream::copy(SgCopyHelp& help) const {
    SgAsmCilDataStream* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilDataStream();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150714 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilErrorHeap::copy(SgCopyHelp& help) const {
    SgAsmCilErrorHeap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilErrorHeap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 150825 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadataHeap::copy(SgCopyHelp& help) const {
    SgAsmCilMetadataHeap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMetadataHeap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ReservedAlwaysZero
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ReservedAlwaysZero
  // case: toBeCopied == COPY_DATA for ReservedAlwaysZero
     uint32_t ReservedAlwaysZero_copy = p_ReservedAlwaysZero; 
     result->p_ReservedAlwaysZero = ReservedAlwaysZero_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint8_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint8_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_HeapSizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HeapSizes
  // case: toBeCopied == COPY_DATA for HeapSizes
     uint8_t HeapSizes_copy = p_HeapSizes; 
     result->p_HeapSizes = HeapSizes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ReservedAlwaysOne
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ReservedAlwaysOne
  // case: toBeCopied == COPY_DATA for ReservedAlwaysOne
     uint8_t ReservedAlwaysOne_copy = p_ReservedAlwaysOne; 
     result->p_ReservedAlwaysOne = ReservedAlwaysOne_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Valid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Valid
  // case: toBeCopied == COPY_DATA for Valid
     uint64_t Valid_copy = p_Valid; 
     result->p_Valid = Valid_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Sorted
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Sorted
  // case: toBeCopied == COPY_DATA for Sorted
     uint64_t Sorted_copy = p_Sorted; 
     result->p_Sorted = Sorted_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NumberOfRows
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NumberOfRows
  // case: toBeCopied == COPY_DATA for NumberOfRows
     std::vector<uint32_t> NumberOfRows_copy = p_NumberOfRows; 
     result->p_NumberOfRows = NumberOfRows_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_DataSizeFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for DataSizeFlags
  // case: toBeCopied == COPY_DATA for DataSizeFlags
     uint64_t DataSizeFlags_copy = p_DataSizeFlags; 
     result->p_DataSizeFlags = DataSizeFlags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyTable
     SgAsmCilAssemblyTable* AssemblyTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyTable
          if (get_AssemblyTable() != nullptr) 
             { 
               AssemblyTable_copy = static_cast<SgAsmCilAssemblyTable*>(help.copyAst(get_AssemblyTable())); 
             } 
            else 
             { 
               AssemblyTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyTable != nullptr) delete result->p_AssemblyTable; 
     result->p_AssemblyTable = AssemblyTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyTable
        if ((AssemblyTable_copy != nullptr) && (AssemblyTable_copy->get_parent() == nullptr) && (isSgType(AssemblyTable_copy) == nullptr)) {
            AssemblyTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyOSTable
     SgAsmCilAssemblyOSTable* AssemblyOSTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyOSTable
          if (get_AssemblyOSTable() != nullptr) 
             { 
               AssemblyOSTable_copy = static_cast<SgAsmCilAssemblyOSTable*>(help.copyAst(get_AssemblyOSTable())); 
             } 
            else 
             { 
               AssemblyOSTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyOSTable != nullptr) delete result->p_AssemblyOSTable; 
     result->p_AssemblyOSTable = AssemblyOSTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyOSTable
        if ((AssemblyOSTable_copy != nullptr) && (AssemblyOSTable_copy->get_parent() == nullptr) && (isSgType(AssemblyOSTable_copy) == nullptr)) {
            AssemblyOSTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyProcessorTable
     SgAsmCilAssemblyProcessorTable* AssemblyProcessorTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyProcessorTable
          if (get_AssemblyProcessorTable() != nullptr) 
             { 
               AssemblyProcessorTable_copy = static_cast<SgAsmCilAssemblyProcessorTable*>(help.copyAst(get_AssemblyProcessorTable())); 
             } 
            else 
             { 
               AssemblyProcessorTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyProcessorTable != nullptr) delete result->p_AssemblyProcessorTable; 
     result->p_AssemblyProcessorTable = AssemblyProcessorTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyProcessorTable
        if ((AssemblyProcessorTable_copy != nullptr) && (AssemblyProcessorTable_copy->get_parent() == nullptr) && (isSgType(AssemblyProcessorTable_copy) == nullptr)) {
            AssemblyProcessorTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefTable
     SgAsmCilAssemblyRefTable* AssemblyRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefTable
          if (get_AssemblyRefTable() != nullptr) 
             { 
               AssemblyRefTable_copy = static_cast<SgAsmCilAssemblyRefTable*>(help.copyAst(get_AssemblyRefTable())); 
             } 
            else 
             { 
               AssemblyRefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefTable != nullptr) delete result->p_AssemblyRefTable; 
     result->p_AssemblyRefTable = AssemblyRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefTable
        if ((AssemblyRefTable_copy != nullptr) && (AssemblyRefTable_copy->get_parent() == nullptr) && (isSgType(AssemblyRefTable_copy) == nullptr)) {
            AssemblyRefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefOSTable
     SgAsmCilAssemblyRefOSTable* AssemblyRefOSTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefOSTable
          if (get_AssemblyRefOSTable() != nullptr) 
             { 
               AssemblyRefOSTable_copy = static_cast<SgAsmCilAssemblyRefOSTable*>(help.copyAst(get_AssemblyRefOSTable())); 
             } 
            else 
             { 
               AssemblyRefOSTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefOSTable != nullptr) delete result->p_AssemblyRefOSTable; 
     result->p_AssemblyRefOSTable = AssemblyRefOSTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefOSTable
        if ((AssemblyRefOSTable_copy != nullptr) && (AssemblyRefOSTable_copy->get_parent() == nullptr) && (isSgType(AssemblyRefOSTable_copy) == nullptr)) {
            AssemblyRefOSTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefProcessorTable
     SgAsmCilAssemblyRefProcessorTable* AssemblyRefProcessorTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)AssemblyRefProcessorTable
          if (get_AssemblyRefProcessorTable() != nullptr) 
             { 
               AssemblyRefProcessorTable_copy = static_cast<SgAsmCilAssemblyRefProcessorTable*>(help.copyAst(get_AssemblyRefProcessorTable())); 
             } 
            else 
             { 
               AssemblyRefProcessorTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_AssemblyRefProcessorTable != nullptr) delete result->p_AssemblyRefProcessorTable; 
     result->p_AssemblyRefProcessorTable = AssemblyRefProcessorTable_copy; 
  // case: not a listType for (using conditionalToSetParent)AssemblyRefProcessorTable
        if ((AssemblyRefProcessorTable_copy != nullptr) && (AssemblyRefProcessorTable_copy->get_parent() == nullptr) && (isSgType(AssemblyRefProcessorTable_copy) == nullptr)) {
            AssemblyRefProcessorTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ClassLayoutTable
     SgAsmCilClassLayoutTable* ClassLayoutTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ClassLayoutTable
          if (get_ClassLayoutTable() != nullptr) 
             { 
               ClassLayoutTable_copy = static_cast<SgAsmCilClassLayoutTable*>(help.copyAst(get_ClassLayoutTable())); 
             } 
            else 
             { 
               ClassLayoutTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ClassLayoutTable != nullptr) delete result->p_ClassLayoutTable; 
     result->p_ClassLayoutTable = ClassLayoutTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ClassLayoutTable
        if ((ClassLayoutTable_copy != nullptr) && (ClassLayoutTable_copy->get_parent() == nullptr) && (isSgType(ClassLayoutTable_copy) == nullptr)) {
            ClassLayoutTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ConstantTable
     SgAsmCilConstantTable* ConstantTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ConstantTable
          if (get_ConstantTable() != nullptr) 
             { 
               ConstantTable_copy = static_cast<SgAsmCilConstantTable*>(help.copyAst(get_ConstantTable())); 
             } 
            else 
             { 
               ConstantTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ConstantTable != nullptr) delete result->p_ConstantTable; 
     result->p_ConstantTable = ConstantTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ConstantTable
        if ((ConstantTable_copy != nullptr) && (ConstantTable_copy->get_parent() == nullptr) && (isSgType(ConstantTable_copy) == nullptr)) {
            ConstantTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_CustomAttributeTable
     SgAsmCilCustomAttributeTable* CustomAttributeTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)CustomAttributeTable
          if (get_CustomAttributeTable() != nullptr) 
             { 
               CustomAttributeTable_copy = static_cast<SgAsmCilCustomAttributeTable*>(help.copyAst(get_CustomAttributeTable())); 
             } 
            else 
             { 
               CustomAttributeTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_CustomAttributeTable != nullptr) delete result->p_CustomAttributeTable; 
     result->p_CustomAttributeTable = CustomAttributeTable_copy; 
  // case: not a listType for (using conditionalToSetParent)CustomAttributeTable
        if ((CustomAttributeTable_copy != nullptr) && (CustomAttributeTable_copy->get_parent() == nullptr) && (isSgType(CustomAttributeTable_copy) == nullptr)) {
            CustomAttributeTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_DeclSecurityTable
     SgAsmCilDeclSecurityTable* DeclSecurityTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)DeclSecurityTable
          if (get_DeclSecurityTable() != nullptr) 
             { 
               DeclSecurityTable_copy = static_cast<SgAsmCilDeclSecurityTable*>(help.copyAst(get_DeclSecurityTable())); 
             } 
            else 
             { 
               DeclSecurityTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_DeclSecurityTable != nullptr) delete result->p_DeclSecurityTable; 
     result->p_DeclSecurityTable = DeclSecurityTable_copy; 
  // case: not a listType for (using conditionalToSetParent)DeclSecurityTable
        if ((DeclSecurityTable_copy != nullptr) && (DeclSecurityTable_copy->get_parent() == nullptr) && (isSgType(DeclSecurityTable_copy) == nullptr)) {
            DeclSecurityTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_EventTable
     SgAsmCilEventTable* EventTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)EventTable
          if (get_EventTable() != nullptr) 
             { 
               EventTable_copy = static_cast<SgAsmCilEventTable*>(help.copyAst(get_EventTable())); 
             } 
            else 
             { 
               EventTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_EventTable != nullptr) delete result->p_EventTable; 
     result->p_EventTable = EventTable_copy; 
  // case: not a listType for (using conditionalToSetParent)EventTable
        if ((EventTable_copy != nullptr) && (EventTable_copy->get_parent() == nullptr) && (isSgType(EventTable_copy) == nullptr)) {
            EventTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_EventMapTable
     SgAsmCilEventMapTable* EventMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)EventMapTable
          if (get_EventMapTable() != nullptr) 
             { 
               EventMapTable_copy = static_cast<SgAsmCilEventMapTable*>(help.copyAst(get_EventMapTable())); 
             } 
            else 
             { 
               EventMapTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_EventMapTable != nullptr) delete result->p_EventMapTable; 
     result->p_EventMapTable = EventMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)EventMapTable
        if ((EventMapTable_copy != nullptr) && (EventMapTable_copy->get_parent() == nullptr) && (isSgType(EventMapTable_copy) == nullptr)) {
            EventMapTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ExportedTypeTable
     SgAsmCilExportedTypeTable* ExportedTypeTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ExportedTypeTable
          if (get_ExportedTypeTable() != nullptr) 
             { 
               ExportedTypeTable_copy = static_cast<SgAsmCilExportedTypeTable*>(help.copyAst(get_ExportedTypeTable())); 
             } 
            else 
             { 
               ExportedTypeTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ExportedTypeTable != nullptr) delete result->p_ExportedTypeTable; 
     result->p_ExportedTypeTable = ExportedTypeTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ExportedTypeTable
        if ((ExportedTypeTable_copy != nullptr) && (ExportedTypeTable_copy->get_parent() == nullptr) && (isSgType(ExportedTypeTable_copy) == nullptr)) {
            ExportedTypeTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_FieldTable
     SgAsmCilFieldTable* FieldTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldTable
          if (get_FieldTable() != nullptr) 
             { 
               FieldTable_copy = static_cast<SgAsmCilFieldTable*>(help.copyAst(get_FieldTable())); 
             } 
            else 
             { 
               FieldTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldTable != nullptr) delete result->p_FieldTable; 
     result->p_FieldTable = FieldTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldTable
        if ((FieldTable_copy != nullptr) && (FieldTable_copy->get_parent() == nullptr) && (isSgType(FieldTable_copy) == nullptr)) {
            FieldTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_FieldLayoutTable
     SgAsmCilFieldLayoutTable* FieldLayoutTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldLayoutTable
          if (get_FieldLayoutTable() != nullptr) 
             { 
               FieldLayoutTable_copy = static_cast<SgAsmCilFieldLayoutTable*>(help.copyAst(get_FieldLayoutTable())); 
             } 
            else 
             { 
               FieldLayoutTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldLayoutTable != nullptr) delete result->p_FieldLayoutTable; 
     result->p_FieldLayoutTable = FieldLayoutTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldLayoutTable
        if ((FieldLayoutTable_copy != nullptr) && (FieldLayoutTable_copy->get_parent() == nullptr) && (isSgType(FieldLayoutTable_copy) == nullptr)) {
            FieldLayoutTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_FieldMarshalTable
     SgAsmCilFieldMarshalTable* FieldMarshalTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldMarshalTable
          if (get_FieldMarshalTable() != nullptr) 
             { 
               FieldMarshalTable_copy = static_cast<SgAsmCilFieldMarshalTable*>(help.copyAst(get_FieldMarshalTable())); 
             } 
            else 
             { 
               FieldMarshalTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldMarshalTable != nullptr) delete result->p_FieldMarshalTable; 
     result->p_FieldMarshalTable = FieldMarshalTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldMarshalTable
        if ((FieldMarshalTable_copy != nullptr) && (FieldMarshalTable_copy->get_parent() == nullptr) && (isSgType(FieldMarshalTable_copy) == nullptr)) {
            FieldMarshalTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_FieldRVATable
     SgAsmCilFieldRVATable* FieldRVATable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FieldRVATable
          if (get_FieldRVATable() != nullptr) 
             { 
               FieldRVATable_copy = static_cast<SgAsmCilFieldRVATable*>(help.copyAst(get_FieldRVATable())); 
             } 
            else 
             { 
               FieldRVATable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FieldRVATable != nullptr) delete result->p_FieldRVATable; 
     result->p_FieldRVATable = FieldRVATable_copy; 
  // case: not a listType for (using conditionalToSetParent)FieldRVATable
        if ((FieldRVATable_copy != nullptr) && (FieldRVATable_copy->get_parent() == nullptr) && (isSgType(FieldRVATable_copy) == nullptr)) {
            FieldRVATable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_FileTable
     SgAsmCilFileTable* FileTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)FileTable
          if (get_FileTable() != nullptr) 
             { 
               FileTable_copy = static_cast<SgAsmCilFileTable*>(help.copyAst(get_FileTable())); 
             } 
            else 
             { 
               FileTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_FileTable != nullptr) delete result->p_FileTable; 
     result->p_FileTable = FileTable_copy; 
  // case: not a listType for (using conditionalToSetParent)FileTable
        if ((FileTable_copy != nullptr) && (FileTable_copy->get_parent() == nullptr) && (isSgType(FileTable_copy) == nullptr)) {
            FileTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_GenericParamTable
     SgAsmCilGenericParamTable* GenericParamTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)GenericParamTable
          if (get_GenericParamTable() != nullptr) 
             { 
               GenericParamTable_copy = static_cast<SgAsmCilGenericParamTable*>(help.copyAst(get_GenericParamTable())); 
             } 
            else 
             { 
               GenericParamTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_GenericParamTable != nullptr) delete result->p_GenericParamTable; 
     result->p_GenericParamTable = GenericParamTable_copy; 
  // case: not a listType for (using conditionalToSetParent)GenericParamTable
        if ((GenericParamTable_copy != nullptr) && (GenericParamTable_copy->get_parent() == nullptr) && (isSgType(GenericParamTable_copy) == nullptr)) {
            GenericParamTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_GenericParamConstraintTable
     SgAsmCilGenericParamConstraintTable* GenericParamConstraintTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)GenericParamConstraintTable
          if (get_GenericParamConstraintTable() != nullptr) 
             { 
               GenericParamConstraintTable_copy = static_cast<SgAsmCilGenericParamConstraintTable*>(help.copyAst(get_GenericParamConstraintTable())); 
             } 
            else 
             { 
               GenericParamConstraintTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_GenericParamConstraintTable != nullptr) delete result->p_GenericParamConstraintTable; 
     result->p_GenericParamConstraintTable = GenericParamConstraintTable_copy; 
  // case: not a listType for (using conditionalToSetParent)GenericParamConstraintTable
        if ((GenericParamConstraintTable_copy != nullptr) && (GenericParamConstraintTable_copy->get_parent() == nullptr) && (isSgType(GenericParamConstraintTable_copy) == nullptr)) {
            GenericParamConstraintTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ImplMapTable
     SgAsmCilImplMapTable* ImplMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ImplMapTable
          if (get_ImplMapTable() != nullptr) 
             { 
               ImplMapTable_copy = static_cast<SgAsmCilImplMapTable*>(help.copyAst(get_ImplMapTable())); 
             } 
            else 
             { 
               ImplMapTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ImplMapTable != nullptr) delete result->p_ImplMapTable; 
     result->p_ImplMapTable = ImplMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ImplMapTable
        if ((ImplMapTable_copy != nullptr) && (ImplMapTable_copy->get_parent() == nullptr) && (isSgType(ImplMapTable_copy) == nullptr)) {
            ImplMapTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_InterfaceImplTable
     SgAsmCilInterfaceImplTable* InterfaceImplTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)InterfaceImplTable
          if (get_InterfaceImplTable() != nullptr) 
             { 
               InterfaceImplTable_copy = static_cast<SgAsmCilInterfaceImplTable*>(help.copyAst(get_InterfaceImplTable())); 
             } 
            else 
             { 
               InterfaceImplTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_InterfaceImplTable != nullptr) delete result->p_InterfaceImplTable; 
     result->p_InterfaceImplTable = InterfaceImplTable_copy; 
  // case: not a listType for (using conditionalToSetParent)InterfaceImplTable
        if ((InterfaceImplTable_copy != nullptr) && (InterfaceImplTable_copy->get_parent() == nullptr) && (isSgType(InterfaceImplTable_copy) == nullptr)) {
            InterfaceImplTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ManifestResourceTable
     SgAsmCilManifestResourceTable* ManifestResourceTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ManifestResourceTable
          if (get_ManifestResourceTable() != nullptr) 
             { 
               ManifestResourceTable_copy = static_cast<SgAsmCilManifestResourceTable*>(help.copyAst(get_ManifestResourceTable())); 
             } 
            else 
             { 
               ManifestResourceTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ManifestResourceTable != nullptr) delete result->p_ManifestResourceTable; 
     result->p_ManifestResourceTable = ManifestResourceTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ManifestResourceTable
        if ((ManifestResourceTable_copy != nullptr) && (ManifestResourceTable_copy->get_parent() == nullptr) && (isSgType(ManifestResourceTable_copy) == nullptr)) {
            ManifestResourceTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_MemberRefTable
     SgAsmCilMemberRefTable* MemberRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MemberRefTable
          if (get_MemberRefTable() != nullptr) 
             { 
               MemberRefTable_copy = static_cast<SgAsmCilMemberRefTable*>(help.copyAst(get_MemberRefTable())); 
             } 
            else 
             { 
               MemberRefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MemberRefTable != nullptr) delete result->p_MemberRefTable; 
     result->p_MemberRefTable = MemberRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MemberRefTable
        if ((MemberRefTable_copy != nullptr) && (MemberRefTable_copy->get_parent() == nullptr) && (isSgType(MemberRefTable_copy) == nullptr)) {
            MemberRefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_MethodDefTable
     SgAsmCilMethodDefTable* MethodDefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodDefTable
          if (get_MethodDefTable() != nullptr) 
             { 
               MethodDefTable_copy = static_cast<SgAsmCilMethodDefTable*>(help.copyAst(get_MethodDefTable())); 
             } 
            else 
             { 
               MethodDefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodDefTable != nullptr) delete result->p_MethodDefTable; 
     result->p_MethodDefTable = MethodDefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodDefTable
        if ((MethodDefTable_copy != nullptr) && (MethodDefTable_copy->get_parent() == nullptr) && (isSgType(MethodDefTable_copy) == nullptr)) {
            MethodDefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_MethodImplTable
     SgAsmCilMethodImplTable* MethodImplTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodImplTable
          if (get_MethodImplTable() != nullptr) 
             { 
               MethodImplTable_copy = static_cast<SgAsmCilMethodImplTable*>(help.copyAst(get_MethodImplTable())); 
             } 
            else 
             { 
               MethodImplTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodImplTable != nullptr) delete result->p_MethodImplTable; 
     result->p_MethodImplTable = MethodImplTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodImplTable
        if ((MethodImplTable_copy != nullptr) && (MethodImplTable_copy->get_parent() == nullptr) && (isSgType(MethodImplTable_copy) == nullptr)) {
            MethodImplTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_MethodSemanticsTable
     SgAsmCilMethodSemanticsTable* MethodSemanticsTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodSemanticsTable
          if (get_MethodSemanticsTable() != nullptr) 
             { 
               MethodSemanticsTable_copy = static_cast<SgAsmCilMethodSemanticsTable*>(help.copyAst(get_MethodSemanticsTable())); 
             } 
            else 
             { 
               MethodSemanticsTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodSemanticsTable != nullptr) delete result->p_MethodSemanticsTable; 
     result->p_MethodSemanticsTable = MethodSemanticsTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodSemanticsTable
        if ((MethodSemanticsTable_copy != nullptr) && (MethodSemanticsTable_copy->get_parent() == nullptr) && (isSgType(MethodSemanticsTable_copy) == nullptr)) {
            MethodSemanticsTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_MethodSpecTable
     SgAsmCilMethodSpecTable* MethodSpecTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)MethodSpecTable
          if (get_MethodSpecTable() != nullptr) 
             { 
               MethodSpecTable_copy = static_cast<SgAsmCilMethodSpecTable*>(help.copyAst(get_MethodSpecTable())); 
             } 
            else 
             { 
               MethodSpecTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_MethodSpecTable != nullptr) delete result->p_MethodSpecTable; 
     result->p_MethodSpecTable = MethodSpecTable_copy; 
  // case: not a listType for (using conditionalToSetParent)MethodSpecTable
        if ((MethodSpecTable_copy != nullptr) && (MethodSpecTable_copy->get_parent() == nullptr) && (isSgType(MethodSpecTable_copy) == nullptr)) {
            MethodSpecTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ModuleTable
     SgAsmCilModuleTable* ModuleTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ModuleTable
          if (get_ModuleTable() != nullptr) 
             { 
               ModuleTable_copy = static_cast<SgAsmCilModuleTable*>(help.copyAst(get_ModuleTable())); 
             } 
            else 
             { 
               ModuleTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ModuleTable != nullptr) delete result->p_ModuleTable; 
     result->p_ModuleTable = ModuleTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ModuleTable
        if ((ModuleTable_copy != nullptr) && (ModuleTable_copy->get_parent() == nullptr) && (isSgType(ModuleTable_copy) == nullptr)) {
            ModuleTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ModuleRefTable
     SgAsmCilModuleRefTable* ModuleRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ModuleRefTable
          if (get_ModuleRefTable() != nullptr) 
             { 
               ModuleRefTable_copy = static_cast<SgAsmCilModuleRefTable*>(help.copyAst(get_ModuleRefTable())); 
             } 
            else 
             { 
               ModuleRefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ModuleRefTable != nullptr) delete result->p_ModuleRefTable; 
     result->p_ModuleRefTable = ModuleRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ModuleRefTable
        if ((ModuleRefTable_copy != nullptr) && (ModuleRefTable_copy->get_parent() == nullptr) && (isSgType(ModuleRefTable_copy) == nullptr)) {
            ModuleRefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_NestedClassTable
     SgAsmCilNestedClassTable* NestedClassTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)NestedClassTable
          if (get_NestedClassTable() != nullptr) 
             { 
               NestedClassTable_copy = static_cast<SgAsmCilNestedClassTable*>(help.copyAst(get_NestedClassTable())); 
             } 
            else 
             { 
               NestedClassTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_NestedClassTable != nullptr) delete result->p_NestedClassTable; 
     result->p_NestedClassTable = NestedClassTable_copy; 
  // case: not a listType for (using conditionalToSetParent)NestedClassTable
        if ((NestedClassTable_copy != nullptr) && (NestedClassTable_copy->get_parent() == nullptr) && (isSgType(NestedClassTable_copy) == nullptr)) {
            NestedClassTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ParamTable
     SgAsmCilParamTable* ParamTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ParamTable
          if (get_ParamTable() != nullptr) 
             { 
               ParamTable_copy = static_cast<SgAsmCilParamTable*>(help.copyAst(get_ParamTable())); 
             } 
            else 
             { 
               ParamTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ParamTable != nullptr) delete result->p_ParamTable; 
     result->p_ParamTable = ParamTable_copy; 
  // case: not a listType for (using conditionalToSetParent)ParamTable
        if ((ParamTable_copy != nullptr) && (ParamTable_copy->get_parent() == nullptr) && (isSgType(ParamTable_copy) == nullptr)) {
            ParamTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_PropertyTable
     SgAsmCilPropertyTable* PropertyTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)PropertyTable
          if (get_PropertyTable() != nullptr) 
             { 
               PropertyTable_copy = static_cast<SgAsmCilPropertyTable*>(help.copyAst(get_PropertyTable())); 
             } 
            else 
             { 
               PropertyTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_PropertyTable != nullptr) delete result->p_PropertyTable; 
     result->p_PropertyTable = PropertyTable_copy; 
  // case: not a listType for (using conditionalToSetParent)PropertyTable
        if ((PropertyTable_copy != nullptr) && (PropertyTable_copy->get_parent() == nullptr) && (isSgType(PropertyTable_copy) == nullptr)) {
            PropertyTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_PropertyMapTable
     SgAsmCilPropertyMapTable* PropertyMapTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)PropertyMapTable
          if (get_PropertyMapTable() != nullptr) 
             { 
               PropertyMapTable_copy = static_cast<SgAsmCilPropertyMapTable*>(help.copyAst(get_PropertyMapTable())); 
             } 
            else 
             { 
               PropertyMapTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_PropertyMapTable != nullptr) delete result->p_PropertyMapTable; 
     result->p_PropertyMapTable = PropertyMapTable_copy; 
  // case: not a listType for (using conditionalToSetParent)PropertyMapTable
        if ((PropertyMapTable_copy != nullptr) && (PropertyMapTable_copy->get_parent() == nullptr) && (isSgType(PropertyMapTable_copy) == nullptr)) {
            PropertyMapTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_StandAloneSigTable
     SgAsmCilStandAloneSigTable* StandAloneSigTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)StandAloneSigTable
          if (get_StandAloneSigTable() != nullptr) 
             { 
               StandAloneSigTable_copy = static_cast<SgAsmCilStandAloneSigTable*>(help.copyAst(get_StandAloneSigTable())); 
             } 
            else 
             { 
               StandAloneSigTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_StandAloneSigTable != nullptr) delete result->p_StandAloneSigTable; 
     result->p_StandAloneSigTable = StandAloneSigTable_copy; 
  // case: not a listType for (using conditionalToSetParent)StandAloneSigTable
        if ((StandAloneSigTable_copy != nullptr) && (StandAloneSigTable_copy->get_parent() == nullptr) && (isSgType(StandAloneSigTable_copy) == nullptr)) {
            StandAloneSigTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_TypeDefTable
     SgAsmCilTypeDefTable* TypeDefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeDefTable
          if (get_TypeDefTable() != nullptr) 
             { 
               TypeDefTable_copy = static_cast<SgAsmCilTypeDefTable*>(help.copyAst(get_TypeDefTable())); 
             } 
            else 
             { 
               TypeDefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeDefTable != nullptr) delete result->p_TypeDefTable; 
     result->p_TypeDefTable = TypeDefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeDefTable
        if ((TypeDefTable_copy != nullptr) && (TypeDefTable_copy->get_parent() == nullptr) && (isSgType(TypeDefTable_copy) == nullptr)) {
            TypeDefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_TypeRefTable
     SgAsmCilTypeRefTable* TypeRefTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeRefTable
          if (get_TypeRefTable() != nullptr) 
             { 
               TypeRefTable_copy = static_cast<SgAsmCilTypeRefTable*>(help.copyAst(get_TypeRefTable())); 
             } 
            else 
             { 
               TypeRefTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeRefTable != nullptr) delete result->p_TypeRefTable; 
     result->p_TypeRefTable = TypeRefTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeRefTable
        if ((TypeRefTable_copy != nullptr) && (TypeRefTable_copy->get_parent() == nullptr) && (isSgType(TypeRefTable_copy) == nullptr)) {
            TypeRefTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_TypeSpecTable
     SgAsmCilTypeSpecTable* TypeSpecTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)TypeSpecTable
          if (get_TypeSpecTable() != nullptr) 
             { 
               TypeSpecTable_copy = static_cast<SgAsmCilTypeSpecTable*>(help.copyAst(get_TypeSpecTable())); 
             } 
            else 
             { 
               TypeSpecTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_TypeSpecTable != nullptr) delete result->p_TypeSpecTable; 
     result->p_TypeSpecTable = TypeSpecTable_copy; 
  // case: not a listType for (using conditionalToSetParent)TypeSpecTable
        if ((TypeSpecTable_copy != nullptr) && (TypeSpecTable_copy->get_parent() == nullptr) && (isSgType(TypeSpecTable_copy) == nullptr)) {
            TypeSpecTable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 151665 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilUint32Heap::copy(SgCopyHelp& help) const {
    SgAsmCilUint32Heap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilUint32Heap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Stream
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Stream
  // case: toBeCopied == COPY_DATA for Stream
     std::vector<uint32_t> Stream_copy = p_Stream; 
     result->p_Stream = Stream_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 151781 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilUint8Heap::copy(SgCopyHelp& help) const {
    SgAsmCilUint8Heap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilUint8Heap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Size
  // case: toBeCopied == COPY_DATA for Size
     uint32_t Size_copy = p_Size; 
     result->p_Size = Size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     std::string Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NamePadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NamePadding
  // case: toBeCopied == COPY_DATA for NamePadding
     uint32_t NamePadding_copy = p_NamePadding; 
     result->p_NamePadding = NamePadding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Stream
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Stream
  // case: toBeCopied == COPY_DATA for Stream
     std::vector<uint8_t> Stream_copy = p_Stream; 
     result->p_Stream = Stream_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 151897 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExceptionData::copy(SgCopyHelp& help) const {
    SgAsmCilExceptionData* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilExceptionData();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     std::uint32_t flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_tryOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tryOffset
  // case: toBeCopied == COPY_DATA for tryOffset
     std::uint32_t tryOffset_copy = p_tryOffset; 
     result->p_tryOffset = tryOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_tryLength
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tryLength
  // case: toBeCopied == COPY_DATA for tryLength
     std::uint32_t tryLength_copy = p_tryLength; 
     result->p_tryLength = tryLength_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_handlerOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlerOffset
  // case: toBeCopied == COPY_DATA for handlerOffset
     std::uint32_t handlerOffset_copy = p_handlerOffset; 
     result->p_handlerOffset = handlerOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_handlerLength
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlerLength
  // case: toBeCopied == COPY_DATA for handlerLength
     std::uint32_t handlerLength_copy = p_handlerLength; 
     result->p_handlerLength = handlerLength_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_classTokenOrFilterOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classTokenOrFilterOffset
  // case: toBeCopied == COPY_DATA for classTokenOrFilterOffset
     std::uint32_t classTokenOrFilterOffset_copy = p_classTokenOrFilterOffset; 
     result->p_classTokenOrFilterOffset = classTokenOrFilterOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_standardConforming
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for standardConforming
  // case: toBeCopied == COPY_DATA for standardConforming
     bool standardConforming_copy = p_standardConforming; 
     result->p_standardConforming = standardConforming_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152023 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadata::copy(SgCopyHelp& help) const {
    SgAsmCilMetadata* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMetadata();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152113 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssembly::copy(SgCopyHelp& help) const {
    SgAsmCilAssembly* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssembly();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_HashAlgId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashAlgId
  // case: toBeCopied == COPY_DATA for HashAlgId
     uint32_t HashAlgId_copy = p_HashAlgId; 
     result->p_HashAlgId = HashAlgId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_BuildNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for BuildNumber
  // case: toBeCopied == COPY_DATA for BuildNumber
     uint16_t BuildNumber_copy = p_BuildNumber; 
     result->p_BuildNumber = BuildNumber_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_RevisionNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RevisionNumber
  // case: toBeCopied == COPY_DATA for RevisionNumber
     uint16_t RevisionNumber_copy = p_RevisionNumber; 
     result->p_RevisionNumber = RevisionNumber_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_PublicKey
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PublicKey
  // case: toBeCopied == COPY_DATA for PublicKey
     uint32_t PublicKey_copy = p_PublicKey; 
     result->p_PublicKey = PublicKey_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Culture
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Culture
  // case: toBeCopied == COPY_DATA for Culture
     uint32_t Culture_copy = p_Culture; 
     result->p_Culture = Culture_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152249 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyOS::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyOS* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyOS();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_OSPlatformID
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSPlatformID
  // case: toBeCopied == COPY_DATA for OSPlatformID
     uint32_t OSPlatformID_copy = p_OSPlatformID; 
     result->p_OSPlatformID = OSPlatformID_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_OSMajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMajorVersion
  // case: toBeCopied == COPY_DATA for OSMajorVersion
     uint32_t OSMajorVersion_copy = p_OSMajorVersion; 
     result->p_OSMajorVersion = OSMajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_OSMinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMinorVersion
  // case: toBeCopied == COPY_DATA for OSMinorVersion
     uint32_t OSMinorVersion_copy = p_OSMinorVersion; 
     result->p_OSMinorVersion = OSMinorVersion_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152355 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyOSTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyOSTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyOSTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyOS*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssemblyOS* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyOS* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyOS*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssemblyOS* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152474 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyProcessor::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyProcessor* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyProcessor();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Processor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Processor
  // case: toBeCopied == COPY_DATA for Processor
     uint32_t Processor_copy = p_Processor; 
     result->p_Processor = Processor_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152570 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyProcessorTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyProcessorTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyProcessorTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyProcessor*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssemblyProcessor* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyProcessor* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyProcessor*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssemblyProcessor* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152689 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRef::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_BuildNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for BuildNumber
  // case: toBeCopied == COPY_DATA for BuildNumber
     uint16_t BuildNumber_copy = p_BuildNumber; 
     result->p_BuildNumber = BuildNumber_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_RevisionNumber
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RevisionNumber
  // case: toBeCopied == COPY_DATA for RevisionNumber
     uint16_t RevisionNumber_copy = p_RevisionNumber; 
     result->p_RevisionNumber = RevisionNumber_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_PublicKeyOrToken
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PublicKeyOrToken
  // case: toBeCopied == COPY_DATA for PublicKeyOrToken
     uint32_t PublicKeyOrToken_copy = p_PublicKeyOrToken; 
     result->p_PublicKeyOrToken = PublicKeyOrToken_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Culture
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Culture
  // case: toBeCopied == COPY_DATA for Culture
     uint32_t Culture_copy = p_Culture; 
     result->p_Culture = Culture_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_HashValue
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashValue
  // case: toBeCopied == COPY_DATA for HashValue
     uint32_t HashValue_copy = p_HashValue; 
     result->p_HashValue = HashValue_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152825 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefOS::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRefOS* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRefOS();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_OSPlatformID
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSPlatformID
  // case: toBeCopied == COPY_DATA for OSPlatformID
     uint32_t OSPlatformID_copy = p_OSPlatformID; 
     result->p_OSPlatformID = OSPlatformID_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_OSMajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMajorVersion
  // case: toBeCopied == COPY_DATA for OSMajorVersion
     uint32_t OSMajorVersion_copy = p_OSMajorVersion; 
     result->p_OSMajorVersion = OSMajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_OSMinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for OSMinorVersion
  // case: toBeCopied == COPY_DATA for OSMinorVersion
     uint32_t OSMinorVersion_copy = p_OSMinorVersion; 
     result->p_OSMinorVersion = OSMinorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyRefOS
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for AssemblyRefOS
  // case: toBeCopied == COPY_DATA for AssemblyRefOS
     uint32_t AssemblyRefOS_copy = p_AssemblyRefOS; 
     result->p_AssemblyRefOS = AssemblyRefOS_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 152936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefOSTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRefOSTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRefOSTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRefOS*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssemblyRefOS* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRefOS* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRefOS*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssemblyRefOS* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153055 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefProcessor::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRefProcessor* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRefProcessor();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Processor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Processor
  // case: toBeCopied == COPY_DATA for Processor
     uint32_t Processor_copy = p_Processor; 
     result->p_Processor = Processor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_AssemblyRef
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for AssemblyRef
  // case: toBeCopied == COPY_DATA for AssemblyRef
     uint32_t AssemblyRef_copy = p_AssemblyRef; 
     result->p_AssemblyRef = AssemblyRef_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153156 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefProcessorTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRefProcessorTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRefProcessorTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRefProcessor*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssemblyRefProcessor* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRefProcessor* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRefProcessor*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssemblyRefProcessor* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyRefTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyRefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyRefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssemblyRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssemblyRef* source_list_element = *source_elements_iterator; 
          SgAsmCilAssemblyRef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssemblyRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssemblyRef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153394 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilAssemblyTable::copy(SgCopyHelp& help) const {
    SgAsmCilAssemblyTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilAssemblyTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilAssembly*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilAssembly* source_list_element = *source_elements_iterator; 
          SgAsmCilAssembly* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilAssembly*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilAssembly* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153513 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilClassLayout::copy(SgCopyHelp& help) const {
    SgAsmCilClassLayout* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilClassLayout();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_PackingSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PackingSize
  // case: toBeCopied == COPY_DATA for PackingSize
     uint16_t PackingSize_copy = p_PackingSize; 
     result->p_PackingSize = PackingSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ClassSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ClassSize
  // case: toBeCopied == COPY_DATA for ClassSize
     uint32_t ClassSize_copy = p_ClassSize; 
     result->p_ClassSize = ClassSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153619 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilClassLayoutTable::copy(SgCopyHelp& help) const {
    SgAsmCilClassLayoutTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilClassLayoutTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilClassLayout*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilClassLayout* source_list_element = *source_elements_iterator; 
          SgAsmCilClassLayout* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilClassLayout*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilClassLayout* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153738 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilConstant::copy(SgCopyHelp& help) const {
    SgAsmCilConstant* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilConstant();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint8_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Padding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Padding
  // case: toBeCopied == COPY_DATA for Padding
     uint8_t Padding_copy = p_Padding; 
     result->p_Padding = Padding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Value
  // case: toBeCopied == COPY_DATA for Value
     uint32_t Value_copy = p_Value; 
     result->p_Value = Value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153849 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilConstantTable::copy(SgCopyHelp& help) const {
    SgAsmCilConstantTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilConstantTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilConstant*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilConstant* source_list_element = *source_elements_iterator; 
          SgAsmCilConstant* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilConstant*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilConstant* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 153968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilCustomAttribute::copy(SgCopyHelp& help) const {
    SgAsmCilCustomAttribute* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilCustomAttribute();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint32_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Value
  // case: toBeCopied == COPY_DATA for Value
     uint32_t Value_copy = p_Value; 
     result->p_Value = Value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154074 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilCustomAttributeTable::copy(SgCopyHelp& help) const {
    SgAsmCilCustomAttributeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilCustomAttributeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilCustomAttribute*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilCustomAttribute* source_list_element = *source_elements_iterator; 
          SgAsmCilCustomAttribute* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilCustomAttribute*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilCustomAttribute* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154193 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDeclSecurity::copy(SgCopyHelp& help) const {
    SgAsmCilDeclSecurity* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilDeclSecurity();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Action
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Action
  // case: toBeCopied == COPY_DATA for Action
     uint16_t Action_copy = p_Action; 
     result->p_Action = Action_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_PermissionSet
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PermissionSet
  // case: toBeCopied == COPY_DATA for PermissionSet
     uint32_t PermissionSet_copy = p_PermissionSet; 
     result->p_PermissionSet = PermissionSet_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154299 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilDeclSecurityTable::copy(SgCopyHelp& help) const {
    SgAsmCilDeclSecurityTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilDeclSecurityTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilDeclSecurity*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilDeclSecurity* source_list_element = *source_elements_iterator; 
          SgAsmCilDeclSecurity* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilDeclSecurity*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilDeclSecurity* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154418 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEvent::copy(SgCopyHelp& help) const {
    SgAsmCilEvent* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilEvent();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_EventFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventFlags
  // case: toBeCopied == COPY_DATA for EventFlags
     uint16_t EventFlags_copy = p_EventFlags; 
     result->p_EventFlags = EventFlags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_EventType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventType
  // case: toBeCopied == COPY_DATA for EventType
     uint32_t EventType_copy = p_EventType; 
     result->p_EventType = EventType_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154524 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventMap::copy(SgCopyHelp& help) const {
    SgAsmCilEventMap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilEventMap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_EventList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventList
  // case: toBeCopied == COPY_DATA for EventList
     uint32_t EventList_copy = p_EventList; 
     result->p_EventList = EventList_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154625 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventMapTable::copy(SgCopyHelp& help) const {
    SgAsmCilEventMapTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilEventMapTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilEventMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilEventMap* source_list_element = *source_elements_iterator; 
          SgAsmCilEventMap* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilEventMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilEventMap* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilEventTable::copy(SgCopyHelp& help) const {
    SgAsmCilEventTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilEventTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilEvent*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilEvent* source_list_element = *source_elements_iterator; 
          SgAsmCilEvent* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilEvent*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilEvent* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154863 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExportedType::copy(SgCopyHelp& help) const {
    SgAsmCilExportedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilExportedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_EventFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EventFlags
  // case: toBeCopied == COPY_DATA for EventFlags
     uint32_t EventFlags_copy = p_EventFlags; 
     result->p_EventFlags = EventFlags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeDefIdName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeDefIdName
  // case: toBeCopied == COPY_DATA for TypeDefIdName
     uint32_t TypeDefIdName_copy = p_TypeDefIdName; 
     result->p_TypeDefIdName = TypeDefIdName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Implementation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Implementation
  // case: toBeCopied == COPY_DATA for Implementation
     uint32_t Implementation_copy = p_Implementation; 
     result->p_Implementation = Implementation_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 154979 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilExportedTypeTable::copy(SgCopyHelp& help) const {
    SgAsmCilExportedTypeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilExportedTypeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilExportedType*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilExportedType* source_list_element = *source_elements_iterator; 
          SgAsmCilExportedType* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilExportedType*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilExportedType* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155098 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilField::copy(SgCopyHelp& help) const {
    SgAsmCilField* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilField();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155204 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldLayout::copy(SgCopyHelp& help) const {
    SgAsmCilFieldLayout* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldLayout();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Field
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Field
  // case: toBeCopied == COPY_DATA for Field
     uint32_t Field_copy = p_Field; 
     result->p_Field = Field_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155305 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldLayoutTable::copy(SgCopyHelp& help) const {
    SgAsmCilFieldLayoutTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldLayoutTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldLayout*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilFieldLayout* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldLayout* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldLayout*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilFieldLayout* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldMarshal::copy(SgCopyHelp& help) const {
    SgAsmCilFieldMarshal* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldMarshal();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NativeType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NativeType
  // case: toBeCopied == COPY_DATA for NativeType
     uint32_t NativeType_copy = p_NativeType; 
     result->p_NativeType = NativeType_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155525 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldMarshalTable::copy(SgCopyHelp& help) const {
    SgAsmCilFieldMarshalTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldMarshalTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldMarshal*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilFieldMarshal* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldMarshal* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldMarshal*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilFieldMarshal* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155644 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldRVA::copy(SgCopyHelp& help) const {
    SgAsmCilFieldRVA* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldRVA();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_RVA
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RVA
  // case: toBeCopied == COPY_DATA for RVA
     uint32_t RVA_copy = p_RVA; 
     result->p_RVA = RVA_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Field
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Field
  // case: toBeCopied == COPY_DATA for Field
     uint32_t Field_copy = p_Field; 
     result->p_Field = Field_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155745 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldRVATable::copy(SgCopyHelp& help) const {
    SgAsmCilFieldRVATable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldRVATable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFieldRVA*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilFieldRVA* source_list_element = *source_elements_iterator; 
          SgAsmCilFieldRVA* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilFieldRVA*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilFieldRVA* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFieldTable::copy(SgCopyHelp& help) const {
    SgAsmCilFieldTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFieldTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilField*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilField* source_list_element = *source_elements_iterator; 
          SgAsmCilField* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilField*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilField* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 155983 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFile::copy(SgCopyHelp& help) const {
    SgAsmCilFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFile();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_HashValue
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for HashValue
  // case: toBeCopied == COPY_DATA for HashValue
     uint32_t HashValue_copy = p_HashValue; 
     result->p_HashValue = HashValue_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156089 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilFileTable::copy(SgCopyHelp& help) const {
    SgAsmCilFileTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilFileTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilFile*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilFile* source_list_element = *source_elements_iterator; 
          SgAsmCilFile* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilFile*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilFile* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamConstraint::copy(SgCopyHelp& help) const {
    SgAsmCilGenericParamConstraint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilGenericParamConstraint();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Owner
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Owner
  // case: toBeCopied == COPY_DATA for Owner
     uint32_t Owner_copy = p_Owner; 
     result->p_Owner = Owner_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Constraint
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Constraint
  // case: toBeCopied == COPY_DATA for Constraint
     uint32_t Constraint_copy = p_Constraint; 
     result->p_Constraint = Constraint_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156309 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamConstraintTable::copy(SgCopyHelp& help) const {
    SgAsmCilGenericParamConstraintTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilGenericParamConstraintTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilGenericParamConstraint*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilGenericParamConstraint* source_list_element = *source_elements_iterator; 
          SgAsmCilGenericParamConstraint* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilGenericParamConstraint*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilGenericParamConstraint* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156428 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParam::copy(SgCopyHelp& help) const {
    SgAsmCilGenericParam* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilGenericParam();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Number
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Number
  // case: toBeCopied == COPY_DATA for Number
     uint16_t Number_copy = p_Number; 
     result->p_Number = Number_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Owner
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Owner
  // case: toBeCopied == COPY_DATA for Owner
     uint32_t Owner_copy = p_Owner; 
     result->p_Owner = Owner_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156539 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilGenericParamTable::copy(SgCopyHelp& help) const {
    SgAsmCilGenericParamTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilGenericParamTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilGenericParam*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilGenericParam* source_list_element = *source_elements_iterator; 
          SgAsmCilGenericParam* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilGenericParam*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilGenericParam* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156658 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilImplMap::copy(SgCopyHelp& help) const {
    SgAsmCilImplMap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilImplMap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_MappingFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MappingFlags
  // case: toBeCopied == COPY_DATA for MappingFlags
     uint16_t MappingFlags_copy = p_MappingFlags; 
     result->p_MappingFlags = MappingFlags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MemberForwarded
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MemberForwarded
  // case: toBeCopied == COPY_DATA for MemberForwarded
     uint32_t MemberForwarded_copy = p_MemberForwarded; 
     result->p_MemberForwarded = MemberForwarded_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ImportName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImportName
  // case: toBeCopied == COPY_DATA for ImportName
     uint32_t ImportName_copy = p_ImportName; 
     result->p_ImportName = ImportName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ImportScope
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImportScope
  // case: toBeCopied == COPY_DATA for ImportScope
     uint32_t ImportScope_copy = p_ImportScope; 
     result->p_ImportScope = ImportScope_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156769 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilImplMapTable::copy(SgCopyHelp& help) const {
    SgAsmCilImplMapTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilImplMapTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilImplMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilImplMap* source_list_element = *source_elements_iterator; 
          SgAsmCilImplMap* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilImplMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilImplMap* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInterfaceImpl::copy(SgCopyHelp& help) const {
    SgAsmCilInterfaceImpl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilInterfaceImpl();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Interface
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Interface
  // case: toBeCopied == COPY_DATA for Interface
     uint32_t Interface_copy = p_Interface; 
     result->p_Interface = Interface_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 156989 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInterfaceImplTable::copy(SgCopyHelp& help) const {
    SgAsmCilInterfaceImplTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilInterfaceImplTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilInterfaceImpl*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilInterfaceImpl* source_list_element = *source_elements_iterator; 
          SgAsmCilInterfaceImpl* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilInterfaceImpl*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilInterfaceImpl* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157108 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilManifestResource::copy(SgCopyHelp& help) const {
    SgAsmCilManifestResource* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilManifestResource();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Offset
  // case: toBeCopied == COPY_DATA for Offset
     uint32_t Offset_copy = p_Offset; 
     result->p_Offset = Offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Implementation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Implementation
  // case: toBeCopied == COPY_DATA for Implementation
     uint32_t Implementation_copy = p_Implementation; 
     result->p_Implementation = Implementation_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157219 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilManifestResourceTable::copy(SgCopyHelp& help) const {
    SgAsmCilManifestResourceTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilManifestResourceTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilManifestResource*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilManifestResource* source_list_element = *source_elements_iterator; 
          SgAsmCilManifestResource* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilManifestResource*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilManifestResource* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157338 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMemberRef::copy(SgCopyHelp& help) const {
    SgAsmCilMemberRef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMemberRef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157444 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMemberRefTable::copy(SgCopyHelp& help) const {
    SgAsmCilMemberRefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMemberRefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMemberRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilMemberRef* source_list_element = *source_elements_iterator; 
          SgAsmCilMemberRef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilMemberRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilMemberRef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157563 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodDef::copy(SgCopyHelp& help) const {
    SgAsmCilMethodDef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodDef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_RVA
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for RVA
  // case: toBeCopied == COPY_DATA for RVA
     uint32_t RVA_copy = p_RVA; 
     result->p_RVA = RVA_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ImplFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ImplFlags
  // case: toBeCopied == COPY_DATA for ImplFlags
     uint16_t ImplFlags_copy = p_ImplFlags; 
     result->p_ImplFlags = ImplFlags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ParamList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ParamList
  // case: toBeCopied == COPY_DATA for ParamList
     uint32_t ParamList_copy = p_ParamList; 
     result->p_ParamList = ParamList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for body
  // case: toBeCopied == COPY_DATA for body
     SgAsmBlock* body_copy = p_body; 
     result->p_body = body_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_initLocals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for initLocals
  // case: toBeCopied == COPY_DATA for initLocals
     bool initLocals_copy = p_initLocals; 
     result->p_initLocals = initLocals_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_stackSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackSize
  // case: toBeCopied == COPY_DATA for stackSize
     uint32_t stackSize_copy = p_stackSize; 
     result->p_stackSize = stackSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hasMoreSections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hasMoreSections
  // case: toBeCopied == COPY_DATA for hasMoreSections
     bool hasMoreSections_copy = p_hasMoreSections; 
     result->p_hasMoreSections = hasMoreSections_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_localVarSigTok
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for localVarSigTok
  // case: toBeCopied == COPY_DATA for localVarSigTok
     uint32_t localVarSigTok_copy = p_localVarSigTok; 
     result->p_localVarSigTok = localVarSigTok_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_methodData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for methodData
  // case: toBeCopied == COPY_DATA for methodData
     std::vector<SgAsmCilMethodData*> methodData_copy = p_methodData; 
     result->p_methodData = methodData_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bodyState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bodyState
  // case: toBeCopied == COPY_DATA for bodyState
     int bodyState_copy = p_bodyState; 
     result->p_bodyState = bodyState_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157719 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodDefTable::copy(SgCopyHelp& help) const {
    SgAsmCilMethodDefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodDefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodDef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilMethodDef* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodDef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodDef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilMethodDef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157838 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodImpl::copy(SgCopyHelp& help) const {
    SgAsmCilMethodImpl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodImpl();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Class
  // case: toBeCopied == COPY_DATA for Class
     uint32_t Class_copy = p_Class; 
     result->p_Class = Class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MethodBody
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodBody
  // case: toBeCopied == COPY_DATA for MethodBody
     uint32_t MethodBody_copy = p_MethodBody; 
     result->p_MethodBody = MethodBody_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MethodDeclaration
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodDeclaration
  // case: toBeCopied == COPY_DATA for MethodDeclaration
     uint32_t MethodDeclaration_copy = p_MethodDeclaration; 
     result->p_MethodDeclaration = MethodDeclaration_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 157944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodImplTable::copy(SgCopyHelp& help) const {
    SgAsmCilMethodImplTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodImplTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodImpl*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilMethodImpl* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodImpl* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodImpl*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilMethodImpl* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158063 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSemantics::copy(SgCopyHelp& help) const {
    SgAsmCilMethodSemantics* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodSemantics();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Semantics
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Semantics
  // case: toBeCopied == COPY_DATA for Semantics
     uint16_t Semantics_copy = p_Semantics; 
     result->p_Semantics = Semantics_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Method
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Method
  // case: toBeCopied == COPY_DATA for Method
     uint32_t Method_copy = p_Method; 
     result->p_Method = Method_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Association
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Association
  // case: toBeCopied == COPY_DATA for Association
     uint32_t Association_copy = p_Association; 
     result->p_Association = Association_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158169 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSemanticsTable::copy(SgCopyHelp& help) const {
    SgAsmCilMethodSemanticsTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodSemanticsTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodSemantics*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilMethodSemantics* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodSemantics* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodSemantics*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilMethodSemantics* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSpec::copy(SgCopyHelp& help) const {
    SgAsmCilMethodSpec* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodSpec();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Method
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Method
  // case: toBeCopied == COPY_DATA for Method
     uint32_t Method_copy = p_Method; 
     result->p_Method = Method_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Instantiation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Instantiation
  // case: toBeCopied == COPY_DATA for Instantiation
     uint32_t Instantiation_copy = p_Instantiation; 
     result->p_Instantiation = Instantiation_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158389 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodSpecTable::copy(SgCopyHelp& help) const {
    SgAsmCilMethodSpecTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodSpecTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilMethodSpec*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilMethodSpec* source_list_element = *source_elements_iterator; 
          SgAsmCilMethodSpec* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilMethodSpec*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilMethodSpec* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158508 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModule::copy(SgCopyHelp& help) const {
    SgAsmCilModule* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilModule();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Generation
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Generation
  // case: toBeCopied == COPY_DATA for Generation
     uint16_t Generation_copy = p_Generation; 
     result->p_Generation = Generation_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Mvid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Mvid
  // case: toBeCopied == COPY_DATA for Mvid
     uint32_t Mvid_copy = p_Mvid; 
     result->p_Mvid = Mvid_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Encld
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Encld
  // case: toBeCopied == COPY_DATA for Encld
     uint32_t Encld_copy = p_Encld; 
     result->p_Encld = Encld_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_EncBaseId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EncBaseId
  // case: toBeCopied == COPY_DATA for EncBaseId
     uint32_t EncBaseId_copy = p_EncBaseId; 
     result->p_EncBaseId = EncBaseId_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleRef::copy(SgCopyHelp& help) const {
    SgAsmCilModuleRef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilModuleRef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleRefTable::copy(SgCopyHelp& help) const {
    SgAsmCilModuleRefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilModuleRefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilModuleRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilModuleRef* source_list_element = *source_elements_iterator; 
          SgAsmCilModuleRef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilModuleRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilModuleRef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilModuleTable::copy(SgCopyHelp& help) const {
    SgAsmCilModuleTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilModuleTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilModule*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilModule* source_list_element = *source_elements_iterator; 
          SgAsmCilModule* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilModule*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilModule* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 158958 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNestedClass::copy(SgCopyHelp& help) const {
    SgAsmCilNestedClass* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilNestedClass();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_NestedClass
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NestedClass
  // case: toBeCopied == COPY_DATA for NestedClass
     uint32_t NestedClass_copy = p_NestedClass; 
     result->p_NestedClass = NestedClass_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_EnclosingClass
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for EnclosingClass
  // case: toBeCopied == COPY_DATA for EnclosingClass
     uint32_t EnclosingClass_copy = p_EnclosingClass; 
     result->p_EnclosingClass = EnclosingClass_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159059 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilNestedClassTable::copy(SgCopyHelp& help) const {
    SgAsmCilNestedClassTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilNestedClassTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilNestedClass*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilNestedClass* source_list_element = *source_elements_iterator; 
          SgAsmCilNestedClass* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilNestedClass*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilNestedClass* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159178 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilParam::copy(SgCopyHelp& help) const {
    SgAsmCilParam* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilParam();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Sequence
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Sequence
  // case: toBeCopied == COPY_DATA for Sequence
     uint16_t Sequence_copy = p_Sequence; 
     result->p_Sequence = Sequence_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159284 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilParamTable::copy(SgCopyHelp& help) const {
    SgAsmCilParamTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilParamTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilParam*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilParam* source_list_element = *source_elements_iterator; 
          SgAsmCilParam* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilParam*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilParam* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159403 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilProperty::copy(SgCopyHelp& help) const {
    SgAsmCilProperty* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilProperty();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Name
  // case: toBeCopied == COPY_DATA for Name
     uint32_t Name_copy = p_Name; 
     result->p_Name = Name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Type
  // case: toBeCopied == COPY_DATA for Type
     uint32_t Type_copy = p_Type; 
     result->p_Type = Type_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159509 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyMap::copy(SgCopyHelp& help) const {
    SgAsmCilPropertyMap* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilPropertyMap();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Parent
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Parent
  // case: toBeCopied == COPY_DATA for Parent
     uint32_t Parent_copy = p_Parent; 
     result->p_Parent = Parent_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_PropertyList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for PropertyList
  // case: toBeCopied == COPY_DATA for PropertyList
     uint32_t PropertyList_copy = p_PropertyList; 
     result->p_PropertyList = PropertyList_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159610 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyMapTable::copy(SgCopyHelp& help) const {
    SgAsmCilPropertyMapTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilPropertyMapTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilPropertyMap*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilPropertyMap* source_list_element = *source_elements_iterator; 
          SgAsmCilPropertyMap* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilPropertyMap*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilPropertyMap* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159729 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilPropertyTable::copy(SgCopyHelp& help) const {
    SgAsmCilPropertyTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilPropertyTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilProperty*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilProperty* source_list_element = *source_elements_iterator; 
          SgAsmCilProperty* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilProperty*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilProperty* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilStandAloneSig::copy(SgCopyHelp& help) const {
    SgAsmCilStandAloneSig* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilStandAloneSig();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 159944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilStandAloneSigTable::copy(SgCopyHelp& help) const {
    SgAsmCilStandAloneSigTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilStandAloneSigTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilStandAloneSig*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilStandAloneSig* source_list_element = *source_elements_iterator; 
          SgAsmCilStandAloneSig* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilStandAloneSig*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilStandAloneSig* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160063 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeDef::copy(SgCopyHelp& help) const {
    SgAsmCilTypeDef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeDef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint32_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Extends
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Extends
  // case: toBeCopied == COPY_DATA for Extends
     uint32_t Extends_copy = p_Extends; 
     result->p_Extends = Extends_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_FieldList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for FieldList
  // case: toBeCopied == COPY_DATA for FieldList
     uint32_t FieldList_copy = p_FieldList; 
     result->p_FieldList = FieldList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MethodList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MethodList
  // case: toBeCopied == COPY_DATA for MethodList
     uint32_t MethodList_copy = p_MethodList; 
     result->p_MethodList = MethodList_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeDefTable::copy(SgCopyHelp& help) const {
    SgAsmCilTypeDefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeDefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeDef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilTypeDef* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeDef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeDef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilTypeDef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160303 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeRef::copy(SgCopyHelp& help) const {
    SgAsmCilTypeRef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeRef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_ResolutionScope
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ResolutionScope
  // case: toBeCopied == COPY_DATA for ResolutionScope
     uint16_t ResolutionScope_copy = p_ResolutionScope; 
     result->p_ResolutionScope = ResolutionScope_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeName
  // case: toBeCopied == COPY_DATA for TypeName
     uint32_t TypeName_copy = p_TypeName; 
     result->p_TypeName = TypeName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_TypeNamespace
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for TypeNamespace
  // case: toBeCopied == COPY_DATA for TypeNamespace
     uint32_t TypeNamespace_copy = p_TypeNamespace; 
     result->p_TypeNamespace = TypeNamespace_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160409 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeRefTable::copy(SgCopyHelp& help) const {
    SgAsmCilTypeRefTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeRefTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeRef*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilTypeRef* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeRef* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeRef*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilTypeRef* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeSpec::copy(SgCopyHelp& help) const {
    SgAsmCilTypeSpec* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeSpec();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilTypeSpecTable::copy(SgCopyHelp& help) const {
    SgAsmCilTypeSpecTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilTypeSpecTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_elements
     std::vector<SgAsmCilTypeSpec*> elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto source_elements_iterator = get_elements().begin();
    for (/* empty by design */; source_elements_iterator != get_elements().end(); ++source_elements_iterator) {
          SgAsmCilTypeSpec* source_list_element = *source_elements_iterator; 
          SgAsmCilTypeSpec* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilTypeSpec*>(help.copyAst(*source_elements_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          elements_copy.push_back(copy_list_element); 
        }
     result->p_elements = elements_copy; 
  // case: listType for elements
  // case: listType (typeIsPointerToList == false) for elements
    auto elements_iterator = result->get_elements().begin();
    for (/* empty by design */; elements_iterator != result->get_elements().end(); ++elements_iterator) {
          SgAsmCilTypeSpec* list_element = *elements_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160743 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMetadataRoot::copy(SgCopyHelp& help) const {
    SgAsmCilMetadataRoot* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMetadataRoot();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_Signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Signature
  // case: toBeCopied == COPY_DATA for Signature
     uint32_t Signature_copy = p_Signature; 
     result->p_Signature = Signature_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MajorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MajorVersion
  // case: toBeCopied == COPY_DATA for MajorVersion
     uint16_t MajorVersion_copy = p_MajorVersion; 
     result->p_MajorVersion = MajorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_MinorVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for MinorVersion
  // case: toBeCopied == COPY_DATA for MinorVersion
     uint16_t MinorVersion_copy = p_MinorVersion; 
     result->p_MinorVersion = MinorVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Reserved0
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Reserved0
  // case: toBeCopied == COPY_DATA for Reserved0
     uint32_t Reserved0_copy = p_Reserved0; 
     result->p_Reserved0 = Reserved0_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Length
  // case: toBeCopied == COPY_DATA for Length
     uint32_t Length_copy = p_Length; 
     result->p_Length = Length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Version
  // case: toBeCopied == COPY_DATA for Version
     std::string Version_copy = p_Version; 
     result->p_Version = Version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_VersionPadding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for VersionPadding
  // case: toBeCopied == COPY_DATA for VersionPadding
     uint32_t VersionPadding_copy = p_VersionPadding; 
     result->p_VersionPadding = VersionPadding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for Flags
  // case: toBeCopied == COPY_DATA for Flags
     uint16_t Flags_copy = p_Flags; 
     result->p_Flags = Flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_NumberOfStreams
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for NumberOfStreams
  // case: toBeCopied == COPY_DATA for NumberOfStreams
     uint16_t NumberOfStreams_copy = p_NumberOfStreams; 
     result->p_NumberOfStreams = NumberOfStreams_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_Streams
     std::vector<SgAsmCilDataStream*> Streams_copy; 
  // case: listType for Streams
  // case: listType (typeIsPointerToList == false) for Streams
    auto source_Streams_iterator = get_Streams().begin();
    for (/* empty by design */; source_Streams_iterator != get_Streams().end(); ++source_Streams_iterator) {
          SgAsmCilDataStream* source_list_element = *source_Streams_iterator; 
          SgAsmCilDataStream* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCilDataStream*>(help.copyAst(*source_Streams_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          Streams_copy.push_back(copy_list_element); 
        }
     result->p_Streams = Streams_copy; 
  // case: listType for Streams
  // case: listType (typeIsPointerToList == false) for Streams
    auto Streams_iterator = result->get_Streams().begin();
    for (/* empty by design */; Streams_iterator != result->get_Streams().end(); ++Streams_iterator) {
          SgAsmCilDataStream* list_element = *Streams_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_ErrorFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ErrorFree
  // case: toBeCopied == COPY_DATA for ErrorFree
     bool ErrorFree_copy = p_ErrorFree; 
     result->p_ErrorFree = ErrorFree_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 160912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilMethodData::copy(SgCopyHelp& help) const {
    SgAsmCilMethodData* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilMethodData();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_clauses
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for clauses
  // case: toBeCopied == COPY_DATA for clauses
     std::vector<SgAsmCilExceptionData*> clauses_copy = p_clauses; 
     result->p_clauses = clauses_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawHeader
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawHeader
  // case: toBeCopied == COPY_DATA for rawHeader
     std::uint32_t rawHeader_copy = p_rawHeader; 
     result->p_rawHeader = rawHeader_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161013 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExecutableFileFormat::copy(SgCopyHelp& help) const {
    SgAsmExecutableFileFormat* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmExecutableFileFormat();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161103 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbolList::copy(SgCopyHelp& help) const {
    SgAsmCoffSymbolList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCoffSymbolList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmCoffSymbolPtrList symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
    auto source_symbols_iterator = get_symbols().begin();
    for (/* empty by design */; source_symbols_iterator != get_symbols().end(); ++source_symbols_iterator) {
          SgAsmCoffSymbol* source_list_element = *source_symbols_iterator; 
          SgAsmCoffSymbol* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmCoffSymbol*>(help.copyAst(*source_symbols_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          symbols_copy.push_back(copy_list_element); 
        }
     result->p_symbols = symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
    auto symbols_iterator = result->get_symbols().begin();
    for (/* empty by design */; symbols_iterator != result->get_symbols().end(); ++symbols_iterator) {
          SgAsmCoffSymbol* list_element = *symbols_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161222 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInformation::copy(SgCopyHelp& help) const {
    SgAsmDwarfInformation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfInformation();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCompilationUnitList::copy(SgCopyHelp& help) const {
    SgAsmDwarfCompilationUnitList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCompilationUnitList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_cu_list
     SgAsmDwarfCompilationUnitPtrList cu_list_copy; 
  // case: listType for cu_list
  // case: listType (typeIsPointerToList == false) for cu_list
    auto source_cu_list_iterator = get_cu_list().begin();
    for (/* empty by design */; source_cu_list_iterator != get_cu_list().end(); ++source_cu_list_iterator) {
          SgAsmDwarfCompilationUnit* source_list_element = *source_cu_list_iterator; 
          SgAsmDwarfCompilationUnit* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmDwarfCompilationUnit*>(help.copyAst(*source_cu_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          cu_list_copy.push_back(copy_list_element); 
        }
     result->p_cu_list = cu_list_copy; 
  // case: listType for cu_list
  // case: listType (typeIsPointerToList == false) for cu_list
    auto cu_list_iterator = result->get_cu_list().begin();
    for (/* empty by design */; cu_list_iterator != result->get_cu_list().end(); ++cu_list_iterator) {
          SgAsmDwarfCompilationUnit* list_element = *cu_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161431 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstruct::copy(SgCopyHelp& help) const {
    SgAsmDwarfConstruct* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfConstruct();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161562 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfAccessDeclaration::copy(SgCopyHelp& help) const {
    SgAsmDwarfAccessDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfAccessDeclaration();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161693 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfArrayType::copy(SgCopyHelp& help) const {
    SgAsmDwarfArrayType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfArrayType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfBaseType::copy(SgCopyHelp& help) const {
    SgAsmDwarfBaseType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfBaseType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 161973 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCatchBlock::copy(SgCopyHelp& help) const {
    SgAsmDwarfCatchBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCatchBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfClassTemplate::copy(SgCopyHelp& help) const {
    SgAsmDwarfClassTemplate* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfClassTemplate();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162235 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfClassType::copy(SgCopyHelp& help) const {
    SgAsmDwarfClassType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfClassType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCommonBlock::copy(SgCopyHelp& help) const {
    SgAsmDwarfCommonBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCommonBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162533 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCommonInclusion::copy(SgCopyHelp& help) const {
    SgAsmDwarfCommonInclusion* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCommonInclusion();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCompilationUnit::copy(SgCopyHelp& help) const {
    SgAsmDwarfCompilationUnit* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCompilationUnit();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_producer
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for producer
  // case: toBeCopied == COPY_DATA for producer
     std::string producer_copy = p_producer; 
     result->p_producer = producer_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_language
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for language
  // case: toBeCopied == COPY_DATA for language
     std::string language_copy = p_language; 
     result->p_language = language_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_low_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for low_pc
  // case: toBeCopied == COPY_DATA for low_pc
     uint64_t low_pc_copy = p_low_pc; 
     result->p_low_pc = low_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hi_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hi_pc
  // case: toBeCopied == COPY_DATA for hi_pc
     uint64_t hi_pc_copy = p_hi_pc; 
     result->p_hi_pc = hi_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_version_stamp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version_stamp
  // case: toBeCopied == COPY_DATA for version_stamp
     int version_stamp_copy = p_version_stamp; 
     result->p_version_stamp = version_stamp_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_abbrev_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abbrev_offset
  // case: toBeCopied == COPY_DATA for abbrev_offset
     uint64_t abbrev_offset_copy = p_abbrev_offset; 
     result->p_abbrev_offset = abbrev_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_address_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address_size
  // case: toBeCopied == COPY_DATA for address_size
     uint64_t address_size_copy = p_address_size; 
     result->p_address_size = address_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset_length
  // case: toBeCopied == COPY_DATA for offset_length
     uint64_t offset_length_copy = p_offset_length; 
     result->p_offset_length = offset_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_signature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_signature
  // case: toBeCopied == COPY_DATA for type_signature
     std::string type_signature_copy = p_type_signature; 
     result->p_type_signature = type_signature_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comp_dir
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comp_dir
  // case: toBeCopied == COPY_DATA for comp_dir
     std::string comp_dir_copy = p_comp_dir; 
     result->p_comp_dir = comp_dir_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_line_info
     SgAsmDwarfLineList* line_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)line_info
          if (get_line_info() != nullptr) 
             { 
               line_info_copy = static_cast<SgAsmDwarfLineList*>(help.copyAst(get_line_info())); 
             } 
            else 
             { 
               line_info_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_line_info != nullptr) delete result->p_line_info; 
     result->p_line_info = line_info_copy; 
  // case: not a listType for (using conditionalToSetParent)line_info
        if ((line_info_copy != nullptr) && (line_info_copy->get_parent() == nullptr) && (isSgType(line_info_copy) == nullptr)) {
            line_info_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_language_constructs
     SgAsmDwarfConstructList* language_constructs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)language_constructs
          if (get_language_constructs() != nullptr) 
             { 
               language_constructs_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_language_constructs())); 
             } 
            else 
             { 
               language_constructs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_language_constructs != nullptr) delete result->p_language_constructs; 
     result->p_language_constructs = language_constructs_copy; 
  // case: not a listType for (using conditionalToSetParent)language_constructs
        if ((language_constructs_copy != nullptr) && (language_constructs_copy->get_parent() == nullptr) && (isSgType(language_constructs_copy) == nullptr)) {
            language_constructs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_macro_info
     SgAsmDwarfMacroList* macro_info_copy; 
  // case: not a listType for (using conditionalToCopyVariable)macro_info
          if (get_macro_info() != nullptr) 
             { 
               macro_info_copy = static_cast<SgAsmDwarfMacroList*>(help.copyAst(get_macro_info())); 
             } 
            else 
             { 
               macro_info_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_macro_info != nullptr) delete result->p_macro_info; 
     result->p_macro_info = macro_info_copy; 
  // case: not a listType for (using conditionalToSetParent)macro_info
        if ((macro_info_copy != nullptr) && (macro_info_copy->get_parent() == nullptr) && (isSgType(macro_info_copy) == nullptr)) {
            macro_info_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 162899 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfCondition::copy(SgCopyHelp& help) const {
    SgAsmDwarfCondition* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfCondition();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163030 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstant::copy(SgCopyHelp& help) const {
    SgAsmDwarfConstant* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfConstant();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163161 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstType::copy(SgCopyHelp& help) const {
    SgAsmDwarfConstType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfConstType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163292 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfDwarfProcedure::copy(SgCopyHelp& help) const {
    SgAsmDwarfDwarfProcedure* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfDwarfProcedure();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163423 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEntryPoint::copy(SgCopyHelp& help) const {
    SgAsmDwarfEntryPoint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfEntryPoint();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163554 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEnumerationType::copy(SgCopyHelp& help) const {
    SgAsmDwarfEnumerationType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfEnumerationType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_is_enum_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_enum_class
  // case: toBeCopied == COPY_DATA for is_enum_class
     bool is_enum_class_copy = p_is_enum_class; 
     result->p_is_enum_class = is_enum_class_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163708 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfEnumerator::copy(SgCopyHelp& help) const {
    SgAsmDwarfEnumerator* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfEnumerator();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFileType::copy(SgCopyHelp& help) const {
    SgAsmDwarfFileType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfFileType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 163970 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFormalParameter::copy(SgCopyHelp& help) const {
    SgAsmDwarfFormalParameter* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfFormalParameter();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164101 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFormatLabel::copy(SgCopyHelp& help) const {
    SgAsmDwarfFormatLabel* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfFormatLabel();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFriend::copy(SgCopyHelp& help) const {
    SgAsmDwarfFriend* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfFriend();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164363 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfFunctionTemplate::copy(SgCopyHelp& help) const {
    SgAsmDwarfFunctionTemplate* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfFunctionTemplate();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164494 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedDeclaration::copy(SgCopyHelp& help) const {
    SgAsmDwarfImportedDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfImportedDeclaration();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164625 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedModule::copy(SgCopyHelp& help) const {
    SgAsmDwarfImportedModule* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfImportedModule();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164756 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfImportedUnit::copy(SgCopyHelp& help) const {
    SgAsmDwarfImportedUnit* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfImportedUnit();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 164887 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInheritance::copy(SgCopyHelp& help) const {
    SgAsmDwarfInheritance* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfInheritance();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165018 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInlinedSubroutine::copy(SgCopyHelp& help) const {
    SgAsmDwarfInlinedSubroutine* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfInlinedSubroutine();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165167 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfInterfaceType::copy(SgCopyHelp& help) const {
    SgAsmDwarfInterfaceType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfInterfaceType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165298 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLabel::copy(SgCopyHelp& help) const {
    SgAsmDwarfLabel* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfLabel();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165429 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLexicalBlock::copy(SgCopyHelp& help) const {
    SgAsmDwarfLexicalBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfLexicalBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165578 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMember::copy(SgCopyHelp& help) const {
    SgAsmDwarfMember* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfMember();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data_bit_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_bit_offset
  // case: toBeCopied == COPY_DATA for data_bit_offset
     uint64_t data_bit_offset_copy = p_data_bit_offset; 
     result->p_data_bit_offset = data_bit_offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165714 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfModule::copy(SgCopyHelp& help) const {
    SgAsmDwarfModule* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfModule();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165845 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMutableType::copy(SgCopyHelp& help) const {
    SgAsmDwarfMutableType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfMutableType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 165976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamelist::copy(SgCopyHelp& help) const {
    SgAsmDwarfNamelist* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfNamelist();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166107 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamelistItem::copy(SgCopyHelp& help) const {
    SgAsmDwarfNamelistItem* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfNamelistItem();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166238 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfNamespace::copy(SgCopyHelp& help) const {
    SgAsmDwarfNamespace* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfNamespace();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166387 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPackedType::copy(SgCopyHelp& help) const {
    SgAsmDwarfPackedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfPackedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166518 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPartialUnit::copy(SgCopyHelp& help) const {
    SgAsmDwarfPartialUnit* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfPartialUnit();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166649 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPointerType::copy(SgCopyHelp& help) const {
    SgAsmDwarfPointerType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfPointerType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166780 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfPtrToMemberType::copy(SgCopyHelp& help) const {
    SgAsmDwarfPtrToMemberType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfPtrToMemberType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 166911 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfReferenceType::copy(SgCopyHelp& help) const {
    SgAsmDwarfReferenceType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfReferenceType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167042 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfRestrictType::copy(SgCopyHelp& help) const {
    SgAsmDwarfRestrictType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfRestrictType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167173 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSetType::copy(SgCopyHelp& help) const {
    SgAsmDwarfSetType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfSetType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSharedType::copy(SgCopyHelp& help) const {
    SgAsmDwarfSharedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfSharedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167435 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfStringType::copy(SgCopyHelp& help) const {
    SgAsmDwarfStringType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfStringType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167566 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfStructureType::copy(SgCopyHelp& help) const {
    SgAsmDwarfStructureType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfStructureType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167715 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubprogram::copy(SgCopyHelp& help) const {
    SgAsmDwarfSubprogram* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfSubprogram();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_linkage_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage_name
  // case: toBeCopied == COPY_DATA for linkage_name
     std::string linkage_name_copy = p_linkage_name; 
     result->p_linkage_name = linkage_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_is_main_subprogram
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_main_subprogram
  // case: toBeCopied == COPY_DATA for is_main_subprogram
     bool is_main_subprogram_copy = p_is_main_subprogram; 
     result->p_is_main_subprogram = is_main_subprogram_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_is_const_expr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_const_expr
  // case: toBeCopied == COPY_DATA for is_const_expr
     bool is_const_expr_copy = p_is_const_expr; 
     result->p_is_const_expr = is_const_expr_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 167879 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubrangeType::copy(SgCopyHelp& help) const {
    SgAsmDwarfSubrangeType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfSubrangeType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168010 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfSubroutineType::copy(SgCopyHelp& help) const {
    SgAsmDwarfSubroutineType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfSubroutineType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168159 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTemplateTypeParameter::copy(SgCopyHelp& help) const {
    SgAsmDwarfTemplateTypeParameter* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfTemplateTypeParameter();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168290 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTemplateValueParameter::copy(SgCopyHelp& help) const {
    SgAsmDwarfTemplateValueParameter* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfTemplateValueParameter();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168421 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfThrownType::copy(SgCopyHelp& help) const {
    SgAsmDwarfThrownType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfThrownType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTryBlock::copy(SgCopyHelp& help) const {
    SgAsmDwarfTryBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfTryBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168683 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfTypedef::copy(SgCopyHelp& help) const {
    SgAsmDwarfTypedef* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfTypedef();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168814 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnionType::copy(SgCopyHelp& help) const {
    SgAsmDwarfUnionType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUnionType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_body
     SgAsmDwarfConstructList* body_copy; 
  // case: not a listType for (using conditionalToCopyVariable)body
          if (get_body() != nullptr) 
             { 
               body_copy = static_cast<SgAsmDwarfConstructList*>(help.copyAst(get_body())); 
             } 
            else 
             { 
               body_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_body != nullptr) delete result->p_body; 
     result->p_body = body_copy; 
  // case: not a listType for (using conditionalToSetParent)body
        if ((body_copy != nullptr) && (body_copy->get_parent() == nullptr) && (isSgType(body_copy) == nullptr)) {
            body_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 168963 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnknownConstruct::copy(SgCopyHelp& help) const {
    SgAsmDwarfUnknownConstruct* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUnknownConstruct();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169094 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnspecifiedParameters::copy(SgCopyHelp& help) const {
    SgAsmDwarfUnspecifiedParameters* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUnspecifiedParameters();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169225 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUnspecifiedType::copy(SgCopyHelp& help) const {
    SgAsmDwarfUnspecifiedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUnspecifiedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169356 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcRelaxedType::copy(SgCopyHelp& help) const {
    SgAsmDwarfUpcRelaxedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUpcRelaxedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169487 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcSharedType::copy(SgCopyHelp& help) const {
    SgAsmDwarfUpcSharedType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUpcSharedType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169618 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfUpcStrictType::copy(SgCopyHelp& help) const {
    SgAsmDwarfUpcStrictType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfUpcStrictType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169749 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariable::copy(SgCopyHelp& help) const {
    SgAsmDwarfVariable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfVariable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkage_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkage_name
  // case: toBeCopied == COPY_DATA for linkage_name
     std::string linkage_name_copy = p_linkage_name; 
     result->p_linkage_name = linkage_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_is_const_expr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_const_expr
  // case: toBeCopied == COPY_DATA for is_const_expr
     bool is_const_expr_copy = p_is_const_expr; 
     result->p_is_const_expr = is_const_expr_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 169890 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariant::copy(SgCopyHelp& help) const {
    SgAsmDwarfVariant* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfVariant();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170021 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVariantPart::copy(SgCopyHelp& help) const {
    SgAsmDwarfVariantPart* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfVariantPart();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfVolatileType::copy(SgCopyHelp& help) const {
    SgAsmDwarfVolatileType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfVolatileType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170283 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfWithStmt::copy(SgCopyHelp& help) const {
    SgAsmDwarfWithStmt* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfWithStmt();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nesting_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nesting_level
  // case: toBeCopied == COPY_DATA for nesting_level
     int nesting_level_copy = p_nesting_level; 
     result->p_nesting_level = nesting_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_overall_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for overall_offset
  // case: toBeCopied == COPY_DATA for overall_offset
     uint64_t overall_offset_copy = p_overall_offset; 
     result->p_overall_offset = overall_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_source_position
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for source_position
  // case: toBeCopied == COPY_DATA for source_position
     SgAsmDwarfLine* source_position_copy = p_source_position; 
     result->p_source_position = source_position_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_file_id
  // case: toBeCopied == COPY_DATA for decl_file_id
     int decl_file_id_copy = p_decl_file_id; 
     result->p_decl_file_id = decl_file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_line
  // case: toBeCopied == COPY_DATA for decl_line
     int decl_line_copy = p_decl_line; 
     result->p_decl_line = decl_line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_decl_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for decl_column
  // case: toBeCopied == COPY_DATA for decl_column
     int decl_column_copy = p_decl_column; 
     result->p_decl_column = decl_column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170414 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfConstructList::copy(SgCopyHelp& help) const {
    SgAsmDwarfConstructList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfConstructList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_list
     SgAsmDwarfConstructPtrList list_copy; 
  // case: listType for list
  // case: listType (typeIsPointerToList == false) for list
    auto source_list_iterator = get_list().begin();
    for (/* empty by design */; source_list_iterator != get_list().end(); ++source_list_iterator) {
          SgAsmDwarfConstruct* source_list_element = *source_list_iterator; 
          SgAsmDwarfConstruct* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmDwarfConstruct*>(help.copyAst(*source_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          list_copy.push_back(copy_list_element); 
        }
     result->p_list = list_copy; 
  // case: listType for list
  // case: listType (typeIsPointerToList == false) for list
    auto list_iterator = result->get_list().begin();
    for (/* empty by design */; list_iterator != result->get_list().end(); ++list_iterator) {
          SgAsmDwarfConstruct* list_element = *list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170533 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLine::copy(SgCopyHelp& help) const {
    SgAsmDwarfLine* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfLine();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     uint64_t address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_file_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file_id
  // case: toBeCopied == COPY_DATA for file_id
     int file_id_copy = p_file_id; 
     result->p_file_id = file_id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_line
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line
  // case: toBeCopied == COPY_DATA for line
     int line_copy = p_line; 
     result->p_line = line_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_column
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for column
  // case: toBeCopied == COPY_DATA for column
     int column_copy = p_column; 
     result->p_column = column_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170644 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfLineList::copy(SgCopyHelp& help) const {
    SgAsmDwarfLineList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfLineList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_line_list
     SgAsmDwarfLinePtrList line_list_copy; 
  // case: listType for line_list
  // case: listType (typeIsPointerToList == false) for line_list
    auto source_line_list_iterator = get_line_list().begin();
    for (/* empty by design */; source_line_list_iterator != get_line_list().end(); ++source_line_list_iterator) {
          SgAsmDwarfLine* source_list_element = *source_line_list_iterator; 
          SgAsmDwarfLine* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmDwarfLine*>(help.copyAst(*source_line_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          line_list_copy.push_back(copy_list_element); 
        }
     result->p_line_list = line_list_copy; 
  // case: listType for line_list
  // case: listType (typeIsPointerToList == false) for line_list
    auto line_list_iterator = result->get_line_list().begin();
    for (/* empty by design */; line_list_iterator != result->get_line_list().end(); ++line_list_iterator) {
          SgAsmDwarfLine* list_element = *line_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170763 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMacro::copy(SgCopyHelp& help) const {
    SgAsmDwarfMacro* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfMacro();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_macro_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for macro_string
  // case: toBeCopied == COPY_DATA for macro_string
     std::string macro_string_copy = p_macro_string; 
     result->p_macro_string = macro_string_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170859 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDwarfMacroList::copy(SgCopyHelp& help) const {
    SgAsmDwarfMacroList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDwarfMacroList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_macro_list
     SgAsmDwarfMacroPtrList macro_list_copy; 
  // case: listType for macro_list
  // case: listType (typeIsPointerToList == false) for macro_list
    auto source_macro_list_iterator = get_macro_list().begin();
    for (/* empty by design */; source_macro_list_iterator != get_macro_list().end(); ++source_macro_list_iterator) {
          SgAsmDwarfMacro* source_list_element = *source_macro_list_iterator; 
          SgAsmDwarfMacro* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmDwarfMacro*>(help.copyAst(*source_macro_list_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          macro_list_copy.push_back(copy_list_element); 
        }
     result->p_macro_list = macro_list_copy; 
  // case: listType for macro_list
  // case: listType (typeIsPointerToList == false) for macro_list
    auto macro_list_iterator = result->get_macro_list().begin();
    for (/* empty by design */; macro_list_iterator != result->get_macro_list().end(); ++macro_list_iterator) {
          SgAsmDwarfMacro* list_element = *macro_list_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 170978 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicEntry::copy(SgCopyHelp& help) const {
    SgAsmElfDynamicEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfDynamicEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_d_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for d_tag
  // case: toBeCopied == COPY_DATA for d_tag
     SgAsmElfDynamicEntry::EntryType d_tag_copy = p_d_tag; 
     result->p_d_tag = d_tag_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_d_val
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for d_val
  // case: toBeCopied == COPY_DATA for d_val
     Rose::BinaryAnalysis::RelativeVirtualAddress d_val_copy = p_d_val; 
     result->p_d_val = d_val_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171102 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfDynamicEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfDynamicEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfDynamicEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171198 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryCI::copy(SgCopyHelp& help) const {
    SgAsmElfEHFrameEntryCI* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfEHFrameEntryCI();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     int version_copy = p_version; 
     result->p_version = version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_augmentation_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_string
  // case: toBeCopied == COPY_DATA for augmentation_string
     std::string augmentation_string_copy = p_augmentation_string; 
     result->p_augmentation_string = augmentation_string_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_eh_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for eh_data
  // case: toBeCopied == COPY_DATA for eh_data
     uint64_t eh_data_copy = p_eh_data; 
     result->p_eh_data = eh_data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_code_alignment_factor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_alignment_factor
  // case: toBeCopied == COPY_DATA for code_alignment_factor
     uint64_t code_alignment_factor_copy = p_code_alignment_factor; 
     result->p_code_alignment_factor = code_alignment_factor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data_alignment_factor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data_alignment_factor
  // case: toBeCopied == COPY_DATA for data_alignment_factor
     int64_t data_alignment_factor_copy = p_data_alignment_factor; 
     result->p_data_alignment_factor = data_alignment_factor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_augmentation_data_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_data_length
  // case: toBeCopied == COPY_DATA for augmentation_data_length
     uint64_t augmentation_data_length_copy = p_augmentation_data_length; 
     result->p_augmentation_data_length = augmentation_data_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lsda_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lsda_encoding
  // case: toBeCopied == COPY_DATA for lsda_encoding
     int lsda_encoding_copy = p_lsda_encoding; 
     result->p_lsda_encoding = lsda_encoding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_prh_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_encoding
  // case: toBeCopied == COPY_DATA for prh_encoding
     int prh_encoding_copy = p_prh_encoding; 
     result->p_prh_encoding = prh_encoding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_prh_arg
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_arg
  // case: toBeCopied == COPY_DATA for prh_arg
     unsigned prh_arg_copy = p_prh_arg; 
     result->p_prh_arg = prh_arg_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_prh_addr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for prh_addr
  // case: toBeCopied == COPY_DATA for prh_addr
     Rose::BinaryAnalysis::Address prh_addr_copy = p_prh_addr; 
     result->p_prh_addr = prh_addr_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_addr_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addr_encoding
  // case: toBeCopied == COPY_DATA for addr_encoding
     int addr_encoding_copy = p_addr_encoding; 
     result->p_addr_encoding = addr_encoding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sig_frame
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sig_frame
  // case: toBeCopied == COPY_DATA for sig_frame
     bool sig_frame_copy = p_sig_frame; 
     result->p_sig_frame = sig_frame_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgUnsignedCharList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fd_entries
     SgAsmElfEHFrameEntryFDList* fd_entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fd_entries
          if (get_fd_entries() != nullptr) 
             { 
               fd_entries_copy = static_cast<SgAsmElfEHFrameEntryFDList*>(help.copyAst(get_fd_entries())); 
             } 
            else 
             { 
               fd_entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fd_entries != nullptr) delete result->p_fd_entries; 
     result->p_fd_entries = fd_entries_copy; 
  // case: not a listType for (using conditionalToSetParent)fd_entries
        if ((fd_entries_copy != nullptr) && (fd_entries_copy->get_parent() == nullptr) && (isSgType(fd_entries_copy) == nullptr)) {
            fd_entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171372 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryCIList::copy(SgCopyHelp& help) const {
    SgAsmElfEHFrameEntryCIList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfEHFrameEntryCIList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfEHFrameEntryCIPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171468 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryFD::copy(SgCopyHelp& help) const {
    SgAsmElfEHFrameEntryFD* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfEHFrameEntryFD();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_begin_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for begin_rva
  // case: toBeCopied == COPY_DATA for begin_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress begin_rva_copy = p_begin_rva; 
     result->p_begin_rva = begin_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_augmentation_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for augmentation_data
  // case: toBeCopied == COPY_DATA for augmentation_data
     SgUnsignedCharList augmentation_data_copy = p_augmentation_data; 
     result->p_augmentation_data = augmentation_data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgUnsignedCharList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171579 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameEntryFDList::copy(SgCopyHelp& help) const {
    SgAsmElfEHFrameEntryFDList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfEHFrameEntryFDList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfEHFrameEntryFDPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171675 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteEntry::copy(SgCopyHelp& help) const {
    SgAsmElfNoteEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfNoteEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     unsigned type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_payload
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for payload
  // case: toBeCopied == COPY_DATA for payload
     SgUnsignedCharList payload_copy = p_payload; 
     result->p_payload = payload_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171794 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfNoteEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfNoteEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmElfNoteEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 171890 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocEntry::copy(SgCopyHelp& help) const {
    SgAsmElfRelocEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfRelocEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_r_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for r_offset
  // case: toBeCopied == COPY_DATA for r_offset
     Rose::BinaryAnalysis::Address r_offset_copy = p_r_offset; 
     result->p_r_offset = r_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_r_addend
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for r_addend
  // case: toBeCopied == COPY_DATA for r_addend
     Rose::BinaryAnalysis::Address r_addend_copy = p_r_addend; 
     result->p_r_addend = r_addend_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sym
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sym
  // case: toBeCopied == COPY_DATA for sym
     unsigned long sym_copy = p_sym; 
     result->p_sym = sym_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmElfRelocEntry::RelocType type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172006 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfRelocEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfRelocEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfRelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfRelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfRelocEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfRelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfRelocEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172125 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSectionTableEntry::copy(SgCopyHelp& help) const {
    SgAsmElfSectionTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSectionTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_sh_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_name
  // case: toBeCopied == COPY_DATA for sh_name
     unsigned sh_name_copy = p_sh_name; 
     result->p_sh_name = sh_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_type
  // case: toBeCopied == COPY_DATA for sh_type
     SgAsmElfSectionTableEntry::SectionType sh_type_copy = p_sh_type; 
     result->p_sh_type = sh_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_link
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_link
  // case: toBeCopied == COPY_DATA for sh_link
     unsigned long sh_link_copy = p_sh_link; 
     result->p_sh_link = sh_link_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_info
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_info
  // case: toBeCopied == COPY_DATA for sh_info
     unsigned long sh_info_copy = p_sh_info; 
     result->p_sh_info = sh_info_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_flags
  // case: toBeCopied == COPY_DATA for sh_flags
     uint64_t sh_flags_copy = p_sh_flags; 
     result->p_sh_flags = sh_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_addr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_addr
  // case: toBeCopied == COPY_DATA for sh_addr
     Rose::BinaryAnalysis::Address sh_addr_copy = p_sh_addr; 
     result->p_sh_addr = sh_addr_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_offset
  // case: toBeCopied == COPY_DATA for sh_offset
     Rose::BinaryAnalysis::Address sh_offset_copy = p_sh_offset; 
     result->p_sh_offset = sh_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_size
  // case: toBeCopied == COPY_DATA for sh_size
     Rose::BinaryAnalysis::Address sh_size_copy = p_sh_size; 
     result->p_sh_size = sh_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_addralign
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_addralign
  // case: toBeCopied == COPY_DATA for sh_addralign
     Rose::BinaryAnalysis::Address sh_addralign_copy = p_sh_addralign; 
     result->p_sh_addralign = sh_addralign_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sh_entsize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sh_entsize
  // case: toBeCopied == COPY_DATA for sh_entsize
     Rose::BinaryAnalysis::Address sh_entsize_copy = p_sh_entsize; 
     result->p_sh_entsize = sh_entsize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172271 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTableEntry::copy(SgCopyHelp& help) const {
    SgAsmElfSegmentTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSegmentTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmElfSegmentTableEntry::SegmentType type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmElfSegmentTableEntry::SegmentFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_vaddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vaddr
  // case: toBeCopied == COPY_DATA for vaddr
     Rose::BinaryAnalysis::Address vaddr_copy = p_vaddr; 
     result->p_vaddr = vaddr_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_paddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for paddr
  // case: toBeCopied == COPY_DATA for paddr
     Rose::BinaryAnalysis::Address paddr_copy = p_paddr; 
     result->p_paddr = paddr_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_filesz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for filesz
  // case: toBeCopied == COPY_DATA for filesz
     Rose::BinaryAnalysis::Address filesz_copy = p_filesz; 
     result->p_filesz = filesz_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_memsz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for memsz
  // case: toBeCopied == COPY_DATA for memsz
     Rose::BinaryAnalysis::Address memsz_copy = p_memsz; 
     result->p_memsz = memsz_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for align
  // case: toBeCopied == COPY_DATA for align
     Rose::BinaryAnalysis::Address align_copy = p_align; 
     result->p_align = align_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172412 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTableEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfSegmentTableEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSegmentTableEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSegmentTableEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSegmentTableEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSegmentTableEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSegmentTableEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172531 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbolList::copy(SgCopyHelp& help) const {
    SgAsmElfSymbolList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymbolList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmElfSymbolPtrList symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
    auto source_symbols_iterator = get_symbols().begin();
    for (/* empty by design */; source_symbols_iterator != get_symbols().end(); ++source_symbols_iterator) {
          SgAsmElfSymbol* source_list_element = *source_symbols_iterator; 
          SgAsmElfSymbol* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymbol*>(help.copyAst(*source_symbols_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          symbols_copy.push_back(copy_list_element); 
        }
     result->p_symbols = symbols_copy; 
  // case: listType for symbols
  // case: listType (typeIsPointerToList == false) for symbols
    auto symbols_iterator = result->get_symbols().begin();
    for (/* empty by design */; symbols_iterator != result->get_symbols().end(); ++symbols_iterator) {
          SgAsmElfSymbol* list_element = *symbols_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172650 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedAux::copy(SgCopyHelp& help) const {
    SgAsmElfSymverDefinedAux* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverDefinedAux();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172759 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedAuxList::copy(SgCopyHelp& help) const {
    SgAsmElfSymverDefinedAuxList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverDefinedAuxList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedAuxPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSymverDefinedAux* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverDefinedAux* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverDefinedAux*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSymverDefinedAux* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 172878 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedEntry::copy(SgCopyHelp& help) const {
    SgAsmElfSymverDefinedEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverDefinedEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     size_t version_copy = p_version; 
     result->p_version = version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     int flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hash
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hash
  // case: toBeCopied == COPY_DATA for hash
     uint32_t hash_copy = p_hash; 
     result->p_hash = hash_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedAuxList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfSymverDefinedAuxList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173007 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfSymverDefinedEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverDefinedEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSymverDefinedEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverDefinedEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverDefinedEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSymverDefinedEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173126 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverEntry::copy(SgCopyHelp& help) const {
    SgAsmElfSymverEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     size_t value_copy = p_value; 
     result->p_value = value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173222 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfSymverEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSymverEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSymverEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173341 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededAux::copy(SgCopyHelp& help) const {
    SgAsmElfSymverNeededAux* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverNeededAux();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_hash
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hash
  // case: toBeCopied == COPY_DATA for hash
     uint32_t hash_copy = p_hash; 
     result->p_hash = hash_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     int flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_other
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for other
  // case: toBeCopied == COPY_DATA for other
     size_t other_copy = p_other; 
     result->p_other = other_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173465 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededAuxList::copy(SgCopyHelp& help) const {
    SgAsmElfSymverNeededAuxList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverNeededAuxList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededAuxPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSymverNeededAux* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverNeededAux* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverNeededAux*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSymverNeededAux* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededEntry::copy(SgCopyHelp& help) const {
    SgAsmElfSymverNeededEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverNeededEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     size_t version_copy = p_version; 
     result->p_version = version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileName
     SgAsmGenericString* fileName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)fileName
          if (get_fileName() != nullptr) 
             { 
               fileName_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_fileName())); 
             } 
            else 
             { 
               fileName_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_fileName != nullptr) delete result->p_fileName; 
     result->p_fileName = fileName_copy; 
  // case: not a listType for (using conditionalToSetParent)fileName
        if ((fileName_copy != nullptr) && (fileName_copy->get_parent() == nullptr) && (isSgType(fileName_copy) == nullptr)) {
            fileName_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededAuxList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfSymverNeededAuxList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173716 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededEntryList::copy(SgCopyHelp& help) const {
    SgAsmElfSymverNeededEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverNeededEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmElfSymverNeededEntry* source_list_element = *source_entries_iterator; 
          SgAsmElfSymverNeededEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmElfSymverNeededEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmElfSymverNeededEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173835 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericDLL::copy(SgCopyHelp& help) const {
    SgAsmGenericDLL* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericDLL();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_symbols
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbols
  // case: toBeCopied == COPY_DATA for symbols
     SgStringList symbols_copy = p_symbols; 
     result->p_symbols = symbols_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 173949 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericDLLList::copy(SgCopyHelp& help) const {
    SgAsmGenericDLLList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericDLLList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_dlls
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dlls
  // case: toBeCopied == COPY_DATA for dlls
     SgAsmGenericDLLPtrList dlls_copy = p_dlls; 
     result->p_dlls = dlls_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174045 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFile::copy(SgCopyHelp& help) const {
    SgAsmGenericFile* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericFile();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_dwarfInfo
     SgAsmDwarfCompilationUnitList* dwarfInfo_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dwarfInfo
          if (get_dwarfInfo() != nullptr) 
             { 
               dwarfInfo_copy = static_cast<SgAsmDwarfCompilationUnitList*>(help.copyAst(get_dwarfInfo())); 
             } 
            else 
             { 
               dwarfInfo_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dwarfInfo != nullptr) delete result->p_dwarfInfo; 
     result->p_dwarfInfo = dwarfInfo_copy; 
  // case: not a listType for (using conditionalToSetParent)dwarfInfo
        if ((dwarfInfo_copy != nullptr) && (dwarfInfo_copy->get_parent() == nullptr) && (isSgType(dwarfInfo_copy) == nullptr)) {
            dwarfInfo_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fd
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fd
  // case: toBeCopied == COPY_DATA for fd
     int fd_copy = p_fd; 
     result->p_fd = fd_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sb
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sb
  // case: toBeCopied == COPY_DATA for sb
     struct stat sb_copy = p_sb; 
     result->p_sb = sb_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_headers
     SgAsmGenericHeaderList* headers_copy; 
  // case: not a listType for (using conditionalToCopyVariable)headers
          if (get_headers() != nullptr) 
             { 
               headers_copy = static_cast<SgAsmGenericHeaderList*>(help.copyAst(get_headers())); 
             } 
            else 
             { 
               headers_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_headers != nullptr) delete result->p_headers; 
     result->p_headers = headers_copy; 
  // case: not a listType for (using conditionalToSetParent)headers
        if ((headers_copy != nullptr) && (headers_copy->get_parent() == nullptr) && (isSgType(headers_copy) == nullptr)) {
            headers_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_holes
     SgAsmGenericSectionList* holes_copy; 
  // case: not a listType for (using conditionalToCopyVariable)holes
          if (get_holes() != nullptr) 
             { 
               holes_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_holes())); 
             } 
            else 
             { 
               holes_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_holes != nullptr) delete result->p_holes; 
     result->p_holes = holes_copy; 
  // case: not a listType for (using conditionalToSetParent)holes
        if ((holes_copy != nullptr) && (holes_copy->get_parent() == nullptr) && (isSgType(holes_copy) == nullptr)) {
            holes_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_truncateZeros
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for truncateZeros
  // case: toBeCopied == COPY_DATA for truncateZeros
     bool truncateZeros_copy = p_truncateZeros; 
     result->p_truncateZeros = truncateZeros_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_trackingReferences
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for trackingReferences
  // case: toBeCopied == COPY_DATA for trackingReferences
     bool trackingReferences_copy = p_trackingReferences; 
     result->p_trackingReferences = trackingReferences_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_referencedExtents
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for referencedExtents
  // case: toBeCopied == COPY_DATA for referencedExtents
     Rose::BinaryAnalysis::AddressIntervalSet referencedExtents_copy = p_referencedExtents; 
     result->p_referencedExtents = referencedExtents_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_neuter
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for neuter
  // case: toBeCopied == COPY_DATA for neuter
     bool neuter_copy = p_neuter; 
     result->p_neuter = neuter_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174230 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFormat::copy(SgCopyHelp& help) const {
    SgAsmGenericFormat* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericFormat();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_family
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for family
  // case: toBeCopied == COPY_DATA for family
     SgAsmGenericFormat::ExecFamily family_copy = p_family; 
     result->p_family = family_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericFormat::ExecPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sex
  // case: toBeCopied == COPY_DATA for sex
     Rose::BinaryAnalysis::ByteOrder::Endianness sex_copy = p_sex; 
     result->p_sex = sex_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for version
  // case: toBeCopied == COPY_DATA for version
     unsigned version_copy = p_version; 
     result->p_version = version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isCurrentVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isCurrentVersion
  // case: toBeCopied == COPY_DATA for isCurrentVersion
     bool isCurrentVersion_copy = p_isCurrentVersion; 
     result->p_isCurrentVersion = isCurrentVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_abi
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abi
  // case: toBeCopied == COPY_DATA for abi
     SgAsmGenericFormat::ExecABI abi_copy = p_abi; 
     result->p_abi = abi_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_abiVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for abiVersion
  // case: toBeCopied == COPY_DATA for abiVersion
     unsigned abiVersion_copy = p_abiVersion; 
     result->p_abiVersion = abiVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_wordSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for wordSize
  // case: toBeCopied == COPY_DATA for wordSize
     size_t wordSize_copy = p_wordSize; 
     result->p_wordSize = wordSize_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174361 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericHeaderList::copy(SgCopyHelp& help) const {
    SgAsmGenericHeaderList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericHeaderList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_headers
     SgAsmGenericHeaderPtrList headers_copy; 
  // case: listType for headers
  // case: listType (typeIsPointerToList == false) for headers
    auto source_headers_iterator = get_headers().begin();
    for (/* empty by design */; source_headers_iterator != get_headers().end(); ++source_headers_iterator) {
          SgAsmGenericHeader* source_list_element = *source_headers_iterator; 
          SgAsmGenericHeader* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmGenericHeader*>(help.copyAst(*source_headers_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          headers_copy.push_back(copy_list_element); 
        }
     result->p_headers = headers_copy; 
  // case: listType for headers
  // case: listType (typeIsPointerToList == false) for headers
    auto headers_iterator = result->get_headers().begin();
    for (/* empty by design */; headers_iterator != result->get_headers().end(); ++headers_iterator) {
          SgAsmGenericHeader* list_element = *headers_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSection::copy(SgCopyHelp& help) const {
    SgAsmGenericSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174666 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCliHeader::copy(SgCopyHelp& help) const {
    SgAsmCliHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCliHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_cb
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cb
  // case: toBeCopied == COPY_DATA for cb
     uint32_t cb_copy = p_cb; 
     result->p_cb = cb_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorRuntimeVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorRuntimeVersion
  // case: toBeCopied == COPY_DATA for majorRuntimeVersion
     uint16_t majorRuntimeVersion_copy = p_majorRuntimeVersion; 
     result->p_majorRuntimeVersion = majorRuntimeVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_minorRuntimeVersion
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorRuntimeVersion
  // case: toBeCopied == COPY_DATA for minorRuntimeVersion
     uint16_t minorRuntimeVersion_copy = p_minorRuntimeVersion; 
     result->p_minorRuntimeVersion = minorRuntimeVersion_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_metaData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for metaData
  // case: toBeCopied == COPY_DATA for metaData
     uint64_t metaData_copy = p_metaData; 
     result->p_metaData = metaData_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     uint32_t flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryPointToken
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryPointToken
  // case: toBeCopied == COPY_DATA for entryPointToken
     uint32_t entryPointToken_copy = p_entryPointToken; 
     result->p_entryPointToken = entryPointToken_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_resources
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for resources
  // case: toBeCopied == COPY_DATA for resources
     uint64_t resources_copy = p_resources; 
     result->p_resources = resources_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_strongNameSignature
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strongNameSignature
  // case: toBeCopied == COPY_DATA for strongNameSignature
     uint64_t strongNameSignature_copy = p_strongNameSignature; 
     result->p_strongNameSignature = strongNameSignature_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_codeManagerTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for codeManagerTable
  // case: toBeCopied == COPY_DATA for codeManagerTable
     uint64_t codeManagerTable_copy = p_codeManagerTable; 
     result->p_codeManagerTable = codeManagerTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_vTableFixups
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vTableFixups
  // case: toBeCopied == COPY_DATA for vTableFixups
     uint64_t vTableFixups_copy = p_vTableFixups; 
     result->p_vTableFixups = vTableFixups_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exportAddressTableJumps
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exportAddressTableJumps
  // case: toBeCopied == COPY_DATA for exportAddressTableJumps
     uint64_t exportAddressTableJumps_copy = p_exportAddressTableJumps; 
     result->p_exportAddressTableJumps = exportAddressTableJumps_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_managedNativeHeader
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for managedNativeHeader
  // case: toBeCopied == COPY_DATA for managedNativeHeader
     uint64_t managedNativeHeader_copy = p_managedNativeHeader; 
     result->p_managedNativeHeader = managedNativeHeader_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_metadataRoot
     SgAsmCilMetadataRoot* metadataRoot_copy; 
  // case: not a listType for (using conditionalToCopyVariable)metadataRoot
          if (get_metadataRoot() != nullptr) 
             { 
               metadataRoot_copy = static_cast<SgAsmCilMetadataRoot*>(help.copyAst(get_metadataRoot())); 
             } 
            else 
             { 
               metadataRoot_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_metadataRoot != nullptr) delete result->p_metadataRoot; 
     result->p_metadataRoot = metadataRoot_copy; 
  // case: not a listType for (using conditionalToSetParent)metadataRoot
        if ((metadataRoot_copy != nullptr) && (metadataRoot_copy->get_parent() == nullptr) && (isSgType(metadataRoot_copy) == nullptr)) {
            metadataRoot_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 174930 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbolTable::copy(SgCopyHelp& help) const {
    SgAsmCoffSymbolTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCoffSymbolTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmGenericSection* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmCoffSymbolList* symbols_copy; 
  // case: not a listType for (using conditionalToCopyVariable)symbols
          if (get_symbols() != nullptr) 
             { 
               symbols_copy = static_cast<SgAsmCoffSymbolList*>(help.copyAst(get_symbols())); 
             } 
            else 
             { 
               symbols_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_symbols != nullptr) delete result->p_symbols; 
     result->p_symbols = symbols_copy; 
  // case: not a listType for (using conditionalToSetParent)symbols
        if ((symbols_copy != nullptr) && (symbols_copy->get_parent() == nullptr) && (isSgType(symbols_copy) == nullptr)) {
            symbols_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 175139 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDOSExtendedHeader::copy(SgCopyHelp& help) const {
    SgAsmDOSExtendedHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDOSExtendedHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_oemid
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_oemid
  // case: toBeCopied == COPY_DATA for e_oemid
     unsigned e_oemid_copy = p_e_oemid; 
     result->p_e_oemid = e_oemid_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_oeminfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_oeminfo
  // case: toBeCopied == COPY_DATA for e_oeminfo
     unsigned e_oeminfo_copy = p_e_oeminfo; 
     result->p_e_oeminfo = e_oeminfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res2
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res2
  // case: toBeCopied == COPY_DATA for e_res2
     unsigned e_res2_copy = p_e_res2; 
     result->p_e_res2 = e_res2_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res3
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res3
  // case: toBeCopied == COPY_DATA for e_res3
     unsigned e_res3_copy = p_e_res3; 
     result->p_e_res3 = e_res3_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res4
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res4
  // case: toBeCopied == COPY_DATA for e_res4
     unsigned e_res4_copy = p_e_res4; 
     result->p_e_res4 = e_res4_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res5
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res5
  // case: toBeCopied == COPY_DATA for e_res5
     unsigned e_res5_copy = p_e_res5; 
     result->p_e_res5 = e_res5_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res6
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res6
  // case: toBeCopied == COPY_DATA for e_res6
     unsigned e_res6_copy = p_e_res6; 
     result->p_e_res6 = e_res6_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_lfanew
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lfanew
  // case: toBeCopied == COPY_DATA for e_lfanew
     Rose::BinaryAnalysis::Address e_lfanew_copy = p_e_lfanew; 
     result->p_e_lfanew = e_lfanew_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 175370 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSection::copy(SgCopyHelp& help) const {
    SgAsmElfSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 175597 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfDynamicSection::copy(SgCopyHelp& help) const {
    SgAsmElfDynamicSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfDynamicSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfDynamicEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfDynamicEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 175842 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfEHFrameSection::copy(SgCopyHelp& help) const {
    SgAsmElfEHFrameSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfEHFrameSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ci_entries
     SgAsmElfEHFrameEntryCIList* ci_entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)ci_entries
          if (get_ci_entries() != nullptr) 
             { 
               ci_entries_copy = static_cast<SgAsmElfEHFrameEntryCIList*>(help.copyAst(get_ci_entries())); 
             } 
            else 
             { 
               ci_entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_ci_entries != nullptr) delete result->p_ci_entries; 
     result->p_ci_entries = ci_entries_copy; 
  // case: not a listType for (using conditionalToSetParent)ci_entries
        if ((ci_entries_copy != nullptr) && (ci_entries_copy->get_parent() == nullptr) && (isSgType(ci_entries_copy) == nullptr)) {
            ci_entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 176087 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfNoteSection::copy(SgCopyHelp& help) const {
    SgAsmElfNoteSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfNoteSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfNoteEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfNoteEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 176332 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfRelocSection::copy(SgCopyHelp& help) const {
    SgAsmElfRelocSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfRelocSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_usesAddend
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for usesAddend
  // case: toBeCopied == COPY_DATA for usesAddend
     bool usesAddend_copy = p_usesAddend; 
     result->p_usesAddend = usesAddend_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_targetSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for targetSection
  // case: toBeCopied == COPY_DATA for targetSection
     SgAsmElfSection* targetSection_copy = p_targetSection; 
     result->p_targetSection = targetSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfRelocEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfRelocEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 176587 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfStringSection::copy(SgCopyHelp& help) const {
    SgAsmElfStringSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfStringSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmElfStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 176819 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbolSection::copy(SgCopyHelp& help) const {
    SgAsmElfSymbolSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymbolSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_isDynamic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isDynamic
  // case: toBeCopied == COPY_DATA for isDynamic
     bool isDynamic_copy = p_isDynamic; 
     result->p_isDynamic = isDynamic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbols
     SgAsmElfSymbolList* symbols_copy; 
  // case: not a listType for (using conditionalToCopyVariable)symbols
          if (get_symbols() != nullptr) 
             { 
               symbols_copy = static_cast<SgAsmElfSymbolList*>(help.copyAst(get_symbols())); 
             } 
            else 
             { 
               symbols_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_symbols != nullptr) delete result->p_symbols; 
     result->p_symbols = symbols_copy; 
  // case: not a listType for (using conditionalToSetParent)symbols
        if ((symbols_copy != nullptr) && (symbols_copy->get_parent() == nullptr) && (isSgType(symbols_copy) == nullptr)) {
            symbols_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 177069 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverDefinedSection::copy(SgCopyHelp& help) const {
    SgAsmElfSymverDefinedSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverDefinedSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverDefinedEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfSymverDefinedEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 177314 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverNeededSection::copy(SgCopyHelp& help) const {
    SgAsmElfSymverNeededSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverNeededSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverNeededEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfSymverNeededEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 177559 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymverSection::copy(SgCopyHelp& help) const {
    SgAsmElfSymverSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymverSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_linkedSection
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for linkedSection
  // case: toBeCopied == COPY_DATA for linkedSection
     SgAsmElfSection* linkedSection_copy = p_linkedSection; 
     result->p_linkedSection = linkedSection_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionEntry
     SgAsmElfSectionTableEntry* sectionEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionEntry
          if (get_sectionEntry() != nullptr) 
             { 
               sectionEntry_copy = static_cast<SgAsmElfSectionTableEntry*>(help.copyAst(get_sectionEntry())); 
             } 
            else 
             { 
               sectionEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionEntry != nullptr) delete result->p_sectionEntry; 
     result->p_sectionEntry = sectionEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionEntry
        if ((sectionEntry_copy != nullptr) && (sectionEntry_copy->get_parent() == nullptr) && (isSgType(sectionEntry_copy) == nullptr)) {
            sectionEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segmentEntry
     SgAsmElfSegmentTableEntry* segmentEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segmentEntry
          if (get_segmentEntry() != nullptr) 
             { 
               segmentEntry_copy = static_cast<SgAsmElfSegmentTableEntry*>(help.copyAst(get_segmentEntry())); 
             } 
            else 
             { 
               segmentEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segmentEntry != nullptr) delete result->p_segmentEntry; 
     result->p_segmentEntry = segmentEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)segmentEntry
        if ((segmentEntry_copy != nullptr) && (segmentEntry_copy->get_parent() == nullptr) && (isSgType(segmentEntry_copy) == nullptr)) {
            segmentEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmElfSymverEntryList* entries_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entries
          if (get_entries() != nullptr) 
             { 
               entries_copy = static_cast<SgAsmElfSymverEntryList*>(help.copyAst(get_entries())); 
             } 
            else 
             { 
               entries_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entries != nullptr) delete result->p_entries; 
     result->p_entries = entries_copy; 
  // case: not a listType for (using conditionalToSetParent)entries
        if ((entries_copy != nullptr) && (entries_copy->get_parent() == nullptr) && (isSgType(entries_copy) == nullptr)) {
            entries_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 177804 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSectionTable::copy(SgCopyHelp& help) const {
    SgAsmElfSectionTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSectionTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 177990 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSegmentTable::copy(SgCopyHelp& help) const {
    SgAsmElfSegmentTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSegmentTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 178176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericHeader::copy(SgCopyHelp& help) const {
    SgAsmGenericHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 178436 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDOSFileHeader::copy(SgCopyHelp& help) const {
    SgAsmDOSFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDOSFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_e_last_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_last_page_size
  // case: toBeCopied == COPY_DATA for e_last_page_size
     uint16_t e_last_page_size_copy = p_e_last_page_size; 
     result->p_e_last_page_size = e_last_page_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_total_pages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_total_pages
  // case: toBeCopied == COPY_DATA for e_total_pages
     uint16_t e_total_pages_copy = p_e_total_pages; 
     result->p_e_total_pages = e_total_pages_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nrelocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nrelocs
  // case: toBeCopied == COPY_DATA for e_nrelocs
     uint16_t e_nrelocs_copy = p_e_nrelocs; 
     result->p_e_nrelocs = e_nrelocs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_header_paragraphs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_header_paragraphs
  // case: toBeCopied == COPY_DATA for e_header_paragraphs
     uint16_t e_header_paragraphs_copy = p_e_header_paragraphs; 
     result->p_e_header_paragraphs = e_header_paragraphs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_minalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_minalloc
  // case: toBeCopied == COPY_DATA for e_minalloc
     uint16_t e_minalloc_copy = p_e_minalloc; 
     result->p_e_minalloc = e_minalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_maxalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_maxalloc
  // case: toBeCopied == COPY_DATA for e_maxalloc
     uint16_t e_maxalloc_copy = p_e_maxalloc; 
     result->p_e_maxalloc = e_maxalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ss
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ss
  // case: toBeCopied == COPY_DATA for e_ss
     uint16_t e_ss_copy = p_e_ss; 
     result->p_e_ss = e_ss_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_sp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sp
  // case: toBeCopied == COPY_DATA for e_sp
     uint16_t e_sp_copy = p_e_sp; 
     result->p_e_sp = e_sp_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cksum
  // case: toBeCopied == COPY_DATA for e_cksum
     uint16_t e_cksum_copy = p_e_cksum; 
     result->p_e_cksum = e_cksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ip
  // case: toBeCopied == COPY_DATA for e_ip
     uint16_t e_ip_copy = p_e_ip; 
     result->p_e_ip = e_ip_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_cs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cs
  // case: toBeCopied == COPY_DATA for e_cs
     uint16_t e_cs_copy = p_e_cs; 
     result->p_e_cs = e_cs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_overlay
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_overlay
  // case: toBeCopied == COPY_DATA for e_overlay
     uint16_t e_overlay_copy = p_e_overlay; 
     result->p_e_overlay = e_overlay_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_relocs_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_relocs_offset
  // case: toBeCopied == COPY_DATA for e_relocs_offset
     Rose::BinaryAnalysis::Address e_relocs_offset_copy = p_e_relocs_offset; 
     result->p_e_relocs_offset = e_relocs_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_relocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for relocs
  // case: toBeCopied == COPY_DATA for relocs
     SgAsmGenericSection* relocs_copy = p_relocs; 
     result->p_relocs = relocs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rm_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rm_section
  // case: toBeCopied == COPY_DATA for rm_section
     SgAsmGenericSection* rm_section_copy = p_rm_section; 
     result->p_rm_section = rm_section_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 178776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfFileHeader::copy(SgCopyHelp& help) const {
    SgAsmElfFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_e_ident_file_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_file_class
  // case: toBeCopied == COPY_DATA for e_ident_file_class
     unsigned char e_ident_file_class_copy = p_e_ident_file_class; 
     result->p_e_ident_file_class = e_ident_file_class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ident_data_encoding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_data_encoding
  // case: toBeCopied == COPY_DATA for e_ident_data_encoding
     unsigned char e_ident_data_encoding_copy = p_e_ident_data_encoding; 
     result->p_e_ident_data_encoding = e_ident_data_encoding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ident_file_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_file_version
  // case: toBeCopied == COPY_DATA for e_ident_file_version
     unsigned char e_ident_file_version_copy = p_e_ident_file_version; 
     result->p_e_ident_file_version = e_ident_file_version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ident_padding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ident_padding
  // case: toBeCopied == COPY_DATA for e_ident_padding
     SgUnsignedCharList e_ident_padding_copy = p_e_ident_padding; 
     result->p_e_ident_padding = e_ident_padding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_type
  // case: toBeCopied == COPY_DATA for e_type
     unsigned long e_type_copy = p_e_type; 
     result->p_e_type = e_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_machine
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_machine
  // case: toBeCopied == COPY_DATA for e_machine
     unsigned long e_machine_copy = p_e_machine; 
     result->p_e_machine = e_machine_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned long e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ehsize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ehsize
  // case: toBeCopied == COPY_DATA for e_ehsize
     unsigned long e_ehsize_copy = p_e_ehsize; 
     result->p_e_ehsize = e_ehsize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_phextrasz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for phextrasz
  // case: toBeCopied == COPY_DATA for phextrasz
     unsigned long phextrasz_copy = p_phextrasz; 
     result->p_phextrasz = phextrasz_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_phnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_phnum
  // case: toBeCopied == COPY_DATA for e_phnum
     unsigned long e_phnum_copy = p_e_phnum; 
     result->p_e_phnum = e_phnum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shextrasz
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shextrasz
  // case: toBeCopied == COPY_DATA for shextrasz
     unsigned long shextrasz_copy = p_shextrasz; 
     result->p_shextrasz = shextrasz_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_shnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_shnum
  // case: toBeCopied == COPY_DATA for e_shnum
     unsigned long e_shnum_copy = p_e_shnum; 
     result->p_e_shnum = e_shnum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_shstrndx
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_shstrndx
  // case: toBeCopied == COPY_DATA for e_shstrndx
     unsigned long e_shstrndx_copy = p_e_shstrndx; 
     result->p_e_shstrndx = e_shstrndx_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmElfSectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_segmentTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for segmentTable
  // case: toBeCopied == COPY_DATA for segmentTable
     SgAsmElfSegmentTable* segmentTable_copy = p_segmentTable; 
     result->p_segmentTable = segmentTable_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 179111 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmFileHeader::copy(SgCopyHelp& help) const {
    SgAsmJvmFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_minor_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minor_version
  // case: toBeCopied == COPY_DATA for minor_version
     uint16_t minor_version_copy = p_minor_version; 
     result->p_minor_version = minor_version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_major_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for major_version
  // case: toBeCopied == COPY_DATA for major_version
     uint16_t major_version_copy = p_major_version; 
     result->p_major_version = major_version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_this_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for this_class
  // case: toBeCopied == COPY_DATA for this_class
     uint16_t this_class_copy = p_this_class; 
     result->p_this_class = this_class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_super_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for super_class
  // case: toBeCopied == COPY_DATA for super_class
     uint16_t super_class_copy = p_super_class; 
     result->p_super_class = super_class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_constant_pool
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for constant_pool
  // case: toBeCopied == COPY_DATA for constant_pool
     SgAsmJvmConstantPool* constant_pool_copy = p_constant_pool; 
     result->p_constant_pool = constant_pool_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_interfaces
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for interfaces
  // case: toBeCopied == COPY_DATA for interfaces
     std::list<uint16_t> interfaces_copy = p_interfaces; 
     result->p_interfaces = interfaces_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_field_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for field_table
  // case: toBeCopied == COPY_DATA for field_table
     SgAsmJvmFieldTable* field_table_copy = p_field_table; 
     result->p_field_table = field_table_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_method_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for method_table
  // case: toBeCopied == COPY_DATA for method_table
     SgAsmJvmMethodTable* method_table_copy = p_method_table; 
     result->p_method_table = method_table_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_table
  // case: toBeCopied == COPY_DATA for attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy = p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 179421 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEFileHeader::copy(SgCopyHelp& help) const {
    SgAsmLEFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLEFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_e_byte_order
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_byte_order
  // case: toBeCopied == COPY_DATA for e_byte_order
     unsigned e_byte_order_copy = p_e_byte_order; 
     result->p_e_byte_order = e_byte_order_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_word_order
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_word_order
  // case: toBeCopied == COPY_DATA for e_word_order
     unsigned e_word_order_copy = p_e_word_order; 
     result->p_e_word_order = e_word_order_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_format_level
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_format_level
  // case: toBeCopied == COPY_DATA for e_format_level
     unsigned e_format_level_copy = p_e_format_level; 
     result->p_e_format_level = e_format_level_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_cpu_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cpu_type
  // case: toBeCopied == COPY_DATA for e_cpu_type
     unsigned e_cpu_type_copy = p_e_cpu_type; 
     result->p_e_cpu_type = e_cpu_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_os_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_type
  // case: toBeCopied == COPY_DATA for e_os_type
     unsigned e_os_type_copy = p_e_os_type; 
     result->p_e_os_type = e_os_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_module_version
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_module_version
  // case: toBeCopied == COPY_DATA for e_module_version
     unsigned e_module_version_copy = p_e_module_version; 
     result->p_e_module_version = e_module_version_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_eip_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_eip_section
  // case: toBeCopied == COPY_DATA for e_eip_section
     unsigned e_eip_section_copy = p_e_eip_section; 
     result->p_e_eip_section = e_eip_section_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_esp_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_esp_section
  // case: toBeCopied == COPY_DATA for e_esp_section
     unsigned e_esp_section_copy = p_e_esp_section; 
     result->p_e_esp_section = e_esp_section_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_last_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_last_page_size
  // case: toBeCopied == COPY_DATA for e_last_page_size
     unsigned e_last_page_size_copy = p_e_last_page_size; 
     result->p_e_last_page_size = e_last_page_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_page_offset_shift
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_page_offset_shift
  // case: toBeCopied == COPY_DATA for e_page_offset_shift
     unsigned e_page_offset_shift_copy = p_e_page_offset_shift; 
     result->p_e_page_offset_shift = e_page_offset_shift_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fixup_sect_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_sect_cksum
  // case: toBeCopied == COPY_DATA for e_fixup_sect_cksum
     unsigned e_fixup_sect_cksum_copy = p_e_fixup_sect_cksum; 
     result->p_e_fixup_sect_cksum = e_fixup_sect_cksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_loader_sect_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_sect_cksum
  // case: toBeCopied == COPY_DATA for e_loader_sect_cksum
     unsigned e_loader_sect_cksum_copy = p_e_loader_sect_cksum; 
     result->p_e_loader_sect_cksum = e_loader_sect_cksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_secttab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_secttab_nentries
  // case: toBeCopied == COPY_DATA for e_secttab_nentries
     unsigned e_secttab_nentries_copy = p_e_secttab_nentries; 
     result->p_e_secttab_nentries = e_secttab_nentries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_nentries
  // case: toBeCopied == COPY_DATA for e_rsrctab_nentries
     unsigned e_rsrctab_nentries_copy = p_e_rsrctab_nentries; 
     result->p_e_rsrctab_nentries = e_rsrctab_nentries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fmtdirtab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fmtdirtab_nentries
  // case: toBeCopied == COPY_DATA for e_fmtdirtab_nentries
     unsigned e_fmtdirtab_nentries_copy = p_e_fmtdirtab_nentries; 
     result->p_e_fmtdirtab_nentries = e_fmtdirtab_nentries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_import_modtab_nentries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_modtab_nentries
  // case: toBeCopied == COPY_DATA for e_import_modtab_nentries
     unsigned e_import_modtab_nentries_copy = p_e_import_modtab_nentries; 
     result->p_e_import_modtab_nentries = e_import_modtab_nentries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_preload_npages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_preload_npages
  // case: toBeCopied == COPY_DATA for e_preload_npages
     unsigned e_preload_npages_copy = p_e_preload_npages; 
     result->p_e_preload_npages = e_preload_npages_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_size
  // case: toBeCopied == COPY_DATA for e_nonresnametab_size
     unsigned e_nonresnametab_size_copy = p_e_nonresnametab_size; 
     result->p_e_nonresnametab_size = e_nonresnametab_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_cksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_cksum
  // case: toBeCopied == COPY_DATA for e_nonresnametab_cksum
     unsigned e_nonresnametab_cksum_copy = p_e_nonresnametab_cksum; 
     result->p_e_nonresnametab_cksum = e_nonresnametab_cksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_auto_ds_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_auto_ds_section
  // case: toBeCopied == COPY_DATA for e_auto_ds_section
     unsigned e_auto_ds_section_copy = p_e_auto_ds_section; 
     result->p_e_auto_ds_section = e_auto_ds_section_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_debug_info_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_debug_info_size
  // case: toBeCopied == COPY_DATA for e_debug_info_size
     unsigned e_debug_info_size_copy = p_e_debug_info_size; 
     result->p_e_debug_info_size = e_debug_info_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_num_instance_preload
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_instance_preload
  // case: toBeCopied == COPY_DATA for e_num_instance_preload
     unsigned e_num_instance_preload_copy = p_e_num_instance_preload; 
     result->p_e_num_instance_preload = e_num_instance_preload_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_num_instance_demand
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_instance_demand
  // case: toBeCopied == COPY_DATA for e_num_instance_demand
     unsigned e_num_instance_demand_copy = p_e_num_instance_demand; 
     result->p_e_num_instance_demand = e_num_instance_demand_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_heap_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_size
  // case: toBeCopied == COPY_DATA for e_heap_size
     unsigned e_heap_size_copy = p_e_heap_size; 
     result->p_e_heap_size = e_heap_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_npages
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_npages
  // case: toBeCopied == COPY_DATA for e_npages
     Rose::BinaryAnalysis::Address e_npages_copy = p_e_npages; 
     result->p_e_npages = e_npages_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_eip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_eip
  // case: toBeCopied == COPY_DATA for e_eip
     Rose::BinaryAnalysis::Address e_eip_copy = p_e_eip; 
     result->p_e_eip = e_eip_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_esp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_esp
  // case: toBeCopied == COPY_DATA for e_esp
     Rose::BinaryAnalysis::Address e_esp_copy = p_e_esp; 
     result->p_e_esp = e_esp_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_page_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_page_size
  // case: toBeCopied == COPY_DATA for e_page_size
     Rose::BinaryAnalysis::Address e_page_size_copy = p_e_page_size; 
     result->p_e_page_size = e_page_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fixup_sect_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_sect_size
  // case: toBeCopied == COPY_DATA for e_fixup_sect_size
     Rose::BinaryAnalysis::Address e_fixup_sect_size_copy = p_e_fixup_sect_size; 
     result->p_e_fixup_sect_size = e_fixup_sect_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_loader_sect_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_sect_size
  // case: toBeCopied == COPY_DATA for e_loader_sect_size
     Rose::BinaryAnalysis::Address e_loader_sect_size_copy = p_e_loader_sect_size; 
     result->p_e_loader_sect_size = e_loader_sect_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_secttab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_secttab_rfo
  // case: toBeCopied == COPY_DATA for e_secttab_rfo
     Rose::BinaryAnalysis::Address e_secttab_rfo_copy = p_e_secttab_rfo; 
     result->p_e_secttab_rfo = e_secttab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_pagetab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_pagetab_rfo
  // case: toBeCopied == COPY_DATA for e_pagetab_rfo
     Rose::BinaryAnalysis::Address e_pagetab_rfo_copy = p_e_pagetab_rfo; 
     result->p_e_pagetab_rfo = e_pagetab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_iterpages_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_iterpages_offset
  // case: toBeCopied == COPY_DATA for e_iterpages_offset
     Rose::BinaryAnalysis::Address e_iterpages_offset_copy = p_e_iterpages_offset; 
     result->p_e_iterpages_offset = e_iterpages_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_rfo
  // case: toBeCopied == COPY_DATA for e_rsrctab_rfo
     Rose::BinaryAnalysis::Address e_rsrctab_rfo_copy = p_e_rsrctab_rfo; 
     result->p_e_rsrctab_rfo = e_rsrctab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_resnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_resnametab_rfo
  // case: toBeCopied == COPY_DATA for e_resnametab_rfo
     Rose::BinaryAnalysis::Address e_resnametab_rfo_copy = p_e_resnametab_rfo; 
     result->p_e_resnametab_rfo = e_resnametab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_rfo
  // case: toBeCopied == COPY_DATA for e_entrytab_rfo
     Rose::BinaryAnalysis::Address e_entrytab_rfo_copy = p_e_entrytab_rfo; 
     result->p_e_entrytab_rfo = e_entrytab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fmtdirtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fmtdirtab_rfo
  // case: toBeCopied == COPY_DATA for e_fmtdirtab_rfo
     Rose::BinaryAnalysis::Address e_fmtdirtab_rfo_copy = p_e_fmtdirtab_rfo; 
     result->p_e_fmtdirtab_rfo = e_fmtdirtab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fixup_pagetab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_pagetab_rfo
  // case: toBeCopied == COPY_DATA for e_fixup_pagetab_rfo
     Rose::BinaryAnalysis::Address e_fixup_pagetab_rfo_copy = p_e_fixup_pagetab_rfo; 
     result->p_e_fixup_pagetab_rfo = e_fixup_pagetab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fixup_rectab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fixup_rectab_rfo
  // case: toBeCopied == COPY_DATA for e_fixup_rectab_rfo
     Rose::BinaryAnalysis::Address e_fixup_rectab_rfo_copy = p_e_fixup_rectab_rfo; 
     result->p_e_fixup_rectab_rfo = e_fixup_rectab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_import_modtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_modtab_rfo
  // case: toBeCopied == COPY_DATA for e_import_modtab_rfo
     Rose::BinaryAnalysis::Address e_import_modtab_rfo_copy = p_e_import_modtab_rfo; 
     result->p_e_import_modtab_rfo = e_import_modtab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_import_proctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_import_proctab_rfo
  // case: toBeCopied == COPY_DATA for e_import_proctab_rfo
     Rose::BinaryAnalysis::Address e_import_proctab_rfo_copy = p_e_import_proctab_rfo; 
     result->p_e_import_proctab_rfo = e_import_proctab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_ppcksumtab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_ppcksumtab_rfo
  // case: toBeCopied == COPY_DATA for e_ppcksumtab_rfo
     Rose::BinaryAnalysis::Address e_ppcksumtab_rfo_copy = p_e_ppcksumtab_rfo; 
     result->p_e_ppcksumtab_rfo = e_ppcksumtab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_data_pages_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_pages_offset
  // case: toBeCopied == COPY_DATA for e_data_pages_offset
     Rose::BinaryAnalysis::Address e_data_pages_offset_copy = p_e_data_pages_offset; 
     result->p_e_data_pages_offset = e_data_pages_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_offset
  // case: toBeCopied == COPY_DATA for e_nonresnametab_offset
     Rose::BinaryAnalysis::Address e_nonresnametab_offset_copy = p_e_nonresnametab_offset; 
     result->p_e_nonresnametab_offset = e_nonresnametab_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_debug_info_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_debug_info_rfo
  // case: toBeCopied == COPY_DATA for e_debug_info_rfo
     Rose::BinaryAnalysis::Address e_debug_info_rfo_copy = p_e_debug_info_rfo; 
     result->p_e_debug_info_rfo = e_debug_info_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dos2Header
     SgAsmDOSExtendedHeader* dos2Header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dos2Header
          if (get_dos2Header() != nullptr) 
             { 
               dos2Header_copy = static_cast<SgAsmDOSExtendedHeader*>(help.copyAst(get_dos2Header())); 
             } 
            else 
             { 
               dos2Header_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dos2Header != nullptr) delete result->p_dos2Header; 
     result->p_dos2Header = dos2Header_copy; 
  // case: not a listType for (using conditionalToSetParent)dos2Header
        if ((dos2Header_copy != nullptr) && (dos2Header_copy->get_parent() == nullptr) && (isSgType(dos2Header_copy) == nullptr)) {
            dos2Header_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmLESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_pageTable
     SgAsmLEPageTable* pageTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)pageTable
          if (get_pageTable() != nullptr) 
             { 
               pageTable_copy = static_cast<SgAsmLEPageTable*>(help.copyAst(get_pageTable())); 
             } 
            else 
             { 
               pageTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_pageTable != nullptr) delete result->p_pageTable; 
     result->p_pageTable = pageTable_copy; 
  // case: not a listType for (using conditionalToSetParent)pageTable
        if ((pageTable_copy != nullptr) && (pageTable_copy->get_parent() == nullptr) && (isSgType(pageTable_copy) == nullptr)) {
            pageTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_residentNameTable
     SgAsmLENameTable* residentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)residentNameTable
          if (get_residentNameTable() != nullptr) 
             { 
               residentNameTable_copy = static_cast<SgAsmLENameTable*>(help.copyAst(get_residentNameTable())); 
             } 
            else 
             { 
               residentNameTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_residentNameTable != nullptr) delete result->p_residentNameTable; 
     result->p_residentNameTable = residentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)residentNameTable
        if ((residentNameTable_copy != nullptr) && (residentNameTable_copy->get_parent() == nullptr) && (isSgType(residentNameTable_copy) == nullptr)) {
            residentNameTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_nonresidentNameTable
     SgAsmLENameTable* nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)nonresidentNameTable
          if (get_nonresidentNameTable() != nullptr) 
             { 
               nonresidentNameTable_copy = static_cast<SgAsmLENameTable*>(help.copyAst(get_nonresidentNameTable())); 
             } 
            else 
             { 
               nonresidentNameTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_nonresidentNameTable != nullptr) delete result->p_nonresidentNameTable; 
     result->p_nonresidentNameTable = nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)nonresidentNameTable
        if ((nonresidentNameTable_copy != nullptr) && (nonresidentNameTable_copy->get_parent() == nullptr) && (isSgType(nonresidentNameTable_copy) == nullptr)) {
            nonresidentNameTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entryTable
     SgAsmLEEntryTable* entryTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryTable
          if (get_entryTable() != nullptr) 
             { 
               entryTable_copy = static_cast<SgAsmLEEntryTable*>(help.copyAst(get_entryTable())); 
             } 
            else 
             { 
               entryTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryTable != nullptr) delete result->p_entryTable; 
     result->p_entryTable = entryTable_copy; 
  // case: not a listType for (using conditionalToSetParent)entryTable
        if ((entryTable_copy != nullptr) && (entryTable_copy->get_parent() == nullptr) && (isSgType(entryTable_copy) == nullptr)) {
            entryTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_relocationTable
     SgAsmLERelocTable* relocationTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)relocationTable
          if (get_relocationTable() != nullptr) 
             { 
               relocationTable_copy = static_cast<SgAsmLERelocTable*>(help.copyAst(get_relocationTable())); 
             } 
            else 
             { 
               relocationTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_relocationTable != nullptr) delete result->p_relocationTable; 
     result->p_relocationTable = relocationTable_copy; 
  // case: not a listType for (using conditionalToSetParent)relocationTable
        if ((relocationTable_copy != nullptr) && (relocationTable_copy->get_parent() == nullptr) && (isSgType(relocationTable_copy) == nullptr)) {
            relocationTable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 180024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEFileHeader::copy(SgCopyHelp& help) const {
    SgAsmNEFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNEFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_e_linker_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_linker_major
  // case: toBeCopied == COPY_DATA for e_linker_major
     unsigned e_linker_major_copy = p_e_linker_major; 
     result->p_e_linker_major = e_linker_major_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_linker_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_linker_minor
  // case: toBeCopied == COPY_DATA for e_linker_minor
     unsigned e_linker_minor_copy = p_e_linker_minor; 
     result->p_e_linker_minor = e_linker_minor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_checksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_checksum
  // case: toBeCopied == COPY_DATA for e_checksum
     unsigned e_checksum_copy = p_e_checksum; 
     result->p_e_checksum = e_checksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_flags1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags1
  // case: toBeCopied == COPY_DATA for e_flags1
     unsigned e_flags1_copy = p_e_flags1; 
     result->p_e_flags1 = e_flags1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_autodata_sn
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_autodata_sn
  // case: toBeCopied == COPY_DATA for e_autodata_sn
     unsigned e_autodata_sn_copy = p_e_autodata_sn; 
     result->p_e_autodata_sn = e_autodata_sn_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_bss_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_bss_size
  // case: toBeCopied == COPY_DATA for e_bss_size
     unsigned e_bss_size_copy = p_e_bss_size; 
     result->p_e_bss_size = e_bss_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_stack_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_size
  // case: toBeCopied == COPY_DATA for e_stack_size
     unsigned e_stack_size_copy = p_e_stack_size; 
     result->p_e_stack_size = e_stack_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_csip
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_csip
  // case: toBeCopied == COPY_DATA for e_csip
     unsigned e_csip_copy = p_e_csip; 
     result->p_e_csip = e_csip_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_sssp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sssp
  // case: toBeCopied == COPY_DATA for e_sssp
     unsigned e_sssp_copy = p_e_sssp; 
     result->p_e_sssp = e_sssp_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nsections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nsections
  // case: toBeCopied == COPY_DATA for e_nsections
     unsigned e_nsections_copy = p_e_nsections; 
     result->p_e_nsections = e_nsections_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nmodrefs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nmodrefs
  // case: toBeCopied == COPY_DATA for e_nmodrefs
     unsigned e_nmodrefs_copy = p_e_nmodrefs; 
     result->p_e_nmodrefs = e_nmodrefs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nnonresnames
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nnonresnames
  // case: toBeCopied == COPY_DATA for e_nnonresnames
     unsigned e_nnonresnames_copy = p_e_nnonresnames; 
     result->p_e_nnonresnames = e_nnonresnames_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nmovable_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nmovable_entries
  // case: toBeCopied == COPY_DATA for e_nmovable_entries
     unsigned e_nmovable_entries_copy = p_e_nmovable_entries; 
     result->p_e_nmovable_entries = e_nmovable_entries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_sector_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sector_align
  // case: toBeCopied == COPY_DATA for e_sector_align
     unsigned e_sector_align_copy = p_e_sector_align; 
     result->p_e_sector_align = e_sector_align_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nresources
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nresources
  // case: toBeCopied == COPY_DATA for e_nresources
     unsigned e_nresources_copy = p_e_nresources; 
     result->p_e_nresources = e_nresources_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_exetype
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_exetype
  // case: toBeCopied == COPY_DATA for e_exetype
     unsigned e_exetype_copy = p_e_exetype; 
     result->p_e_exetype = e_exetype_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_flags2
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags2
  // case: toBeCopied == COPY_DATA for e_flags2
     unsigned e_flags2_copy = p_e_flags2; 
     result->p_e_flags2 = e_flags2_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_res1
  // case: toBeCopied == COPY_DATA for e_res1
     unsigned e_res1_copy = p_e_res1; 
     result->p_e_res1 = e_res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_winvers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_winvers
  // case: toBeCopied == COPY_DATA for e_winvers
     unsigned e_winvers_copy = p_e_winvers; 
     result->p_e_winvers = e_winvers_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_rfo
  // case: toBeCopied == COPY_DATA for e_entrytab_rfo
     Rose::BinaryAnalysis::Address e_entrytab_rfo_copy = p_e_entrytab_rfo; 
     result->p_e_entrytab_rfo = e_entrytab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_entrytab_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_entrytab_size
  // case: toBeCopied == COPY_DATA for e_entrytab_size
     Rose::BinaryAnalysis::Address e_entrytab_size_copy = p_e_entrytab_size; 
     result->p_e_entrytab_size = e_entrytab_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_sectab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_sectab_rfo
  // case: toBeCopied == COPY_DATA for e_sectab_rfo
     Rose::BinaryAnalysis::Address e_sectab_rfo_copy = p_e_sectab_rfo; 
     result->p_e_sectab_rfo = e_sectab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_rsrctab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rsrctab_rfo
  // case: toBeCopied == COPY_DATA for e_rsrctab_rfo
     Rose::BinaryAnalysis::Address e_rsrctab_rfo_copy = p_e_rsrctab_rfo; 
     result->p_e_rsrctab_rfo = e_rsrctab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_resnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_resnametab_rfo
  // case: toBeCopied == COPY_DATA for e_resnametab_rfo
     Rose::BinaryAnalysis::Address e_resnametab_rfo_copy = p_e_resnametab_rfo; 
     result->p_e_resnametab_rfo = e_resnametab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_modreftab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_modreftab_rfo
  // case: toBeCopied == COPY_DATA for e_modreftab_rfo
     Rose::BinaryAnalysis::Address e_modreftab_rfo_copy = p_e_modreftab_rfo; 
     result->p_e_modreftab_rfo = e_modreftab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_importnametab_rfo
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_importnametab_rfo
  // case: toBeCopied == COPY_DATA for e_importnametab_rfo
     Rose::BinaryAnalysis::Address e_importnametab_rfo_copy = p_e_importnametab_rfo; 
     result->p_e_importnametab_rfo = e_importnametab_rfo_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nonresnametab_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nonresnametab_offset
  // case: toBeCopied == COPY_DATA for e_nonresnametab_offset
     Rose::BinaryAnalysis::Address e_nonresnametab_offset_copy = p_e_nonresnametab_offset; 
     result->p_e_nonresnametab_offset = e_nonresnametab_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fastload_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fastload_sector
  // case: toBeCopied == COPY_DATA for e_fastload_sector
     Rose::BinaryAnalysis::Address e_fastload_sector_copy = p_e_fastload_sector; 
     result->p_e_fastload_sector = e_fastload_sector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_fastload_nsectors
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_fastload_nsectors
  // case: toBeCopied == COPY_DATA for e_fastload_nsectors
     Rose::BinaryAnalysis::Address e_fastload_nsectors_copy = p_e_fastload_nsectors; 
     result->p_e_fastload_nsectors = e_fastload_nsectors_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dos2Header
     SgAsmDOSExtendedHeader* dos2Header_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dos2Header
          if (get_dos2Header() != nullptr) 
             { 
               dos2Header_copy = static_cast<SgAsmDOSExtendedHeader*>(help.copyAst(get_dos2Header())); 
             } 
            else 
             { 
               dos2Header_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dos2Header != nullptr) delete result->p_dos2Header; 
     result->p_dos2Header = dos2Header_copy; 
  // case: not a listType for (using conditionalToSetParent)dos2Header
        if ((dos2Header_copy != nullptr) && (dos2Header_copy->get_parent() == nullptr) && (isSgType(dos2Header_copy) == nullptr)) {
            dos2Header_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmNESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_residentNameTable
     SgAsmNENameTable* residentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)residentNameTable
          if (get_residentNameTable() != nullptr) 
             { 
               residentNameTable_copy = static_cast<SgAsmNENameTable*>(help.copyAst(get_residentNameTable())); 
             } 
            else 
             { 
               residentNameTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_residentNameTable != nullptr) delete result->p_residentNameTable; 
     result->p_residentNameTable = residentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)residentNameTable
        if ((residentNameTable_copy != nullptr) && (residentNameTable_copy->get_parent() == nullptr) && (isSgType(residentNameTable_copy) == nullptr)) {
            residentNameTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_nonresidentNameTable
     SgAsmNENameTable* nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)nonresidentNameTable
          if (get_nonresidentNameTable() != nullptr) 
             { 
               nonresidentNameTable_copy = static_cast<SgAsmNENameTable*>(help.copyAst(get_nonresidentNameTable())); 
             } 
            else 
             { 
               nonresidentNameTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_nonresidentNameTable != nullptr) delete result->p_nonresidentNameTable; 
     result->p_nonresidentNameTable = nonresidentNameTable_copy; 
  // case: not a listType for (using conditionalToSetParent)nonresidentNameTable
        if ((nonresidentNameTable_copy != nullptr) && (nonresidentNameTable_copy->get_parent() == nullptr) && (isSgType(nonresidentNameTable_copy) == nullptr)) {
            nonresidentNameTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_moduleTable
     SgAsmNEModuleTable* moduleTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)moduleTable
          if (get_moduleTable() != nullptr) 
             { 
               moduleTable_copy = static_cast<SgAsmNEModuleTable*>(help.copyAst(get_moduleTable())); 
             } 
            else 
             { 
               moduleTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_moduleTable != nullptr) delete result->p_moduleTable; 
     result->p_moduleTable = moduleTable_copy; 
  // case: not a listType for (using conditionalToSetParent)moduleTable
        if ((moduleTable_copy != nullptr) && (moduleTable_copy->get_parent() == nullptr) && (isSgType(moduleTable_copy) == nullptr)) {
            moduleTable_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_entryTable
     SgAsmNEEntryTable* entryTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)entryTable
          if (get_entryTable() != nullptr) 
             { 
               entryTable_copy = static_cast<SgAsmNEEntryTable*>(help.copyAst(get_entryTable())); 
             } 
            else 
             { 
               entryTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_entryTable != nullptr) delete result->p_entryTable; 
     result->p_entryTable = entryTable_copy; 
  // case: not a listType for (using conditionalToSetParent)entryTable
        if ((entryTable_copy != nullptr) && (entryTable_copy->get_parent() == nullptr) && (isSgType(entryTable_copy) == nullptr)) {
            entryTable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 180524 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEFileHeader::copy(SgCopyHelp& help) const {
    SgAsmPEFileHeader* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEFileHeader();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_executableFormat
     SgAsmGenericFormat* executableFormat_copy; 
  // case: not a listType for (using conditionalToCopyVariable)executableFormat
          if (get_executableFormat() != nullptr) 
             { 
               executableFormat_copy = static_cast<SgAsmGenericFormat*>(help.copyAst(get_executableFormat())); 
             } 
            else 
             { 
               executableFormat_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_executableFormat != nullptr) delete result->p_executableFormat; 
     result->p_executableFormat = executableFormat_copy; 
  // case: not a listType for (using conditionalToSetParent)executableFormat
        if ((executableFormat_copy != nullptr) && (executableFormat_copy->get_parent() == nullptr) && (isSgType(executableFormat_copy) == nullptr)) {
            executableFormat_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for magic
  // case: toBeCopied == COPY_DATA for magic
     SgCharList magic_copy = p_magic; 
     result->p_magic = magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isa
  // case: toBeCopied == COPY_DATA for isa
     SgAsmGenericFormat::InsSetArchitecture isa_copy = p_isa; 
     result->p_isa = isa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseVa
  // case: toBeCopied == COPY_DATA for baseVa
     Rose::BinaryAnalysis::Address baseVa_copy = p_baseVa; 
     result->p_baseVa = baseVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryRvas
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryRvas
  // case: toBeCopied == COPY_DATA for entryRvas
     SgRVAList entryRvas_copy = p_entryRvas; 
     result->p_entryRvas = entryRvas_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dlls
     SgAsmGenericDLLList* dlls_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dlls
          if (get_dlls() != nullptr) 
             { 
               dlls_copy = static_cast<SgAsmGenericDLLList*>(help.copyAst(get_dlls())); 
             } 
            else 
             { 
               dlls_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dlls != nullptr) delete result->p_dlls; 
     result->p_dlls = dlls_copy; 
  // case: not a listType for (using conditionalToSetParent)dlls
        if ((dlls_copy != nullptr) && (dlls_copy->get_parent() == nullptr) && (isSgType(dlls_copy) == nullptr)) {
            dlls_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionList* sections_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sections
          if (get_sections() != nullptr) 
             { 
               sections_copy = static_cast<SgAsmGenericSectionList*>(help.copyAst(get_sections())); 
             } 
            else 
             { 
               sections_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sections != nullptr) delete result->p_sections; 
     result->p_sections = sections_copy; 
  // case: not a listType for (using conditionalToSetParent)sections
        if ((sections_copy != nullptr) && (sections_copy->get_parent() == nullptr) && (isSgType(sections_copy) == nullptr)) {
            sections_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_e_cpu_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_cpu_type
  // case: toBeCopied == COPY_DATA for e_cpu_type
     unsigned e_cpu_type_copy = p_e_cpu_type; 
     result->p_e_cpu_type = e_cpu_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nsections
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nsections
  // case: toBeCopied == COPY_DATA for e_nsections
     unsigned e_nsections_copy = p_e_nsections; 
     result->p_e_nsections = e_nsections_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_time
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_time
  // case: toBeCopied == COPY_DATA for e_time
     unsigned e_time_copy = p_e_time; 
     result->p_e_time = e_time_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_coff_symtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_coff_symtab
  // case: toBeCopied == COPY_DATA for e_coff_symtab
     Rose::BinaryAnalysis::Address e_coff_symtab_copy = p_e_coff_symtab; 
     result->p_e_coff_symtab = e_coff_symtab_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_nt_hdr_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_nt_hdr_size
  // case: toBeCopied == COPY_DATA for e_nt_hdr_size
     Rose::BinaryAnalysis::Address e_nt_hdr_size_copy = p_e_nt_hdr_size; 
     result->p_e_nt_hdr_size = e_nt_hdr_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_coff_nsyms
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_coff_nsyms
  // case: toBeCopied == COPY_DATA for e_coff_nsyms
     unsigned e_coff_nsyms_copy = p_e_coff_nsyms; 
     result->p_e_coff_nsyms = e_coff_nsyms_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_flags
  // case: toBeCopied == COPY_DATA for e_flags
     unsigned e_flags_copy = p_e_flags; 
     result->p_e_flags = e_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_opt_magic
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_opt_magic
  // case: toBeCopied == COPY_DATA for e_opt_magic
     unsigned e_opt_magic_copy = p_e_opt_magic; 
     result->p_e_opt_magic = e_opt_magic_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_lmajor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lmajor
  // case: toBeCopied == COPY_DATA for e_lmajor
     unsigned e_lmajor_copy = p_e_lmajor; 
     result->p_e_lmajor = e_lmajor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_lminor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_lminor
  // case: toBeCopied == COPY_DATA for e_lminor
     unsigned e_lminor_copy = p_e_lminor; 
     result->p_e_lminor = e_lminor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_code_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_code_size
  // case: toBeCopied == COPY_DATA for e_code_size
     unsigned e_code_size_copy = p_e_code_size; 
     result->p_e_code_size = e_code_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_data_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_size
  // case: toBeCopied == COPY_DATA for e_data_size
     unsigned e_data_size_copy = p_e_data_size; 
     result->p_e_data_size = e_data_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_bss_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_bss_size
  // case: toBeCopied == COPY_DATA for e_bss_size
     unsigned e_bss_size_copy = p_e_bss_size; 
     result->p_e_bss_size = e_bss_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_code_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_code_rva
  // case: toBeCopied == COPY_DATA for e_code_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_code_rva_copy = p_e_code_rva; 
     result->p_e_code_rva = e_code_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_data_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_data_rva
  // case: toBeCopied == COPY_DATA for e_data_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_data_rva_copy = p_e_data_rva; 
     result->p_e_data_rva = e_data_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_section_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_section_align
  // case: toBeCopied == COPY_DATA for e_section_align
     unsigned e_section_align_copy = p_e_section_align; 
     result->p_e_section_align = e_section_align_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_file_align
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_file_align
  // case: toBeCopied == COPY_DATA for e_file_align
     unsigned e_file_align_copy = p_e_file_align; 
     result->p_e_file_align = e_file_align_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_os_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_major
  // case: toBeCopied == COPY_DATA for e_os_major
     unsigned e_os_major_copy = p_e_os_major; 
     result->p_e_os_major = e_os_major_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_os_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_os_minor
  // case: toBeCopied == COPY_DATA for e_os_minor
     unsigned e_os_minor_copy = p_e_os_minor; 
     result->p_e_os_minor = e_os_minor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_user_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_user_major
  // case: toBeCopied == COPY_DATA for e_user_major
     unsigned e_user_major_copy = p_e_user_major; 
     result->p_e_user_major = e_user_major_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_user_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_user_minor
  // case: toBeCopied == COPY_DATA for e_user_minor
     unsigned e_user_minor_copy = p_e_user_minor; 
     result->p_e_user_minor = e_user_minor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_subsys_major
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsys_major
  // case: toBeCopied == COPY_DATA for e_subsys_major
     unsigned e_subsys_major_copy = p_e_subsys_major; 
     result->p_e_subsys_major = e_subsys_major_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_subsys_minor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsys_minor
  // case: toBeCopied == COPY_DATA for e_subsys_minor
     unsigned e_subsys_minor_copy = p_e_subsys_minor; 
     result->p_e_subsys_minor = e_subsys_minor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_reserved9
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_reserved9
  // case: toBeCopied == COPY_DATA for e_reserved9
     unsigned e_reserved9_copy = p_e_reserved9; 
     result->p_e_reserved9 = e_reserved9_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_image_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_image_size
  // case: toBeCopied == COPY_DATA for e_image_size
     unsigned e_image_size_copy = p_e_image_size; 
     result->p_e_image_size = e_image_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_header_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_header_size
  // case: toBeCopied == COPY_DATA for e_header_size
     unsigned e_header_size_copy = p_e_header_size; 
     result->p_e_header_size = e_header_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_file_checksum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_file_checksum
  // case: toBeCopied == COPY_DATA for e_file_checksum
     unsigned e_file_checksum_copy = p_e_file_checksum; 
     result->p_e_file_checksum = e_file_checksum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_subsystem
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_subsystem
  // case: toBeCopied == COPY_DATA for e_subsystem
     unsigned e_subsystem_copy = p_e_subsystem; 
     result->p_e_subsystem = e_subsystem_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_dll_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_dll_flags
  // case: toBeCopied == COPY_DATA for e_dll_flags
     unsigned e_dll_flags_copy = p_e_dll_flags; 
     result->p_e_dll_flags = e_dll_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_stack_reserve_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_reserve_size
  // case: toBeCopied == COPY_DATA for e_stack_reserve_size
     unsigned e_stack_reserve_size_copy = p_e_stack_reserve_size; 
     result->p_e_stack_reserve_size = e_stack_reserve_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_stack_commit_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_stack_commit_size
  // case: toBeCopied == COPY_DATA for e_stack_commit_size
     unsigned e_stack_commit_size_copy = p_e_stack_commit_size; 
     result->p_e_stack_commit_size = e_stack_commit_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_heap_reserve_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_reserve_size
  // case: toBeCopied == COPY_DATA for e_heap_reserve_size
     unsigned e_heap_reserve_size_copy = p_e_heap_reserve_size; 
     result->p_e_heap_reserve_size = e_heap_reserve_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_heap_commit_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_heap_commit_size
  // case: toBeCopied == COPY_DATA for e_heap_commit_size
     unsigned e_heap_commit_size_copy = p_e_heap_commit_size; 
     result->p_e_heap_commit_size = e_heap_commit_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_loader_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_loader_flags
  // case: toBeCopied == COPY_DATA for e_loader_flags
     unsigned e_loader_flags_copy = p_e_loader_flags; 
     result->p_e_loader_flags = e_loader_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_num_rvasize_pairs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_num_rvasize_pairs
  // case: toBeCopied == COPY_DATA for e_num_rvasize_pairs
     unsigned e_num_rvasize_pairs_copy = p_e_num_rvasize_pairs; 
     result->p_e_num_rvasize_pairs = e_num_rvasize_pairs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rvaSizePairs
     SgAsmPERVASizePairList* rvaSizePairs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rvaSizePairs
          if (get_rvaSizePairs() != nullptr) 
             { 
               rvaSizePairs_copy = static_cast<SgAsmPERVASizePairList*>(help.copyAst(get_rvaSizePairs())); 
             } 
            else 
             { 
               rvaSizePairs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rvaSizePairs != nullptr) delete result->p_rvaSizePairs; 
     result->p_rvaSizePairs = rvaSizePairs_copy; 
  // case: not a listType for (using conditionalToSetParent)rvaSizePairs
        if ((rvaSizePairs_copy != nullptr) && (rvaSizePairs_copy->get_parent() == nullptr) && (isSgType(rvaSizePairs_copy) == nullptr)) {
            rvaSizePairs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionTable
  // case: toBeCopied == COPY_DATA for sectionTable
     SgAsmPESectionTable* sectionTable_copy = p_sectionTable; 
     result->p_sectionTable = sectionTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_coffSymbolTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coffSymbolTable
  // case: toBeCopied == COPY_DATA for coffSymbolTable
     SgAsmCoffSymbolTable* coffSymbolTable_copy = p_coffSymbolTable; 
     result->p_coffSymbolTable = coffSymbolTable_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 180987 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmAttributeTable::copy(SgCopyHelp& help) const {
    SgAsmJvmAttributeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmAttributeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attributes
     SgAsmJvmAttributePtrList attributes_copy; 
  // case: listType for attributes
  // case: listType (typeIsPointerToList == false) for attributes
    auto source_attributes_iterator = get_attributes().begin();
    for (/* empty by design */; source_attributes_iterator != get_attributes().end(); ++source_attributes_iterator) {
          SgAsmJvmAttribute* source_list_element = *source_attributes_iterator; 
          SgAsmJvmAttribute* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmAttribute*>(help.copyAst(*source_attributes_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          attributes_copy.push_back(copy_list_element); 
        }
     result->p_attributes = attributes_copy; 
  // case: listType for attributes
  // case: listType (typeIsPointerToList == false) for attributes
    auto attributes_iterator = result->get_attributes().begin();
    for (/* empty by design */; attributes_iterator != result->get_attributes().end(); ++attributes_iterator) {
          SgAsmJvmAttribute* list_element = *attributes_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 181201 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmConstantPool::copy(SgCopyHelp& help) const {
    SgAsmJvmConstantPool* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmConstantPool();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmJvmConstantPoolEntryPtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 181392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmFieldTable::copy(SgCopyHelp& help) const {
    SgAsmJvmFieldTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmFieldTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fields
     SgAsmJvmFieldPtrList fields_copy; 
  // case: listType for fields
  // case: listType (typeIsPointerToList == false) for fields
    auto source_fields_iterator = get_fields().begin();
    for (/* empty by design */; source_fields_iterator != get_fields().end(); ++source_fields_iterator) {
          SgAsmJvmField* source_list_element = *source_fields_iterator; 
          SgAsmJvmField* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmField*>(help.copyAst(*source_fields_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          fields_copy.push_back(copy_list_element); 
        }
     result->p_fields = fields_copy; 
  // case: listType for fields
  // case: listType (typeIsPointerToList == false) for fields
    auto fields_iterator = result->get_fields().begin();
    for (/* empty by design */; fields_iterator != result->get_fields().end(); ++fields_iterator) {
          SgAsmJvmField* list_element = *fields_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 181606 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodTable::copy(SgCopyHelp& help) const {
    SgAsmJvmMethodTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmMethodTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_methods
     SgAsmJvmMethodPtrList methods_copy; 
  // case: listType for methods
  // case: listType (typeIsPointerToList == false) for methods
    auto source_methods_iterator = get_methods().begin();
    for (/* empty by design */; source_methods_iterator != get_methods().end(); ++source_methods_iterator) {
          SgAsmJvmMethod* source_list_element = *source_methods_iterator; 
          SgAsmJvmMethod* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmMethod*>(help.copyAst(*source_methods_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          methods_copy.push_back(copy_list_element); 
        }
     result->p_methods = methods_copy; 
  // case: listType for methods
  // case: listType (typeIsPointerToList == false) for methods
    auto methods_iterator = result->get_methods().begin();
    for (/* empty by design */; methods_iterator != result->get_methods().end(); ++methods_iterator) {
          SgAsmJvmMethod* list_element = *methods_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 181820 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEEntryTable::copy(SgCopyHelp& help) const {
    SgAsmLEEntryTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLEEntryTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bundle_sizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bundle_sizes
  // case: toBeCopied == COPY_DATA for bundle_sizes
     SgSizeTList bundle_sizes_copy = p_bundle_sizes; 
     result->p_bundle_sizes = bundle_sizes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmLEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmLEEntryPoint* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmLEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmLEEntryPoint* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 182039 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLENameTable::copy(SgCopyHelp& help) const {
    SgAsmLENameTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLENameTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ordinals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals
  // case: toBeCopied == COPY_DATA for ordinals
     SgUnsignedList ordinals_copy = p_ordinals; 
     result->p_ordinals = ordinals_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 182235 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEPageTable::copy(SgCopyHelp& help) const {
    SgAsmLEPageTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLEPageTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEPageTableEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmLEPageTableEntry* source_list_element = *source_entries_iterator; 
          SgAsmLEPageTableEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmLEPageTableEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmLEPageTableEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 182449 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLERelocTable::copy(SgCopyHelp& help) const {
    SgAsmLERelocTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLERelocTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLERelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmLERelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmLERelocEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmLERelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmLERelocEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 182663 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESection::copy(SgCopyHelp& help) const {
    SgAsmLESection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLESection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTableEntry
     SgAsmLESectionTableEntry* sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionTableEntry
          if (get_sectionTableEntry() != nullptr) 
             { 
               sectionTableEntry_copy = static_cast<SgAsmLESectionTableEntry*>(help.copyAst(get_sectionTableEntry())); 
             } 
            else 
             { 
               sectionTableEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionTableEntry != nullptr) delete result->p_sectionTableEntry; 
     result->p_sectionTableEntry = sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionTableEntry
        if ((sectionTableEntry_copy != nullptr) && (sectionTableEntry_copy->get_parent() == nullptr) && (isSgType(sectionTableEntry_copy) == nullptr)) {
            sectionTableEntry_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 182867 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESectionTable::copy(SgCopyHelp& help) const {
    SgAsmLESectionTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLESectionTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 183053 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEEntryTable::copy(SgCopyHelp& help) const {
    SgAsmNEEntryTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNEEntryTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bundle_sizes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bundle_sizes
  // case: toBeCopied == COPY_DATA for bundle_sizes
     SgSizeTList bundle_sizes_copy = p_bundle_sizes; 
     result->p_bundle_sizes = bundle_sizes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmNEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmNEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmNEEntryPoint* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmNEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmNEEntryPoint* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 183272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEModuleTable::copy(SgCopyHelp& help) const {
    SgAsmNEModuleTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNEModuleTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_strtab
     SgAsmNEStringTable* strtab_copy; 
  // case: not a listType for (using conditionalToCopyVariable)strtab
          if (get_strtab() != nullptr) 
             { 
               strtab_copy = static_cast<SgAsmNEStringTable*>(help.copyAst(get_strtab())); 
             } 
            else 
             { 
               strtab_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_strtab != nullptr) delete result->p_strtab; 
     result->p_strtab = strtab_copy; 
  // case: not a listType for (using conditionalToSetParent)strtab
        if ((strtab_copy != nullptr) && (strtab_copy->get_parent() == nullptr) && (isSgType(strtab_copy) == nullptr)) {
            strtab_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_nameOffsets
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameOffsets
  // case: toBeCopied == COPY_DATA for nameOffsets
     SgAddressList nameOffsets_copy = p_nameOffsets; 
     result->p_nameOffsets = nameOffsets_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 183486 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNENameTable::copy(SgCopyHelp& help) const {
    SgAsmNENameTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNENameTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_names
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for names
  // case: toBeCopied == COPY_DATA for names
     SgStringList names_copy = p_names; 
     result->p_names = names_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ordinals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals
  // case: toBeCopied == COPY_DATA for ordinals
     SgUnsignedList ordinals_copy = p_ordinals; 
     result->p_ordinals = ordinals_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 183682 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNERelocTable::copy(SgCopyHelp& help) const {
    SgAsmNERelocTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNERelocTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmNERelocEntryPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmNERelocEntry* source_list_element = *source_entries_iterator; 
          SgAsmNERelocEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmNERelocEntry*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmNERelocEntry* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 183896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESection::copy(SgCopyHelp& help) const {
    SgAsmNESection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNESection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionTableEntry
     SgAsmNESectionTableEntry* sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)sectionTableEntry
          if (get_sectionTableEntry() != nullptr) 
             { 
               sectionTableEntry_copy = static_cast<SgAsmNESectionTableEntry*>(help.copyAst(get_sectionTableEntry())); 
             } 
            else 
             { 
               sectionTableEntry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_sectionTableEntry != nullptr) delete result->p_sectionTableEntry; 
     result->p_sectionTableEntry = sectionTableEntry_copy; 
  // case: not a listType for (using conditionalToSetParent)sectionTableEntry
        if ((sectionTableEntry_copy != nullptr) && (sectionTableEntry_copy->get_parent() == nullptr) && (isSgType(sectionTableEntry_copy) == nullptr)) {
            sectionTableEntry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_relocationTable
     SgAsmNERelocTable* relocationTable_copy; 
  // case: not a listType for (using conditionalToCopyVariable)relocationTable
          if (get_relocationTable() != nullptr) 
             { 
               relocationTable_copy = static_cast<SgAsmNERelocTable*>(help.copyAst(get_relocationTable())); 
             } 
            else 
             { 
               relocationTable_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_relocationTable != nullptr) delete result->p_relocationTable; 
     result->p_relocationTable = relocationTable_copy; 
  // case: not a listType for (using conditionalToSetParent)relocationTable
        if ((relocationTable_copy != nullptr) && (relocationTable_copy->get_parent() == nullptr) && (isSgType(relocationTable_copy) == nullptr)) {
            relocationTable_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 184118 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESectionTable::copy(SgCopyHelp& help) const {
    SgAsmNESectionTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNESectionTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sector
  // case: toBeCopied == COPY_DATA for sector
     unsigned sector_copy = p_sector; 
     result->p_sector = sector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physicalSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physicalSize
  // case: toBeCopied == COPY_DATA for physicalSize
     Rose::BinaryAnalysis::Address physicalSize_copy = p_physicalSize; 
     result->p_physicalSize = physicalSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_virtualSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtualSize
  // case: toBeCopied == COPY_DATA for virtualSize
     Rose::BinaryAnalysis::Address virtualSize_copy = p_virtualSize; 
     result->p_virtualSize = virtualSize_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 184324 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEStringTable::copy(SgCopyHelp& help) const {
    SgAsmNEStringTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNEStringTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 184510 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESection::copy(SgCopyHelp& help) const {
    SgAsmPESection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPESection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != nullptr) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != nullptr) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
        if ((section_entry_copy != nullptr) && (section_entry_copy->get_parent() == nullptr) && (isSgType(section_entry_copy) == nullptr)) {
            section_entry_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 184714 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportSection::copy(SgCopyHelp& help) const {
    SgAsmPEExportSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEExportSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != nullptr) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != nullptr) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
        if ((section_entry_copy != nullptr) && (section_entry_copy->get_parent() == nullptr) && (isSgType(section_entry_copy) == nullptr)) {
            section_entry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_exportDirectory
     SgAsmPEExportDirectory* exportDirectory_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exportDirectory
          if (get_exportDirectory() != nullptr) 
             { 
               exportDirectory_copy = static_cast<SgAsmPEExportDirectory*>(help.copyAst(get_exportDirectory())); 
             } 
            else 
             { 
               exportDirectory_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exportDirectory != nullptr) delete result->p_exportDirectory; 
     result->p_exportDirectory = exportDirectory_copy; 
  // case: not a listType for (using conditionalToSetParent)exportDirectory
        if ((exportDirectory_copy != nullptr) && (exportDirectory_copy->get_parent() == nullptr) && (isSgType(exportDirectory_copy) == nullptr)) {
            exportDirectory_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_exports
     SgAsmPEExportEntryList* exports_copy; 
  // case: not a listType for (using conditionalToCopyVariable)exports
          if (get_exports() != nullptr) 
             { 
               exports_copy = static_cast<SgAsmPEExportEntryList*>(help.copyAst(get_exports())); 
             } 
            else 
             { 
               exports_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_exports != nullptr) delete result->p_exports; 
     result->p_exports = exports_copy; 
  // case: not a listType for (using conditionalToSetParent)exports
        if ((exports_copy != nullptr) && (exports_copy->get_parent() == nullptr) && (isSgType(exports_copy) == nullptr)) {
            exports_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 184954 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportSection::copy(SgCopyHelp& help) const {
    SgAsmPEImportSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEImportSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != nullptr) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != nullptr) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
        if ((section_entry_copy != nullptr) && (section_entry_copy->get_parent() == nullptr) && (isSgType(section_entry_copy) == nullptr)) {
            section_entry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_importDirectories
     SgAsmPEImportDirectoryList* importDirectories_copy; 
  // case: not a listType for (using conditionalToCopyVariable)importDirectories
          if (get_importDirectories() != nullptr) 
             { 
               importDirectories_copy = static_cast<SgAsmPEImportDirectoryList*>(help.copyAst(get_importDirectories())); 
             } 
            else 
             { 
               importDirectories_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_importDirectories != nullptr) delete result->p_importDirectories; 
     result->p_importDirectories = importDirectories_copy; 
  // case: not a listType for (using conditionalToSetParent)importDirectories
        if ((importDirectories_copy != nullptr) && (importDirectories_copy->get_parent() == nullptr) && (isSgType(importDirectories_copy) == nullptr)) {
            importDirectories_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEStringSection::copy(SgCopyHelp& help) const {
    SgAsmPEStringSection* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEStringSection();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_section_entry
     SgAsmPESectionTableEntry* section_entry_copy; 
  // case: not a listType for (using conditionalToCopyVariable)section_entry
          if (get_section_entry() != nullptr) 
             { 
               section_entry_copy = static_cast<SgAsmPESectionTableEntry*>(help.copyAst(get_section_entry())); 
             } 
            else 
             { 
               section_entry_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_section_entry != nullptr) delete result->p_section_entry; 
     result->p_section_entry = section_entry_copy; 
  // case: not a listType for (using conditionalToSetParent)section_entry
        if ((section_entry_copy != nullptr) && (section_entry_copy->get_parent() == nullptr) && (isSgType(section_entry_copy) == nullptr)) {
            section_entry_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmCoffStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185385 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESectionTable::copy(SgCopyHelp& help) const {
    SgAsmPESectionTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPESectionTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_file
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for file
  // case: toBeCopied == COPY_DATA for file
     SgAsmGenericFile* file_copy = p_file; 
     result->p_file = file_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_header
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for header
  // case: toBeCopied == COPY_DATA for header
     SgAsmGenericHeader* header_copy = p_header; 
     result->p_header = header_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_fileAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for fileAlignment
  // case: toBeCopied == COPY_DATA for fileAlignment
     Rose::BinaryAnalysis::Address fileAlignment_copy = p_fileAlignment; 
     result->p_fileAlignment = fileAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_data
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for data
  // case: toBeCopied == COPY_DATA for data
     SgFileContentList data_copy = p_data; 
     result->p_data = data_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_purpose
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for purpose
  // case: toBeCopied == COPY_DATA for purpose
     SgAsmGenericSection::SectionPurpose purpose_copy = p_purpose; 
     result->p_purpose = purpose_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_synthesized
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for synthesized
  // case: toBeCopied == COPY_DATA for synthesized
     bool synthesized_copy = p_synthesized; 
     result->p_synthesized = synthesized_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     int id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     SgAsmGenericString* name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_shortName
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for shortName
  // case: toBeCopied == COPY_DATA for shortName
     std::string shortName_copy = p_shortName; 
     result->p_shortName = shortName_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedPreferredRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedPreferredRva
  // case: toBeCopied == COPY_DATA for mappedPreferredRva
     Rose::BinaryAnalysis::Address mappedPreferredRva_copy = p_mappedPreferredRva; 
     result->p_mappedPreferredRva = mappedPreferredRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedAlignment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedAlignment
  // case: toBeCopied == COPY_DATA for mappedAlignment
     Rose::BinaryAnalysis::Address mappedAlignment_copy = p_mappedAlignment; 
     result->p_mappedAlignment = mappedAlignment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedReadPermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedReadPermission
  // case: toBeCopied == COPY_DATA for mappedReadPermission
     bool mappedReadPermission_copy = p_mappedReadPermission; 
     result->p_mappedReadPermission = mappedReadPermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedWritePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedWritePermission
  // case: toBeCopied == COPY_DATA for mappedWritePermission
     bool mappedWritePermission_copy = p_mappedWritePermission; 
     result->p_mappedWritePermission = mappedWritePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedExecutePermission
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedExecutePermission
  // case: toBeCopied == COPY_DATA for mappedExecutePermission
     bool mappedExecutePermission_copy = p_mappedExecutePermission; 
     result->p_mappedExecutePermission = mappedExecutePermission_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_containsCode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for containsCode
  // case: toBeCopied == COPY_DATA for containsCode
     bool containsCode_copy = p_containsCode; 
     result->p_containsCode = containsCode_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedActualVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedActualVa
  // case: toBeCopied == COPY_DATA for mappedActualVa
     Rose::BinaryAnalysis::Address mappedActualVa_copy = p_mappedActualVa; 
     result->p_mappedActualVa = mappedActualVa_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185571 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSectionList::copy(SgCopyHelp& help) const {
    SgAsmGenericSectionList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericSectionList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_sections
     SgAsmGenericSectionPtrList sections_copy; 
  // case: listType for sections
  // case: listType (typeIsPointerToList == false) for sections
    auto source_sections_iterator = get_sections().begin();
    for (/* empty by design */; source_sections_iterator != get_sections().end(); ++source_sections_iterator) {
          SgAsmGenericSection* source_list_element = *source_sections_iterator; 
          SgAsmGenericSection* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmGenericSection*>(help.copyAst(*source_sections_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          sections_copy.push_back(copy_list_element); 
        }
     result->p_sections = sections_copy; 
  // case: listType for sections
  // case: listType (typeIsPointerToList == false) for sections
    auto sections_iterator = result->get_sections().begin();
    for (/* empty by design */; sections_iterator != result->get_sections().end(); ++sections_iterator) {
          SgAsmGenericSection* list_element = *sections_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185690 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericString::copy(SgCopyHelp& help) const {
    SgAsmGenericString* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericString();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185780 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBasicString::copy(SgCopyHelp& help) const {
    SgAsmBasicString* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBasicString();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     std::string string_copy = p_string; 
     result->p_string = string_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185876 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStoredString::copy(SgCopyHelp& help) const {
    SgAsmStoredString* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStoredString();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_storage
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storage
  // case: toBeCopied == COPY_DATA for storage
     SgAsmStringStorage* storage_copy = p_storage; 
     result->p_storage = storage_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 185972 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericStrtab::copy(SgCopyHelp& help) const {
    SgAsmGenericStrtab* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericStrtab();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffStrtab::copy(SgCopyHelp& help) const {
    SgAsmCoffStrtab* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCoffStrtab();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186204 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfStrtab::copy(SgCopyHelp& help) const {
    SgAsmElfStrtab* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfStrtab();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_container
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for container
  // case: toBeCopied == COPY_DATA for container
     SgAsmGenericSection* container_copy = p_container; 
     result->p_container = container_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_storageList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for storageList
  // case: toBeCopied == COPY_DATA for storageList
     SgAsmGenericStrtab::referenced_t storageList_copy = p_storageList; 
     result->p_storageList = storageList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_freeList
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for freeList
  // case: toBeCopied == COPY_DATA for freeList
     Rose::BinaryAnalysis::AddressIntervalSet freeList_copy = p_freeList; 
     result->p_freeList = freeList_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dontFree
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dontFree
  // case: toBeCopied == COPY_DATA for dontFree
     SgAsmStringStorage* dontFree_copy = p_dontFree; 
     result->p_dontFree = dontFree_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_numberFreed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for numberFreed
  // case: toBeCopied == COPY_DATA for numberFreed
     size_t numberFreed_copy = p_numberFreed; 
     result->p_numberFreed = numberFreed_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSymbol::copy(SgCopyHelp& help) const {
    SgAsmGenericSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericSymbol();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186459 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCoffSymbol::copy(SgCopyHelp& help) const {
    SgAsmCoffSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCoffSymbol();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_st_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_name
  // case: toBeCopied == COPY_DATA for st_name
     std::string st_name_copy = p_st_name; 
     result->p_st_name = st_name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_name_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_name_offset
  // case: toBeCopied == COPY_DATA for st_name_offset
     Rose::BinaryAnalysis::Address st_name_offset_copy = p_st_name_offset; 
     result->p_st_name_offset = st_name_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_section_num
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_section_num
  // case: toBeCopied == COPY_DATA for st_section_num
     int st_section_num_copy = p_st_section_num; 
     result->p_st_section_num = st_section_num_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_type
  // case: toBeCopied == COPY_DATA for st_type
     unsigned st_type_copy = p_st_type; 
     result->p_st_type = st_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_storage_class
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_storage_class
  // case: toBeCopied == COPY_DATA for st_storage_class
     unsigned st_storage_class_copy = p_st_storage_class; 
     result->p_st_storage_class = st_storage_class_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_num_aux_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_num_aux_entries
  // case: toBeCopied == COPY_DATA for st_num_aux_entries
     unsigned st_num_aux_entries_copy = p_st_num_aux_entries; 
     result->p_st_num_aux_entries = st_num_aux_entries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_auxiliaryData
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for auxiliaryData
  // case: toBeCopied == COPY_DATA for auxiliaryData
     SgUnsignedCharList auxiliaryData_copy = p_auxiliaryData; 
     result->p_auxiliaryData = auxiliaryData_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186633 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmElfSymbol::copy(SgCopyHelp& help) const {
    SgAsmElfSymbol* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmElfSymbol();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_definitionState
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for definitionState
  // case: toBeCopied == COPY_DATA for definitionState
     SgAsmGenericSymbol::SymbolDefState definitionState_copy = p_definitionState; 
     result->p_definitionState = definitionState_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_binding
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for binding
  // case: toBeCopied == COPY_DATA for binding
     SgAsmGenericSymbol::SymbolBinding binding_copy = p_binding; 
     result->p_binding = binding_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmGenericSymbol::SymbolType type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     Rose::BinaryAnalysis::Address value_copy = p_value; 
     result->p_value = value_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for size
  // case: toBeCopied == COPY_DATA for size
     Rose::BinaryAnalysis::Address size_copy = p_size; 
     result->p_size = size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bound
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound
  // case: toBeCopied == COPY_DATA for bound
     SgAsmGenericSection* bound_copy = p_bound; 
     result->p_bound = bound_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_st_info
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_info
  // case: toBeCopied == COPY_DATA for st_info
     unsigned char st_info_copy = p_st_info; 
     result->p_st_info = st_info_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_res1
  // case: toBeCopied == COPY_DATA for st_res1
     unsigned char st_res1_copy = p_st_res1; 
     result->p_st_res1 = st_res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_shndx
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_shndx
  // case: toBeCopied == COPY_DATA for st_shndx
     unsigned st_shndx_copy = p_st_shndx; 
     result->p_st_shndx = st_shndx_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_st_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for st_size
  // case: toBeCopied == COPY_DATA for st_size
     Rose::BinaryAnalysis::Address st_size_copy = p_st_size; 
     result->p_st_size = st_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_extra
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for extra
  // case: toBeCopied == COPY_DATA for extra
     SgUnsignedCharList extra_copy = p_extra; 
     result->p_extra = extra_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186797 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericSymbolList::copy(SgCopyHelp& help) const {
    SgAsmGenericSymbolList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericSymbolList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_symbols
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbols
  // case: toBeCopied == COPY_DATA for symbols
     SgAsmGenericSymbolPtrList symbols_copy = p_symbols; 
     result->p_symbols = symbols_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 186893 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEEntryPoint::copy(SgCopyHelp& help) const {
    SgAsmLEEntryPoint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLEEntryPoint();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_entries
     SgAsmLEEntryPointPtrList entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto source_entries_iterator = get_entries().begin();
    for (/* empty by design */; source_entries_iterator != get_entries().end(); ++source_entries_iterator) {
          SgAsmLEEntryPoint* source_list_element = *source_entries_iterator; 
          SgAsmLEEntryPoint* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmLEEntryPoint*>(help.copyAst(*source_entries_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          entries_copy.push_back(copy_list_element); 
        }
     result->p_entries = entries_copy; 
  // case: listType for entries
  // case: listType (typeIsPointerToList == false) for entries
    auto entries_iterator = result->get_entries().begin();
    for (/* empty by design */; entries_iterator != result->get_entries().end(); ++entries_iterator) {
          SgAsmLEEntryPoint* list_element = *entries_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_objnum
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for objnum
  // case: toBeCopied == COPY_DATA for objnum
     unsigned objnum_copy = p_objnum; 
     result->p_objnum = objnum_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entry_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entry_type
  // case: toBeCopied == COPY_DATA for entry_type
     unsigned entry_type_copy = p_entry_type; 
     result->p_entry_type = entry_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entry_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entry_offset
  // case: toBeCopied == COPY_DATA for entry_offset
     Rose::BinaryAnalysis::Address entry_offset_copy = p_entry_offset; 
     result->p_entry_offset = entry_offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187037 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLEPageTableEntry::copy(SgCopyHelp& help) const {
    SgAsmLEPageTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLEPageTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_pageno
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageno
  // case: toBeCopied == COPY_DATA for pageno
     unsigned pageno_copy = p_pageno; 
     result->p_pageno = pageno_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187138 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmLESectionTableEntry::copy(SgCopyHelp& help) const {
    SgAsmLESectionTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmLESectionTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_pageMapIndex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageMapIndex
  // case: toBeCopied == COPY_DATA for pageMapIndex
     unsigned pageMapIndex_copy = p_pageMapIndex; 
     result->p_pageMapIndex = pageMapIndex_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_pageMapNEntries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for pageMapNEntries
  // case: toBeCopied == COPY_DATA for pageMapNEntries
     unsigned pageMapNEntries_copy = p_pageMapNEntries; 
     result->p_pageMapNEntries = pageMapNEntries_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mappedSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mappedSize
  // case: toBeCopied == COPY_DATA for mappedSize
     Rose::BinaryAnalysis::Address mappedSize_copy = p_mappedSize; 
     result->p_mappedSize = mappedSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseAddr
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseAddr
  // case: toBeCopied == COPY_DATA for baseAddr
     Rose::BinaryAnalysis::Address baseAddr_copy = p_baseAddr; 
     result->p_baseAddr = baseAddr_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187259 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNEEntryPoint::copy(SgCopyHelp& help) const {
    SgAsmNEEntryPoint* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNEEntryPoint();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmNEEntryPoint::NEEntryFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_int3f
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for int3f
  // case: toBeCopied == COPY_DATA for int3f
     unsigned int3f_copy = p_int3f; 
     result->p_int3f = int3f_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionIndex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionIndex
  // case: toBeCopied == COPY_DATA for sectionIndex
     unsigned sectionIndex_copy = p_sectionIndex; 
     result->p_sectionIndex = sectionIndex_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sectionOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sectionOffset
  // case: toBeCopied == COPY_DATA for sectionOffset
     unsigned sectionOffset_copy = p_sectionOffset; 
     result->p_sectionOffset = sectionOffset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187370 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNERelocEntry::copy(SgCopyHelp& help) const {
    SgAsmNERelocEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNERelocEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_src_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for src_type
  // case: toBeCopied == COPY_DATA for src_type
     SgAsmNERelocEntry::NERelocSrcType src_type_copy = p_src_type; 
     result->p_src_type = src_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_modifier
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modifier
  // case: toBeCopied == COPY_DATA for modifier
     SgAsmNERelocEntry::NERelocModifiers modifier_copy = p_modifier; 
     result->p_modifier = modifier_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_tgt_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tgt_type
  // case: toBeCopied == COPY_DATA for tgt_type
     SgAsmNERelocEntry::NERelocTgtType tgt_type_copy = p_tgt_type; 
     result->p_tgt_type = tgt_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     SgAsmNERelocEntry::NERelocFlags flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_src_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for src_offset
  // case: toBeCopied == COPY_DATA for src_offset
     Rose::BinaryAnalysis::Address src_offset_copy = p_src_offset; 
     result->p_src_offset = src_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iref
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iref
  // case: toBeCopied == COPY_DATA for iref
     SgAsmNERelocEntry::iref_type iref_copy = p_iref; 
     result->p_iref = iref_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iord
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iord
  // case: toBeCopied == COPY_DATA for iord
     SgAsmNERelocEntry::iord_type iord_copy = p_iord; 
     result->p_iord = iord_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iname
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iname
  // case: toBeCopied == COPY_DATA for iname
     SgAsmNERelocEntry::iname_type iname_copy = p_iname; 
     result->p_iname = iname_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_osfixup
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for osfixup
  // case: toBeCopied == COPY_DATA for osfixup
     SgAsmNERelocEntry::osfixup_type osfixup_copy = p_osfixup; 
     result->p_osfixup = osfixup_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187506 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNESectionTableEntry::copy(SgCopyHelp& help) const {
    SgAsmNESectionTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNESectionTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_sector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for sector
  // case: toBeCopied == COPY_DATA for sector
     unsigned sector_copy = p_sector; 
     result->p_sector = sector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physicalSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physicalSize
  // case: toBeCopied == COPY_DATA for physicalSize
     Rose::BinaryAnalysis::Address physicalSize_copy = p_physicalSize; 
     result->p_physicalSize = physicalSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_virtualSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtualSize
  // case: toBeCopied == COPY_DATA for virtualSize
     Rose::BinaryAnalysis::Address virtualSize_copy = p_virtualSize; 
     result->p_virtualSize = virtualSize_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187617 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportDirectory::copy(SgCopyHelp& help) const {
    SgAsmPEExportDirectory* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEExportDirectory();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_res1
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for res1
  // case: toBeCopied == COPY_DATA for res1
     unsigned res1_copy = p_res1; 
     result->p_res1 = res1_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_timestamp
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for timestamp
  // case: toBeCopied == COPY_DATA for timestamp
     time_t timestamp_copy = p_timestamp; 
     result->p_timestamp = timestamp_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_vmajor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vmajor
  // case: toBeCopied == COPY_DATA for vmajor
     unsigned vmajor_copy = p_vmajor; 
     result->p_vmajor = vmajor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_vminor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for vminor
  // case: toBeCopied == COPY_DATA for vminor
     unsigned vminor_copy = p_vminor; 
     result->p_vminor = vminor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_rva
  // case: toBeCopied == COPY_DATA for name_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress name_rva_copy = p_name_rva; 
     result->p_name_rva = name_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ord_base
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ord_base
  // case: toBeCopied == COPY_DATA for ord_base
     unsigned ord_base_copy = p_ord_base; 
     result->p_ord_base = ord_base_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_expaddr_n
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expaddr_n
  // case: toBeCopied == COPY_DATA for expaddr_n
     size_t expaddr_n_copy = p_expaddr_n; 
     result->p_expaddr_n = expaddr_n_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nameptr_n
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameptr_n
  // case: toBeCopied == COPY_DATA for nameptr_n
     size_t nameptr_n_copy = p_nameptr_n; 
     result->p_nameptr_n = nameptr_n_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_expaddr_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for expaddr_rva
  // case: toBeCopied == COPY_DATA for expaddr_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress expaddr_rva_copy = p_expaddr_rva; 
     result->p_expaddr_rva = expaddr_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nameptr_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameptr_rva
  // case: toBeCopied == COPY_DATA for nameptr_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress nameptr_rva_copy = p_nameptr_rva; 
     result->p_nameptr_rva = nameptr_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ordinals_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinals_rva
  // case: toBeCopied == COPY_DATA for ordinals_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress ordinals_rva_copy = p_ordinals_rva; 
     result->p_ordinals_rva = ordinals_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187781 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportEntry::copy(SgCopyHelp& help) const {
    SgAsmPEExportEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEExportEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinal
  // case: toBeCopied == COPY_DATA for ordinal
     unsigned ordinal_copy = p_ordinal; 
     result->p_ordinal = ordinal_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exportRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exportRva
  // case: toBeCopied == COPY_DATA for exportRva
     Rose::BinaryAnalysis::RelativeVirtualAddress exportRva_copy = p_exportRva; 
     result->p_exportRva = exportRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_forwarder
     SgAsmGenericString* forwarder_copy; 
  // case: not a listType for (using conditionalToCopyVariable)forwarder
          if (get_forwarder() != nullptr) 
             { 
               forwarder_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_forwarder())); 
             } 
            else 
             { 
               forwarder_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_forwarder != nullptr) delete result->p_forwarder; 
     result->p_forwarder = forwarder_copy; 
  // case: not a listType for (using conditionalToSetParent)forwarder
        if ((forwarder_copy != nullptr) && (forwarder_copy->get_parent() == nullptr) && (isSgType(forwarder_copy) == nullptr)) {
            forwarder_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 187918 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEExportEntryList::copy(SgCopyHelp& help) const {
    SgAsmPEExportEntryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEExportEntryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_exports
     SgAsmPEExportEntryPtrList exports_copy; 
  // case: listType for exports
  // case: listType (typeIsPointerToList == false) for exports
    auto source_exports_iterator = get_exports().begin();
    for (/* empty by design */; source_exports_iterator != get_exports().end(); ++source_exports_iterator) {
          SgAsmPEExportEntry* source_list_element = *source_exports_iterator; 
          SgAsmPEExportEntry* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmPEExportEntry*>(help.copyAst(*source_exports_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          exports_copy.push_back(copy_list_element); 
        }
     result->p_exports = exports_copy; 
  // case: listType for exports
  // case: listType (typeIsPointerToList == false) for exports
    auto exports_iterator = result->get_exports().begin();
    for (/* empty by design */; exports_iterator != result->get_exports().end(); ++exports_iterator) {
          SgAsmPEExportEntry* list_element = *exports_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188037 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportDirectory::copy(SgCopyHelp& help) const {
    SgAsmPEImportDirectory* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEImportDirectory();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_dllName
     SgAsmGenericString* dllName_copy; 
  // case: not a listType for (using conditionalToCopyVariable)dllName
          if (get_dllName() != nullptr) 
             { 
               dllName_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_dllName())); 
             } 
            else 
             { 
               dllName_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_dllName != nullptr) delete result->p_dllName; 
     result->p_dllName = dllName_copy; 
  // case: not a listType for (using conditionalToSetParent)dllName
        if ((dllName_copy != nullptr) && (dllName_copy->get_parent() == nullptr) && (isSgType(dllName_copy) == nullptr)) {
            dllName_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_dllNameRva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dllNameRva
  // case: toBeCopied == COPY_DATA for dllNameRva
     Rose::BinaryAnalysis::RelativeVirtualAddress dllNameRva_copy = p_dllNameRva; 
     result->p_dllNameRva = dllNameRva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dll_name_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dll_name_nalloc
  // case: toBeCopied == COPY_DATA for dll_name_nalloc
     size_t dll_name_nalloc_copy = p_dll_name_nalloc; 
     result->p_dll_name_nalloc = dll_name_nalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_time
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for time
  // case: toBeCopied == COPY_DATA for time
     time_t time_copy = p_time; 
     result->p_time = time_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_forwarder_chain
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for forwarder_chain
  // case: toBeCopied == COPY_DATA for forwarder_chain
     unsigned forwarder_chain_copy = p_forwarder_chain; 
     result->p_forwarder_chain = forwarder_chain_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ilt_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ilt_rva
  // case: toBeCopied == COPY_DATA for ilt_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress ilt_rva_copy = p_ilt_rva; 
     result->p_ilt_rva = ilt_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ilt_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ilt_nalloc
  // case: toBeCopied == COPY_DATA for ilt_nalloc
     size_t ilt_nalloc_copy = p_ilt_nalloc; 
     result->p_ilt_nalloc = ilt_nalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iat_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_rva
  // case: toBeCopied == COPY_DATA for iat_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress iat_rva_copy = p_iat_rva; 
     result->p_iat_rva = iat_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iat_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_nalloc
  // case: toBeCopied == COPY_DATA for iat_nalloc
     size_t iat_nalloc_copy = p_iat_nalloc; 
     result->p_iat_nalloc = iat_nalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_imports
     SgAsmPEImportItemList* imports_copy; 
  // case: not a listType for (using conditionalToCopyVariable)imports
          if (get_imports() != nullptr) 
             { 
               imports_copy = static_cast<SgAsmPEImportItemList*>(help.copyAst(get_imports())); 
             } 
            else 
             { 
               imports_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_imports != nullptr) delete result->p_imports; 
     result->p_imports = imports_copy; 
  // case: not a listType for (using conditionalToSetParent)imports
        if ((imports_copy != nullptr) && (imports_copy->get_parent() == nullptr) && (isSgType(imports_copy) == nullptr)) {
            imports_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188204 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportDirectoryList::copy(SgCopyHelp& help) const {
    SgAsmPEImportDirectoryList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEImportDirectoryList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_vector
     SgAsmPEImportDirectoryPtrList vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
    auto source_vector_iterator = get_vector().begin();
    for (/* empty by design */; source_vector_iterator != get_vector().end(); ++source_vector_iterator) {
          SgAsmPEImportDirectory* source_list_element = *source_vector_iterator; 
          SgAsmPEImportDirectory* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmPEImportDirectory*>(help.copyAst(*source_vector_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          vector_copy.push_back(copy_list_element); 
        }
     result->p_vector = vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
    auto vector_iterator = result->get_vector().begin();
    for (/* empty by design */; vector_iterator != result->get_vector().end(); ++vector_iterator) {
          SgAsmPEImportDirectory* list_element = *vector_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188323 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportItem::copy(SgCopyHelp& help) const {
    SgAsmPEImportItem* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEImportItem();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_by_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for by_ordinal
  // case: toBeCopied == COPY_DATA for by_ordinal
     bool by_ordinal_copy = p_by_ordinal; 
     result->p_by_ordinal = by_ordinal_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_ordinal
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for ordinal
  // case: toBeCopied == COPY_DATA for ordinal
     unsigned ordinal_copy = p_ordinal; 
     result->p_ordinal = ordinal_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hint
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hint
  // case: toBeCopied == COPY_DATA for hint
     unsigned hint_copy = p_hint; 
     result->p_hint = hint_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
     SgAsmGenericString* name_copy; 
  // case: not a listType for (using conditionalToCopyVariable)name
          if (get_name() != nullptr) 
             { 
               name_copy = static_cast<SgAsmGenericString*>(help.copyAst(get_name())); 
             } 
            else 
             { 
               name_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_name != nullptr) delete result->p_name; 
     result->p_name = name_copy; 
  // case: not a listType for (using conditionalToSetParent)name
        if ((name_copy != nullptr) && (name_copy->get_parent() == nullptr) && (isSgType(name_copy) == nullptr)) {
            name_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_hintname_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hintname_rva
  // case: toBeCopied == COPY_DATA for hintname_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress hintname_rva_copy = p_hintname_rva; 
     result->p_hintname_rva = hintname_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hintname_nalloc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hintname_nalloc
  // case: toBeCopied == COPY_DATA for hintname_nalloc
     size_t hintname_nalloc_copy = p_hintname_nalloc; 
     result->p_hintname_nalloc = hintname_nalloc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bound_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bound_rva
  // case: toBeCopied == COPY_DATA for bound_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress bound_rva_copy = p_bound_rva; 
     result->p_bound_rva = bound_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_iat_written
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for iat_written
  // case: toBeCopied == COPY_DATA for iat_written
     bool iat_written_copy = p_iat_written; 
     result->p_iat_written = iat_written_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188467 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPEImportItemList::copy(SgCopyHelp& help) const {
    SgAsmPEImportItemList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPEImportItemList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_vector
     SgAsmPEImportItemPtrList vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
    auto source_vector_iterator = get_vector().begin();
    for (/* empty by design */; source_vector_iterator != get_vector().end(); ++source_vector_iterator) {
          SgAsmPEImportItem* source_list_element = *source_vector_iterator; 
          SgAsmPEImportItem* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmPEImportItem*>(help.copyAst(*source_vector_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          vector_copy.push_back(copy_list_element); 
        }
     result->p_vector = vector_copy; 
  // case: listType for vector
  // case: listType (typeIsPointerToList == false) for vector
    auto vector_iterator = result->get_vector().begin();
    for (/* empty by design */; vector_iterator != result->get_vector().end(); ++vector_iterator) {
          SgAsmPEImportItem* list_element = *vector_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188586 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPERVASizePair::copy(SgCopyHelp& help) const {
    SgAsmPERVASizePair* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPERVASizePair();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_e_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_rva
  // case: toBeCopied == COPY_DATA for e_rva
     Rose::BinaryAnalysis::RelativeVirtualAddress e_rva_copy = p_e_rva; 
     result->p_e_rva = e_rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_e_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for e_size
  // case: toBeCopied == COPY_DATA for e_size
     Rose::BinaryAnalysis::Address e_size_copy = p_e_size; 
     result->p_e_size = e_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_section
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for section
  // case: toBeCopied == COPY_DATA for section
     SgAsmGenericSection* section_copy = p_section; 
     result->p_section = section_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188692 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPERVASizePairList::copy(SgCopyHelp& help) const {
    SgAsmPERVASizePairList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPERVASizePairList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_pairs
     SgAsmPERVASizePairPtrList pairs_copy; 
  // case: listType for pairs
  // case: listType (typeIsPointerToList == false) for pairs
    auto source_pairs_iterator = get_pairs().begin();
    for (/* empty by design */; source_pairs_iterator != get_pairs().end(); ++source_pairs_iterator) {
          SgAsmPERVASizePair* source_list_element = *source_pairs_iterator; 
          SgAsmPERVASizePair* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmPERVASizePair*>(help.copyAst(*source_pairs_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          pairs_copy.push_back(copy_list_element); 
        }
     result->p_pairs = pairs_copy; 
  // case: listType for pairs
  // case: listType (typeIsPointerToList == false) for pairs
    auto pairs_iterator = result->get_pairs().begin();
    for (/* empty by design */; pairs_iterator != result->get_pairs().end(); ++pairs_iterator) {
          SgAsmPERVASizePair* list_element = *pairs_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188811 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPESectionTableEntry::copy(SgCopyHelp& help) const {
    SgAsmPESectionTableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPESectionTableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_virtual_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for virtual_size
  // case: toBeCopied == COPY_DATA for virtual_size
     Rose::BinaryAnalysis::Address virtual_size_copy = p_virtual_size; 
     result->p_virtual_size = virtual_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rva
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rva
  // case: toBeCopied == COPY_DATA for rva
     Rose::BinaryAnalysis::Address rva_copy = p_rva; 
     result->p_rva = rva_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physical_size
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_size
  // case: toBeCopied == COPY_DATA for physical_size
     Rose::BinaryAnalysis::Address physical_size_copy = p_physical_size; 
     result->p_physical_size = physical_size_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_physical_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for physical_offset
  // case: toBeCopied == COPY_DATA for physical_offset
     Rose::BinaryAnalysis::Address physical_offset_copy = p_physical_offset; 
     result->p_physical_offset = physical_offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_coff_line_nums
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coff_line_nums
  // case: toBeCopied == COPY_DATA for coff_line_nums
     unsigned coff_line_nums_copy = p_coff_line_nums; 
     result->p_coff_line_nums = coff_line_nums_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_n_relocs
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for n_relocs
  // case: toBeCopied == COPY_DATA for n_relocs
     unsigned n_relocs_copy = p_n_relocs; 
     result->p_n_relocs = n_relocs_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_n_coff_line_nums
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for n_coff_line_nums
  // case: toBeCopied == COPY_DATA for n_coff_line_nums
     unsigned n_coff_line_nums_copy = p_n_coff_line_nums; 
     result->p_n_coff_line_nums = n_coff_line_nums_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 188947 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStringStorage::copy(SgCopyHelp& help) const {
    SgAsmStringStorage* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStringStorage();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_strtab
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for strtab
  // case: toBeCopied == COPY_DATA for strtab
     SgAsmGenericStrtab* strtab_copy = p_strtab; 
     result->p_strtab = strtab_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_string
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string
  // case: toBeCopied == COPY_DATA for string
     std::string string_copy = p_string; 
     result->p_string = string_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::Address offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189053 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExpression::copy(SgCopyHelp& help) const {
    SgAsmExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189154 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmAarch32Coprocessor::copy(SgCopyHelp& help) const {
    SgAsmAarch32Coprocessor* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmAarch32Coprocessor();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189255 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryExpression::copy(SgCopyHelp& help) const {
    SgAsmBinaryExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAdd::copy(SgCopyHelp& help) const {
    SgAsmBinaryAdd* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryAdd();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189529 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryAsr::copy(SgCopyHelp& help) const {
    SgAsmBinaryAsr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryAsr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189666 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryConcat::copy(SgCopyHelp& help) const {
    SgAsmBinaryConcat* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryConcat();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189803 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryDivide::copy(SgCopyHelp& help) const {
    SgAsmBinaryDivide* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryDivide();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 189940 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryLsl::copy(SgCopyHelp& help) const {
    SgAsmBinaryLsl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryLsl();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190077 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryLsr::copy(SgCopyHelp& help) const {
    SgAsmBinaryLsr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryLsr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190214 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMod::copy(SgCopyHelp& help) const {
    SgAsmBinaryMod* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryMod();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190351 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMsl::copy(SgCopyHelp& help) const {
    SgAsmBinaryMsl* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryMsl();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryMultiply::copy(SgCopyHelp& help) const {
    SgAsmBinaryMultiply* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryMultiply();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190625 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryPostupdate::copy(SgCopyHelp& help) const {
    SgAsmBinaryPostupdate* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryPostupdate();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190762 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryPreupdate::copy(SgCopyHelp& help) const {
    SgAsmBinaryPreupdate* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryPreupdate();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 190899 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinaryRor::copy(SgCopyHelp& help) const {
    SgAsmBinaryRor* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinaryRor();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191036 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBinarySubtract::copy(SgCopyHelp& help) const {
    SgAsmBinarySubtract* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBinarySubtract();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lhs
     SgAsmExpression* lhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)lhs
          if (get_lhs() != nullptr) 
             { 
               lhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_lhs())); 
             } 
            else 
             { 
               lhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_lhs != nullptr) delete result->p_lhs; 
     result->p_lhs = lhs_copy; 
  // case: not a listType for (using conditionalToSetParent)lhs
        if ((lhs_copy != nullptr) && (lhs_copy->get_parent() == nullptr) && (isSgType(lhs_copy) == nullptr)) {
            lhs_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_rhs
     SgAsmExpression* rhs_copy; 
  // case: not a listType for (using conditionalToCopyVariable)rhs
          if (get_rhs() != nullptr) 
             { 
               rhs_copy = static_cast<SgAsmExpression*>(help.copyAst(get_rhs())); 
             } 
            else 
             { 
               rhs_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_rhs != nullptr) delete result->p_rhs; 
     result->p_rhs = rhs_copy; 
  // case: not a listType for (using conditionalToSetParent)rhs
        if ((rhs_copy != nullptr) && (rhs_copy->get_parent() == nullptr) && (isSgType(rhs_copy) == nullptr)) {
            rhs_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191173 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmByteOrder::copy(SgCopyHelp& help) const {
    SgAsmByteOrder* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmByteOrder();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191274 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCommonSubExpression::copy(SgCopyHelp& help) const {
    SgAsmCommonSubExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCommonSubExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_subexpression
     SgAsmExpression* subexpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subexpression
          if (get_subexpression() != nullptr) 
             { 
               subexpression_copy = static_cast<SgAsmExpression*>(help.copyAst(get_subexpression())); 
             } 
            else 
             { 
               subexpression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_subexpression != nullptr) delete result->p_subexpression; 
     result->p_subexpression = subexpression_copy; 
  // case: not a listType for (using conditionalToSetParent)subexpression
        if ((subexpression_copy != nullptr) && (subexpression_copy->get_parent() == nullptr) && (isSgType(subexpression_copy) == nullptr)) {
            subexpression_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191393 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmControlFlagsExpression::copy(SgCopyHelp& help) const {
    SgAsmControlFlagsExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmControlFlagsExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitFlags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitFlags
  // case: toBeCopied == COPY_DATA for bitFlags
     unsigned long bitFlags_copy = p_bitFlags; 
     result->p_bitFlags = bitFlags_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191499 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmExprListExp::copy(SgCopyHelp& help) const {
    SgAsmExprListExp* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmExprListExp();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_expressions
     SgAsmExpressionPtrList expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto source_expressions_iterator = get_expressions().begin();
    for (/* empty by design */; source_expressions_iterator != get_expressions().end(); ++source_expressions_iterator) {
          SgAsmExpression* source_list_element = *source_expressions_iterator; 
          SgAsmExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmExpression*>(help.copyAst(*source_expressions_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          expressions_copy.push_back(copy_list_element); 
        }
     result->p_expressions = expressions_copy; 
  // case: listType for expressions
  // case: listType (typeIsPointerToList == false) for expressions
    auto expressions_iterator = result->get_expressions().begin();
    for (/* empty by design */; expressions_iterator != result->get_expressions().end(); ++expressions_iterator) {
          SgAsmExpression* list_element = *expressions_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191628 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmMemoryReferenceExpression::copy(SgCopyHelp& help) const {
    SgAsmMemoryReferenceExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmMemoryReferenceExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_address
     SgAsmExpression* address_copy; 
  // case: not a listType for (using conditionalToCopyVariable)address
          if (get_address() != nullptr) 
             { 
               address_copy = static_cast<SgAsmExpression*>(help.copyAst(get_address())); 
             } 
            else 
             { 
               address_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_address != nullptr) delete result->p_address; 
     result->p_address = address_copy; 
  // case: not a listType for (using conditionalToSetParent)address
        if ((address_copy != nullptr) && (address_copy->get_parent() == nullptr) && (isSgType(address_copy) == nullptr)) {
            address_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_segment
     SgAsmExpression* segment_copy; 
  // case: not a listType for (using conditionalToCopyVariable)segment
          if (get_segment() != nullptr) 
             { 
               segment_copy = static_cast<SgAsmExpression*>(help.copyAst(get_segment())); 
             } 
            else 
             { 
               segment_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_segment != nullptr) delete result->p_segment; 
     result->p_segment = segment_copy; 
  // case: not a listType for (using conditionalToSetParent)segment
        if ((segment_copy != nullptr) && (segment_copy->get_parent() == nullptr) && (isSgType(segment_copy) == nullptr)) {
            segment_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191765 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRegisterNames::copy(SgCopyHelp& help) const {
    SgAsmRegisterNames* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmRegisterNames();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_registers
     SgAsmRegisterReferenceExpressionPtrList registers_copy; 
  // case: listType for registers
  // case: listType (typeIsPointerToList == false) for registers
    auto source_registers_iterator = get_registers().begin();
    for (/* empty by design */; source_registers_iterator != get_registers().end(); ++source_registers_iterator) {
          SgAsmRegisterReferenceExpression* source_list_element = *source_registers_iterator; 
          SgAsmRegisterReferenceExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmRegisterReferenceExpression*>(help.copyAst(*source_registers_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          registers_copy.push_back(copy_list_element); 
        }
     result->p_registers = registers_copy; 
  // case: listType for registers
  // case: listType (typeIsPointerToList == false) for registers
    auto registers_iterator = result->get_registers().begin();
    for (/* empty by design */; registers_iterator != result->get_registers().end(); ++registers_iterator) {
          SgAsmRegisterReferenceExpression* list_element = *registers_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_mask
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mask
  // case: toBeCopied == COPY_DATA for mask
     unsigned mask_copy = p_mask; 
     result->p_mask = mask_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 191899 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRegisterReferenceExpression::copy(SgCopyHelp& help) const {
    SgAsmRegisterReferenceExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmRegisterReferenceExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192010 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmDirectRegisterExpression::copy(SgCopyHelp& help) const {
    SgAsmDirectRegisterExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmDirectRegisterExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192121 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIndirectRegisterExpression::copy(SgCopyHelp& help) const {
    SgAsmIndirectRegisterExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmIndirectRegisterExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor
  // case: toBeCopied == COPY_DATA for descriptor
     Rose::BinaryAnalysis::RegisterDescriptor descriptor_copy = p_descriptor; 
     result->p_descriptor = descriptor_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_adjustment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for adjustment
  // case: toBeCopied == COPY_DATA for adjustment
     int adjustment_copy = p_adjustment; 
     result->p_adjustment = adjustment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_stride
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stride
  // case: toBeCopied == COPY_DATA for stride
     Rose::BinaryAnalysis::RegisterDescriptor stride_copy = p_stride; 
     result->p_stride = stride_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     Rose::BinaryAnalysis::RegisterDescriptor offset_copy = p_offset; 
     result->p_offset = offset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     size_t index_copy = p_index; 
     result->p_index = index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_modulus
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for modulus
  // case: toBeCopied == COPY_DATA for modulus
     size_t modulus_copy = p_modulus; 
     result->p_modulus = modulus_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192252 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmRiscOperation::copy(SgCopyHelp& help) const {
    SgAsmRiscOperation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmRiscOperation();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_riscOperator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for riscOperator
  // case: toBeCopied == COPY_DATA for riscOperator
     SgAsmRiscOperation::RiscOperator riscOperator_copy = p_riscOperator; 
     result->p_riscOperator = riscOperator_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgAsmExprListExp* operands_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operands
          if (get_operands() != nullptr) 
             { 
               operands_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_operands())); 
             } 
            else 
             { 
               operands_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operands != nullptr) delete result->p_operands; 
     result->p_operands = operands_copy; 
  // case: not a listType for (using conditionalToSetParent)operands
        if ((operands_copy != nullptr) && (operands_copy->get_parent() == nullptr) && (isSgType(operands_copy) == nullptr)) {
            operands_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStackExpression::copy(SgCopyHelp& help) const {
    SgAsmStackExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStackExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_stackPosition
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackPosition
  // case: toBeCopied == COPY_DATA for stackPosition
     int stackPosition_copy = p_stackPosition; 
     result->p_stackPosition = stackPosition_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192482 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryExpression::copy(SgCopyHelp& help) const {
    SgAsmUnaryExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192601 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryMinus::copy(SgCopyHelp& help) const {
    SgAsmUnaryMinus* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryMinus();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryPlus::copy(SgCopyHelp& help) const {
    SgAsmUnaryPlus* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryPlus();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryRrx::copy(SgCopyHelp& help) const {
    SgAsmUnaryRrx* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryRrx();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 192958 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnarySignedExtend::copy(SgCopyHelp& help) const {
    SgAsmUnarySignedExtend* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnarySignedExtend();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193077 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryTruncate::copy(SgCopyHelp& help) const {
    SgAsmUnaryTruncate* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryTruncate();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193196 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUnaryUnsignedExtend::copy(SgCopyHelp& help) const {
    SgAsmUnaryUnsignedExtend* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUnaryUnsignedExtend();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operand
     SgAsmExpression* operand_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operand
          if (get_operand() != nullptr) 
             { 
               operand_copy = static_cast<SgAsmExpression*>(help.copyAst(get_operand())); 
             } 
            else 
             { 
               operand_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operand != nullptr) delete result->p_operand; 
     result->p_operand = operand_copy; 
  // case: not a listType for (using conditionalToSetParent)operand
        if ((operand_copy != nullptr) && (operand_copy->get_parent() == nullptr) && (isSgType(operand_copy) == nullptr)) {
            operand_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193315 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmValueExpression::copy(SgCopyHelp& help) const {
    SgAsmValueExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmValueExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != nullptr) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != nullptr) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
        if ((unfoldedExpression_copy != nullptr) && (unfoldedExpression_copy->get_parent() == nullptr) && (isSgType(unfoldedExpression_copy) == nullptr)) {
            unfoldedExpression_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193449 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmConstantExpression::copy(SgCopyHelp& help) const {
    SgAsmConstantExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmConstantExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != nullptr) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != nullptr) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
        if ((unfoldedExpression_copy != nullptr) && (unfoldedExpression_copy->get_parent() == nullptr) && (isSgType(unfoldedExpression_copy) == nullptr)) {
            unfoldedExpression_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193588 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFloatValueExpression::copy(SgCopyHelp& help) const {
    SgAsmFloatValueExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmFloatValueExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != nullptr) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != nullptr) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
        if ((unfoldedExpression_copy != nullptr) && (unfoldedExpression_copy->get_parent() == nullptr) && (isSgType(unfoldedExpression_copy) == nullptr)) {
            unfoldedExpression_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193727 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIntegerValueExpression::copy(SgCopyHelp& help) const {
    SgAsmIntegerValueExpression* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmIntegerValueExpression();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type
  // case: toBeCopied == COPY_DATA for type
     SgAsmType* type_copy = p_type; 
     result->p_type = type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_unfoldedExpression
     SgAsmValueExpression* unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToCopyVariable)unfoldedExpression
          if (get_unfoldedExpression() != nullptr) 
             { 
               unfoldedExpression_copy = static_cast<SgAsmValueExpression*>(help.copyAst(get_unfoldedExpression())); 
             } 
            else 
             { 
               unfoldedExpression_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_unfoldedExpression != nullptr) delete result->p_unfoldedExpression; 
     result->p_unfoldedExpression = unfoldedExpression_copy; 
  // case: not a listType for (using conditionalToSetParent)unfoldedExpression
        if ((unfoldedExpression_copy != nullptr) && (unfoldedExpression_copy->get_parent() == nullptr) && (isSgType(unfoldedExpression_copy) == nullptr)) {
            unfoldedExpression_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_bitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitOffset
  // case: toBeCopied == COPY_DATA for bitOffset
     unsigned short bitOffset_copy = p_bitOffset; 
     result->p_bitOffset = bitOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitSize
  // case: toBeCopied == COPY_DATA for bitSize
     unsigned short bitSize_copy = p_bitSize; 
     result->p_bitSize = bitSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbol
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbol
  // case: toBeCopied == COPY_DATA for symbol
     SgSymbol* symbol_copy = p_symbol; 
     result->p_symbol = symbol_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bitVector
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bitVector
  // case: toBeCopied == COPY_DATA for bitVector
     Sawyer::Container::BitVector bitVector_copy = p_bitVector; 
     result->p_bitVector = bitVector_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseNode
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseNode
  // case: toBeCopied == COPY_DATA for baseNode
     SgNode* baseNode_copy = p_baseNode; 
     result->p_baseNode = baseNode_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193871 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmGenericFileList::copy(SgCopyHelp& help) const {
    SgAsmGenericFileList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmGenericFileList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_files
     SgAsmGenericFilePtrList files_copy; 
  // case: listType for files
  // case: listType (typeIsPointerToList == false) for files
    auto source_files_iterator = get_files().begin();
    for (/* empty by design */; source_files_iterator != get_files().end(); ++source_files_iterator) {
          SgAsmGenericFile* source_list_element = *source_files_iterator; 
          SgAsmGenericFile* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmGenericFile*>(help.copyAst(*source_files_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          files_copy.push_back(copy_list_element); 
        }
     result->p_files = files_copy; 
  // case: listType for files
  // case: listType (typeIsPointerToList == false) for files
    auto files_iterator = result->get_files().begin();
    for (/* empty by design */; files_iterator != result->get_files().end(); ++files_iterator) {
          SgAsmGenericFile* list_element = *files_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 193990 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInstructionList::copy(SgCopyHelp& help) const {
    SgAsmInstructionList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmInstructionList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_instructions
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instructions
  // case: toBeCopied == COPY_DATA for instructions
     SgAsmInstructionPtrList instructions_copy = p_instructions; 
     result->p_instructions = instructions_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194086 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInterpretation::copy(SgCopyHelp& help) const {
    SgAsmInterpretation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmInterpretation();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_headers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for headers
  // case: toBeCopied == COPY_DATA for headers
     SgAsmGenericHeaderList* headers_copy = p_headers; 
     result->p_headers = headers_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_globalBlock
     SgAsmBlock* globalBlock_copy; 
  // case: not a listType for (using conditionalToCopyVariable)globalBlock
          if (get_globalBlock() != nullptr) 
             { 
               globalBlock_copy = static_cast<SgAsmBlock*>(help.copyAst(get_globalBlock())); 
             } 
            else 
             { 
               globalBlock_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_globalBlock != nullptr) delete result->p_globalBlock; 
     result->p_globalBlock = globalBlock_copy; 
  // case: not a listType for (using conditionalToSetParent)globalBlock
        if ((globalBlock_copy != nullptr) && (globalBlock_copy->get_parent() == nullptr) && (isSgType(globalBlock_copy) == nullptr)) {
            globalBlock_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_coverageComputed
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for coverageComputed
  // case: toBeCopied == COPY_DATA for coverageComputed
     bool coverageComputed_copy = p_coverageComputed; 
     result->p_coverageComputed = coverageComputed_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_percentageCoverage
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for percentageCoverage
  // case: toBeCopied == COPY_DATA for percentageCoverage
     double percentageCoverage_copy = p_percentageCoverage; 
     result->p_percentageCoverage = percentageCoverage_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194210 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInterpretationList::copy(SgCopyHelp& help) const {
    SgAsmInterpretationList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmInterpretationList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_interpretations
     SgAsmInterpretationPtrList interpretations_copy; 
  // case: listType for interpretations
  // case: listType (typeIsPointerToList == false) for interpretations
    auto source_interpretations_iterator = get_interpretations().begin();
    for (/* empty by design */; source_interpretations_iterator != get_interpretations().end(); ++source_interpretations_iterator) {
          SgAsmInterpretation* source_list_element = *source_interpretations_iterator; 
          SgAsmInterpretation* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmInterpretation*>(help.copyAst(*source_interpretations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          interpretations_copy.push_back(copy_list_element); 
        }
     result->p_interpretations = interpretations_copy; 
  // case: listType for interpretations
  // case: listType (typeIsPointerToList == false) for interpretations
    auto interpretations_iterator = result->get_interpretations().begin();
    for (/* empty by design */; interpretations_iterator != result->get_interpretations().end(); ++interpretations_iterator) {
          SgAsmInterpretation* list_element = *interpretations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194329 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmNode::copy(SgCopyHelp& help) const {
    SgAsmJvmNode* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmNode();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194419 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmAttribute::copy(SgCopyHelp& help) const {
    SgAsmJvmAttribute* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmAttribute();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmAnnotationDefault::copy(SgCopyHelp& help) const {
    SgAsmJvmAnnotationDefault* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmAnnotationDefault();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_default_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for default_value
  // case: toBeCopied == COPY_DATA for default_value
     SgAsmJvmRuntimeAnnotationValue* default_value_copy = p_default_value; 
     result->p_default_value = default_value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194626 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmBootstrapMethods::copy(SgCopyHelp& help) const {
    SgAsmJvmBootstrapMethods* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmBootstrapMethods();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bootstrap_methods
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_methods
  // case: toBeCopied == COPY_DATA for bootstrap_methods
     SgAsmJvmBootstrapMethodPtrList bootstrap_methods_copy = p_bootstrap_methods; 
     result->p_bootstrap_methods = bootstrap_methods_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194732 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmCodeAttribute::copy(SgCopyHelp& help) const {
    SgAsmJvmCodeAttribute* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmCodeAttribute();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_max_stack
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for max_stack
  // case: toBeCopied == COPY_DATA for max_stack
     uint16_t max_stack_copy = p_max_stack; 
     result->p_max_stack = max_stack_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_max_locals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for max_locals
  // case: toBeCopied == COPY_DATA for max_locals
     uint16_t max_locals_copy = p_max_locals; 
     result->p_max_locals = max_locals_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_code_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_length
  // case: toBeCopied == COPY_DATA for code_length
     uint32_t code_length_copy = p_code_length; 
     result->p_code_length = code_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_code
    const char* code_copy; 
  // case: typeName == char* or char** for code
     if (p_code != nullptr) 
          code_copy = strdup(p_code); 
     code_copy = p_code; 
     result->p_code = code_copy;
  // case: typeName == char* or char** for code
    // Copy non-constructor parameter data member (no access function): result->p_exception_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exception_table
  // case: toBeCopied == COPY_DATA for exception_table
     SgAsmJvmExceptionTable* exception_table_copy = p_exception_table; 
     result->p_exception_table = exception_table_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_table
  // case: toBeCopied == COPY_DATA for attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy = p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_code_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for code_offset
  // case: toBeCopied == COPY_DATA for code_offset
     Rose::BinaryAnalysis::Address code_offset_copy = p_code_offset; 
     result->p_code_offset = code_offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194871 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmEnclosingMethod::copy(SgCopyHelp& help) const {
    SgAsmJvmEnclosingMethod* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmEnclosingMethod();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_index
  // case: toBeCopied == COPY_DATA for class_index
     uint16_t class_index_copy = p_class_index; 
     result->p_class_index = class_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_method_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for method_index
  // case: toBeCopied == COPY_DATA for method_index
     uint16_t method_index_copy = p_method_index; 
     result->p_method_index = method_index_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 194982 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmIndexedAttr::copy(SgCopyHelp& help) const {
    SgAsmJvmIndexedAttr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmIndexedAttr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_type
  // case: toBeCopied == COPY_DATA for attribute_type
     unsigned attribute_type_copy = p_attribute_type; 
     result->p_attribute_type = attribute_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     uint16_t index_copy = p_index; 
     result->p_index = index_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195093 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmIndexTableAttr::copy(SgCopyHelp& help) const {
    SgAsmJvmIndexTableAttr* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmIndexTableAttr();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_type
  // case: toBeCopied == COPY_DATA for attribute_type
     unsigned attribute_type_copy = p_attribute_type; 
     result->p_attribute_type = attribute_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for table
  // case: toBeCopied == COPY_DATA for table
     std::vector<uint16_t> table_copy = p_table; 
     result->p_table = table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195204 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmInnerClasses::copy(SgCopyHelp& help) const {
    SgAsmJvmInnerClasses* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmInnerClasses();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_classes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for classes
  // case: toBeCopied == COPY_DATA for classes
     std::vector<SgAsmJvmInnerClasses::Entry*> classes_copy = p_classes; 
     result->p_classes = classes_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195310 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLineNumberTable::copy(SgCopyHelp& help) const {
    SgAsmJvmLineNumberTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmLineNumberTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_line_number_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for line_number_table
  // case: toBeCopied == COPY_DATA for line_number_table
     std::vector<SgAsmJvmLineNumberTable::Entry*> line_number_table_copy = p_line_number_table; 
     result->p_line_number_table = line_number_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTable::copy(SgCopyHelp& help) const {
    SgAsmJvmLocalVariableTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmLocalVariableTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_local_variable_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_variable_table
  // case: toBeCopied == COPY_DATA for local_variable_table
     SgAsmJvmLocalVariableEntryPtrList local_variable_table_copy = p_local_variable_table; 
     result->p_local_variable_table = local_variable_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195522 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTypeTable::copy(SgCopyHelp& help) const {
    SgAsmJvmLocalVariableTypeTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmLocalVariableTypeTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_local_variable_type_table
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for local_variable_type_table
  // case: toBeCopied == COPY_DATA for local_variable_type_table
     SgAsmJvmLocalVariableTypeEntryPtrList local_variable_type_table_copy = p_local_variable_type_table; 
     result->p_local_variable_type_table = local_variable_type_table_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195628 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodParameters::copy(SgCopyHelp& help) const {
    SgAsmJvmMethodParameters* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmMethodParameters();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_parameters
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for parameters
  // case: toBeCopied == COPY_DATA for parameters
     SgAsmJvmMethodParametersEntryPtrList parameters_copy = p_parameters; 
     result->p_parameters = parameters_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195734 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmModule::copy(SgCopyHelp& help) const {
    SgAsmJvmModule* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmModule();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_module_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_name_index
  // case: toBeCopied == COPY_DATA for module_name_index
     uint16_t module_name_index_copy = p_module_name_index; 
     result->p_module_name_index = module_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_module_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_flags
  // case: toBeCopied == COPY_DATA for module_flags
     uint16_t module_flags_copy = p_module_flags; 
     result->p_module_flags = module_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_module_version_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for module_version_index
  // case: toBeCopied == COPY_DATA for module_version_index
     uint16_t module_version_index_copy = p_module_version_index; 
     result->p_module_version_index = module_version_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_requires
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for requires
  // case: toBeCopied == COPY_DATA for requires
     std::vector<SgAsmJvmModule::Requires*> requires_copy = p_requires; 
     result->p_requires = requires_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exports
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exports
  // case: toBeCopied == COPY_DATA for exports
     std::vector<SgAsmJvmModule::Exports*> exports_copy = p_exports; 
     result->p_exports = exports_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_opens
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for opens
  // case: toBeCopied == COPY_DATA for opens
     std::vector<SgAsmJvmModule::Opens*> opens_copy = p_opens; 
     result->p_opens = opens_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_uses_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for uses_index
  // case: toBeCopied == COPY_DATA for uses_index
     std::vector<uint16_t> uses_index_copy = p_uses_index; 
     result->p_uses_index = uses_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_provides
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for provides
  // case: toBeCopied == COPY_DATA for provides
     std::vector<SgAsmJvmModule::Provides*> provides_copy = p_provides; 
     result->p_provides = provides_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 195875 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeInvisibleAnnotations::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeInvisibleAnnotations* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeInvisibleAnnotations();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_annotations
     std::vector<SgAsmJvmRuntimeAnnotation*> annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto source_annotations_iterator = get_annotations().begin();
    for (/* empty by design */; source_annotations_iterator != get_annotations().end(); ++source_annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* source_list_element = *source_annotations_iterator; 
          SgAsmJvmRuntimeAnnotation* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeAnnotation*>(help.copyAst(*source_annotations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          annotations_copy.push_back(copy_list_element); 
        }
     result->p_annotations = annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto annotations_iterator = result->get_annotations().begin();
    for (/* empty by design */; annotations_iterator != result->get_annotations().end(); ++annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* list_element = *annotations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196004 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeVisibilityParamAnnotations::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeVisibilityParamAnnotations* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeVisibilityParamAnnotations();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_parameter_annotations
     std::vector<SgAsmJvmRuntimeParameterAnnotation*> parameter_annotations_copy; 
  // case: listType for parameter_annotations
  // case: listType (typeIsPointerToList == false) for parameter_annotations
    auto source_parameter_annotations_iterator = get_parameter_annotations().begin();
    for (/* empty by design */; source_parameter_annotations_iterator != get_parameter_annotations().end(); ++source_parameter_annotations_iterator) {
          SgAsmJvmRuntimeParameterAnnotation* source_list_element = *source_parameter_annotations_iterator; 
          SgAsmJvmRuntimeParameterAnnotation* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeParameterAnnotation*>(help.copyAst(*source_parameter_annotations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          parameter_annotations_copy.push_back(copy_list_element); 
        }
     result->p_parameter_annotations = parameter_annotations_copy; 
  // case: listType for parameter_annotations
  // case: listType (typeIsPointerToList == false) for parameter_annotations
    auto parameter_annotations_iterator = result->get_parameter_annotations().begin();
    for (/* empty by design */; parameter_annotations_iterator != result->get_parameter_annotations().end(); ++parameter_annotations_iterator) {
          SgAsmJvmRuntimeParameterAnnotation* list_element = *parameter_annotations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_isVisible
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isVisible
  // case: toBeCopied == COPY_DATA for isVisible
     bool isVisible_copy = p_isVisible; 
     result->p_isVisible = isVisible_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196138 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeVisibleAnnotations::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeVisibleAnnotations* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeVisibleAnnotations();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_annotations
     std::vector<SgAsmJvmRuntimeAnnotation*> annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto source_annotations_iterator = get_annotations().begin();
    for (/* empty by design */; source_annotations_iterator != get_annotations().end(); ++source_annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* source_list_element = *source_annotations_iterator; 
          SgAsmJvmRuntimeAnnotation* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeAnnotation*>(help.copyAst(*source_annotations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          annotations_copy.push_back(copy_list_element); 
        }
     result->p_annotations = annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto annotations_iterator = result->get_annotations().begin();
    for (/* empty by design */; annotations_iterator != result->get_annotations().end(); ++annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* list_element = *annotations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196267 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapTable::copy(SgCopyHelp& help) const {
    SgAsmJvmStackMapTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmStackMapTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_attribute_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_name_index
  // case: toBeCopied == COPY_DATA for attribute_name_index
     uint16_t attribute_name_index_copy = p_attribute_name_index; 
     result->p_attribute_name_index = attribute_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for attribute_length
  // case: toBeCopied == COPY_DATA for attribute_length
     uint32_t attribute_length_copy = p_attribute_length; 
     result->p_attribute_length = attribute_length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entries
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entries
  // case: toBeCopied == COPY_DATA for entries
     SgAsmJvmStackMapFramePtrList entries_copy = p_entries; 
     result->p_entries = entries_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196373 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmBootstrapMethod::copy(SgCopyHelp& help) const {
    SgAsmJvmBootstrapMethod* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmBootstrapMethod();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_bootstrap_method_ref
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_method_ref
  // case: toBeCopied == COPY_DATA for bootstrap_method_ref
     uint16_t bootstrap_method_ref_copy = p_bootstrap_method_ref; 
     result->p_bootstrap_method_ref = bootstrap_method_ref_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bootstrap_arguments
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_arguments
  // case: toBeCopied == COPY_DATA for bootstrap_arguments
     SgUnsigned16List bootstrap_arguments_copy = p_bootstrap_arguments; 
     result->p_bootstrap_arguments = bootstrap_arguments_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196474 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmClass::copy(SgCopyHelp& help) const {
    SgAsmJvmClass* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmClass();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196564 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmConstantPoolEntry::copy(SgCopyHelp& help) const {
    SgAsmJvmConstantPoolEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmConstantPoolEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tag
  // case: toBeCopied == COPY_DATA for tag
     SgAsmJvmConstantPoolEntry::Kind tag_copy = p_tag; 
     result->p_tag = tag_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bytes
  // case: toBeCopied == COPY_DATA for bytes
     uint32_t bytes_copy = p_bytes; 
     result->p_bytes = bytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_hi_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for hi_bytes
  // case: toBeCopied == COPY_DATA for hi_bytes
     uint32_t hi_bytes_copy = p_hi_bytes; 
     result->p_hi_bytes = hi_bytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_low_bytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for low_bytes
  // case: toBeCopied == COPY_DATA for low_bytes
     uint32_t low_bytes_copy = p_low_bytes; 
     result->p_low_bytes = low_bytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_bootstrap_method_attr_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for bootstrap_method_attr_index
  // case: toBeCopied == COPY_DATA for bootstrap_method_attr_index
     uint16_t bootstrap_method_attr_index_copy = p_bootstrap_method_attr_index; 
     result->p_bootstrap_method_attr_index = bootstrap_method_attr_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_class_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_index
  // case: toBeCopied == COPY_DATA for class_index
     uint16_t class_index_copy = p_class_index; 
     result->p_class_index = class_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_and_type_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_and_type_index
  // case: toBeCopied == COPY_DATA for name_and_type_index
     uint16_t name_and_type_index_copy = p_name_and_type_index; 
     result->p_name_and_type_index = name_and_type_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_reference_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_index
  // case: toBeCopied == COPY_DATA for reference_index
     uint16_t reference_index_copy = p_reference_index; 
     result->p_reference_index = reference_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_reference_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reference_kind
  // case: toBeCopied == COPY_DATA for reference_kind
     uint8_t reference_kind_copy = p_reference_kind; 
     result->p_reference_kind = reference_kind_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_string_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for string_index
  // case: toBeCopied == COPY_DATA for string_index
     uint16_t string_index_copy = p_string_index; 
     result->p_string_index = string_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_utf8_bytes
    char* utf8_bytes_copy; 
  // case: typeName == char* or char** for utf8_bytes
     if (p_utf8_bytes != nullptr) 
          utf8_bytes_copy = strdup(p_utf8_bytes); 
     utf8_bytes_copy = p_utf8_bytes; 
     result->p_utf8_bytes = utf8_bytes_copy;
  // case: typeName == char* or char** for utf8_bytes

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmExceptionHandler::copy(SgCopyHelp& help) const {
    SgAsmJvmExceptionHandler* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmExceptionHandler();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_end_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for end_pc
  // case: toBeCopied == COPY_DATA for end_pc
     uint16_t end_pc_copy = p_end_pc; 
     result->p_end_pc = end_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_handler_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handler_pc
  // case: toBeCopied == COPY_DATA for handler_pc
     uint16_t handler_pc_copy = p_handler_pc; 
     result->p_handler_pc = handler_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_catch_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for catch_type
  // case: toBeCopied == COPY_DATA for catch_type
     uint16_t catch_type_copy = p_catch_type; 
     result->p_catch_type = catch_type_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196839 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmExceptionTable::copy(SgCopyHelp& help) const {
    SgAsmJvmExceptionTable* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmExceptionTable();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_handlers
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for handlers
  // case: toBeCopied == COPY_DATA for handlers
     SgAsmJvmExceptionHandlerPtrList handlers_copy = p_handlers; 
     result->p_handlers = handlers_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 196935 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmField::copy(SgCopyHelp& help) const {
    SgAsmJvmField* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmField();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute_table
          if (get_attribute_table() != nullptr) 
             { 
               attribute_table_copy = static_cast<SgAsmJvmAttributeTable*>(help.copyAst(get_attribute_table())); 
             } 
            else 
             { 
               attribute_table_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attribute_table != nullptr) delete result->p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute_table
        if ((attribute_table_copy != nullptr) && (attribute_table_copy->get_parent() == nullptr) && (isSgType(attribute_table_copy) == nullptr)) {
            attribute_table_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197059 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableEntry::copy(SgCopyHelp& help) const {
    SgAsmJvmLocalVariableEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmLocalVariableEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     uint16_t index_copy = p_index; 
     result->p_index = index_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197175 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmLocalVariableTypeEntry::copy(SgCopyHelp& help) const {
    SgAsmJvmLocalVariableTypeEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmLocalVariableTypeEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_start_pc
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for start_pc
  // case: toBeCopied == COPY_DATA for start_pc
     uint16_t start_pc_copy = p_start_pc; 
     result->p_start_pc = start_pc_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_length
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for length
  // case: toBeCopied == COPY_DATA for length
     uint16_t length_copy = p_length; 
     result->p_length = length_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_signature_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for signature_index
  // case: toBeCopied == COPY_DATA for signature_index
     uint16_t signature_index_copy = p_signature_index; 
     result->p_signature_index = signature_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for index
  // case: toBeCopied == COPY_DATA for index
     uint16_t index_copy = p_index; 
     result->p_index = index_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197291 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethod::copy(SgCopyHelp& help) const {
    SgAsmJvmMethod* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmMethod();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_descriptor_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for descriptor_index
  // case: toBeCopied == COPY_DATA for descriptor_index
     uint16_t descriptor_index_copy = p_descriptor_index; 
     result->p_descriptor_index = descriptor_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_attribute_table
     SgAsmJvmAttributeTable* attribute_table_copy; 
  // case: not a listType for (using conditionalToCopyVariable)attribute_table
          if (get_attribute_table() != nullptr) 
             { 
               attribute_table_copy = static_cast<SgAsmJvmAttributeTable*>(help.copyAst(get_attribute_table())); 
             } 
            else 
             { 
               attribute_table_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_attribute_table != nullptr) delete result->p_attribute_table; 
     result->p_attribute_table = attribute_table_copy; 
  // case: not a listType for (using conditionalToSetParent)attribute_table
        if ((attribute_table_copy != nullptr) && (attribute_table_copy->get_parent() == nullptr) && (isSgType(attribute_table_copy) == nullptr)) {
            attribute_table_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_instruction_list
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for instruction_list
  // case: toBeCopied == COPY_DATA for instruction_list
     SgAsmInstructionList* instruction_list_copy = p_instruction_list; 
     result->p_instruction_list = instruction_list_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197420 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmMethodParametersEntry::copy(SgCopyHelp& help) const {
    SgAsmJvmMethodParametersEntry* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmMethodParametersEntry();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name_index
  // case: toBeCopied == COPY_DATA for name_index
     uint16_t name_index_copy = p_name_index; 
     result->p_name_index = name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_access_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for access_flags
  // case: toBeCopied == COPY_DATA for access_flags
     uint16_t access_flags_copy = p_access_flags; 
     result->p_access_flags = access_flags_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197521 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeAnnotation::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeAnnotation();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_type_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_index
  // case: toBeCopied == COPY_DATA for type_index
     uint16_t type_index_copy = p_type_index; 
     result->p_type_index = type_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_element_value_pairs
     std::vector<SgAsmJvmRuntimeAnnotationPair*> element_value_pairs_copy; 
  // case: listType for element_value_pairs
  // case: listType (typeIsPointerToList == false) for element_value_pairs
    auto source_element_value_pairs_iterator = get_element_value_pairs().begin();
    for (/* empty by design */; source_element_value_pairs_iterator != get_element_value_pairs().end(); ++source_element_value_pairs_iterator) {
          SgAsmJvmRuntimeAnnotationPair* source_list_element = *source_element_value_pairs_iterator; 
          SgAsmJvmRuntimeAnnotationPair* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeAnnotationPair*>(help.copyAst(*source_element_value_pairs_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          element_value_pairs_copy.push_back(copy_list_element); 
        }
     result->p_element_value_pairs = element_value_pairs_copy; 
  // case: listType for element_value_pairs
  // case: listType (typeIsPointerToList == false) for element_value_pairs
    auto element_value_pairs_iterator = result->get_element_value_pairs().begin();
    for (/* empty by design */; element_value_pairs_iterator != result->get_element_value_pairs().end(); ++element_value_pairs_iterator) {
          SgAsmJvmRuntimeAnnotationPair* list_element = *element_value_pairs_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197645 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeAnnotationPair::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeAnnotationPair* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeAnnotationPair();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_element_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for element_name_index
  // case: toBeCopied == COPY_DATA for element_name_index
     uint16_t element_name_index_copy = p_element_name_index; 
     result->p_element_name_index = element_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for value
  // case: toBeCopied == COPY_DATA for value
     SgAsmJvmRuntimeAnnotationValue* value_copy = p_value; 
     result->p_value = value_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197746 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeAnnotationValue::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeAnnotationValue* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeAnnotationValue();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tag
  // case: toBeCopied == COPY_DATA for tag
     uint8_t tag_copy = p_tag; 
     result->p_tag = tag_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_const_value_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for const_value_index
  // case: toBeCopied == COPY_DATA for const_value_index
     uint16_t const_value_index_copy = p_const_value_index; 
     result->p_const_value_index = const_value_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_type_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for type_name_index
  // case: toBeCopied == COPY_DATA for type_name_index
     uint16_t type_name_index_copy = p_type_name_index; 
     result->p_type_name_index = type_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_const_name_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for const_name_index
  // case: toBeCopied == COPY_DATA for const_name_index
     uint16_t const_name_index_copy = p_const_name_index; 
     result->p_const_name_index = const_name_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_class_info_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for class_info_index
  // case: toBeCopied == COPY_DATA for class_info_index
     uint16_t class_info_index_copy = p_class_info_index; 
     result->p_class_info_index = class_info_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_is_annotation_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for is_annotation_value
  // case: toBeCopied == COPY_DATA for is_annotation_value
     bool is_annotation_value_copy = p_is_annotation_value; 
     result->p_is_annotation_value = is_annotation_value_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_annotation_value
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for annotation_value
  // case: toBeCopied == COPY_DATA for annotation_value
     SgAsmJvmRuntimeAnnotation* annotation_value_copy = p_annotation_value; 
     result->p_annotation_value = annotation_value_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_values
     std::vector<SgAsmJvmRuntimeAnnotationValue*> values_copy; 
  // case: listType for values
  // case: listType (typeIsPointerToList == false) for values
    auto source_values_iterator = get_values().begin();
    for (/* empty by design */; source_values_iterator != get_values().end(); ++source_values_iterator) {
          SgAsmJvmRuntimeAnnotationValue* source_list_element = *source_values_iterator; 
          SgAsmJvmRuntimeAnnotationValue* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeAnnotationValue*>(help.copyAst(*source_values_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          values_copy.push_back(copy_list_element); 
        }
     result->p_values = values_copy; 
  // case: listType for values
  // case: listType (typeIsPointerToList == false) for values
    auto values_iterator = result->get_values().begin();
    for (/* empty by design */; values_iterator != result->get_values().end(); ++values_iterator) {
          SgAsmJvmRuntimeAnnotationValue* list_element = *values_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 197900 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmRuntimeParameterAnnotation::copy(SgCopyHelp& help) const {
    SgAsmJvmRuntimeParameterAnnotation* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmRuntimeParameterAnnotation();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_annotations
     std::vector<SgAsmJvmRuntimeAnnotation*> annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto source_annotations_iterator = get_annotations().begin();
    for (/* empty by design */; source_annotations_iterator != get_annotations().end(); ++source_annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* source_list_element = *source_annotations_iterator; 
          SgAsmJvmRuntimeAnnotation* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmJvmRuntimeAnnotation*>(help.copyAst(*source_annotations_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          annotations_copy.push_back(copy_list_element); 
        }
     result->p_annotations = annotations_copy; 
  // case: listType for annotations
  // case: listType (typeIsPointerToList == false) for annotations
    auto annotations_iterator = result->get_annotations().begin();
    for (/* empty by design */; annotations_iterator != result->get_annotations().end(); ++annotations_iterator) {
          SgAsmJvmRuntimeAnnotation* list_element = *annotations_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198019 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapFrame::copy(SgCopyHelp& help) const {
    SgAsmJvmStackMapFrame* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmStackMapFrame();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_stack
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stack
  // case: toBeCopied == COPY_DATA for stack
     SgAsmJvmStackMapVerificationTypePtrList stack_copy = p_stack; 
     result->p_stack = stack_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_locals
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for locals
  // case: toBeCopied == COPY_DATA for locals
     SgAsmJvmStackMapVerificationTypePtrList locals_copy = p_locals; 
     result->p_locals = locals_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_frame_type
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for frame_type
  // case: toBeCopied == COPY_DATA for frame_type
     uint8_t frame_type_copy = p_frame_type; 
     result->p_frame_type = frame_type_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset_delta
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset_delta
  // case: toBeCopied == COPY_DATA for offset_delta
     uint16_t offset_delta_copy = p_offset_delta; 
     result->p_offset_delta = offset_delta_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198130 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmStackMapVerificationType::copy(SgCopyHelp& help) const {
    SgAsmJvmStackMapVerificationType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmStackMapVerificationType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_tag
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for tag
  // case: toBeCopied == COPY_DATA for tag
     uint8_t tag_copy = p_tag; 
     result->p_tag = tag_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_cpool_index
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cpool_index
  // case: toBeCopied == COPY_DATA for cpool_index
     uint16_t cpool_index_copy = p_cpool_index; 
     result->p_cpool_index = cpool_index_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint16_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198236 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmOperandList::copy(SgCopyHelp& help) const {
    SgAsmOperandList* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmOperandList();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_operands
     SgAsmExpressionPtrList operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto source_operands_iterator = get_operands().begin();
    for (/* empty by design */; source_operands_iterator != get_operands().end(); ++source_operands_iterator) {
          SgAsmExpression* source_list_element = *source_operands_iterator; 
          SgAsmExpression* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmExpression*>(help.copyAst(*source_operands_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          operands_copy.push_back(copy_list_element); 
        }
     result->p_operands = operands_copy; 
  // case: listType for operands
  // case: listType (typeIsPointerToList == false) for operands
    auto operands_iterator = result->get_operands().begin();
    for (/* empty by design */; operands_iterator != result->get_operands().end(); ++operands_iterator) {
          SgAsmExpression* list_element = *operands_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198355 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStatement::copy(SgCopyHelp& help) const {
    SgAsmStatement* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStatement();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmBlock::copy(SgCopyHelp& help) const {
    SgAsmBlock* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmBlock();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_id
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for id
  // case: toBeCopied == COPY_DATA for id
     Rose::BinaryAnalysis::Address id_copy = p_id; 
     result->p_id = id_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_reason
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reason
  // case: toBeCopied == COPY_DATA for reason
     unsigned reason_copy = p_reason; 
     result->p_reason = reason_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_statementList
     SgAsmStatementPtrList statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
    auto source_statementList_iterator = get_statementList().begin();
    for (/* empty by design */; source_statementList_iterator != get_statementList().end(); ++source_statementList_iterator) {
          SgAsmStatement* source_list_element = *source_statementList_iterator; 
          SgAsmStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmStatement*>(help.copyAst(*source_statementList_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statementList_copy.push_back(copy_list_element); 
        }
     result->p_statementList = statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
    auto statementList_iterator = result->get_statementList().begin();
    for (/* empty by design */; statementList_iterator != result->get_statementList().end(); ++statementList_iterator) {
          SgAsmStatement* list_element = *statementList_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_successors
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for successors
  // case: toBeCopied == COPY_DATA for successors
     SgAsmIntegerValuePtrList successors_copy = p_successors; 
     result->p_successors = successors_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_successorsComplete
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for successorsComplete
  // case: toBeCopied == COPY_DATA for successorsComplete
     bool successorsComplete_copy = p_successorsComplete; 
     result->p_successorsComplete = successorsComplete_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_immediateDominator
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for immediateDominator
  // case: toBeCopied == COPY_DATA for immediateDominator
     SgAsmBlock* immediateDominator_copy = p_immediateDominator; 
     result->p_immediateDominator = immediateDominator_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_cachedVertex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cachedVertex
  // case: toBeCopied == COPY_DATA for cachedVertex
     size_t cachedVertex_copy = p_cachedVertex; 
     result->p_cachedVertex = cachedVertex_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_codeLikelihood
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for codeLikelihood
  // case: toBeCopied == COPY_DATA for codeLikelihood
     double codeLikelihood_copy = p_codeLikelihood; 
     result->p_codeLikelihood = codeLikelihood_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_stackDeltaOut
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackDeltaOut
  // case: toBeCopied == COPY_DATA for stackDeltaOut
     int64_t stackDeltaOut_copy = p_stackDeltaOut; 
     result->p_stackDeltaOut = stackDeltaOut_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198625 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmInstruction::copy(SgCopyHelp& help) const {
    SgAsmInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198777 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmCilInstruction::copy(SgCopyHelp& help) const {
    SgAsmCilInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmCilInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::CilInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 198934 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmJvmInstruction::copy(SgCopyHelp& help) const {
    SgAsmJvmInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmJvmInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::JvmInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199091 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmM68kInstruction::copy(SgCopyHelp& help) const {
    SgAsmM68kInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmM68kInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::M68kInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_dataFormat
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dataFormat
  // case: toBeCopied == COPY_DATA for dataFormat
     Rose::BinaryAnalysis::M68kDataFormat dataFormat_copy = p_dataFormat; 
     result->p_dataFormat = dataFormat_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199253 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmMipsInstruction::copy(SgCopyHelp& help) const {
    SgAsmMipsInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmMipsInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::MipsInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199410 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmNullInstruction::copy(SgCopyHelp& help) const {
    SgAsmNullInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmNullInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199562 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPowerpcInstruction::copy(SgCopyHelp& help) const {
    SgAsmPowerpcInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPowerpcInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::PowerpcInstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199719 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmUserInstruction::copy(SgCopyHelp& help) const {
    SgAsmUserInstruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmUserInstruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     unsigned kind_copy = p_kind; 
     result->p_kind = kind_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 199876 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmX86Instruction::copy(SgCopyHelp& help) const {
    SgAsmX86Instruction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmX86Instruction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_architectureId
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for architectureId
  // case: toBeCopied == COPY_DATA for architectureId
     uint8_t architectureId_copy = p_architectureId; 
     result->p_architectureId = architectureId_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandList
     SgAsmOperandList* operandList_copy; 
  // case: not a listType for (using conditionalToCopyVariable)operandList
          if (get_operandList() != nullptr) 
             { 
               operandList_copy = static_cast<SgAsmOperandList*>(help.copyAst(get_operandList())); 
             } 
            else 
             { 
               operandList_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_operandList != nullptr) delete result->p_operandList; 
     result->p_operandList = operandList_copy; 
  // case: not a listType for (using conditionalToSetParent)operandList
        if ((operandList_copy != nullptr) && (operandList_copy->get_parent() == nullptr) && (isSgType(operandList_copy) == nullptr)) {
            operandList_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_delaySlot
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for delaySlot
  // case: toBeCopied == COPY_DATA for delaySlot
     SgAsmInstruction* delaySlot_copy = p_delaySlot; 
     result->p_delaySlot = delaySlot_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_semantics
     SgAsmExprListExp* semantics_copy; 
  // case: not a listType for (using conditionalToCopyVariable)semantics
          if (get_semantics() != nullptr) 
             { 
               semantics_copy = static_cast<SgAsmExprListExp*>(help.copyAst(get_semantics())); 
             } 
            else 
             { 
               semantics_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_semantics != nullptr) delete result->p_semantics; 
     result->p_semantics = semantics_copy; 
  // case: not a listType for (using conditionalToSetParent)semantics
        if ((semantics_copy != nullptr) && (semantics_copy->get_parent() == nullptr) && (isSgType(semantics_copy) == nullptr)) {
            semantics_copy->set_parent(result);
       } 
    // Copy non-constructor parameter data member (no access function): result->p_kind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for kind
  // case: toBeCopied == COPY_DATA for kind
     Rose::BinaryAnalysis::X86InstructionKind kind_copy = p_kind; 
     result->p_kind = kind_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_baseSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for baseSize
  // case: toBeCopied == COPY_DATA for baseSize
     Rose::BinaryAnalysis::X86InstructionSize baseSize_copy = p_baseSize; 
     result->p_baseSize = baseSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_operandSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for operandSize
  // case: toBeCopied == COPY_DATA for operandSize
     Rose::BinaryAnalysis::X86InstructionSize operandSize_copy = p_operandSize; 
     result->p_operandSize = operandSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_addressSize
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for addressSize
  // case: toBeCopied == COPY_DATA for addressSize
     Rose::BinaryAnalysis::X86InstructionSize addressSize_copy = p_addressSize; 
     result->p_addressSize = addressSize_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_lockPrefix
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for lockPrefix
  // case: toBeCopied == COPY_DATA for lockPrefix
     bool lockPrefix_copy = p_lockPrefix; 
     result->p_lockPrefix = lockPrefix_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_repeatPrefix
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for repeatPrefix
  // case: toBeCopied == COPY_DATA for repeatPrefix
     Rose::BinaryAnalysis::X86RepeatPrefix repeatPrefix_copy = p_repeatPrefix; 
     result->p_repeatPrefix = repeatPrefix_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_branchPrediction
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for branchPrediction
  // case: toBeCopied == COPY_DATA for branchPrediction
     Rose::BinaryAnalysis::X86BranchPrediction branchPrediction_copy = p_branchPrediction; 
     result->p_branchPrediction = branchPrediction_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_segmentOverride
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for segmentOverride
  // case: toBeCopied == COPY_DATA for segmentOverride
     Rose::BinaryAnalysis::X86SegmentRegister segmentOverride_copy = p_segmentOverride; 
     result->p_segmentOverride = segmentOverride_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200068 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmStaticData::copy(SgCopyHelp& help) const {
    SgAsmStaticData* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmStaticData();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_rawBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for rawBytes
  // case: toBeCopied == COPY_DATA for rawBytes
     SgUnsignedCharList rawBytes_copy = p_rawBytes; 
     result->p_rawBytes = rawBytes_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200174 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedDeclaration::copy(SgCopyHelp& help) const {
    SgAsmSynthesizedDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmSynthesizedDeclaration();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200275 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFunction::copy(SgCopyHelp& help) const {
    SgAsmFunction* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmFunction();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_reason
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reason
  // case: toBeCopied == COPY_DATA for reason
     unsigned reason_copy = p_reason; 
     result->p_reason = reason_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_reasonComment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for reasonComment
  // case: toBeCopied == COPY_DATA for reasonComment
     std::string reasonComment_copy = p_reasonComment; 
     result->p_reasonComment = reasonComment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_functionKind
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for functionKind
  // case: toBeCopied == COPY_DATA for functionKind
     SgAsmFunction::function_kind_enum functionKind_copy = p_functionKind; 
     result->p_functionKind = functionKind_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_mayReturn
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for mayReturn
  // case: toBeCopied == COPY_DATA for mayReturn
     SgAsmFunction::MayReturn mayReturn_copy = p_mayReturn; 
     result->p_mayReturn = mayReturn_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nameMd5
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nameMd5
  // case: toBeCopied == COPY_DATA for nameMd5
     std::string nameMd5_copy = p_nameMd5; 
     result->p_nameMd5 = nameMd5_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_statementList
     SgAsmStatementPtrList statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
    auto source_statementList_iterator = get_statementList().begin();
    for (/* empty by design */; source_statementList_iterator != get_statementList().end(); ++source_statementList_iterator) {
          SgAsmStatement* source_list_element = *source_statementList_iterator; 
          SgAsmStatement* copy_list_element = nullptr; 
          if (source_list_element != nullptr) 
             { 
               copy_list_element = static_cast<SgAsmStatement*>(help.copyAst(*source_statementList_iterator)); 
             } 
            else 
             { 
               copy_list_element = nullptr; 
             } 
          statementList_copy.push_back(copy_list_element); 
        }
     result->p_statementList = statementList_copy; 
  // case: listType for statementList
  // case: listType (typeIsPointerToList == false) for statementList
    auto statementList_iterator = result->get_statementList().begin();
    for (/* empty by design */; statementList_iterator != result->get_statementList().end(); ++statementList_iterator) {
          SgAsmStatement* list_element = *statementList_iterator; 
        if ((list_element != nullptr) && (list_element->get_parent() == nullptr) && (isSgType(list_element) == nullptr)) {
            list_element->set_parent(result);
       } 
        }
    // Copy non-constructor parameter data member (no access function): result->p_dest
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for dest
  // case: toBeCopied == COPY_DATA for dest
     SgAsmStatementPtrList dest_copy = p_dest; 
     result->p_dest = dest_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_entryVa
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for entryVa
  // case: toBeCopied == COPY_DATA for entryVa
     Rose::BinaryAnalysis::Address entryVa_copy = p_entryVa; 
     result->p_entryVa = entryVa_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_symbolTable
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for symbolTable
  // case: toBeCopied == COPY_DATA for symbolTable
     SgSymbolTable* symbolTable_copy = p_symbolTable; 
     result->p_symbolTable = symbolTable_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_cachedVertex
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for cachedVertex
  // case: toBeCopied == COPY_DATA for cachedVertex
     size_t cachedVertex_copy = p_cachedVertex; 
     result->p_cachedVertex = cachedVertex_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_stackDelta
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for stackDelta
  // case: toBeCopied == COPY_DATA for stackDelta
     int64_t stackDelta_copy = p_stackDelta; 
     result->p_stackDelta = stackDelta_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_callingConvention
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for callingConvention
  // case: toBeCopied == COPY_DATA for callingConvention
     std::string callingConvention_copy = p_callingConvention; 
     result->p_callingConvention = callingConvention_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedDataStructureDeclaration::copy(SgCopyHelp& help) const {
    SgAsmSynthesizedDataStructureDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmSynthesizedDataStructureDeclaration();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200565 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmSynthesizedFieldDeclaration::copy(SgCopyHelp& help) const {
    SgAsmSynthesizedFieldDeclaration* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmSynthesizedFieldDeclaration();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_address
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for address
  // case: toBeCopied == COPY_DATA for address
     Rose::BinaryAnalysis::Address address_copy = p_address; 
     result->p_address = address_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_comment
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for comment
  // case: toBeCopied == COPY_DATA for comment
     std::string comment_copy = p_comment; 
     result->p_comment = comment_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_name
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for name
  // case: toBeCopied == COPY_DATA for name
     std::string name_copy = p_name; 
     result->p_name = name_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_offset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for offset
  // case: toBeCopied == COPY_DATA for offset
     uint64_t offset_copy = p_offset; 
     result->p_offset = offset_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200676 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmType::copy(SgCopyHelp& help) const {
    SgAsmType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmType();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200766 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmScalarType::copy(SgCopyHelp& help) const {
    SgAsmScalarType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmScalarType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 200877 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmFloatType::copy(SgCopyHelp& help) const {
    SgAsmFloatType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmFloatType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_significandOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for significandOffset
  // case: toBeCopied == COPY_DATA for significandOffset
     size_t significandOffset_copy = p_significandOffset; 
     result->p_significandOffset = significandOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_significandNBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for significandNBits
  // case: toBeCopied == COPY_DATA for significandNBits
     size_t significandNBits_copy = p_significandNBits; 
     result->p_significandNBits = significandNBits_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_signBitOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for signBitOffset
  // case: toBeCopied == COPY_DATA for signBitOffset
     size_t signBitOffset_copy = p_signBitOffset; 
     result->p_signBitOffset = signBitOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exponentOffset
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentOffset
  // case: toBeCopied == COPY_DATA for exponentOffset
     size_t exponentOffset_copy = p_exponentOffset; 
     result->p_exponentOffset = exponentOffset_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exponentNBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentNBits
  // case: toBeCopied == COPY_DATA for exponentNBits
     size_t exponentNBits_copy = p_exponentNBits; 
     result->p_exponentNBits = exponentNBits_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_exponentBias
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for exponentBias
  // case: toBeCopied == COPY_DATA for exponentBias
     uint64_t exponentBias_copy = p_exponentBias; 
     result->p_exponentBias = exponentBias_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_flags
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for flags
  // case: toBeCopied == COPY_DATA for flags
     unsigned flags_copy = p_flags; 
     result->p_flags = flags_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 201023 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmIntegerType::copy(SgCopyHelp& help) const {
    SgAsmIntegerType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmIntegerType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_isSigned
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for isSigned
  // case: toBeCopied == COPY_DATA for isSigned
     bool isSigned_copy = p_isSigned; 
     result->p_isSigned = isSigned_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 201139 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmPointerType::copy(SgCopyHelp& help) const {
    SgAsmPointerType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmPointerType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_minorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for minorOrder
  // case: toBeCopied == COPY_DATA for minorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness minorOrder_copy = p_minorOrder; 
     result->p_minorOrder = minorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorOrder
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorOrder
  // case: toBeCopied == COPY_DATA for majorOrder
     Rose::BinaryAnalysis::ByteOrder::Endianness majorOrder_copy = p_majorOrder; 
     result->p_majorOrder = majorOrder_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_majorNBytes
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for majorNBytes
  // case: toBeCopied == COPY_DATA for majorNBytes
     size_t majorNBytes_copy = p_majorNBytes; 
     result->p_majorNBytes = majorNBytes_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_nBits
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nBits
  // case: toBeCopied == COPY_DATA for nBits
     size_t nBits_copy = p_nBits; 
     result->p_nBits = nBits_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_subtype
     SgAsmType * subtype_copy; 
  // case: not a listType for (using conditionalToCopyVariable)subtype
          if (get_subtype() != nullptr) 
             { 
               subtype_copy = static_cast<SgAsmType *>(help.copyAst(get_subtype())); 
             } 
            else 
             { 
               subtype_copy = nullptr; 
             } 
  /* check for a valid pointer and delete if present */ 
     if (result->p_subtype != nullptr) delete result->p_subtype; 
     result->p_subtype = subtype_copy; 
  // case: not a listType for (using conditionalToSetParent)subtype
        if ((subtype_copy != nullptr) && (subtype_copy->get_parent() == nullptr) && (isSgType(subtype_copy) == nullptr)) {
            subtype_copy->set_parent(result);
       } 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 201268 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmVectorType::copy(SgCopyHelp& help) const {
    SgAsmVectorType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmVectorType();
    ASSERT_not_null(result);

    // Copy data members of "this" class
    // Copy non-constructor parameter data member (no access function): result->p_nElmts
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for nElmts
  // case: toBeCopied == COPY_DATA for nElmts
     size_t nElmts_copy = p_nElmts; 
     result->p_nElmts = nElmts_copy; 
    // Copy non-constructor parameter data member (no access function): result->p_elmtType
  // case: (toBeTraversed == false) && (toBeCopied != CLONE_TREE) for elmtType
  // case: toBeCopied == COPY_DATA for elmtType
     SgAsmType* elmtType_copy = p_elmtType; 
     result->p_elmtType = elmtType_copy; 

/* #line 19 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}


/* #line 201369 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarCopyMemberFunctions.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/copyMemberFunction.macro" */

SgNode* SgAsmVoidType::copy(SgCopyHelp& help) const {
    SgAsmVoidType* result{nullptr};

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    // The default value of the depth is 0, so after this call the depth is 1!
    help.incrementDepth();

    // Copy data members from base classes
 

    // Build an empty copy of this object (NOTE: the parent MUST be set by the caller)
    result = new SgAsmVoidType();
    ASSERT_not_null(result);

    // Copy data members of "this" class


    // DQ (10/16/2007): The semantics of copy should include setting the parents of all
    // the Sg_File_Info objects that are built for the new IR nodes.
    SgLocatedNode* locatedNode = isSgLocatedNode(result);
    if (locatedNode != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = locatedNode->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(locatedNode);
        ASSERT_not_null(start->get_parent());

        Sg_File_Info* end = locatedNode->get_endOfConstruct();
        if (end == nullptr) {
            printf ("Warning, input to AST copy was not a correct AST (with valid endOfConstruct pointer) this = %p = %s \n",this,this->class_name().c_str());
        }
        else {
            end->set_parent(locatedNode);
            ASSERT_not_null(end->get_parent());
        }

        SgExpression* expression = isSgExpression(result);
        if (isSgExpression(this) != nullptr) {
            Sg_File_Info* operatorPosition = expression->get_operatorPosition();

            // Not all SgExpression IR nodes have a valid operatorPosition (only applies to actual operators)
            if (operatorPosition != nullptr) {
                operatorPosition->set_parent(expression);
                ASSERT_not_null(operatorPosition->get_parent());
            }
        }
    }

    // Set the Sg_File_Info parents for the SgInitializedName
    SgInitializedName* initializedName = isSgInitializedName(result);
    if (initializedName != nullptr) {
        // If this is a SgLocatedNode then check the parents of the Sg_File_Info objects
        Sg_File_Info* start = initializedName->get_startOfConstruct();
        ASSERT_not_null(start);

        start->set_parent(initializedName);
        ASSERT_not_null(start->get_parent());
    }

    // DQ (10/15/2007): Add everything to the map held internally so that we can more easily reference things like scopes.
    help.insertCopiedNodePair(this,result);

    // Record the depth of the copy so that we build an clear internal state (new feature)
    // used to associated old and new symbols in the original AST and the copy of the AST.
    help.decrementDepth();

    // Test if this is the root of the copy!
    if (help.get_depth() == 0) {
        // This is the original calling node.

        // Call the fixup mechanism to correct details of the AST copy mechanism.
#if ALT_FIXUP_COPY
        fixupCopy_scopes(result, help);
        fixupCopy_symbols(result, help);
        fixupCopy_references(result, help);
#else
        fixupCopy(result, help);
#endif
    }

    return result;
}



